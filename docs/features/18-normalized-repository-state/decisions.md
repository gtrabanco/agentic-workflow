# 18 — normalized-repository-state · decisions

## Resolved

- D1 — use a committed Markdown ledger at `docs/workflow/REPOSITORY_STATE.md`,
  mirrored in `template/`; it is portable and reviewable.
- D2 — facts are immutable during a frozen snapshot. The resolver owns revisions.
- D3 — all other skills are read-only consumers and may only propose contradictions.

## Open questions

none
