# 19 — architectural-invariants · decisions

## Resolved

- D1 — use an optional project-owned document at `docs/architecture/ARCHITECTURAL_INVARIANTS.md`; equivalent paths are declared in the documentation map.
- D2 — classify every applicable rule as `preserves`, `violates`, `introduces`, or `changes`; only `preserves` continues without an architectural decision.
- D3 — frozen NRS is optional evidence, never a required dependency or a replacement for repository inspection.

## Open questions

none
