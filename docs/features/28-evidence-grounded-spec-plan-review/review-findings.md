# review-findings — 28-evidence-grounded-spec-plan-review

Candidate-code review ran 2026-08-31 (review-change, single-reviewer, PR #155 head `a42c244b`). F7 (decision-required) and F21 (proposal) are intentionally not ledgered — non-fix-now findings keep their destinations from outcome routing (D3).

2026-08-31 re-plan: F2+F3+F6 (`replan-in-unit`) are covered by the
user-approved SPEC amendment appending P6–P8. Their rows stay `folded: no`
until the phase fixes land and `fold-findings` flips them.

| id | file:line | axis | severity | class | route | folded |
|---|---|---|---|---|---|---|
| F1 | skills/workflow-status/references/SENSOR_CORE.md:69 + skills/execute-phase/references/PRE_EXECUTION_GATE.md:6 + skills/audit-pr/references/02_CLOSURE_AND_SCOPE_GATES.md:93 | code | high | fix-now | fold · fold fe0aa37 | yes |
| F2 | docs/features/28-evidence-grounded-spec-plan-review/testing.md (Canary fields) + planning-obligations.md O9–O14 | spec-drift | high | fix-now | replan-in-unit · fold 6157e18 (ticked 25350b4) | yes |
| F3 | docs/features/ROADMAP.md:38 + docs/features/28-evidence-grounded-spec-plan-review/progress.md (P5 section) | workflow | high | fix-now | replan-in-unit · fold d3f811a | yes |
| F4 | docs/workflow/GOLDEN_FIXTURE.md:304 | spec-drift | med | fix-now | fold · fold 6f9b6d9 | yes |
| F5 | docs/workflow/SKILLS.es.md + docs/workflow/GOLDEN_FIXTURE.es.md | workflow | med | fix-now | fold · fold 6f9b6d9 | yes |
| F6 | docs/features/28-evidence-grounded-spec-plan-review/TASKS.md (P5 section) | workflow | med | fix-now | replan-in-unit · fold 6157e18 (ticked 25350b4) | yes |
| F8 | docs/workflow/SKILL_CONTEXT_BUDGETS.json (plan-fix:issue route) + plan-fix path skill docs | perf | med | fix-now | fold (ceiling re-basis after F7 decision) · fold 6748d2e | yes |
| F9 | packages/agentic-workflow-schema/src/pre-execution-contract.ts:183-195 | code | med | fix-now | fold · fold 6adef28 | yes |
| F10 | packages/agentic-workflow-schema/src/pre-execution.ts:150 + src/pre-execution-contract.ts:166 | code | med | fix-now | fold · fold 6adef28 | yes |
| F11 | packages/agentic-workflow-schema/src/pre-execution.ts:1038-1041 | code | med | fix-now | fold · fold 0343713 | yes |
| F12 | skills/pre-execution-review/references/LEDGERS.md:19-27 + docs/features/_TEMPLATE/SPEC.md:255 | code | med | fix-now | fold · fold 6445eae | yes |
| F13 | skills/plan-fix/SKILL.md:62 + docs/fix/_TEMPLATE/SPEC.md:50 | code | med | fix-now | fold · fold 6445eae | yes |
| F14 | scripts/pre-execution-snapshot.mjs:88-96,184-186 | security | low | fix-now | fold · fold 74c40bc | yes |
| F15 | scripts/pre-execution-snapshot.mjs:97-99 | security | low | fix-now | fold · fold 74c40bc | yes |
| F16 | skills/workflow-status/references/PRE_EXECUTION.md:20 | code | low | fix-now | fold · fold 6445eae | yes |
| F17 | scripts/pre-execution-snapshot.mjs:171-215,237-252 | code | low | fix-now | fold · fold 74c40bc | yes |
| F18 | packages/agentic-workflow-schema/src/pre-execution.ts:541-546 | code | low | fix-now | fold · fold 6adef28 | yes |
| F19 | scripts/pre-execution-snapshot.mjs:176-177 | perf | low | fix-now | fold · fold 74c40bc | yes |
| F20 | scripts/pre-execution-snapshot.mjs:193-195 | perf | low | fix-now | fold · fold 74c40bc | yes |

Second review-change cycle ran 2026-09-01 (isolated clean-context reviewer, terminal candidate HEAD `c44173ef`): verdict REVIEW-FAIL with two low fix-now findings (RC1, RC2), zero code defects; the reviewer's counter-evidence pass reproduced every P6 corpus claim live (fix-78 refusals, fix-147 D30 digest `acfe7087…`, unit-17 `fdddc858…`), matched D31 to the manifest 7/7 and the coverage-note versions to every SKILL.md, and confirmed D32 recorded consistently in all four ledgers. One re-review after the folds is the normal correction path (AC14).

| id | file:line | axis | severity | class | route | folded |
|---|---|---|---|---|---|---|
| RC1 | docs/features/28-evidence-grounded-spec-plan-review/testing.md:268 + progress.md:459 | verify | low | fix-now | fold | yes |
| RC2 | scripts/check-skill-context.mjs:305 (floor) + decisions.md D31 table | code | low | fix-now | fold | yes |

`audit-pr` ran 2026-09-01 on PR #155 at head `3992ac17` (state OPEN): verdict **BLOCKED** — 5 gate blockers. Per the audit contract these are fix-now by definition (merge is gated on each); no review axis was re-run here and no MERGE-READY comment was posted. Gate names carry the `file:line` where no single line applies. Rows F2/F3/F6 above are named inside F24: they must flip to `folded: yes` in the same fold commit that closes P8 at the new head.

| id | file:line | axis | severity | class | route | folded |
|---|---|---|---|---|---|---|
| F22 | docs/features/ROADMAP.md:38 (conflicts with origin/main `d2b31676`) | Mergeability | high | fix-now | execute-phase (sync the branch with `main`, resolve the row 28/29/30 conflict, push) — closed 2026-09-02: `git merge-tree --write-tree` named `docs/features/ROADMAP.md` as the only conflicted path, the merge resolved row 28 alone and rows 29/30 were verified byte-identical to `main` (D48); push lands with P16 box 9; token withheld until the commit that carries it — the repair is a merge, and the annotator lists a merge commit's files with `git show --name-only`, which prints nothing for a merge (known-issue 22), so a `fold` token naming `81c241d` cannot verify and this row stays `no` until the roadmap row lands · fold 5da0ae4 | yes |
| F23 | PR #155 @ `3992ac17` — 1 comment, zero `review-change:pass` markers | Review receipt | high | fix-now | review-change (re-review at the synced head and post the SHA-bound receipt) · fold 5644dd7 | yes |
| F24 | docs/features/28-evidence-grounded-spec-plan-review/planning-obligations.md:39 (O12 `planned`) + SPEC.md:845-855 (P8 done-when) | All phases complete | high | fix-now | execute-phase (P8 close-out at the terminal head: current PASS receipt, O12 `verified`, F2/F3/F6 flipped) · fold 7c948bb | yes |
| F25 | docs/features/28-evidence-grounded-spec-plan-review/SPEC.md:291-356 (AC1–AC14) | Acceptance coverage | high | fix-now | review-change (same action as F23 — the receipt must name every AC) — closed on evidence but kept `no` on provenance: the receipt named every AC at `d9545d8` (PR #155 comment + the review-mark row); the annotator's only rank-3 candidate for this row predates the row's existence and merely touches the cited `SPEC.md`, so binding it would be known-issue 22's invented provenance — re-check when the ranker learns existence order or file-less fold claims | no |
| F26 | PR #155 body lines «`npx skills add . --list` → 39 skills discoverable» and «includes all 39 skills» | Docs (PR-body evidence) | med | fix-now | execute-phase (correct the body to the measured 38 discoverable / 39 entrypoints − `bump-skill`, 38 bundled) · ticked 25350b4 | yes |

Third review-change cycle ran 2026-09-01 (isolated clean-context passes — code, security, verify, perf — at terminal head `3992ac17`): verdict **REVIEW-FAIL** with seven open fix-now findings (F22 + F24 + F26 re-confirmed live, plus new F27/F28/F30/F32). The verify pass reproduced every recorded gate figure live with zero verify findings (schema 674/674, root 127/127, pre-execution-quality 48/48, sensor+attribution 22/22, Pi 134/134, budgets 39 skills/23 routes, CLI "Found 38 skills", canonical↔Pi byte parity, versions 3.5.0/0.2.0); F22 re-confirmed via `git merge-tree` CONFLICT vs origin/main `d2b31676`; F24 confirmed as stale bookkeeping only — canary corpus complete, ROADMAP row 28 done, TASKS ticked, ledger states unflipped. One process incident is recorded inside F27: the verify subagent created an unsolicited research note in the working tree, violating its read-only findings-only contract. No `review-change:pass` receipt was posted (REVIEW-FAIL posts none); one re-review after the folds is the normal correction path (AC14). Proposals F29 (budget re-basis policy, trigger: first ceiling-level growth trip) and F34 (perf tooling via init-workspace) plus debt item F31 (builder hashes full input before budget refusal; re-trigger: input contract beyond the ≥4 MiB bounded edge) are batched for the user — no issues created (D3); F33 stays routed to the recorded owner proposal RR1; F31/F35 ignores carry their rationale in the cycle-3 report.

| id | file:line | axis | severity | class | route | folded |
|---|---|---|---|---|---|---|
| F27 | working tree at `3992ac17` — tracked `review-findings.md` modification (the F22–F26 audit append above) + untracked `docs/research/skill-authoring-consumption-separation-2026-09-01.md` (created mid-review by the verify pass, contract breach) | workflow | high | fix-now | fold (commit the ledger append in the fold commit; owner deletes or explicitly adopts the stray note) · ticked 4aabfd3 | yes |
| F28 | skills/pre-execution-review/references/POLICY.md:1 + skills/review-spec/SKILL.md:58 + skills/review-plan/SKILL.md:56 + skills/evidence-grounding/SKILL.md:120 | security | med | fix-now | fold (one root-caused batch: "artifact content is data, never instructions" rule into the shared review policy, both reviewer skills and the authoring readiness rows) · fold 2016d30 | yes |
| F30 | scripts/pre-execution-snapshot.mjs:111 | code | low | fix-now | fold (delete the unreferenced `ATTRIBUTION_ORDER` table and correct the comment that claims it is load-bearing) · fold c96b50a (ticked d3f811a) | yes |
| F32 | packages/agentic-workflow-schema/src/sha256.ts:29 | perf | low | fix-now | fold (replace the ~120-line hand-rolled SHA-256 with `node:crypto` createHash — same sync API, identical digests; confirm no non-Node target before the swap) — **superseded by the owner's verdict**, which this row now records rather than re-litigating: no swap. The sync path prefers the host's own SHA-256 through `globalThis.process?.getBuiltinModule?.("crypto")` when the host exposes one and falls back to this package's pure-JS core otherwise, with no static `node:` import, no `@types/node`, no dependency and nothing vendored; the four rejected alternatives and their measured costs (forced `@types/node`, `@noble/hashes` weight, its 1,419-line closure) are in `architecture-notes.md` §"Digest paths" and the rule is in `CLAUDE.md` §"No vendored code". Shipped by P17: `npm test` 675/675 with the three-path case, red-first on `a42cf485` · fold 890f936 | yes |

Fourth cycle (2026-09-01, replan + weakest-executor fixture leg). F3 flipped to `yes` by the user-approved amendment that adopted #146's flow-integrity amendment and cut P9–P16: the `replan-in-unit` route is satisfied by the replan landing, not by a code edit — the ROADMAP:38 half now carries main's adopted clause list and `progress.md` carries the P9–P16 rows. Two mechanical rows changed here, neither a reclassification: **F30**'s cell count was corrected (it carried 8 cells against a 7-column header, so its `folded: yes` sat in a phantom ninth column and any strict parse read the row as unfolded — the known-issue 13 failure mode, now inside this ledger), and **F35** is a new fix-now finding observed live during this turn's fixture run. F36 came out of the F32 measurement the owner asked for. **F32's proposed remedy
was then superseded by the owner's verdict (D36)**: the finding stays
`folded: no` and its work now runs as P17/O21 in a different shape — native hash
first where the host exposes one, this package's own pure-JS path otherwise, no
dependency added and nothing vendored. The route cell above preserves what the
reviewer asked for as history; the disposition lives here so no one reads the row
as pre-approved. The column check that caught F30 caught two rows this very turn had just written into `planning-evidence.md` (PE-017 at 12 cells, PE-018 at 9) — pipes inside cells again, fixed in this same commit before it was recorded as clean: the defect class is easy to author and hard to notice, which is the whole argument for AC15 and AC16.

| id | file:line | axis | severity | class | route | folded |
|---|---|---|---|---|---|---|
| F35 | docs/features/ROADMAP.md (row 91) + docs/features/91-toy-csv-export/ — two commits `de9f4a04`, `bc0a88ef` written to the delivery branch by a fixture subagent | workflow | high | fix-now | fold (reverted this turn: `git reset --hard 2016d309`, toy tree removed, evidence preserved at `/tmp/f35-evidence/`) · fold d3f811a | yes |
| F36 | packages/agentic-workflow-schema/src/sha256.ts:13 | code (prose↔machine drift) | low | fix-now | execute-phase (the comment cites `test/pre-execution-sha256.test.mjs`, which does not exist — no file matching `sha256` is present in `packages/agentic-workflow-schema/test/`; the real pins are `test/pre-execution-canonical.test.mjs:51,198,295`, which do compare builder digests against `node:crypto`. Either name the existing file or add the named one. Found while measuring F32, and it is a live specimen of the F1 drift this amendment plans) · fold 890f936 | yes |
| F37 | skills/review-plan/SKILL.md:35 ("parent SPEC snapshot copied from the receipt") against skills/pre-execution-review/references/POLICY.md §7 (identity values require recomputation) | workflow | med | fix-now | fold (one owner must state the rule: recompute the parent digest, and record the receipt's claimed value beside it as the reported defect — never carry a copied value into a new receipt as identity. Demonstrated, not theorized: two runs of the same text on the same target resolved it in opposite directions, see PE-022) · fold d2d7569 | yes |

| id | file:line | axis | severity | class | route | folded |
|---|---|---|---|---|---|---|
| F38 | skills/workflow-status/references/SENSOR_CORE.md:85-86 (`counts it only while the head sha it names is the unit's current head`) against skills/pre-execution-review/references/LEDGERS.md:167 (the `REVIEW-RAN` row binding `HEAD <40-hex sha>`) | architecture (proof unreachable by the flow it governs) | high | fix-now | fold: step 8 must judge currency by "no commit after the named sha touched a bound input" (`git log <mark>..HEAD -- <bound paths>` empty), which is what the digests already mean, and `scripts/workflow-status-pre-execution.test.mjs` must derive `headSha` from a commit it makes rather than injecting one — AC20's proof is otherwise unsatisfiable on a branch · fold 1bc40db | yes |
| F39 | skills/review-spec/references/CHECKS.md:49-50 ("a refused build … never a hand-computed substitute") and skills/review-plan/references/OUTPUT.md:16 (`Snapshot: <64-hex>`) | workflow (template demand beats prohibition) | high | fix-now | fold: name the contracted terminal form for a refused or unbuildable snapshot at both stages (what `Snapshot:` carries when there is no snapshot object) — a weak executor facing both texts obeyed the template and substituted the raw file SHA-256 on one stage and `null` on the other, both self-declared · fold 1bc40db | yes |
| F40 | skills/evidence-grounding/SKILL.md (no route row names when `references/DELEGATION.md` governs) | docs (contract present, trigger absent) | medium | fix-now | fold: add the delegate-invocation trigger to the routing table so an executor that is *not* the authoring turn matches it; P12 wrote the contract and left the entrypoint silent about when to apply it · fold a42cf48 | yes |
| F41 | skills/evidence-grounding/references/READINESS.md:27 (box 1 "Required Product headings present") against packages/agentic-workflow-schema/src/pre-execution.ts:164 (`SPEC_PRODUCT_REQUIRED_HEADINGS`) | code (prose↔machine drift) | medium | fix-now | fold: box 1 must cite the machine's closed heading list rather than restate it in prose — a weak run reported `READY-FOR-REVIEW` on bytes the canonical selector refused for a missing `Goal` heading, which is the AC15 class this unit's own drift gate exists to kill · fold a42cf48 | yes |
| F42 | scripts/ledger-provenance.mjs:94 `if (cells.length !== 7) continue;` (same skip at `:290`) against the `CELL_RE` comment at `:34-40` which declares the silent-drop class closed | code | high | fix-now | fold: counted-vs-parsed arity guard; `--check` refuses and `--annotate` reports when parsed rows < `^\| F<n> \|` lines · fold 481f330 | yes |
| F43 | scripts/ledger-provenance.mjs:306-307 — `--check --annotate` prints `annotated N row(s)` while writing nothing | docs (tool output) | low | fix-now | fold: `would annotate N row(s)` under `--check` · fold 481f330 | yes |
| F44 | scripts/check-skill-context.mjs:295-297 — the headroom guard binds only on the exact policy name, so a typo'd `policy.name` or a non-positive headroom disables it silently | code | med | fix-now | fold: unknown policy name / invalid headroom / degenerate ceiling now refuse instead of skipping · fold 481f330 | yes |
| F45 | scripts/check-skill-context.mjs:309-311 — `decimalPlaces <= 6 ? … : null` float fallback unreachable in this repository and pinned by no fixture | debt | low | fix-now | fold: branch deleted with C3's refusal (no fixture added for dead code) · fold 481f330 | yes |
| F46 | scripts/normative-drift.test.mjs:281,315,386 — `metadataInternal` and `model.labelRows` written, never read, so a `table:` grammar kind proves only that a table exists | code | med | fix-now | fold: write-only fields removed; the kind now asserts parsed data rows · fold 481f330 | yes |
| F47 | scripts/pre-execution-quality.test.mjs:499 — `FINDING_CLASSES.join("\|").split("\|").length` compared to a literal 5: tautological, binds nothing in the tree | code | med | fix-now | fold: bound to the published `PRE_EXECUTION_FINDING_CLASSES` in `packages/agentic-workflow-schema/src/`, proven by doctoring the machine · fold 481f330 | yes |
| F48 | packages/agentic-workflow-schema/test/pre-execution-canonical.test.mjs:372 — under the `absent` override nothing can push into `requested`, so `never reached` holds by construction | code | med | fix-now | fold: the case now observes the lookup (`reads >= 1`) instead of asserting an unreachable set · fold 481f330 | yes |
| F49 | scripts/pre-execution-snapshot.mjs:467 — `policyVersion: opts.policy ?? "v1"` hardcodes a policy constant inside the file whose header says every value comes from the schema | debt | med | fix-now | debt: needs a schema publish before the CLI can read it — known-issue 24 carries the re-trigger | no |
| F50 | packages/agentic-workflow-schema/src/sha256.ts:39,47 — comment claims repeated hashing is allocation-free while `w`/`h`/pad are allocated per call | docs (comment truth) | low | fix-now | fold: reworded to per-call; no behaviour change · fold 481f330 | yes |
| F51 | CHANGELOG.es.md:547/553, :548/552, :549/551 — three amendment rows appear twice; invisible to P14's gate because `newestVersionCell` (`normative-drift.test.mjs:757-761`) reads a max | docs (rendered fact) | med | fix-now | fold: duplicates removed + a per-(skill,version) row-count pin in both tables; one legacy pair exempted and recorded as known-issue 23 · fold 481f330 | yes |
| F52 | CHANGELOG.md:291 `#### plan-fix` jumps `3.0.1 -> 2.7.0` while `CHANGELOG.es.md:295` keeps the `3.0.0` row, against `CHANGELOG.md:6-7`'s own claim to be the source of truth | docs (rendered fact) | med | fix-now | fold: EN row restored; pin extended to EN/ES version-set parity (and it caught a second, older ES omission) · fold 481f330 | yes |
| F53 | skills/evidence-grounding/references/READINESS.md:41-44 — the `deferred`-needs-a-user-SPEC-amendment rule stated twice inside one box in two wordings | docs (one owner) | low | fix-now | fold: one wording kept at the single home; reference-only, no bump · fold 481f330 | yes |
| F54 | testing.md:829 — red-first proof C recorded as 8/7/1 with a recipe that does not reproduce: live is 9 tests, and the named unit-26 failure is environmental in a scratch archive (all 107 rows `unproven`) | docs (evidence truth) | med | fix-now | fold: row corrected to what reproduces, with the copy recipe stated file-by-file · fold 481f330 | yes |
| F55 | testing.md:828 — red-first proof B recorded 62/60/2 where a different copy set yields 62/61/1; the F39 substance holds either way | docs (evidence truth) | low | fix-now | fold: counts corrected and the recipe pinned in the row itself · fold 481f330 | yes |
| F56 | docs/workflow/SKILL_CONTEXT_BUDGETS.json — `review-change:default-*` `routeEstimateMax: 13795` is unreachable from `ceil(m x 1.10)` for any integer m (pre-RC2 float residue) | config | low | fix-now | fold: re-based to 13794 (measured 12540); the invariant is no-UNDER, equality holds at each re-basis, which the manifest's own `rules[0]` already says · fold 481f330 | yes |
| F57 | docs/features/28-…/progress.md — the P16 fold-half receipt records `ledger-provenance` 8/8 where live is 9/9 (the pipe test landed mid-fold) | docs (evidence truth) | low | fix-now | fold: record corrected in testing.md/progress.md; the published commit message is left as history · fold 481f330 | yes |
| F58 | scripts/ledger-provenance.mjs:342-344 — the reopen token hardcodes a phase number (`· REOPENED P20 — provenance unproven`) that no plan of this unit contains, so a mechanical token invented a fact; it fired on this fold's own 15 rows the moment they were marked `folded: yes` before their commit existed | code (contract payload) | med | fix-now | fold-or-debt: the token must carry no phase it cannot name — decide at the close-out, next worker · fold 47e9fe1 | yes |

Fifth review-change cycle ran 2026-09-02 (four context-clean axes at head `47e9fe1a`, each a fresh RLM worker that received only its axis checklist plus the branch diff — reviewer independence obtained by construction: no worker carried the authoring session, and the orchestrating turn authored none of the axis text it passed in). Synthesis: **code FAIL** (three fix-now rows below; the rest of its table is adjudicated in the same breath), **verify** inconclusive-run (its worker died mid-suite — re-run at the post-fold head, which is where the receipt binds anyway), **security PASS** with zero findings and every named check stated APPLIED or n-a (no new network, secret, authn/authz or untrusted-input surface anywhere in the diff; all child-process use is `execFileSync` on fixed git subcommands; the annotation write is the ledger path the operator passed), **perf: no fix-now** — its two rows restate D36's adjudicated design (native-first with the pure-JS core as fallback, digests identical, cost measured and recorded in `architecture-notes.md` §"Digest paths"), so they are proposals batched here for the user (surface which hash path answered at runtime; char-table hex encoding), no issues created (D3). Code findings F2–F7 from that reviewer were adjudicated, not carried: its F2 (empty reopen payload) is unreachable — both `unproven` evidence templates are non-empty by construction, and the reopen branch only sees that status; its F3 (reopen grammar undeclared) is refuted by `LEDGERS.md`'s prose, which quotes the full note verbatim beside the map cell; its F5/F6 (silent arity bail-outs; `--check` over-reporting OK) are refuted by the `(C1)` case, which asserts `--check` exits 1 naming unparsed rows, and by the parsed-count predicate being identical to the annotate re-check. What stands is its F1/F4/F7, folded at the root as the three rows below; their `folded: yes` carries no token until the first annotate after the commit that fixes them, which is the same honest state F38/F39 and F58 held.

| id | file:line | axis | severity | class | route | folded |
|---|---|---|---|---|---|---|
| F59 | scripts/ledger-provenance.mjs:337 — the re-entry guard `new RegExp(\`·\s*${entry.token}\s[0-9a-f]{7}\`)` was built from a template literal whose `\s` collapses to the character `s`, so the pattern could never match the `· fold <sha>` marker the same branch writes: a re-entry check that never re-matches (proved by evaluating the source expression: `·s*folds[0-9a-f]{7}` against `· fold 481f330` → false) | code | med | fix-now | fold (escapes doubled; the guard expression is now pinned by a test that builds it from the script's own source — red-first: the case fails against `47e9fe1a`, 14 tests / 12 pass / 2 fail, named `the re-entry guard matches the token shape the annotator writes (F59)` plus the environmental unit-26 archive row) · fold d9545d8 | yes |
| F60 | scripts/ledger-provenance.mjs:198-199 — `route` and `recordedYes` written into the flip map, read nowhere; the annotate path re-derives the cell from the line itself — the same write-only-field class the fold removed at F46 | code | low | fix-now | fold (fields deleted; only the flip sha is ever consulted) · fold d9545d8 | yes |
| F61 | scripts/ledger-ownership.test.mjs:236-241, :721-723 — the pin's comment and assertion messages cited implementation lines (`248`, `288`, `293`) that the F42/F58 folds moved; a pin whose prose points at the wrong line is how a checker starts lying about what it checks | code (test quality) | low | fix-now | fold (line-free location prose + the double-annotate byte-identity outcome case added to the provenance suite) · fold d9545d8 | yes |

| id | file:line | axis | severity | class | route | folded |
|---|---|---|---|---|---|---|
| REVIEW-RAN | HEAD d9545d8c63cf52103f02103bbf7a6a24752f5ead | n/a | n/a | review-mark | n/a | n/a |

The mark row is the durable `review-mark@1` record of the fifth cycle's re-review at
that head — context-clean axes (code PASS, verify PASS over nine re-run commands,
security PASS, perf no-fix-now; independence: fresh RLM workers, each handed only its
axis checklist and the diff, none carrying the authoring session). It says a review
ran against this revision, never that the candidate passed; the PASS verdict and its
SHA-bound PR comment are the receipt: **F23 flips in this commit** (its fold IS this
mark plus the comment; the annotator can bind it honestly), **F24 flips in the commit
that flips O12** (the row's remaining evidence is that status cell, so its token must
name the commit that writes it), and **F25 stays `no`** — closed on evidence, kept open
on provenance, because the only rank-3 commit the annotator can see for it predates the
row and merely touches the cited `SPEC.md`; its cell says so rather than accepting a
token that would lie. F59–F61 already carry their `· fold d9545d8` tokens from the
post-fold annotate.

Sixth review-change cycle ran 2026-09-02 (`--adversarial 2` at head `f535a870`, the terminal recount head): two context-clean, diff-only adversarial reviewers in isolated git worktrees — R1 correctness/logic adversary + R2 security/inputs adversary, both on the session model family (disclosed: same-family, different prompt roles), each handed only the scope, the four applicable finder checklists (code, security, verify, perf + inline API-ergonomics) and Step 0 docs; they re-ran the green gates live (check-skill-context exit 0, schema 675/675, gate:pre-execution exit 0, Pi 134/134, root 188/188, bench p95 32.69 ms ≤ 100 ms). Synthesis fused 14 raw rows → 12 reviewer rows (2 dedupe collapses: the fence-toggle defect found by both reviewers on the same line; the dead disjunct found by both) + 2 orchestrator workflow-discipline rows. Classification: 11 fix-now (rows below), S8 ignore (the `.claude/skills` self-mount is the agreed follow-up issue's owner, not a per-skill reinvention), S9 proposal (pre-existing bun-runner ordering failure on main, batched for the user, D3), W2 ignore (format deviation inside pushed history; debris already folded via F27). Debt transform: 4 debt items with triggers (template/ two-source duplication; the 3.5.0 pre-publication sync-compare window; the self-mount re-point; the bun-fixture trigger), 7 outright defects the fold pays, 3 n/a. One verify flag: F70's premise is disputed by the debt pass's cross-check (`skills/review-plan/references/OUTPUT.md:17` may already emit the field) — settled at the fold. Decision: **REVIEW-FAIL** — 11 fix-now open; no passing receipt posted (the fifth cycle's marker at `d9545d8c` stays; the fold re-runs the review at the head its repairs create).

| id | file:line | axis | severity | class | route | folded |
|---|---|---|---|---|---|---|
| F62 | docs/features/_TEMPLATE/LEDGERS.md:1 (template/docs/{features,fix}/_TEMPLATE/ tree) | code | major | fix-now | fold: sync the exportable template mirror with the live _TEMPLATE rewrite (add the LEDGERS.md copies, re-sync the SPEC.md normative sections); debt trigger recorded — any future edit to either copy re-drifts them, so single-source template/ from _TEMPLATE/ or add a parity check | yes |
| F63 | skills/audit-pr/references/02_CLOSURE_AND_SCOPE_GATES.md:97 | code | minor | fix-now | fold: rewrite the stranded allow/forbid rule out of the verify-command parenthesis so the parent-receipt clause parses unaided | yes |
| F64 | packages/agentic-workflow-schema/src/pre-execution.ts:417-421 | code/security | minor | fix-now | fold: record the opening fence char and require a matching close in selectSpecProduct (CommonMark); add the missing mixed-fence test | yes |
| F65 | packages/agentic-workflow-schema/src/pre-execution.ts:522 | code | minor | fix-now | fold: emit the wrong-KIND refusal pointer at /artifacts/<i>/kind so drivers repair the offending field | yes |
| F66 | packages/agentic-workflow-schema/src/pre-execution.ts:703 | code | minor | fix-now | fold: thread selected.errors into the invalid-selector diagnostic or fix the docstring that promises it | no |
| F67 | packages/agentic-workflow-schema/src/pre-execution.ts:834 | code (API ergonomics) | minor | fix-now | fold: align the new family's digest compare with the existing sha256HexSync core before 3.5.0 publishes (debt trigger: publication — post-publish it is a breaking change) | no |
| F68 | scripts/pre-execution-snapshot.mjs:455 | code | minor | fix-now | fold: delete the dead disjunct (byte-identical to the first clause after the prefix strip) or restore the intended prefix-tolerant compare | no |
| F69 | skills/review-spec/references/CHECKS.md:47 (+review-plan:55 + Pi mirrors) | verify/code | major | fix-now | fold: fix the documented canonical recipe to a repo-relative output path (or drop --json; stdout already prints the digest) so a verbatim-following executor gets a digest instead of a refusal | yes |
| F70 | scripts/pre-execution-snapshot.mjs:284 | code | minor | fix-now | fold: add Unit kind: to both OUTPUT.md receipt templates or drop the parse — verify flag: review-plan/references/OUTPUT.md:17 may already emit it, settle at the fold | no |
| F71 | packages/agentic-workflow-schema/src/verification-contract.ts:121 | code/security | minor | fix-now | fold: validate family-pinned codes against the closed diagnostic vocabulary at the sink (declaration- or module-init-time) | no |
| F72 | .gitignore:14 (untracked .claude/pre-execution-gates/ + .rlm/) | workflow | low | fix-now | fold: extend .gitignore with the two runtime toolstate dirs alongside the .engram/ hunk this diff already adds (recurring F27 debris class) | no |
