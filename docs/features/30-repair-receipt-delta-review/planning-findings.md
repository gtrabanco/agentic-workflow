# Planning findings — 30-repair-receipt-delta-review

One stage-aware table, both stages; reviewers append rows, only the stage's
author resolves. Row shape per `pre-execution-review/references/LEDGERS.md`:
`finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision`

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| F1 | spec | info | product | 42c0091e965f62f27bacfd4bae73dbc9f8a9610725092e2fea9a5bb39728b6d3 | The SPEC deviates from governing issue #170's receipt design (issue: receipt "persisted with the ledger tick", batch-class vocabulary `behavioral-high\|behavioral-med\|docs-only`, three route values) without a dated amendment on the issue; the deviation is recorded in-SPEC as user-accepted decisions D30-1/D30-3 and adds a fourth branch (`REPLAN-ROUTE`) the issue's non-goal "no new vocabulary beyond the receipt block and the three route values" does not include | SPEC.md IS-2/IS-3 + Out of scope; decisions.md D30-1/D30-3 2026-09-07; forge issue #170 body §1 "persisted with the ledger tick" and §Non-goals | resolved | Dated deviation note added to SPEC.md §"Deviation from issue #170 (2026-09-07)": all three deviations (impact rule, printed block, REPLAN-ROUTE) documented with rationale; user-accepted status confirmed (D30-1/D30-3) | — |
| F2 | spec | info | product | 42c0091e965f62f27bacfd4bae73dbc9f8a9610725092e2fea9a5bb39728b6d3 | Expectation row 13's pointer (AC-10) does not itself assert that the REPAIR-RECEIPT survives the handoff to the outer driver unchanged; the guarantee is carried in effect by AC-01's verbatim text pins of the fixed block, consistent with the repo's wording-pin convention | SPEC.md `### Expectation sweep` row 13 vs AC-01/AC-10; decisions.md D30-3 (printed block) | resolved | Expectation row 13 pointer updated: AC-01 (verbatim block pins ensure the fixed text is emitted verbatim at every emission point; D30-3: no ledger row or schema field is written for it, so an outer driver reading stdout gets the exact same text on every run) | — |
| P30-1 | plan | medium | plan | 92f676a52de5fbab747f83846b52170b1358b4a0e380d1ec168558d9c5fdd6f4 | Obligation rows O8 and O12 each name two phases and two tasks (`P1 + P3`, `P2 + P4`), violating the obligation-ledger row contract ("exactly one phase and one task; a row needing two phases means the phase cut is wrong"); `execute-phase` reads its phase slice by the phase column, so a two-phase row is ambiguous at execution time (L4) | planning-obligations.md rows O8, O12; `skills/pre-execution-review/references/LEDGERS.md` §2 `phase`/`task` column contract | resolved | O8 split into O8 (P1, FOLD_POLICY materialize-reproducer pin) + O13 (P3, LEDGERS consumption sentence + unchanged contract pins); O12 split into O12 (P2, delta-cap + receipt-trail pins) + O14 (P4, manual-path/outer-driver receipt consumption). Every obligation row now names exactly one phase and one task; ids stable, new ids appended; ledger closure mapping updated (planning-obligations.md header + §Closure) | 30-plan-2 |
| P30-2 | plan | medium | plan | 92f676a52de5fbab747f83846b52170b1358b4a0e380d1ec168558d9c5fdd6f4 | The branch-selection decision inputs are unpinned and unscenariod (L3 + L5 + P11): AC-01/AC-03 pin the receipt block, field list, branch literals and the default line, but no pin asserts the docs-only test (E-D3), the frozen-severity-`high` override, or the SKIPPED-requires-prior-consumer-decision rule (E-D2), and testing.md's scenario inventory has no docs-only-batch (OPTIONAL selection) or SKIPPED-without-decision row — the discipline suite stays green while `fold-findings` omits IS-3's materiality condition entirely, so AC-03's validator can pass for the wrong reason | ACCEPTANCE.md AC-01/AC-03 vs SPEC.md §Design §3 (branch table) + IS-3; testing.md §Mandatory scenario inventory; decisions.md E-D2/E-D3 | resolved | Branch-selection decision inputs pinned end to end: AC-03 manifest outcome widened with the three decision-input pins (ACCEPTANCE.md re-freeze note names the repair; SPEC Product half NOT edited — pin coverage lives in plan-stage artifacts, projection digest unchanged 17fce82b…); O3 widened in the obligation ledger; P1 red-first pin + closing-block tasks and done-when (TASKS.md, PLAN.md, SPEC phase block) name E-D2/E-D3; testing.md gains a Branch selection ladder row + fold:docs-only-batch and fold:skipped-without-decision scenarios (mirrored in the SPEC Dev scenarios) | 30-plan-2 |
| P30-3 | plan | info | environment | 92f676a52de5fbab747f83846b52170b1358b4a0e380d1ec168558d9c5fdd6f4 | The parent spec receipt re-verified standalone reports `stale-source-revision` because the plan commit's Engineering-half append rotated the SPEC.md whole-file contentRevision; the Product projection bytes are unchanged (`spec-product-v1` digest `17fce82b…` identical at `9a0e6f8f` and HEAD; recorded parent digest `42c0091e` reproduces exactly from current bytes) — L1 holds on the bytes; a downstream spec-stage verify that fires this code should be judged by the projection evidence, not auto-routed to `review-spec` | `selectSpecProduct` digests at 9a0e6f8f/HEAD; `pre-execution-snapshot.mjs verify --stage spec` output; parent rebuild reproducing 42c0091e | resolved | Expected behavior: the plan receipt's L1 line holds because the Product projection (`17fce82b…`) is byte-identical at both revisions; a downstream spec verify reporting `stale-source-revision` is a limitation of the whole-file digest comparator, not a validity defect. The plan-review-pass receipt rp-30-20260907-002 remains valid. The deviation note (F1 resolution) in SPEC.md also documents the Engineering-half append mechanism. | — |
| P30-4 | plan | info | plan | 92f676a52de5fbab747f83846b52170b1358b4a0e380d1ec168558d9c5fdd6f4 | PE-011's wording overstates: `docs/workflow/SKILL_CONTEXT_BUDGETS.json` `skills` carries entries only for `review-change` and `pre-execution-review`; `fold-findings` rides the `defaults` budget, so growth there means adding a declared entry with source `feature 30`, not re-measuring an existing one (the check stays fail-closed, so O9 remains deliverable) | `docs/workflow/SKILL_CONTEXT_BUDGETS.json` `skills` keys; `scripts/check-skill-context.mjs` line 161 (defaults spread) | resolved | fold-findings now has a declared entry in the working-copy `SKILL_CONTEXT_BUDGETS.json` with source `feature 30` (added by P4 qualification re-measure). At the time this finding was cut (artifact revision 30-plan-2), fold-findings was indeed using the `defaults` spread — the working-copy was updated afterward. The check stays fail-closed so O9 remains deliverable. | — |
