# Planning evidence — 31-planning-review-materiality

Compact source-backed conclusions used to cut this Engineering plan (artifact
revision **`31-plan-3`**, the D-31-6 code-carrier re-cut). This is not an
exploration transcript. All rows were observed at branch head `4b7cad56` unless
a row states otherwise. Row shape and column order are owned by
`skills/evidence-grounding/references/ROWS.md` §"Plan-stage table — one declared
extension"; the two Product-stage homes this plan reads for the wording-only
determination are named in `skills/pre-execution-review/references/LEDGERS.md`
§1.

Parent: Product receipt `spec-review-31-9` @ snapshot
`e15374a3863aedd968a9600b5bec280f4648874d82a069b72b2abfa4fb30a507`, re-verified
fresh with `pre-execution-snapshot.mjs verify --stage spec` at this scaffold's
Product-review gate (PE-002).

| id | claim-or-obligation | authority-kind | source-and-location | observed-revision | affected-decision-or-obligation | freshness | status | owner-or-next-evidence |
|---|---|---|---|---|---|---|---|---|
| PE-001 | The carrier ruling moves the materiality predicate, the two-cycle cap and the wording-only route into code — schema finding record, verify's codes, the decider's refusal, phase-lint — and the `31-plan-1/2` plan set is re-cut, never repaired | document | `docs/features/31-planning-review-materiality/SPEC.md` §Scope items 1–4 and §Out of scope bullet 5; `decisions.md` D-31-6 and §"Owner ruling: redesign + replan" | `4b7cad56` | the whole phase cut (P1–P5); E-D31-8…E-D31-14 | current | proven | re-read before each phase cut (`PLAN.md`) |
| PE-002 | The Product half is designed and carries a current independent review: `spec-review-31-9` returned `spec-review-pass`, 14/14 checks, zero findings, and the CLI re-derives the same digest from the bytes on disk | repository | `docs/features/31-planning-review-materiality/progress.md` (receipt block `spec-review-31-9`); `node scripts/pre-execution-snapshot.mjs verify --stage spec --unit 31-planning-review-materiality` → `current: true`, `structural.fresh: true` | `4b7cad56` | the plan's parent binding; readiness box 1 | current | proven | re-run before the plan review (`PLAN.md` header) |
| PE-003 | The `spec-product-v1` selector projects the SPEC only up to the first level-1/2 heading after `## Design status`, so `## Engineering half` and `## Amendments` sit outside it: the re-cut cannot move a reviewed Product byte | repository | `packages/agentic-workflow-schema/src/pre-execution.ts:482-556` (the selector and its boundary scan); post-write `build --stage spec --unit 31-planning-review-materiality` → `e15374a3863aedd968a9600b5bec280f4648874d82a069b72b2abfa4fb30a507`, unchanged | `4b7cad56` | AC13's workflow-mutated group; R31-01's resolution | current | proven | re-derive the digest after the SPEC write |
| PE-004 | The severity vocabulary comment still reads "`info` is the only immaterial one", and the `severity` field description repeats it | repository | `packages/agentic-workflow-schema/src/pre-execution-contract.ts:101`, `:459` | `4b7cad56` | O2; AC2 | current | proven | rewrite both in P1, then `grep -rn "the only immaterial" packages/agentic-workflow-schema/src/` exits non-zero |
| PE-005 | The runtime materiality predicate is `const material = finding.severity !== "info"`, and the doc comment above the binding entry documents the same line | repository | `packages/agentic-workflow-schema/src/pre-execution.ts:1059`, `:998` | `4b7cad56` | O1; AC1 | current | proven | replace with the `medium`+ membership test in P1 |
| PE-006 | `FINDING_SPEC` is the one field list the record's structure is walked from, and `VerificationFieldSpec.maxLength` is optional — so a bound-less `reproducer` declaration would still pass the structural walk and needs its own anchor | repository | `packages/agentic-workflow-schema/src/pre-execution-contract.ts:455-500`; `packages/agentic-workflow-schema/src/verification-contract.ts:51` | `4b7cad56` | O3; AC3 | current | proven | declare `maxLength` on the field entry and pin it with `grep -A8 'key: "reproducer"' … \| grep -c "maxLength"` |
| PE-007 | `PRE_EXECUTION_LIMITS` publishes every bound the contracts enforce, so the `reproducer` bound belongs there rather than as a literal in the field | repository | `packages/agentic-workflow-schema/src/pre-execution-contract.ts:145-170` (`claimChars`, `evidenceChars`, …) | `4b7cad56` | O3; E-D31-9 | current | proven | add `reproducerChars` in P1 |
| PE-008 | The two Draft-07 projections are written by the package's own generator (write mode by default, `--check` is the drift gate) and the package gate chains the drift check, the package check and the docs test | repository | `packages/agentic-workflow-schema/scripts/generate-pre-execution-schemas.mjs` (usage header); `packages/agentic-workflow-schema/package.json` scripts `gate:pre-execution`, `check:pre-execution-schemas` | `4b7cad56` | O3, O9; AC3, AC9 | current | proven | regenerate in P1; run the gate in P5 |
| PE-009 | The schema package is at 4.2.0 and its newest CHANGELOG companion-package row is 4.2.0, so the additive minor needs a new row in that table | repository | `packages/agentic-workflow-schema/package.json` (`version`); `CHANGELOG.md` §"Companion npm packages" (`#### @gtrabanco/agentic-workflow-schema` table) | `4b7cad56` | O9, O14; AC9, AC14 | current | proven | bump to 4.3.0 in P1 and add the row in P4 |
| PE-010 | The decider's stop vocabulary is closed at six values, its input type is `WorkflowDecisionInput`, and `decideWorkflowAction` is pure and fail-closed — so the cap refusal is one added code plus one added input field | repository | `packages/agentic-workflow-schema/src/index.ts:719-727`, `:746-756`, `:1069-1200` | `4b7cad56` | O5; AC5; E-D31-10 | current | proven | implement in P2 and pin the untouched rows |
| PE-011 | The `review-spec` and `review-plan` transition rows already allow `design-feature`, and the receipt block itself is not a `normative-surfaces@1` row — the declared rows are the CLI's `contract` fenced output, the verdict vocabularies and the snapshot commands | repository | `packages/agentic-workflow-schema/src/index.ts:885-925`; `CLAUDE.md` block `normative-surfaces@1`; block `rendered-facts@1` | `4b7cad56` | O5; AC5; E-D31-11 | current | proven | name the human route in `detail`; reuse the record-block family for the determination |
| PE-012 | The closed ten freshness codes, the comparator's precedence and the `stale-artifact-content` answer for moved bound artifact bytes are all machine-owned, and `verify` exits 3 with no receipt and 4 when the receipt is not current | repository | `packages/agentic-workflow-schema/src/pre-execution.ts:159-171`; `scripts/pre-execution-snapshot.mjs:331-420` (`attributeFreshness`), `:442`, `:487` | `4b7cad56` | O4; AC4; E-D31-12 | current | proven | branch inside `attributeFreshness`; add no code, no flag, no exit code |
| PE-013 | The plan snapshot binds `ACCEPTANCE.md` as an artifact row and the three governing context authorities, so "acceptance fingerprint and bound authorities unmoved" is computable from surfaces the snapshot already carries | repository | `scripts/pre-execution-contract.mjs:33-75` (`STAGE_ARTIFACTS.plan`, `CONTEXT_SOURCES`) | `4b7cad56` | O4; AC4 | current | proven | fingerprint the manifest with `git hash-object`; compare `changedContexts` |
| PE-014 | Exactly one receipt parser exists and both the CLI and the sensor import it, so the determination parser belongs in the same dependency-free module | repository | `scripts/pre-execution-contract.mjs:120-160` (`parseReceipts`), imported by `scripts/pre-execution-snapshot.mjs` and `scripts/workflow-status.mjs` | `4b7cad56` | O4; AC4 | current | proven | add `parseWordingOnlyDeterminations` there in P3 |
| PE-015 | The sensor assembles the decider's input and already reads the unit's receipt blocks; its suite is part of the AC10 pack | repository | `scripts/workflow-status.mjs`; `scripts/workflow-status-pre-execution.test.mjs`; `ACCEPTANCE.md` AC10 | `4b7cad56` | O5, O11; AC5, AC10 | current | proven | derive the count in P2 and extend the sensor suite |
| PE-016 | The discipline suite reads prose surfaces today and carries no planning pin block — AC8 requires the re-aimed pins to read the schema package | repository | `scripts/review-loop-discipline.test.mjs:20-28`, `:134`, `:275-336`; `ACCEPTANCE.md` AC8 | `4b7cad56` | O8; AC8; E-D31-4 | current | proven | re-aim in P3 without removing an assertion |
| PE-017 | Current skill versions are `pre-execution-review` 2.2.1, `review-spec` 1.7.1, `review-plan` 1.6.1 and `design-feature` 3.4.0 — each takes one minor bump | repository | the four `skills/*/SKILL.md` frontmatters; `CHANGELOG.md` per-skill tables | `4b7cad56` | O14; AC14; E-D31-1 | current | proven | run `bump-skill` once per skill in P4 |
| PE-018 | The Pi mirror is written only by the bundler script the package owns, run from the package root; parity is enforced by the package's own suite, and there is no root `package.json` | repository | `packages/pi-agentic-workflow/package.json` script `bundle:skills` (`bun scripts/bundle-skills.mjs`); `packages/pi-agentic-workflow/test/skill-parity.test.mjs`; absence of a root `package.json` | `4b7cad56` | O12; AC11; E-D31-6 | current | proven | bundle from the package root in P5 after the last skill edit |
| PE-019 | Context budgets live in one manifest and one gate; the prose shrink removes bytes, so the posted ceilings keep holding unless a re-measure says otherwise | repository | `docs/workflow/SKILL_CONTEXT_BUDGETS.json`; `scripts/check-skill-context.mjs`; `CLAUDE.md` §Verification | `4b7cad56` | O11; AC10 | current | proven | run the gate in P4 and P5 |
| PE-020 | Every AC7 removal fragment exists today with its sentence standing (so each grep discriminates), and the kept-side fragment `third cycle never` is absent today — the shrink authors it | repository | `skills/design-feature/references/REPAIR.md:64`, `:71`; `skills/pre-execution-review/references/POLICY.md:42`, `:61`, `:82`, `:83`; `skills/review-spec/references/CHECKS.md:104`; `skills/review-plan/references/CHECKS.md:105`; `skills/review-spec/references/OUTPUT.md:109,153`; `skills/review-plan/references/OUTPUT.md:118,160`; `skills/pre-execution-review/references/LEDGERS.md:93`; `grep -n "third cycle never" skills/pre-execution-review/references/POLICY.md` → non-zero today | `4b7cad56` | O7; AC7; E8 | current | proven | rewrite in P4 and re-run the greps in P5 |
| PE-021 | Roadmap row 31 reads `defined`, its hard dependency 29 reads `done · #175` (merged), no other row claims 31, and rows 32/35/42/46 chain after it | repository | `docs/features/ROADMAP.md` rows 29–35, 42, 46 | `4b7cad56` | the roadmap write; readiness box 8 | current | proven | flip `defined → planned` this turn and re-read the row |
| PE-022 | The NRS is `frozen`; F010 declares no project invariants, so the formal classification is `n/a`, and AD-008 is reconciled by D-31-5 rather than contradicted | ledger | `docs/workflow/REPOSITORY_STATE.md` Snapshot status, F010, AD-008; `decisions.md` D-31-5 | `4b7cad56` | O15; architecture classification; E-D31-3 | current | decision | record `preserves` for AD-008 in the SPEC's Architecture impact |
| PE-023 | `CLAUDE.md`'s parsed `normalizer-inventory@1` row names the bundler step and is not editable by this unit, while the invocation form is owned by the package manifest — so the package-root command form is the one the ladder uses | repository | `CLAUDE.md` block `normalizer-inventory@1` (`bun run bundle:skills` step); `scripts/pre-execution-quality.test.mjs` (parses that block) | `4b7cad56` | O12; AC11; E-D31-6 | current | proven | keep the short step name; use the package-root form in P5 |
| PE-024 | Feature 38 pins `decideWorkflowAction()` consumer-side and its suite is outside the AC10 gate pack — so the cap count must reach the decider through the envelope, never by a sensor reference | repository | `scripts/workflow-status-sensor.test.mjs:268-269`; `docs/features/38-workflow-status-sensor-script/SPEC.md:306`; `docs/features/38-workflow-status-sensor-script/architecture-notes.md:66-67`; the superseded `ACCEPTANCE.md` AC10 (no sensor suite) | `7ace8dbc` | O5; AC5, AC10; E-D31-17 | current | proven | project the count in P2; pin the projection and A:12 in the already-packed discipline suite (P3) |
| PE-025 | The envelope's `detail` member is an open object, so `detail.review_loop_cycles` adds no field and moves no closed set; `sensor-envelope-fields` is `must-name: no` | repository | `packages/agentic-workflow-schema/envelope.schema.json:220`; `CLAUDE.md` block `normative-surfaces@1` | `7ace8dbc` | O5; AC5; E-D31-17 | current | proven | project the count in P2; `normative-drift` stays green |
| PE-026 | `progress.md` is not a bound artifact of either stage's snapshot, so an agent-written record block there survives its own write — the review receipts and `## Dependency receipt v1` are the standing proof | repository | `scripts/pre-execution-contract.mjs:33-75` (`STAGE_ARTIFACTS`); `scripts/pre-execution-snapshot.mjs` `receipts()`; `skills/execute-phase/references/PREFLIGHT.md:50` | `7ace8dbc` | O4; AC4; E-D31-15 | current | proven | E5's home moves to `progress.md`; P3 pins it |
| PE-027 | `attributeFreshness`'s documented precedence puts `stale-source-revision` before `stale-artifact-content`, and `sourceRevision` derives from the same bound paths a wording-only repair moves — so the branch must sit before `stale-source-revision` | repository | `scripts/pre-execution-snapshot.mjs` `attributeFreshness()`; `scripts/pre-execution-attribution.test.mjs:77-110` | `7ace8dbc` | O4; AC4; E-D31-16 | current | proven | branch placement and pure inputs in P3 |
| PE-028 | `normative-drift` recomputes the CHANGELOG companion table against `package.json`, so a version bump without its row is a red gate | repository | `scripts/normative-drift.test.mjs:708-715`; `CLAUDE.md` block `rendered-facts@1`; observed `bun test scripts/normative-drift.test.mjs` → 0 fail at `7ace8dbc` (baseline green) | `7ace8dbc` | O9; AC9, AC10; E-D31-18 | current | proven | the CHANGELOG row lands in P1 with the bump |
| PE-029 | The frozen evidence ledger carries 23 rows at artifact revision `31-plan-3` while the Engineering half claimed 21 | repository | `docs/features/31-planning-review-materiality/planning-evidence.md` (23 `| PE-` rows); `testing.md:47` (cites `PE-023`) | `7ace8dbc` | the evidence-range claim; E-D31-19 | current | proven | correct the range in the Engineering half (P31-05) |
