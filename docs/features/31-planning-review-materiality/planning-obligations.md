# Planning obligations — 31-planning-review-materiality

One row per acceptance criterion (AC1…AC14) from the frozen `ACCEPTANCE.md`,
plus O14 for the AD-008 compatibility invariant that no single AC names alone.
Row shape and column order per `pre-execution-review/references/LEDGERS.md` §2.
Status vocabulary: `planned | in-progress | verified | n/a | deferred` — every
row starts `planned`; no row is `deferred` (no obligation is exported to a
follow-up issue).

| obligation-id | Authority source | Affected use case or invariant | Phase | Task | Implementation owner | Validator | Required evidence | Status |
|---|---|---|---|---|---|---|---|---|
| O1 | AC1 | A `low` planning finding is a persisted, visible, non-blocking report-note — never a re-review trigger by itself, resolvable by the stage author without a re-review; the old "only immaterial" sentence is gone | P1 | Move the planning materiality line to report-note semantics | execute-phase | `grep -n "only immaterial" skills/pre-execution-review/references/LEDGERS.md` non-zero + `grep -niE "report-note" …` zero with the rule + the report-note row of the planning pin table (AC14) | suite exit 0 with the LEDGERS report-note pin green + `PLANNING_PIN_FLOOR = 4` at the P1 head | planned |
| O2 | AC2 | Both stage CHECKS files state material = `medium`+; the "anything above `info`" floor is gone from both | P1 | Move the planning materiality line to report-note semantics | execute-phase | `grep -n "anything above" skills/review-spec/references/CHECKS.md skills/review-plan/references/CHECKS.md` non-zero + the new `medium` materiality sentence present in each + both CHECKS rows of the planning pin table (AC14) | suite exit 0 with the CHECKS pins green + `PLANNING_PIN_FLOOR = 4`; both grep outcomes recorded | planned |
| O3 | AC3 | Anti-deflation carried verbatim: a real defect mislabeled `low` classifies at `medium` minimum, and deflating a severity to dodge a review is itself a review defect — stated in the planning-side surfaces | P1 | Move the planning materiality line to report-note semantics | execute-phase | `grep -rn "medium\` minimum" skills/pre-execution-review/references/ skills/review-spec/ skills/review-plan/` exit 0 with the deflation-is-a-defect text in the match | matched lines recorded; discipline suite exit 0 | planned |
| O4 | AC4 | Hard two-cycle cap in POLICY §4: `third cycle never` starts without explicit user instruction; an unconverged loop ends in `NEEDS-DESIGN` routed to the human; the `CONVERGENCE-ANOMALY` block text is byte-unchanged | P2 | End the planning repair loop at a hard two-cycle cap | execute-phase | `grep -n "third cycle never" skills/pre-execution-review/references/POLICY.md` exit 0 + read-verified NEEDS-DESIGN end + read-verified unchanged block against the §4 diff hunk + the cap row of the planning pin table (AC14) | grep line + §4 diff hunk read at qualification + `PLANNING_PIN_FLOOR = 8` at the P2 head | planned |
| O5 | AC5 | `REPAIR.md` §4 mirror: "More cycles stay allowed" is gone; the cap mirror (`third cycle never`) is present | P2 | End the planning repair loop at a hard two-cycle cap | execute-phase | `grep -n "More cycles stay allowed" skills/design-feature/references/REPAIR.md` non-zero + `grep -n "third cycle never" skills/design-feature/references/REPAIR.md` exit 0 | both grep outcomes recorded | planned |
| O6 | AC6 | POLICY §3 wording-only route: a recorded wording-only determination routes a cosmetic batch without the full snapshot re-review; the determination lands in the unit's frozen evidence and `artifactRevisionId` still rotates | P3 | Route wording-only repairs past the full snapshot re-review | execute-phase | read-verified §3 text, anchored by `grep -n "Wording-only" skills/pre-execution-review/references/POLICY.md` exit 0 + the wording-only row of the planning pin table (AC14) | pin green + `PLANNING_PIN_FLOOR = 9` at the P3 head + §3 hunk read at qualification | planned |
| O7 | AC7 | Verdict-side mirror: both `OUTPUT.md` files carry the cap (`third cycle never`); receipt-literal lines and verdict grammar stay byte-identical | P2 | End the planning repair loop at a hard two-cycle cap | execute-phase | `grep -n "third cycle never" skills/review-spec/references/OUTPUT.md skills/review-plan/references/OUTPUT.md` exit 0 + normative-drift green + both mirror rows of the planning pin table (AC14) | grep lines recorded; suite exit 0 | planned |
| O8 | AC8 | Diff scope: POLICY hunks touch only §3/§4 (§1, §2, §5–§8 byte-identical to `main`); the PR diff lists only the In-scope surfaces plus the declared derived-surface set (the Pi mirror under `packages/pi-agentic-workflow/skills/**`, the four edited `version:` lines, the README skill-table cells, `CHANGELOG.md`) | P4 | Qualify the planning-review-materiality unit | execute-phase | read-verified: `git diff main...HEAD -- skills/pre-execution-review/references/POLICY.md` hunk walk + `git diff --name-only main...HEAD` against the In-scope enumeration and the derived set | hunk walk + per-derived-path attribution recorded at the PR head | planned |
| O9 | AC9 | Discipline suite green at the PR head and no existing assertion removed or weakened (additions or equal-strength rewrites only) | P4 | Qualify the planning-review-materiality unit | execute-phase | `bun test scripts/review-loop-discipline.test.mjs` exit 0 (the pin table's floor + discrimination legs green, AC14) + read-verified no-weakening walk of `git diff main...HEAD -- scripts/review-loop-discipline.test.mjs` | suite exit 0 + diff walk recorded | planned |
| O10 | AC10 | Negative integration: the schema package is untouched and green | P4 | Qualify the planning-review-materiality unit | execute-phase | `git diff --name-only main...HEAD -- packages/agentic-workflow-schema` empty + `cd packages/agentic-workflow-schema && bun run test` exit 0 | empty diff listing + suite exit line | planned |
| O11 | AC11 | Health gates: ledger truth classes, quality, provenance, normative drift, and context budgets stay green after the edits and bumps | P4 | Qualify the planning-review-materiality unit | execute-phase | `bun test scripts/ledger-ownership.test.mjs scripts/pre-execution-quality.test.mjs scripts/ledger-provenance.test.mjs scripts/normative-drift.test.mjs && bun scripts/check-skill-context.mjs` exit 0 + the AC14 presence/discrimination legs green in the same head-level run | all exit codes recorded; budgets manifest re-measured | planned |
| O12 | AC12 | Pi mirror parity: the mirror was re-bundled **from the package that owns the script** (`cd packages/pi-agentic-workflow && bun run bundle:skills`; the bare root form exits non-zero — no root manifest) after the last `skills/` edit, and the package suite passes | P4 | Qualify the planning-review-materiality unit | execute-phase | `cd packages/pi-agentic-workflow && bun run bundle:skills && bun run test` exit 0 | bundle run from the package root + suite exit line | planned |
| O13 | AC13 | Bibliography + versions: the Jin & Chen entry (arXiv:2603.00539) sits under a bottom `## References` section in `README.md` at the PR head, and each of the four edited skills carries its minor bump with a CHANGELOG row | P4 | Qualify the planning-review-materiality unit | execute-phase | `grep -n "2603.00539" README.md` exit 0 + read-verified bump-skill diff walk (four `version:` lines + four CHANGELOG rows) | grep line + diff walk recorded | planned |
| O14 | AD-008 (REPOSITORY_STATE) | Compatibility invariant: correctness stays evidence- and obligation-bound, never cycle-count-bound — the cap stops the loop and routes a human decision (D-31-5); it never auto-continues, never waives findings, and never converts a verdict into a dead end | P2 | End the planning repair loop at a hard two-cycle cap | execute-phase | read-verified §4 cap text against AD-008's wording + the cap row of the planning pin table (the NEEDS-DESIGN end) | §4 hunk read recorded at qualification | planned |
| O15 | AC14 | The planning pins are mechanically present and discriminating: one declared `PLANNING_PIN_TABLE` with the frozen row floor (`PLANNING_PIN_FLOOR = 9` at the PR head) and a discrimination leg proving every pin rejects the sentence it supersedes, so a no-op, truncated, or trivially-true pin set can no longer satisfy AC1–AC4, AC6, AC7, AC9 or AC11 | P1 (table shell + floor 4), P2 (floor 8), P3 (floor 9) | Move the planning materiality line to report-note semantics / End the planning repair loop at a hard two-cycle cap / Route wording-only repairs past the full snapshot re-review | execute-phase | `grep -q "PLANNING_PIN_TABLE" scripts/review-loop-discipline.test.mjs && grep -q "assertDiscriminating(" scripts/review-loop-discipline.test.mjs && grep -q "PLANNING_PIN_FLOOR = 9" scripts/review-loop-discipline.test.mjs && bun test scripts/review-loop-discipline.test.mjs` → exit 0 | the three greps' outcomes + the suite exit line, recorded per phase (4/8/9) | planned |

## Closure

- Every acceptance criterion AC1…AC14 maps to exactly one obligation row:
  AC1→O1 (P1), AC2→O2 (P1), AC3→O3 (P1), AC4→O4 (P2), AC5→O5 (P2),
  AC6→O6 (P3), AC7→O7 (P2), AC8→O8 (P4), AC9→O9 (P4), AC10→O10 (P4),
  AC11→O11 (P4), AC12→O12 (P4), AC13→O13 (P4), AC14→O15 (P1/P2/P3, run
  at the PR head); the AD-008 invariant is O14 (P2). No obligation is
  `deferred` or exported.
- No Product-half artifact is edited to produce this ledger.
- One behaviour appears once: the report-note semantics own O1 (O2/O3 cite the
  CHECKS/anti-deflation restatements, not a second copy of the rule); the cap
  owns O4 (O5/O7 are its two mirrors, cited, not duplicated); the pin table's
  mechanical presence and discrimination own O15 (O1–O4, O6, O7, O9, O11 cite
  it, never restate it).
- The `31-plan-2` repair batch (2026-09-17) re-pointed O8/O12, anchored
  O1/O2/O4/O6/O7/O9/O11 to O15, and added O15 for AC14; no obligation was
  removed or weakened.
