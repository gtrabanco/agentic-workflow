# Planning obligations — 31-planning-review-materiality

One row per acceptance criterion (AC1…AC14) from the frozen `ACCEPTANCE.md`, plus
O15 for the AD-008 compatibility invariant that no single criterion names alone.
Row shape and column order per `pre-execution-review/references/LEDGERS.md` §2.
Status vocabulary: `planned | in-progress | verified | n/a | deferred` — every row
starts `planned`; no row is `deferred` (no obligation is exported to a follow-up
issue).

Reading convention for the `phase` / `task` cells: `task` names the single task
in that phase which owns the criterion's primary deliverable; evidence produced
by a sibling task of the same phase is named in `required-evidence`, never as a
second phase or task. One row, one phase, one task — a row needing two phases
means the phase cut is wrong.

| obligation-id | Authority source | Affected use case or invariant | Phase | Task | Implementation owner | Validator | Required evidence | Status |
|---|---|---|---|---|---|---|---|---|
| O1 | AC1 | The machine materiality predicate is `medium`+ — a PASS with open/unverified `low` rows validates, and the same PASS with an open/unverified `medium` row is refused `verdict-mismatch` | P1 | Change the materiality predicate in `pre-execution.ts` | execute-phase | `grep -n 'severity !== "info"' packages/agentic-workflow-schema/src/pre-execution.ts` exits non-zero + `cd packages/agentic-workflow-schema && bun run test` exits 0 with both readiness vectors green | the grep outcome + the package suite exit line + the two vector names recorded in the P1 phase entry | planned |
| O2 | AC2 | The schema's own prose states the new line: no "only immaterial" phrasing survives, and the severity description carries the `material = `medium`` and `report-note` fragments | P1 | Rewrite the severity prose in `pre-execution-contract.ts` | execute-phase | `grep -rn "the only immaterial" packages/agentic-workflow-schema/src/` exits non-zero + `grep -nE 'material = .medium'` and `grep -n "report-note"` on `packages/agentic-workflow-schema/src/pre-execution-contract.ts` each exit 0 | the three grep outcomes recorded in the P1 phase entry | planned |
| O3 | AC3 | The finding record carries the bounded `reproducer`: the field exists, its bound is declared on the field entry, a suite vector exercises it with back-compatibility and the bound refusal, and the receipt contract id is unchanged | P1 | Add the bounded `reproducer` field to the finding-record spec | execute-phase | `grep -n "reproducer" packages/agentic-workflow-schema/src/pre-execution-contract.ts` exits 0 + `grep -A8 'key: "reproducer"' … \| grep -c "maxLength"` ≥ 1 + `grep -rln "reproducer" packages/agentic-workflow-schema/test/` exits 0 + the receipt contract-id count ≥ 1 | the four anchor outcomes + the bound and back-compatibility vector names from the sibling vectors task, recorded in the P1 phase entry | planned |
| O4 | AC4 | The wording-only route is machine-recorded: a rotated revision plus a recorded determination with no material movement keeps `verify` current, material movement answers `stale-artifact-content`, and a rotation without the record is refused | P3 | Add the wording-only branch to `attributeFreshness` | execute-phase | `bun test scripts/pre-execution-sensor.test.mjs scripts/pre-execution-attribution.test.mjs` exits 0 + `grep -rn "wording-only" scripts/pre-execution-sensor.test.mjs scripts/pre-execution-attribution.test.mjs` exits 0 | the suite exit line, the grep outcome and the three vector semantics walked at the PR head, recorded in the P3 phase entry | planned |
| O5 | AC5 | The orchestrator refuses to advance past the cap: after two consecutive unconverged cycles a `review-spec`/`review-plan` invocation stops with the human route named, a PASS resets the count, and `needs-design` routes to `design-feature` | P2 | Implement the refusal in `decideWorkflowAction` | execute-phase | `cd packages/agentic-workflow-schema && bun run test` exits 0 + `grep -rln "review-loop-cap" packages/agentic-workflow-schema/test/` exits 0 | the package suite exit line + the vector-suite file name + the sensor suite exit line from the sibling sensor task, recorded in the P2 phase entry | planned |
| O6 | AC6 | The plan layer is lint-governed: the unit's plan passes the canonical linter at the PR head and its loop phases carry run-and-paste machine done-whens | P5 | Run the plan-layer linter over the unit's plan | execute-phase | `bun scripts/phase-lint.mjs docs/features/31-planning-review-materiality/PLAN.md` exits 0 with a PASS verdict | the linter's stdout block pasted verbatim in the P5 phase entry, plus the `verify` exit codes recorded there | planned |
| O7 | AC7 | Prose shrinks to the non-computable remainder: all nine declared removal greps exit non-zero, the three kept-side greps exit zero, and the `CONVERGENCE-ANOMALY` block plus the receipt-literal lines are byte-unchanged | P4 | Rewrite §4 of `POLICY.md` | execute-phase | the nine removal greps (non-zero) and the three kept greps (zero) from `ACCEPTANCE.md` AC7 + the byte-unchanged walk | the twelve grep outcomes and the diff-hunk walk recorded in the P4 phase entry, re-run in P5 | planned |
| O8 | AC8 | The discipline pins keep the machine rule honest: the suite passes, reads the schema package, and no existing assertion is removed | P3 | Re-aim the planning-side pins at the code carriers | execute-phase | `bun test scripts/review-loop-discipline.test.mjs` exits 0 + `grep -n "packages/agentic-workflow-schema" scripts/review-loop-discipline.test.mjs` exits 0 + `git diff main -- scripts/review-loop-discipline.test.mjs` removes no assertion | the suite exit line, the grep outcome and the no-weakening walk recorded in the P5 phase entry | planned |
| O9 | AC9 | Additive schema release: the package gate passes, the version is bumped with a CHANGELOG row, and no enum value or contract id is removed | P5 | Verify the additive-release surfaces and the frozen manifest | execute-phase | `cd packages/agentic-workflow-schema && bun run gate:pre-execution` exits 0 + the version/CHANGELOG presence check | the gate exit line + the read-verified vocabulary-diff walk recorded in the P5 phase entry | planned |
| O10 | AC10 | The repo gate pack stays green: ledger truth classes, quality, provenance, normative drift, the sensor suite and the context budgets | P5 | Run the full acceptance ladder | execute-phase | `bun test scripts/ledger-ownership.test.mjs scripts/pre-execution-quality.test.mjs scripts/ledger-provenance.test.mjs scripts/normative-drift.test.mjs scripts/workflow-status-pre-execution.test.mjs && bun scripts/check-skill-context.mjs` exits 0 | every exit line recorded in the P5 phase entry | planned |
| O11 | AC11 | Mirror parity: the Pi mirror is re-bundled from the package that owns the bundler after the last skill edit, and the package suite passes | P5 | Re-bundle the Pi mirror and run the package suite | execute-phase | `cd packages/pi-agentic-workflow && bun run bundle:skills && bun run test` exits 0 | the bundle run from the package root and the suite exit line recorded in the P5 phase entry | planned |
| O12 | AC12 | The bibliography obligation lands in the implementation PR: the Jin & Chen entry sits under a bottom `## References` section of the repository README | P4 | Add the bibliography append and the package CHANGELOG row | execute-phase | `grep -n "2603.00539" README.md` exits 0 | the grep line recorded in the P4 phase entry, re-run in P5 | planned |
| O13 | AC13 | At the PR head every path in the diff belongs to one of the three declared groups, and no path outside them | P5 | Record the read-verified walks | execute-phase | `git diff main --name-only -- . ':(exclude)docs/features/31-planning-review-materiality' ':(exclude)docs/features/ROADMAP.md' ':(exclude)docs/LOGS.md'` lists only first- and second-group paths | the anchored diff listing with per-path attribution recorded in the P5 phase entry | planned |
| O14 | AC14 | The four touched skills are bumped with their release surfaces in the same PR: one removed plus one added `version:` line per skill, each a semver-minor increment, with CHANGELOG rows and accurate README cells | P4 | Run the `bump-skill` procedure for the four touched skills | execute-phase | `git diff main -- skills/pre-execution-review/SKILL.md skills/review-spec/SKILL.md skills/review-plan/SKILL.md skills/design-feature/SKILL.md \| grep -cE '^[+-]version: '` returns ≥ 8 + the CHANGELOG/README walk | the bump-hunk count and the CHANGELOG/README walk recorded in the P4 phase entry | planned |
| O15 | AD-008 (`REPOSITORY_STATE.md`) | Compatibility invariant: correctness stays evidence- and obligation-bound, never cycle-count-bound — the cap stops the loop and routes a human decision (D-31-5); it never auto-continues, never waives findings, and never converts a verdict into a dead end | P2 | Add the cap-refusal stop code | execute-phase | the cap vector suite proves the refusal names the human route and never establishes correctness from the count + the `read-verified` §4 cap text against AD-008's wording | the vector names, the `read-verified` §4 hunk walk and the recorded `preserves` classification in the SPEC's Architecture impact | planned |

## Closure

- Every acceptance criterion AC1…AC14 maps to exactly one obligation row:
  AC1→O1 (P1), AC2→O2 (P1), AC3→O3 (P1), AC4→O4 (P3), AC5→O5 (P2), AC6→O6 (P5),
  AC7→O7 (P4), AC8→O8 (P3), AC9→O9 (P5), AC10→O10 (P5), AC11→O11 (P5),
  AC12→O12 (P4), AC13→O13 (P5), AC14→O14 (P4); the AD-008 invariant is O15 (P2).
  No obligation is `deferred` or exported to a follow-up issue.
- No Product-half artifact is edited to produce this ledger.
- One behaviour appears once: the materiality predicate owns O1 (O2 states the
  matching prose, not a second predicate); the finding record's bound owns O3; the
  cap refusal owns O5 (O15 cites it as the invariant's carrier, not a second copy
  of the rule); the wording-only branch owns O4.
- This ledger is the re-cut for artifact revision `31-plan-3`. The superseded
  `31-plan-2` ledger's O15 (`PLANNING_PIN_FLOOR`, three phases in one row) is
  retired with its carrier, and neither the plan-stage finding R31-02 nor the
  row-span defect it named survives in this cut: every row above names one phase
  and one task.
