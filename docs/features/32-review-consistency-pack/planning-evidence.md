# Planning evidence — 32-review-consistency-pack

Compact source-backed conclusions used to cut this Engineering plan. This is not
an exploration transcript. Row shape and column order per
`evidence-grounding/references/ROWS.md` (§ Plan-stage table). All rows observed
at HEAD `2a87c5cd` (branch `feat/32-review-consistency-pack`) unless a row says
otherwise.

Planning baseline: HEAD `2a87c5cd` (tree clean except the uncommitted
`progress.md` receipt block, which the plan snapshot does not bind); the
governing issue `#172` and dependency rows 30/31 were read live by the preceding
Product review (`SPEC-REVIEW-32-4`) and are re-checked at execution.

| id | claim-or-obligation | authority-kind | source-and-location | observed-revision | affected-decision-or-obligation | freshness | status | owner-or-next-evidence |
|---|---|---|---|---|---|---|---|---|
| PE-001 | Three surfaces claim the folded flip (`PERSIST_AND_DECIDE.md:28`, `FOLDING.md:26`, `fold-findings/SKILL.md:153`) while the machine map names exactly one annotator, `fold-findings:folded-flag` | document | `skills/review-change/references/PERSIST_AND_DECIDE.md:28`; `skills/execute-phase/references/FOLDING.md:26`; `skills/fold-findings/SKILL.md:153`; `skills/pre-execution-review/references/LEDGERS.md:139-146` | 2a87c5cd | O1; AC-01 | current | proven | P2 corrects the two contradicting sentences and map-cites the third |
| PE-002 | The `LEDGERS.md` prose credits `triage-issue` with the `folded:` flag, which contradicts the map row whose rows come from `review-change`/`audit-pr`/`triage-issue` and whose annotator is `fold-findings:folded-flag` | document | `skills/pre-execution-review/references/LEDGERS.md:117` vs `:139-146` | 2a87c5cd | O1; AC-01 | current | proven | P2 rewrites the prose sentence only |
| PE-003 | `skills/review-implementation/references/CLASSIFY.md` is the declared single classifier and owns the ledger severity scale `high \| med \| low`, including the anti-deflation floor (a real defect mislabeled `low` is classified at `med` minimum) | document | `skills/review-implementation/references/CLASSIFY.md:1-24`; `:86-88` (severity legend) | 2a87c5cd | O2, O6; AC-02, AC-03 | current | proven | P3 adds the conversion section here; the floor rule is untouched |
| PE-004 | The finder scale `critical \| major \| minor` is live in exactly nine internal review passes and is converted ad hoc at `PERSIST_AND_DECIDE.md:20-21`; the canonical table must cover it | document | `grep -rn "critical\|major\|minor" skills/ --include=*.md` → the nine pass files; `skills/review-change/references/PERSIST_AND_DECIDE.md:20-21` | 2a87c5cd | O2, O5; AC-02 | current | proven | P3 deletes the ad-hoc mapping and re-points its pin |
| PE-005 | The planning scale `info \| low \| medium \| high \| critical` is already the findings-ledger severity vocabulary and is in live use | document | `skills/pre-execution-review/references/LEDGERS.md:90-92`; `docs/features/37-phase-lint-script/planning-findings.md` row F1 (`info`); `docs/features/59-executable-continuations/planning-findings.md` row F1 (`low`) | 2a87c5cd | O2; AC-02 | current | proven | P3 gives the scale its table row |
| PE-006 | `audit-docs` defines checks 1–14 but its fixed output emits `\| # \| Check (1-13) \|` and `Checks run: <n>/13`, and one finding uses the phantom severity `MEDIUM` where the legend allows `high \| low` only | document | `skills/audit-docs/SKILL.md:47-99` (checks 1–14), `:98` (`MEDIUM`), `:121` (`(1-13)` header), `:125` (`<n>/13`) | 2a87c5cd | O3; AC-02 | current | proven | P3 fixes the header, the count, and the severity |
| PE-007 | `product-audit` classifies with the parallel `fix-now \| postpone \| tradeoff` vocabulary, while the pack's closed class set is owned by `review-implementation` | document | `skills/product-audit/SKILL.md:100,105`; `skills/product-audit/references/AUDIT_PROCESS.md:7`; `skills/review-implementation/CLASSIFY.md:29-41` (closed classes) | 2a87c5cd | O4; AC-02 | current | proven | P3 replaces the vocabulary and maps the example row to `proposal` |
| PE-008 | `audit-pr`'s closure-integrity result reads `pass / blocker / warning / n-a` while the two scales beside it read `pass / blocker / n-a`; separately, `references/04_VERDICT.md` prints a co-occurring closure-warning note that is an addition to a verdict, not a scale value | document | `skills/audit-pr/SKILL.md:52` (closure-integrity scale), `:55` (descope scale), `:58` (invariants scale); `skills/audit-pr/references/04_VERDICT.md:26-27,36` | 2a87c5cd | O12; AC-08 | current | proven | ED-32-5 scopes the pin to the scale lines; P3 aligns `:52` |
| PE-009 | `scripts/workflow-status.mjs` declares `NRS_BLOCKING = Set(["missing","draft","contradicted","resolved"])` and consumes it in three places: the next-command resolver, the run-scoped blocker list, and the top-level state reduction | repository | `scripts/workflow-status.mjs:223` (the set), `:964` (resolver), `:1245` (blocker), `:1261` (state) | 2a87c5cd | O9, O20; AC-05 | current | proven | P1 drops `missing` from the set and adds the notice branch |
| PE-010 | The workflow-status skill references state the same missing/non-frozen rule the sensor implements, so the prose and the script change together | document | `skills/workflow-status/references/ENVELOPE_CORE.md:12`; `skills/workflow-status/references/SENSOR_CORE.md:13-19` | 2a87c5cd | O9; AC-05 | current | proven | P2 aligns the two reference files with the new split |
| PE-011 | `scripts/ledger-ownership.test.mjs` reads the live `ledger-ownership@1` block as the single source of truth, freezes exactly the seven AC16 truth classes, forbids two rows declaring one ledger pattern, and demands the identical owner cell in both `docs/*/_TEMPLATE/LEDGERS.md` projections; the grammar-admitting GATE-RAN encoding is therefore an owner-column extension on the existing `review-findings` row, not a new truth class | repository + ledger | `scripts/ledger-ownership.test.mjs:42-43` (projection paths), `:63-71` (`TRUTH_CLASSES`), `:176-196` (one row per class, unique pattern, owner-cell equality); `docs/features/32-review-consistency-pack/planning-findings.md` row F9's resolution (empirical 18 pass / 0 fail for the owner-column encoding) | 2a87c5cd | O7, O8, O19; AC-04 | current | proven | P4 edits the owner cell in three files in one phase |
| PE-012 | The map's `roadmap` row writer column-sets name `plan-feature-scaffold:planned-row` as the `defined → planned` writer and do not list `plan-feature`, whose Confirm roadmap step reads as a writer | document | `skills/pre-execution-review/references/LEDGERS.md:146` (roadmap row); `skills/plan-feature/SKILL.md:92-94`; `skills/plan-feature-scaffold/SKILL.md:110` | 2a87c5cd | O11; AC-07 | current | proven | P2 splits verify from write; no map change is needed |
| PE-013 | `review-change`'s relationship text mentions `triage-issue` only for the independent-proposals route; its three modes are not named | document | `skills/review-change/SKILL.md:44,153`; `skills/review-change/references/PERSIST_AND_DECIDE.md:37`; `skills/review-change/references/OUTPUT_AND_GUARDRAILS.md:38` | 2a87c5cd | O10; AC-06 | current | proven | P2 names the three modes in the skill and both references |
| PE-014 | `scripts/review-loop-discipline.test.mjs` is the discipline pin home for the review/fold contract text and already asserts the ad-hoc finder mapping — that assertion must be re-pointed at the canonical table, never dropped | repository | `scripts/review-loop-discipline.test.mjs:20-46` (owned files + materiality pins), `:40` (`critical.*high.*major.*med.*minor.*low`) | 2a87c5cd | O1, O2, O5, O6, O10, O11; AC-01…AC-03, AC-06, AC-07 | current | proven | P3 re-points the pin in the same commit that deletes the ad-hoc map |
| PE-015 | The two regression suites that read the changed surfaces are `scripts/bounded-delivery-loops.test.mjs` (plan-feature text + roadmap) and `scripts/audit-pr-receipt.test.mjs` (audit-pr skill + process reference); both must stay green | repository | `scripts/bounded-delivery-loops.test.mjs:20-49`; `scripts/audit-pr-receipt.test.mjs:10-12` | 2a87c5cd | O11, O12, O19; AC-07, AC-08 | current | proven | P3 and P4 run them in their done-when |
| PE-016 | The context-budget manifest carries a per-skill entry for every touched skill and its own declared re-basis (`headroom: 0.1` → `ceil(measured × 1.10)` with the growth source named per entry) | repository | `docs/workflow/SKILL_CONTEXT_BUDGETS.json` (`skills.pre-execution-review.referenceEstimateMax: 3557`, `skills.workflow-status`, `skills.plan-feature`, `skills.review-change`, `skills.execute-phase`, `skills.audit-docs`, plus the route entries); `scripts/check-skill-context.mjs` | 2a87c5cd | O13; AC-09 | current | proven | P4 re-bases after every skill text edit is final |
| PE-017 | Version bumps are manual and same-PR: the `bump-skill` routine rewrites the touched `SKILL.md` `version:`, adds the `CHANGELOG.md` row, and updates the README/SKILLS cells | document | `CLAUDE.md:172-176` (version every change), `:281` (bump-skill row), `:421-423` (invoke and what it rewrites) | 2a87c5cd | O15; AC-11 | current | proven | P4 runs it for the eleven touched skills |
| PE-018 | The Pi mirror is generated output whose parity is pinned: the bundle script writes `packages/pi-agentic-workflow/skills/` and the parity suite requires byte-identity with the source tree | repository | `packages/pi-agentic-workflow/package.json:46-47` (`bundle:skills`, `test`); `packages/pi-agentic-workflow/test/skill-parity.test.mjs` | 2a87c5cd | O20; AC-09 | current | proven | P5 re-bundles after the final skill edit |
| PE-019 | The Normalized Repository State is frozen, records no project architectural-invariants document (F010), and records the English-only interim (F011 / AD-002); `docs/workflow/WORKFLOW_INVARIANTS.md:78-80` carries the applicable workflow rows (review-change authority, the two-cycle bound, audit-pr as sole MERGE-READY emitter) | ledger | `docs/workflow/REPOSITORY_STATE.md` rows F010, F011, AD-002; `docs/workflow/WORKFLOW_INVARIANTS.md:78-80`; absence of `docs/architecture/ARCHITECTURAL_INVARIANTS.md` | 2a87c5cd | preflight Stage 2; O16, O17, O18 | current | proven | classification `n/a: no project invariants declared`; workflow rows classified `preserves` |
| PE-020 | The phase-lint target→layer table maps `scripts/` and `packages/` to `config/infra` and `skills/`, `docs/`, `template/` and `*.md` to `docs`, and box 2 judges each task's first path-like target against the phase's declared layer — a phase cannot straddle the sensor and the contract text | repository | `scripts/phase-lint.mjs:232-237` (`layerForTarget`), `:468-487` (box 2 first-target rule) | 2a87c5cd | ED-32-6 (phase cut) | current | proven | the one `config/infra` phase is P1; the rest are `docs` |
| PE-021 | An appended `gate-ran@1` text mark stays invisible to the two row parsers: the router only reads `\|`-led rows with ≥ 7 cells and treats `VF-`/`REVIEW-RAN` ids as marks, and the provenance annotator matches `^\|\s*(F\d+)\s*\|` only | repository | `scripts/unit-route.mjs:114` (`isMarkRow`), `:128` (`cells.length < 7`); `scripts/ledger-provenance.mjs:33` (`ROW_RE`) | 2a87c5cd | O7, O8; AC-04 | current | proven | no parser is extended; ED-32-3 records the text-mark shape |
| PE-022 | Roadmap row 32 is `defined` with `Depends on: 30 31`; row 30 is `done` and merged (PR #188), row 31 is `idea` (unmet execution dependency), and rows 33/35/50 declare a dependency on 32; the router for this unit reports `status: defined`, `open-rows: 0` | document + forge | `docs/features/ROADMAP.md` rows 30, 31, 32, 33, 35, 50; `node scripts/unit-route.mjs 32` → `status: defined`, `open-rows: 0`, `route: execute` | 2a87c5cd | dependency & blocker check; P4 roadmap row | current | proven | the closing recommendation names the unmet chain; execution re-verifies row 31 |
