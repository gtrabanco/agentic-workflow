# Planning findings — 32-review-consistency-pack

One stage-aware table for this unit (ledger contract:
`pre-execution-review/references/LEDGERS.md` § 3). Reviewers append rows; only the
owning skill resolves them (`design-feature` for `class: product`). Column order is
fixed:

```text
finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision
```

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| F1 | spec | high | product | 5852f381958322e3ab690af196254c4c542672c021e86d2c10e9c03e7cc624a4 | (C10, C11) The half's severity-scale inventory contradicts the repository: IS-2, `## Context` item 2, and D32-2 assert the finder scale `critical\|major\|minor` "does not exist anywhere in the repo at authoring time (E-09)" and deliberately exclude it from the canonical conversion table ("closed over existing scales"), but that scale is live in nine internal review passes and is already converted ad hoc at the exact surface this feature exists to unify — so the shipped table is closed over a set that omits nine producers, E-09's `proven`/`current` row is false, and the deviation is justified by a false premise (issue #172 item 2 states the finder scale "is already mapped" and asks the table to be canonical) | SPEC.md IS-2, `## Context` item 2, `### Evidence rows` E-09, `### Product decisions` D32-2, `### Deferred decisions` DD-1; `grep -rn "critical\|major\|minor" skills/ --include=*.md` → 9 hits (`skills/review-code/SKILL.md:65`, `review-design:54`, `review-a11y:54`, `review-brand:59`, `review-perf:73`, `review-seo:59`, `review-security:59`, `review-debt:53`, `review-verify:59`); `skills/review-change/references/PERSIST_AND_DECIDE.md:20-21` maps `critical`→`high`, `major`→`med`, `minor`→`low`; issue #172 body item 2 (fetched 2026-09-18) | resolved | IS-2, Context item 2, and the D32-2 summary revised; D32-6 appended in decisions.md (supersedes D32-2's scale list, original text preserved): the canonical table covers the finder scale `critical\|major\|minor` emitted by the nine review passes and replaces the ad-hoc mapping at `PERSIST_AND_DECIDE.md:20-21`; E-09 rewritten as a document row (grep → 9 hits, re-verified at 929c6cb2) | 929c6cb28a6319c3014628bb7b7d1c9af7e94f25 |
| F2 | spec | medium | product | 5852f381958322e3ab690af196254c4c542672c021e86d2c10e9c03e7cc624a4 | (C6, C10) The half asserts "The project has no `docs/CAPABILITIES.md` inventory file" (Integration closure intro) and repeats it as a ticked Spec-lint box, then records a seed offer for a file that already exists — so the Integration closure walked a self-derived 17-row inventory instead of the declared inventory `design-feature` is required to reconcile against (roles table + six subsystem rows), and the seed offer duplicates an existing file; feature 59 records the accurate form for this repository ("is the unfilled template in this repository (no live inventory)") | SPEC.md `### Capability closure` §3 ("The project has no `docs/CAPABILITIES.md` inventory file") and Spec-lint box ("no `docs/CAPABILITIES.md` exists"); `git ls-files docs/CAPABILITIES.md` → tracked; `git log -1 --format='%H %s' -- docs/CAPABILITIES.md` → `1bab6e60 feat(docs): seed CAPABILITIES.md from template (feature 30 prep)`; `wc -l docs/CAPABILITIES.md` → 47 (roles table + 6 subsystem rows, byte-identical to `template/docs/CAPABILITIES.md`); `skills/design-feature/references/WRITE_AND_UPSERT.md:24-31`; `skills/review-spec/references/CHECKS.md` C6; `docs/features/59-executable-continuations/SPEC.md:244-245` | resolved | Integration closure now states `docs/CAPABILITIES.md` is the unfilled template (no live inventory) per feature 59's phrasing, cites E-22 (tracked since `1bab6e60`, 47 lines), and reconciles the derived inventory against the template's roles table + subsystem floor; the seed offer is replaced by a corrected fill offer (D32-6); Spec-lint box updated to match | 929c6cb28a6319c3014628bb7b7d1c9af7e94f25 |
| F3 | spec | medium | product | 5852f381958322e3ab690af196254c4c542672c021e86d2c10e9c03e7cc624a4 | (C14, C10) IS-2, `## Context` item 2, and DD-1 date the planning scale `info\|low\|medium\|high\|critical` to feature 31 and export the conversion-table row out of this unit ("the planning scale does not exist until feature 31 (#171) merges … owner: `plan-feature` (fills the row during 31's planning/execution)"), but that vocabulary is already the findings-ledger severity contract at this HEAD and is in active use, so a current-unit obligation is exported on a false premise and the table ships an empty slot for a scale that exists today | SPEC.md IS-2, `## Context` item 2, `### Deferred decisions` DD-1; `grep -n "info\|low\|medium\|high\|critical" skills/pre-execution-review/references/LEDGERS.md` → `:92` (findings-ledger severity vocabulary, `info` the only immaterial value); live rows `docs/features/37-phase-lint-script/planning-findings.md` F1 (`info`), `docs/features/59-executable-continuations/planning-findings.md` F1 (`low`); issue #172 item 2 asks for the planning-scale row in the same table | resolved | IS-2, Context item 2, Dependencies, and the out-of-scope bullet revised: the planning-scale row lands in this unit (E-23: `LEDGERS.md:92` + live rows 37-F1 `info`, 59-F1 `low`); DD-1 resolved, `Deferred decisions` reads `none`, sweep row 15 now in-scope; feature 31 stays a hard execution dependency on shared surfaces only | 929c6cb28a6319c3014628bb7b7d1c9af7e94f25 |
| F4 | spec | medium | product | 5852f381958322e3ab690af196254c4c542672c021e86d2c10e9c03e7cc624a4 | (C12, C3) GATE-RAN's home ledger is left to the implementer: IS-4 says the mark is "appended to the unit's ledger (home ledger + single writer fixed by a `ledger-ownership@1` map row added in the same change)", D32-3 repeats it, and AC-04 requires only that "the `ledger-ownership@1` map contains the GATE-RAN row with a single writer" — no ledger file is named anywhere in the half, so the planning turn must invent which durable ledger receives the mark (the arbitration this feature exists to remove) and the open choice is absent from `Deferred decisions` | SPEC.md IS-4 (:158-160), capability closure GATE-RAN (:232-241), AC-04 (:412-417), D32-3; `grep -n "progress.md\|review-findings.md\|known-issues.md" docs/features/32-review-consistency-pack/SPEC.md` → no match; `ledger-ownership@1` rows in `skills/pre-execution-review/references/LEDGERS.md` each name a `ledger` file, and `review-mark@1`'s precedent home is that ledger's `review-findings.md` | resolved | Home ledger named: the unit's `review-findings.md` (feature and fix variants) — the `review-mark@1` precedent home — in IS-4, the integration-closure `ledger-ownership@1` row, AC-04, and the D32-3 summary; the map row names home ledger + recorder set | 929c6cb28a6319c3014628bb7b7d1c9af7e94f25 |
| F5 | spec | medium | product | 5852f381958322e3ab690af196254c4c542672c021e86d2c10e9c03e7cc624a4 | (C9) IS-4 says GATE-RAN is "recorded by whoever runs the project's gate", but the role matrix denies `agent-reviewer` the Record GATE-RAN capability and the executor cell claims to be the "sole recorder"; reviewer-side gate runs are mandated in this repository, so a gate-running reviewer is a recorder the matrix excludes — the same authority is asserted two ways and the weaker reading (executors only) silently narrows the 4× reduction the feature is sold on | SPEC.md IS-4 (:158) vs `### Capability closure` §2 role-matrix row "Record GATE-RAN" (:299); `skills/review-verify/SKILL.md:28` ("The project's verification gate ran in THIS review"); `skills/review-change/references/REVIEW_PROCESS.md:33` ("Require the **gate green at the reviewed head**") | resolved | Role matrix row `Record GATE-RAN` now allows `agent-reviewer` (reviewers run the gate: `review-verify`'s checklist, `review-change`'s fold review) alongside the executor; the executor cell's "sole recorder" claim removed; IS-4 reads "executor phase gates and reviewer gate runs alike"; sweep row 16 added | 929c6cb28a6319c3014628bb7b7d1c9af7e94f25 |
| F6 | spec | low | product | 5852f381958322e3ab690af196254c4c542672c021e86d2c10e9c03e7cc624a4 | (C8) IS-2/AC-02 pin only one of the two `audit-docs` count instances the governing issue names: requiring `Checks run: <n>/14` and a `MEDIUM`-free file leaves the fixed output contract's `\| # \| Check (1-13) \|` header untouched and uncovered by any criterion, so the named "1-13" bug can survive a green AC-02 | SPEC.md IS-2, AC-02; `skills/audit-docs/SKILL.md:121` (`\| # \| Check (1-13) \|`) vs `:125` (`Checks run: <n>/13`) vs `:98` (`MEDIUM`); issue #172 item 2 ("14 checks defined but \"1-13\"/\"<n>/13\" emitted") | resolved | AC-02 (with IS-2 and the integration-closure `audit-docs` row) pins the fixed-output header `\| # \| Check (1-14) \|` alongside `<n>/14` | 929c6cb28a6319c3014628bb7b7d1c9af7e94f25 |
| F7 | spec | low | product | 5852f381958322e3ab690af196254c4c542672c021e86d2c10e9c03e7cc624a4 | (C8) IS-1's third clause — "A scan of `docs/workflow/` tutorials for restated flip claims verifies no contradicting restatement survives" — has no acceptance criterion: AC-01 pins only `PERSIST_AND_DECIDE.md`, `FOLDING.md`, and the `LEDGERS.md` prose sentence, so the scan's outcome is unverifiable from the frozen set and can be skipped while every criterion stays green | SPEC.md IS-1 (in-scope bullet) vs AC-01; the capability-closure Update row's "pin asserting `grep` for the old claims returns nothing" is a test note, not an acceptance criterion | resolved | AC-01 now pins IS-1's tutorial scan: `grep -rnE "only step that ever flips\|one and only ledger state transition" docs/workflow/ --include=*.md` returns nothing outside `GOLDEN_FIXTURE.md`'s dated run-log history (ran green at 929c6cb2) | 929c6cb28a6319c3014628bb7b7d1c9af7e94f25 |
| F8 | spec | info | product | 5852f381958322e3ab690af196254c4c542672c021e86d2c10e9c03e7cc624a4 | (C10) D32-4 attributes the 2026-09-07 amendment to issue #182; the amendment is carried by the governing issue #172 (whose amendment section opens "Per the amendment on #182"), so the decision's authority citation names the wrong artifact — the SPEC's own Goal and E-13 attribute it correctly | `docs/features/32-review-consistency-pack/decisions.md` D32-4 ("The 2026-09-07 amendment on issue #182 is adopted verbatim"); issue #172 body §"Amendment — gate-run receipt binding must stay extensible (2026-09-07)" | resolved | D32-6 in decisions.md records the append-only correction: the 2026-09-07 amendment is carried by issue #172's amendment section; #182's amendment is its provenance, not its carrier; the SPEC's D32-4 summary bullet cites #172 | 929c6cb28a6319c3014628bb7b7d1c9af7e94f25 |
| F9 | spec | high | product | 4efd6ffacf65996d0e606de0dd75d3248f6aeab666d19412160500aa621726a4 | (C9, C10) IS-4, AC-04 and the Integration-closure `ledger-ownership@1` row require the ownership map to gain a GATE-RAN row whose ledger is the unit's `review-findings.md` (feature + fix variants) and whose recorder set is the gate-running skills, but that map is machine-read by `scripts/ledger-ownership.test.mjs`, whose grammar admits exactly the seven AC16 truth classes, forbids two rows from declaring one ledger pattern, and asserts one row per class in fixed order — so the row cannot be added while AC-01 requires that suite to stay green, and the half names no alternative (grammar/version change, or an owner-column extension on the existing `review-findings` row). The same grammar allows one writer per column set, so a multi-skill "recorder set" also needs distinct column-set names the half never names. | SPEC.md IS-4 (:158), AC-01 (:407-417), AC-04 (:455-463), Capability closure §3 `ledger-ownership@1` row (:110-114); `skills/pre-execution-review/references/LEDGERS.md:139-148` (map block); `scripts/ledger-ownership.test.mjs:66-74` (`TRUTH_CLASSES` = 7), `:158-164` (exactly one row per class; foreign class fails), `:176-186` (unique ledger per row), `:712-718` (`rows.map(truth-class)` deepEquals `TRUTH_CLASSES`); empirical: in a throwaway repo copy, adding the required row (`gate-ran \| docs/features/<NN>-<slug>/review-findings.md · docs/fix/<issue>-<topic>/review-findings.md \| …`) makes `node --test scripts/ledger-ownership.test.mjs` exit 1 with `repository: map truth class "gate-ran" is not one of the seven AC16 classes` and `ledger "docs/features/<NN>-<slug>/review-findings.md" is already declared by "review-findings"` (baseline exit 0) | resolved | Grammar-admitting encoding adopted (repair instruction chose the owner-column extension over a grammar change): no new truth-class row — the machine-read grammar freezes exactly the seven AC16 truth classes and forbids a second declaration of the `review-findings.md` ledger pattern; instead the existing `review-findings` truth-class row's owner cell gains the gate-running recorders as new column-sets `execute-phase:gate-ran-marks` (executor phase gates) + `review-change:review-gate-ran-marks` (reviewer gate runs) — distinct column-set names per the one-writer-per-column-set rule — and both template projections (`docs/features/_TEMPLATE/LEDGERS.md`, `docs/fix/_TEMPLATE/LEDGERS.md`) gain the identical owner cell in the same change. SPEC.md IS-4, the GATE-RAN entity-closure Create row, the Integration-closure `ledger-ownership@1` row, AC-04 and the D32-3 summary bullet now pin this encoding; D32-7 in decisions.md records the supersession of D32-3's new-map-row encoding. Home ledger and recorder-set intent unchanged: the unit's `review-findings.md` (feature + fix variants), every mark naming its recorder | 0f93f7ea4e736f41df373753a45bafce2af9ac8d |
| F10 | spec | low | product | 4efd6ffacf65996d0e606de0dd75d3248f6aeab666d19412160500aa621726a4 | (C9) The Spec-lint self-description miscounts its own Capability closure: it says "five entities × six rows" while each of the five entities has five rows (Create, Read/list, Update, Delete, State transitions), and "seventeen inventory rows" while the Integration closure has eighteen rows. | SPEC.md Spec-lint box (:579-580); `docs/features/_TEMPLATE/SPEC.md:87-91` (the entity template defines five rows per entity); SPEC.md Capability closure §1 = 5 entities × 5 rows = 25 checkbox rows; §3 = 18 `- [x]` rows (:110-160); counts re-derived programmatically | resolved | Spec-lint box now states the true counts: "five entities × five rows = 25 closure rows: Create / Read-list / Update / Delete / State transitions; seven capabilities; eighteen inventory rows" — counts re-derived after the edit (25 entity rows, 5 entities, 18 inventory rows, 7 capability rows × 4 roles, 17 sweep rows) and all other Spec-lint product boxes re-verified green on the new bytes | 0f93f7ea4e736f41df373753a45bafce2af9ac8d |
| F11 | spec | medium | product | eb2e7a29d80eaf9c66de29efc11f0a81eb69e8bf83ba7ddb5c34b4494ce4d9e7 | (C9) The `#### Out of scope / non-goals` bullet "No `template/` change — the GATE-RAN mark is recorded output, not a ledger convention the template mirrors" asserts the ledger-ownership convention is not mirrored by "the template", while IS-4, AC-04 and the Integration-closure `ledger-ownership@1` row (all added by the D32-7 repair) require both `docs/features/_TEMPLATE/LEDGERS.md` and `docs/fix/_TEMPLATE/LEDGERS.md` — the projections the scaffold reads, called "the template the scaffold reads" in their own text — to gain the identical GATE-RAN owner cell. The half never disambiguates "template projections" (the two `docs/*/_TEMPLATE/LEDGERS.md` files the test reads) from the `template/` export mirror (`template/docs/*/_TEMPLATE/LEDGERS.md`), so the same word carries two meanings and the non-goal's stated reason does not hold for the surfaces the repair makes change | SPEC.md IS-4 (:168-176), AC-04 (:462-473), Capability closure §3 `ledger-ownership@1` row (:110-114), out-of-scope bullet 8 (:229-230); `scripts/ledger-ownership.test.mjs:42-43` (`FEATURE_TEMPLATE_REL`/`FIX_TEMPLATE_REL` read only the two `docs/*/_TEMPLATE/LEDGERS.md`), `:186-196` (`squash(mapRow.owner) !== squash(row.owner)` demands the identical owner cell); `docs/features/_TEMPLATE/LEDGERS.md` ("the template the scaffold reads"); `template/docs/features/_TEMPLATE/LEDGERS.md` + `template/docs/fix/_TEMPLATE/LEDGERS.md` (export mirror, already drifted); `docs/features/30-repair-receipt-delta-review/SPEC.md:206-207` (the copied ruling, where no template-projection change existed); `git log --oneline -- template/docs/features/_TEMPLATE/LEDGERS.md docs/features/_TEMPLATE/LEDGERS.md` → `c6daf5ec docs(28): fold F62 - sync the exportable template mirror with the live _TEMPLATE rewrite` | resolved | The out-of-scope bullet is reworded to disambiguate the two "template" file sets: the two LEDGERS.md **template projections** the scaffold reads (`docs/features/_TEMPLATE/LEDGERS.md`, `docs/fix/_TEMPLATE/LEDGERS.md`) are explicitly named as changing — they gain the identical `review-findings` owner cell per IS-4/D32-7 (`scripts/ledger-ownership.test.mjs:42-43` reads exactly those two; `:186-196` demands map/projection owner-cell equality) — while the unchanged surfaces are scoped precisely: the GATE-RAN mark itself stays recorded output (feature 30's ruling scoped to the SPEC/ACCEPTANCE template bodies) and the `template/` export mirror (`template/docs/*/_TEMPLATE/LEDGERS.md`) is untouched (already drifted; re-sync is feature 28 `c6daf5ec`'s surface). Verified at 7cb1398f; D32-8 in decisions.md records the batch | 7cb1398f38355306ab4b74ce9ff387cec3466a3b |
| F12 | spec | low | product | eb2e7a29d80eaf9c66de29efc11f0a81eb69e8bf83ba7ddb5c34b4494ce4d9e7 | (C11) IS-5(c)'s premise — `### Context` item 5's claim that `plan-feature`'s roadmap wording "blurs verify-vs-write against `ledger-ownership@1`" — is the only in-scope item's supporting claim with no evidence row: E-01…E-23 ground IS-1, IS-2, IS-3, IS-5(a)/(b)/(d), IS-6 and IS-7's dependencies, but no row cites `skills/plan-feature/SKILL.md` (or `plan-feature-scaffold`), so a material claim that drives an in-scope change (IS-5(c) → AC-07) cannot be falsified from the frozen set | SPEC.md `### Context` item 5 (:104-110), IS-5(c) (:195-197), AC-07 (:485-489); `### Evidence rows` E-01…E-23 (`awk '/^### Evidence rows/,/^### Spec-lint/' docs/features/32-review-consistency-pack/SPEC.md | grep -n "plan-feature"` → no match; 23 rows); `skills/plan-feature/SKILL.md:92-93` ("Verify the feature is registered in `docs/features/ROADMAP.md` … fix the entry now — never leave registration for later") vs `skills/pre-execution-review/references/LEDGERS.md:146` (`plan-feature-scaffold:planned-row` is the roadmap writer) | resolved | Evidence row E-24 added to the Product half: the `ledger-ownership@1` roadmap row's writer column-sets (`LEDGERS.md:146`) name `plan-feature-scaffold:planned-row` as the `defined → planned` writer and omit `plan-feature`; `skills/plan-feature/SKILL.md:92-94`'s "Confirm roadmap" step verifies/repairs registration only; `skills/plan-feature-scaffold/SKILL.md:110` owns the `defined → planned` write — IS-5(c)'s verify-vs-write claim (→ AC-07) is now falsifiable from the frozen set (verified at 7cb1398f); D32-8 in decisions.md records the batch | 7cb1398f38355306ab4b74ce9ff387cec3466a3b |
| F13 | plan | high | plan | 102238d5e10dd88444b947fed646915fe77daf7096c4c5f5322608aab4044c97 | (L1) The bound parent Product receipt is stale at the review's source revision. `SPEC-REVIEW-32-4` was recorded when `project-guide` (`CLAUDE.md`) hashed `f5c8e142…`; at `e1c008bc` `CLAUDE.md` hashes `45af6d85…`, so the spec snapshot recomputes to `e4b293e3…` and `bun scripts/pre-execution-snapshot.mjs verify --stage spec --unit 32-review-consistency-pack` answers `structural.fresh: false`, `reasonCode: "stale-context"`, `changedPaths: ["CLAUDE.md"]`. L1 requires the Product bytes **and contexts** to be unmoved, so no current `SPEC-REVIEW-PASS` exists to parent this plan and the lineage `execute-phase` binds cannot be established. | `progress.md` SPEC-REVIEW-32-4 block; `verify --stage spec` JSON; `git log --oneline -- CLAUDE.md` (`87d5b2db`→`b968c33e`), blob `4eed9a3a`→`9d385234`; the `spec-product-v1` projection digest `675349ef…` is byte-identical to the reviewed bytes | resolved | Resolved by `SPEC-REVIEW-32-5` (`spec-review-pass`, snapshot `e4b293e3f62f2ad543423b172fa4766c211604a7d77d6ea719700dd949aeb9a7`, `structural.fresh: true`, `current: true`, `contextClean: true`, zero open material rows) recorded at `e1c008bc`, and the `32-plan-2` snapshot parents to it (`--parent e4b293e3…`). The Product projection is byte-identical to the bytes `SPEC-REVIEW-32-4` reviewed (`675349ef…`, 48622 B); only the `project-guide` context moved (feature 60's `CLAUDE.md` rows). | 32-plan-2 |
| F14 | plan | high | plan | 102238d5e10dd88444b947fed646915fe77daf7096c4c5f5322608aab4044c97 | (L2, P2, P12) The plan set was cut at `2a87c5cd`/`852c29ef` before the branch merged `origin/main` at `e1c008bc`, which brought feature 31 — the SPEC's declared **hard execution dependency** — and feature 60. 8+ planned target surfaces moved after the evidence revision (`LEDGERS.md` +4, `PERSIST_AND_DECIDE.md` −15, `review-change/SKILL.md` +2, `audit-pr/SKILL.md` +2, `fold-findings/SKILL.md` ±24, `workflow-status.mjs` +2, `review-loop-discipline.test.mjs` +41, `audit-pr-receipt.test.mjs` rewritten, `SKILL_CONTEXT_BUDGETS.json` 164 lines), yet every `planning-evidence.md` row still claims `freshness: current`/`status: proven` and `SPEC.md ## Dependencies` still reads feature 31 "not yet merged (roadmap status `idea`)". PE-022 is falsified at `e1c008bc`: roadmap row 31 is `done · #243` and the committed row 32 is `idea`. | `git diff --stat 7cb1398f HEAD -- <the cited surfaces>`; `SPEC.md ## Dependencies`; `planning-evidence.md` PE-022; `git show HEAD:docs/features/ROADMAP.md` rows 31/32 | resolved | Resolved by the `32-plan-2` re-cut: every `planning-evidence.md` row was re-read at `e1c008bc` with its `path:line` re-based, `PE-022` rewritten to the satisfied closure (rows 30 and 31 both `done` and merged, PR #188 and PR #243), `PE-016` corrected (the budget manifest declares entries for only some touched skills; the rest ride `defaults`), and `PE-023` added for the drift-gate collision. `SPEC.md ## Dependencies` still records feature 31 as unmerged — that text is Product half, filed as F20 (product, low) and re-based by `design-feature`, not by this plan batch. | 32-plan-2 |
| F15 | plan | high | plan | 102238d5e10dd88444b947fed646915fe77daf7096c4c5f5322608aab4044c97 | (P10) The plan names a validator that is already red at the bound source revision and gives it no task. AC-09 and obligation O13 require `node --test scripts/normative-drift.test.mjs` → exit 0, and PLAN/TASKS P4 task 6 runs it; at `e1c008bc` the suite is 18/19 with `a changelog version row appears once per table` failing — "CHANGELOG.md states pre-execution-review more than once in one table: 2.3.0 ×2" (`CHANGELOG.md:648` feature 60 P3 row, `:663` feature 31 row). The collision is present on `main` at `71b3b0fd` too, and no plan task dedupes it, so the phase/close-out gate cannot reach exit 0. | observed `node --test scripts/normative-drift.test.mjs` (fail 1); `ACCEPTANCE.md` AC-09; `planning-obligations.md` O13; `PLAN.md`/`TASKS.md` P4 task 6; `CHANGELOG.md:648,663` | resolved | Resolved in the plan: `PE-023` records the collision and P4 task 1 resolves it (the later-merged feature 60 row takes `pre-execution-review` 2.4.0 with the matching frontmatter, `LEGACY_DUPLICATE_VERSION_ROWS` left unextended per `ED-32-8`), and P4's done-when plus O13 now include `node --test scripts/normative-drift.test.mjs` → exit 0. The gate goes green when P4 executes; it is red at the planning head by design. | 32-plan-2 |
| F16 | plan | medium | plan | 102238d5e10dd88444b947fed646915fe77daf7096c4c5f5322608aab4044c97 | (P12) `planning-evidence.md` `path:line` citations no longer match the repository at `sourceRevision`. The merge shifted the cited surfaces: `LEDGERS.md` `:117`→`:121`, `:139-146`→`:142-149`, `:90-92`→`:91-93`, `:146`→`:149` (PE-001/002/005/012); `workflow-status.mjs` `:223`→`:225` and `:964/1245/1261`→`:966/1247/1263` (PE-009); `fold-findings/SKILL.md:153` no longer carries the "this skill only flips" claim (PE-001); `scripts/audit-pr-receipt.test.mjs:10-12` was rewritten and now reads `audit-pr-gate.mjs` (PE-015). Most claims survive in substance, but P12 requires cited `path:line` to match at sourceRevision and L2 forbids a surviving stale row. | `git diff 7cb1398f HEAD -- <the cited paths>`; current line reads at `e1c008bc` | resolved | Resolved by the `32-plan-2` re-cut: every moved citation was updated at `e1c008bc` (`LEDGERS.md:121` / `:142-149` / `:145` / `:150`, `fold-findings/SKILL.md:157`, `audit-pr/SKILL.md:53-54`, `workflow-status.mjs:225,966,1247,1263`, `CLAUDE.md:172-176,281,421-425`, `audit-pr-receipt.test.mjs:11-14`, `ledger-ownership.test.mjs:66-74`), each location re-read before it was written. | 32-plan-2 |
| F17 | plan | medium | plan | 102238d5e10dd88444b947fed646915fe77daf7096c4c5f5322608aab4044c97 | (P12; L3/L4 note) `PLAN.md`'s own ledger references are stale: lines 13-14 read "`planning-evidence.md` (PE-001…PE-020) and `planning-obligations.md` (O1…O24)", but the evidence table has 22 rows (PE-001…PE-022) and the obligations table 25 (O1…O25) — as `SPEC.md` §Planning evidence ("22 rows, PE-001…PE-022") and §Obligations ("O1…O25") already state. An executor following PLAN.md can miss PE-021/PE-022 and O25. | `PLAN.md:13-14`; `grep -c '^\| PE-' planning-evidence.md` → 22; `grep -c '^\| O[0-9]' planning-obligations.md` → 25; `SPEC.md ### Planning evidence` / `### Obligations` | resolved | Resolved: `PLAN.md`'s ledger references now read `PE-001…PE-023` and `O1…O25`, matching the two ledgers' actual row counts (23 and 25), and the SPEC's `### Planning evidence` and artifact list were corrected to the same counts. | 32-plan-2 |
| F18 | plan | medium | plan | 102238d5e10dd88444b947fed646915fe77daf7096c4c5f5322608aab4044c97 | (P1, P2) Feature 31 (now merged) lands a materially overlapping contract on the exact surfaces the plan edits, and the plan was cut against pre-31 text. 31 added the materiality/report-note rule to `LEDGERS.md` §3 ("Material is `medium` and above; a `low` row is a persisted **report-note**", `:91-96`) and the wording-only route plus hard two-cycle cap to `POLICY.md` §3/§4, and `scripts/workflow-status.mjs` now imports/projects `deriveReviewLoopCycles` from `pre-execution-contract.mjs` (the P1 target file, +2 lines). IS-3's `report-note` outcome and the P1 sensor edit must be reconciled with 31's shipped rule; the plan's premise that 31 "must not be re-based mid-flight" is now inverted by the repository state. | `git diff 7cb1398f HEAD -- skills/pre-execution-review/references/LEDGERS.md skills/pre-execution-review/references/POLICY.md scripts/workflow-status.mjs`; `LEDGERS.md:91-96`; `CHANGELOG.md:663` (pre-execution-review 2.3.0, feature 31) | resolved | Resolved by `ED-32-9` plus the P1/P3 task text: the plan cites `LEDGERS.md` §3 (`:91-96`) as the single owner of the `report-note` definition instead of restating it, reads the `low`/`info` coherence rule from `LEDGERS.md:111`, and P1 adds an assertion that the sensor's merged feature 31 review-loop-cycle projection (`deriveReviewLoopCycles`, `workflow-status.mjs:32,1321`) survives the NRS branch; no feature 31 loop policy is re-opened. | 32-plan-2 |
| F19 | plan | low | environment | 102238d5e10dd88444b947fed646915fe77daf7096c4c5f5322608aab4044c97 | The scaffold's `defined → planned` roadmap write (commit `852c29ef`) was clobbered at HEAD by the merge: `git show HEAD:docs/features/ROADMAP.md` row 32 reads `idea` while the working tree reads `planned · 32-plan-1`, leaving `docs/features/ROADMAP.md` dirty. The router reads the working tree (`node scripts/unit-route.mjs 32` → `status: planned`, `route: execute`), so routing works, but the scaffold's recorded write is not durable and P5's roadmap task starts from an inconsistent base. Report-note (non-blocking). | `git show HEAD:docs/features/ROADMAP.md` row 32; `git status --porcelain`; `node scripts/unit-route.mjs 32` | resolved | Resolved: the roadmap row's promotion is re-written as `planned · 32-plan-2` and committed with the re-cut, so the row is durable at HEAD and the working tree no longer carries an unrecorded promotion; `node scripts/unit-route.mjs 32` reads `status: planned` from a committed row. The write is registration repair on an already-`planned` row (the `defined → planned` transition stays `plan-feature-scaffold`'s), recorded as `ED-32-7`; no source fold is owed. | 32-plan-2 |
| F20 | spec | low | product | e4b293e3f62f2ad543423b172fa4766c211604a7d77d6ea719700dd949aeb9a7 | (C10, report-note) The Product half's authoring-time citations and its feature-31 dependency status no longer resolve at `e1c008bc`: the post-authoring merge of this unit's own hard dependency — feature 31 (`#243`) — plus feature 60 (`#245`) shifted the surfaces the Evidence rows cite and merged the dependency those rows record as open. Moved citations: E-01's `fold-findings/SKILL.md:153` is now a diagram line (the "only flips" sentence lives at `:157`) and `LEDGERS.md:139-146` now spans fallback prose (the map block is `:142-149`, the `review-findings` row `:145`); E-02's `LEDGERS.md:117` is now the "one-owner rule" sentence (the `triage-issue` sentence is `:121`); E-06's `audit-pr/SKILL.md:52` is the "no review pass was composed" bullet (the `warning` scale is `:54`, while `:55`/`:58` still carry the pass/blocker/n-a scale); E-07's `workflow-status.mjs:223` is a closing brace (`NRS_BLOCKING` is `:225`); E-18's `LEDGERS.md:123` is the `ledger-provenance` sentence (the machine-read claim is `:127`); E-24's `LEDGERS.md:146` is the `planning-findings` row (the roadmap writer column-sets are `:150`). Stale status: `SPEC.md ## Dependencies` and E-14 still read feature 31 "not yet merged (roadmap status idea)" / "execution dependency open", while roadmap row 31 reads `done · #243` at HEAD; E-15's "row 32 exists at idea" now describes the committed row only (the working tree carries the scaffold's `planned` write — F19). Every claim was re-verified in substance at the bound revision — the sentences, tables and rows exist and say what the half says — so intent, obligation identity, phase topology, validators and authority are unaffected; this is a report-note the stage author re-bases without a re-review. | SPEC.md `## Dependencies` and `### Evidence rows` E-01/E-02/E-06/E-07/E-14/E-15/E-18/E-24; observed at `e1c008bc`: `sed -n '153p;157p' skills/fold-findings/SKILL.md`, `sed -n '117p;121p;123p;127p;139,150p' skills/pre-execution-review/references/LEDGERS.md`, `sed -n '52p;54p;55p;58p' skills/audit-pr/SKILL.md`, `sed -n '223p;225p' scripts/workflow-status.mjs`; `git show HEAD:docs/features/ROADMAP.md` row 31; F16 (same root cause, plan side) | open | — | — |
| F21 | plan | medium | plan | 065459e621797ef57769e42d96d1bb25b9de99b804b8d26d0f5f17da77190cd6 | (L2, P12) `planning-evidence.md` row PE-011 is marked `current`/`proven`, but two of its `path:line` cites do not resolve at the bound `sourceRevision` `aefdccc4`. The row labels `scripts/ledger-ownership.test.mjs:42-43` as "(projection paths)"; lines 42-43 are `const repoRoot = …` and the "Re-pointable" comment, while the projection paths are `FEATURE_TEMPLATE_REL`/`FIX_TEMPLATE_REL` at `:56-57`. It labels `:176-196` as "(one row per class, unique pattern, owner-cell equality)"; one-row-per-class is `:158-163`, the unique-ledger check is `:173`, and the owner-cell equality check is `:202-204` — the cited range contains none of the three. The prior FAIL (F16) required every shifted citation to be re-based at the post-merge head; PE-011 was not, so the row's provenance claim is unsupported at its cited locations. | observed at `aefdccc4`: `sed -n '42,43p;55,57p;66,74p;158,163p;173p;189,196p;202,204p' scripts/ledger-ownership.test.mjs`; `grep -n "FEATURE_TEMPLATE_REL\|FIX_TEMPLATE_REL\|TRUTH_CLASSES\|squash(mapRow.owner)" scripts/ledger-ownership.test.mjs`; PLAN-REVIEW-32-1 finding F16 (same class) | resolved | Resolved in the `32-plan-3` repair batch: PE-011's `path:line` cites re-based at the bound source revision — `:56-57` (the `FEATURE_TEMPLATE_REL`/`FIX_TEMPLATE_REL` projection paths), `:66-74` (`TRUTH_CLASSES`), `:158-163` (one row per class), `:173` (the unique-ledger-per-row check), `:202-204` (the owner-cell equality check); each location re-read at `aefdccc4` before the row was rewritten, claim, freshness and status unchanged | 32-plan-3 |
| F22 | plan | medium | plan | 065459e621797ef57769e42d96d1bb25b9de99b804b8d26d0f5f17da77190cd6 | (L4) `planning-obligations.md` O9 names phase **P1** and a task that reads "Drop `missing` from the blocking set, emit the notice, and align the skill references", but the reference alignment is not a P1 task: `PLAN.md`/`TASKS.md` P2 task 7 aligns `skills/workflow-status/references/ENVELOPE_CORE.md` and `SENSOR_CORE.md` and pins the split in `scripts/review-loop-discipline.test.mjs`, `SPEC.md`'s P1 bullet nevertheless says "the workflow-status references state the same split", and the plan's own PE-010 owner column says "P2 aligns the two reference files". O9's validator (the sensor suite) cannot fail on the reference-alignment component, so at P1 the obligation would read satisfied while part of its task is undone (the layer rule also forbids a `skills/` first target inside the `config/infra` phase). The plan set contradicts itself on where IS-5(a)'s prose half lands. | `planning-obligations.md` O9; `PLAN.md` P1 (4 tasks, none editing references) and P2 task 7; `TASKS.md` P2 task 7; `SPEC.md` P1/P2 bullets (`:927-948`); `planning-evidence.md` PE-010 ("P2 aligns the two reference files"); `P2` done-when names the "IS-5(a)-text" pin; `scripts/phase-lint.mjs:232-237` (layer table) | resolved | Resolved in the `32-plan-3` repair batch: O9 moves to P2 — its task now names the split (the sensor half lands in P1's tasks; the reference alignment is `PLAN.md`/`TASKS.md` P2 task 7), its validator gains the `review-loop-discipline` IS-5(a)-text pin so both halves are falsifiable when the obligation resolves, and its required evidence moves to the P2 handoff; the SPEC's P1 bullet drops the reference clause and the P2 bullet names the workflow-status reference alignment (IS-5(a) text). The plan set no longer contradicts itself on where IS-5(a)'s prose half lands | 32-plan-3 |
| F23 | plan | low | product | 065459e621797ef57769e42d96d1bb25b9de99b804b8d26d0f5f17da77190cd6 | (L5) AC-02's third validator is unrunnable as written: `ACCEPTANCE.md` AC-02 and `SPEC.md` AC-02 (Product half) specify `grep -nE "postpone\|tradeoff" skills/product-audit/` → no match, but `skills/product-audit/` is a directory and `grep` without `-r` exits 2 with `Is a directory` — it can never answer "no match". The plan's O4 copies the same command as its validator. An executor reading the frozen finish line literally can neither satisfy nor falsify it (and may misread exit 2 as "no match"); the substantive check is only covered by the discipline-suite pin P3 also adds. Report-note (non-material). | `grep -nE "postpone|tradeoff" skills/product-audit/` → `grep: skills/product-audit/: Is a directory`, exit 2 (observed); `grep -rnE "postpone|tradeoff" skills/product-audit/` → 3 hits at planning head; ACCEPTANCE.md AC-02; SPEC.md AC-02; `planning-obligations.md` O4 | open | — | — |
| F24 | plan | low | product | 065459e621797ef57769e42d96d1bb25b9de99b804b8d26d0f5f17da77190cd6 | (L5) AC-10's read-verified validator cannot hold as written: it requires "the diff shows the append", but `README.md` already carries the arXiv:2603.00539 entry under `## References` at the bound revision — feature 31 (merged in `e1c008bc`, before this plan's `aefdccc4` base) added it with the same text. The plan's P5 task 4 hedges ("deduping against entries other features added"), so the executor will find the entry present and append nothing, making the frozen validator's append clause unsatisfiable; IS-6's bibliography obligation is already met by feature 31 and the Product half was not re-based after the merge (same root cause as the open F20). Report-note (non-material). | observed at `aefdccc4`: `README.md:792-794` (`## References` + the Jin & Chen `arXiv:2603.00539` entry, "the study on which feature 31's planning materiality floor … is built"); `git log -S "## References" -- README.md`; `docs/features/31-planning-review-materiality/PLAN.md:154`; ACCEPTANCE.md AC-10; `planning-obligations.md` O14; PLAN-REVIEW-32-1's F20 (same root cause) | open | — | — |
