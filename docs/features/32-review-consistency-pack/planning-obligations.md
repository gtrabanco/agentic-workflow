# Planning obligations — 32-review-consistency-pack

One row per normative behaviour, applicable compatibility invariant, affected use
case, and required failure state. Row shape and column order per
`pre-execution-review/references/LEDGERS.md` §2. Status vocabulary:
`planned | in-progress | verified | n/a | deferred` — every row starts `planned`;
no row is `deferred` (no obligation is exported to a follow-up issue).

| obligation-id | Authority source | Affected use case or invariant | Phase | Task | Implementation owner | Validator | Required evidence | Status |
|---|---|---|---|---|---|---|---|---|
| O1 | AC-01 (IS-1) | Sole-flipper wording: the flip is attributed to `ledger-ownership@1` / `fold-findings:folded-flag` at the three claiming surfaces plus `fold-findings`, the corrected `LEDGERS.md` prose sentence, and the tutorial scan finds no survivor | P2 | Correct the flip sentences and the `LEDGERS.md` prose; pin them and the scan | execute-phase | `node --test scripts/review-loop-discipline.test.mjs scripts/ledger-ownership.test.mjs` → exit 0; `grep -rnE "only step that ever flips\|one and only ledger state transition" docs/workflow/ --include=*.md` → no match | the pin names and the empty-scan output recorded in the P2 handoff | planned |
| O2 | AC-02 (IS-2) | Canonical severity conversion table: `CLASSIFY.md` owns one section mapping the ledger, finder, planning, and `audit-docs` scales onto `high \| med \| low`, with unknown scales failing closed | P3 | Add the conversion section and pin every row plus the fail-closed outcome | execute-phase | `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the conversion-table and fail-closed pins green | the pin names recorded in the P3 handoff | planned |
| O3 | AC-02 (IS-2) | `audit-docs`' report contract matches its real check count and its legend: `\| # \| Check (1-14) \|`, `Checks run: <n>/14`, no `MEDIUM` | P3 | Fix the header, the emitted count, and the orphan-finding severity | execute-phase | `grep -n "MEDIUM" skills/audit-docs/SKILL.md` → no match; `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the count/header pins green | the grep output and pin names recorded in the P3 handoff | planned |
| O4 | AC-02 (IS-2) | `product-audit` classifies with the closed class set owned by `review-implementation`, not its parallel `postpone`/`tradeoff` vocabulary, and stays proposes-only | P3 | Replace the vocabulary in the skill and its process reference | execute-phase | `grep -nE "postpone\|tradeoff" skills/product-audit/` → no match; `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the removal pin green | the grep output and pin name recorded in the P3 handoff | planned |
| O5 | AC-02 (IS-2, D32-6) | No ad-hoc conversion survives: the local finder mapping is deleted in favour of the canonical table, and its existing discipline pin is re-pointed rather than weakened | P3 | Delete the ad-hoc mapping and re-point the finder pin at the table rows | execute-phase | `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the re-pointed finder pin green | the reworded assertion quoted in the P3 handoff (AC-11) | planned |
| O6 | AC-03 (IS-3) | Derived blocking gate: a finding blocks only on one cited single-owner failure among the four categories; with no citation the outcome is `report-note`; D10's verdict rule is unchanged | P3 | Add the blocking-gate rule row and pin the four categories, the no-citation outcome, and D10 | execute-phase | `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the blocking-gate and D10 pins green | the pin names recorded in the P3 handoff | planned |
| O7 | AC-04 (IS-4) | `gate-ran@1`: the fixed mark format, the additive-slots rule, the reserved `manifest` slot, the identical-head-only reuse rule, and the changed-head ⇒ re-run rule | P4 | Add the mark block to the owning ledger; state the recorder/consumer rule in execute-phase and review-change | execute-phase | `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the `gate-ran@1` pins green | the pin names recorded in the P4 handoff | planned |
| O8 | AC-04 (IS-4) | Ownership encoding: the map's existing `review-findings` row gains `execute-phase:gate-ran-marks` and `review-change:review-gate-ran-marks`, and both template projections carry the identical owner cell | P4 | Extend the live map row and mirror the cell into both projections | execute-phase | `node --test scripts/ledger-ownership.test.mjs` → exit 0 with one row per truth class, unique ledger patterns, and owner-cell equality | the suite output recorded in the P4 handoff | planned |
| O9 | AC-05 (IS-5(a), D32-1) | NRS optionality: an absent repository-state ledger yields a non-blocking machine-readable notice and zero missing-ledger blockers; `draft`, `contradicted`, and `resolved` still block, and the sensor's merged feature 31 review-loop-cycle projection is left intact | P1 | Drop `missing` from the blocking set, emit the notice, and align the skill references | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0 with the missing-ledger case, the three regression cases, and the review-loop-cycle projection assertion green; `node --test scripts/workflow-status-pre-execution.test.mjs` → exit 0 | the case names and envelope excerpt recorded in the P1 handoff | planned |
| O10 | AC-06 (IS-5(b), E-10) | `review-change` names `triage-issue`'s three modes (independent proposals, audit findings, `--prioritize-now`) in its skill and both references | P2 | Name the three modes and pin them | execute-phase | `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the triage-modes pin green | the pin name and the quoted relationship text recorded in the P2 handoff | planned |
| O11 | AC-07 (IS-5(c), E-24) | Roadmap verify-vs-write: `plan-feature` verifies and repairs registration only; `plan-feature-scaffold` is the sole `defined → planned` writer | P2 | Split verify from write in the plan-feature step and mirror it in the scaffold text | execute-phase | `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the roadmap pin green; `node --test scripts/bounded-delivery-loops.test.mjs` → exit 0 | the pin name and quoted sentences recorded in the P2 handoff | planned |
| O12 | AC-08 (IS-5(d), E-06) | `audit-pr`'s closure-integrity result scale reads `pass \| blocker \| n-a`; the co-occurring closure-warning note is not a scale value | P3 | Align the scale line and pin the scale lines only | execute-phase | `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the scale pin green; `node --test scripts/audit-pr-receipt.test.mjs` → exit 0 | the pin name and the three scale lines quoted in the P3 handoff | planned |
| O13 | AC-09 (IS-7) | Health gates: budgets re-based for every touched skill (declared entries re-based, new entries declared for touched skills past `defaults`), the `CHANGELOG.md` once-per-table collision resolved so the drift gate can reach exit 0, no weakened pin, schema package untouched | P4 | Resolve the version collision, re-base the budget entries, and run the health gates | execute-phase | `node --test scripts/normative-drift.test.mjs` → exit 0 (once-per-table pin green, `LEGACY_DUPLICATE_VERSION_ROWS` unextended); `node scripts/check-skill-context.mjs` → exit 0 | the gate output, the corrected `CHANGELOG.md` rows, and the `policy.declared` growth-source entry recorded in the P4 handoff | planned |
| O14 | AC-10 (IS-6) | Bibliography: the arXiv:2603.00539 entry sits under a bottom `## References` section of the repo README at PR close-out, deduped, never cited before the feature ships | P5 | Append the entry, creating the section when absent | execute-phase | read-verified: the entry and its section quoted in the P5 handoff; the diff shows the append and no other README edit | the quoted README excerpt recorded in the P5 handoff | planned |
| O15 | AC-11 (IS-7) | Versions and pins: every touched skill's frontmatter is minor-incremented, and no discipline pin was removed, skipped, or loosened — additions plus the mandated AC-01/AC-02 rewordings | P4 | Bump the eleven touched skills and add one changelog row each | execute-phase | `grep -n "^version:" skills/*/SKILL.md` shows a minor increment for every touched skill; read-verified: the test diff is additions plus the mandated rewordings | the version table and the quoted reworded assertions recorded in the P4/P5 handoff | planned |
| O16 | WORKFLOW_INVARIANTS.md:78-80 (`review-change` row) | `review-change` stays the final-diff quality authority; this unit only adds text it consumes, never a competing authority | P3 | Add the conversion table and blocking rule under `review-implementation`'s existing ownership | execute-phase | read-verified: `CLASSIFY.md` remains `review-implementation`'s surface and D10's three-state verdict is asserted unchanged | the invariant classification recorded in `architecture-notes.md` | planned |
| O17 | WORKFLOW_INVARIANTS.md:78-80 (`audit-pr` row) | `audit-pr` stays the only MERGE-READY emitter and keeps consuming CI; the scale alignment changes a label, not the gate | P3 | Align the closure-integrity scale without touching the receipt-at-head gate | execute-phase | `node --test scripts/audit-pr-receipt.test.mjs` → exit 0 | the suite output recorded in the P3 handoff | planned |
| O18 | WORKFLOW_INVARIANTS.md:78-80 (`review-change` + `fold-findings` row) | The manual review→fold loop stays bounded at two cycles and never merges or discards findings; this unit adds no loop-policy change | P2 | Correct the flip wording only; no loop-policy sentence is edited | execute-phase | `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the existing loop-cap pins unchanged | the unchanged-pin list recorded in the P2 handoff | planned |
| O19 | SPEC §Out of scope (no schema change) + PE-011/PE-021 | The machine-read grammar is untouched: the map keeps exactly seven truth classes, one declaration per ledger pattern, and the router/provenance row parsers ignore the appended text mark | P4 | Extend the existing owner cell and mirror it; no truth-class row and no parser change | execute-phase | `node --test scripts/ledger-ownership.test.mjs` → exit 0; read-verified: `scripts/unit-route.mjs` and `scripts/ledger-provenance.mjs` are unmodified | the suite output and the unmodified-file diff recorded in the P4 handoff | planned |
| O20 | AC-09 (SPEC integration closure) | The Pi mirror is re-bundled byte-identically after the final skill text, and the schema package stays regression-only | P5 | Re-bundle the mirror and run the parity suite plus the schema/Pi suites | execute-phase | `node --test packages/pi-agentic-workflow/test/skill-parity.test.mjs` → exit 0; `cd packages/agentic-workflow-schema && bun run test` → exit 0; `cd packages/pi-agentic-workflow && npm test` → exit 0 | the suite outputs recorded in the P5 handoff | planned |
| O21 | SPEC dev scenario `sweep:empty-state` | With the old sentences absent no pin can pass spuriously: the tutorial scan and the negative text pins hold on a tree that carries no stale restatement | P2 | Pin the negative assertions and the `docs/workflow/` scan | execute-phase | `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the scan pin green | the scan pin name recorded in the P2 handoff | planned |
| O22 | SPEC dev scenario `sweep:unknown-scale` | An undeclared severity scale fails closed: the consumer returns the finding for a missing table row instead of converting it ad hoc | P3 | Pin the fail-closed outcome beside the table rows | execute-phase | `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the fail-closed pin green | the pin name and its fixture scale recorded in the P3 handoff | planned |
| O23 | SPEC dev scenario `sweep:wrong-role-write` | A non-owner skill is denied the flip and the roadmap write at the contract level: the map names one writer per column set and the prose cites it | P2 | Correct the three claiming surfaces and the roadmap step; pin the sole-writer text | execute-phase | `node --test scripts/review-loop-discipline.test.mjs scripts/ledger-ownership.test.mjs` → exit 0 | the pin names recorded in the P2 handoff | planned |
| O24 | SPEC dev scenario `sweep:stale-head` | A green gate run is reusable only at the identical head; a changed head re-runs the gate, so freshness is never weakened | P4 | Pin the reuse rule and the changed-head re-run rule | execute-phase | `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the reuse/re-run pins green | the pin names recorded in the P4 handoff | planned |
| O25 | SPEC dev scenario `sweep:additive-slots` | A consumer meeting unknown trailing GATE-RAN fields keeps working, and the reserved `manifest` slot is declared so feature 35 needs no format migration | P4 | Pin the additive-slots rule and the reserved slot | execute-phase | `node --test scripts/review-loop-discipline.test.mjs` → exit 0 with the additive-slots and reserved-slot pins green | the pin names recorded in the P4 handoff | planned |
