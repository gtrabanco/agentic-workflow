# Planning evidence — 38-workflow-status-sensor-script

Compact source-backed conclusions used to cut this Engineering plan. This is not an
exploration transcript. All rows observed at HEAD `6be1c287` (branch `main`) unless
a row says otherwise.

Planning baseline: HEAD `6be1c287` + live forge state verified 2026-09-09.

| ID | Claim or question | Authority | Repository evidence and revision | Affected decision or obligation | Freshness | Status | Owner or next evidence |
|---|---|---|---|---|---|---|---|
| PE-001 | `scripts/schema-runtime.mjs` loads the built schema package by explicit path (`packages/agentic-workflow-schema/dist/index.js`), throws "schema runtime is not built" when `dist/` is missing, and deliberately does NOT fall back to a published `@gtrabanco/agentic-workflow-schema` (a published build can be older than the source under review) | repository contract | `scripts/schema-runtime.mjs` lines 5–20, 47–49; `packages/agentic-workflow-schema/dist/` is gitignored (`packages/agentic-workflow-schema/.gitignore`); `packages/agentic-workflow-schema/package.json` line 22 `"main": "./dist/index.js"` | P1; AC2, AC8, AC11 | current | proven | re-verify before editing P1 |
| PE-002 | Schema package v4.1.1 exports the Envelope v2 vocabulary: `Envelope` interface, `validateEnvelope(value: unknown): ValidationResult` (line 263), `parseEnvelope` (line 480), `validateEnvelopeV2Strict` (line 597), and `decideWorkflowAction` (line 1042) in `packages/agentic-workflow-schema/src/index.ts` | repository contract | `packages/agentic-workflow-schema/package.json` line 3 `"version": "4.1.1"`; `src/index.ts` exports at lines 45, 62, 68–80, 82, 89, 95, 104, 112, 122, 130, 137, 143, 148, 153, 159, 167, 195, 263, 480, 501, 506, 597, 619, 657, 682, 689, 692, 1042 | P1; AC2 | current | proven | re-verify before editing P1 |
| PE-003 | The envelope schema `envelope.schema.json` declares `detail` as `unknown` (schema-unconstrained, "documented per skill") — so every new `detail` shape the script emits (degradation codes, workflow_observations, pre_execution) needs no schema-package change | schema package | `packages/agentic-workflow-schema/envelope.schema.json` line 182–184 (or equivalent in the schema): `"detail"` type with unconstrained value, documented per skill | P2; AC2, AC6 | current | proven | no schema change required |
| PE-004 | `scripts/workflow-status.mjs` follows the repo's existing script convention: Node ≥ 18, ES modules, no transpilation, no bundler, `#!/usr/bin/env node` shebang | repository convention | all existing `scripts/*.mjs` files (`check-skill-context.mjs`, `ledger-provenance.mjs`, `schema-runtime.mjs`, etc.) — all use ES modules, Node runtime, no transpilation | P1; AC10, AC11 | current | proven | follow the convention |
| PE-005 | The snapshot verifier `scripts/pre-execution-snapshot.mjs` accepts `build` or `verify` modes with `--stage spec|plan --unit <id> [--parent <64-hex>] [--dir <path>] [--receipt <id|digest>]` and returns a structured JSON envelope with the snapshot digest as the first line of stdout | repository contract | `scripts/pre-execution-snapshot.mjs` — usage line: `pre-execution-snapshot: usage: pre-execution-snapshot.mjs build|verify --stage <spec|plan> --unit <id> [--dir <path>] [--receipt <id|digest>]`; the `verify` mode returns the same structured snapshot JSON as `build` | P2; AC2 | current | proven | re-verify before editing P2 |
| PE-006 | The repo's established CLI convention for unknown flags is: usage diagnostic on stderr + non-zero exit code (1–2); verified live on `scripts/check-skill-context.mjs` → exit 1, `scripts/ledger-provenance.mjs` → exit 2 | repository convention | live control run: `node scripts/check-skill-context.mjs --not-a-real-flag` → exit 1; `node scripts/ledger-provenance.mjs --not-a-real-flag` → exit 2 + usage diagnostic on stderr | P2; AC20 | current | proven | pin the exit value in the suite |
| PE-007 | `scripts/normative-drift.test.mjs` enforces declared versioned grammars and version tables — a surface that restates a skill's version must match the frontmatter, and every normative surface needs its declared grammar; new fixed text must be added to the surface's declared grammar, not as a second normative copy | repository tests | `scripts/normative-drift.test.mjs` §§2–4 (versionedBlock, version-tables checks); the sensor-envelope-fields surface at line 845 references `skills/workflow-status/references/SENSOR_CORE.md` and line 918 references the same file's grammar block | P3; AC9, AC14 | current | proven | add any new normative surface's grammar declaration |
| PE-008 | `scripts/check-skill-context.mjs` reads `docs/workflow/SKILL_CONTEXT_BUDGETS.json` for per-skill budget entries; the skill manifest uses `ceil(UTF-8 bytes / 4)` as the metric; defaults are `mainEstimateMax: 2800`, `referenceEstimateMax: 2200` | repository tests | `scripts/check-skill-context.mjs` line 9; manifest defaults in `docs/workflow/SKILL_CONTEXT_BUDGETS.json` | P3; AC14 | current | proven | re-measure and update the manifest |
| PE-009 | The Pi package's `bundle:skills` (via `scripts/bundle-skills.mjs`) copies skill trees only; root `scripts/` do not travel with the package | repository contract | `packages/pi-agentic-workflow/package.json` line 46; feature 30 known-issues #6 | P4; AC9, AC13, known-issues B-01 | current | proven | never hand-edit bundled copies |
| PE-010 | Bilingual workflow documentation uses `.es.md` siblings with reciprocal switcher links; the bilingual sync rule requires the ES sibling in the same change as the EN file change | repository + CLAUDE.md hard rule | `docs/workflow/REVIEW_AND_CLASSIFY.md` and `.es.md` exist with reciprocal switcher links; `docs/workflow/ORCHESTRATION.md` and `.es.md` exist | P4; AC16 | current | proven | same-commit ES edit |
| PE-011 | Fix #179 (`docs/fix/179-declared-ledger-delta-receipts/` on branch `fix/179-declared-ledger-delta-receipts`, tip `e2daa79e`) declares `Depends on: Features 30 + 31 + 32` and its execution is gated until they merge; the fix index has zero pending rows | forge + branch | `git show fix/179-declared-ledger-delta-receipts:docs/fix/179-declared-ledger-delta-receipts/SPEC.md` §Depends on; `gh issue view 179` → CLOSED; `docs/fix/README.md` (all rows `done`) | blocker check; AC9 | current | proven | no blocking fix-now |
| PE-012 | Idempotence: Envelope v2 has no timestamp field (no volatile fields by construction), so two consecutive runs on the same tree produce byte-identical output verbatim | schema package | `packages/agentic-workflow-schema/src/index.ts` (Envelope interface lines 167–194 — no timestamp field; fields are: unit, pr, gates, findings, dependencies, blockers, recommendations, needs_input, next, detail, state) | P2; AC5 | current | proven | verify with diff on consecutive runs |
| PE-013 | `docs/CAPABILITIES.md` at HEAD is the unfilled seeded template — placeholder `Exists` cells and a template-only roles row; the 13 integration-closure subsystems are the template's fixed floor set | repository file | `docs/CAPABILITIES.md` lines 17–47 (re-verified live 2026-09-09); each integration row in the SPEC is self-grounded with project-specific reasons, not template inventory facts | P2; AC6 | current | proven | seeding `docs/CAPABILITIES.md` proposed to the design owner (upsert-safe) |
| PE-014 | The crash-recovery mapping (CLEAN → OK, RESUMABLE → CONTINUE, AMBIGUOUS → NEEDS_INPUT) and substrate blocker (missing/non-frozen NRS → BLOCKED + discovery command) are from CRASH_RECOVERY.md and SENSOR_CORE.md's NRS section — the script reproduces them as published, no new state | reference files | `skills/workflow-status/references/CRASH_RECOVERY.md` decision table + return format; `skills/workflow-status/references/SENSOR_CORE.md` NRS section | P2; AC2, AC4 | current | proven | reproduce verbatim |
| PE-015 | `docs/workflow/MIGRATION.md` carries additive migration notes (latest sections are version-bump style); an additive note is the established pattern | repository contract | `docs/workflow/MIGRATION.md` tail sections — dated version-bump style notes, additive only, no overwrites | P4; AC16 | current | proven | additive only |
| PE-016 | NRS is `frozen`; F010: no `docs/architecture/ARCHITECTURAL_INVARIANTS.md` exists; AD-008: convergence is evidence-bound, never cycle-count-bound; architecture classification is `n/a: no project invariants declared`; workflow invariants preserved (the script is read-only, envelope vocabulary unchanged) | ledger | `docs/workflow/REPOSITORY_STATE.md` F010, AD-008; `docs/workflow/WORKFLOW_INVARIANTS.md` workflow responsibilities table | P4; AC13 | current | decision | classification `n/a`; workflow invariants `preserves` |
| PE-017 | The `gh` CLI is already used by the existing `workflow-status` skill as part of SENSOR_CORE step 2 — no new external dependency | prior feature | feature 15 (PR #47) merge commit; existing skill text references `gh pr list`, `gh issue list` | P2; AC4, AC21 | current | proven | use the same `gh` invocation patterns |
| PE-018 | `node --test` is the root-suite convention for test files in this repository; `node --test scripts/*.test.mjs` is the established invocation pattern | repository convention | `scripts/workflow-status-pre-execution.test.mjs`, `scripts/bounded-delivery-loops.test.mjs`, `scripts/normative-drift.test.mjs` — all run under `node --test` | P4; AC9, AC14 | current | proven | use `node --test` for all suite invocations |

## Closure

- Material planning claims without evidence: none.
- Unresolved product or architecture decisions: none (open engineering interpretation
  decisions are recorded and frozen in `decisions.md` E-D1…E-D8).
- Revalidation required before execution: current bytes of the four edited skill files,
  the four re-targeted pin suites, the schema package untouched state, the current forge
  state of #185.