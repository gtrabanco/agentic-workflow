# Planning findings — 38-workflow-status-sensor-script

One stage-aware table, both stages; reviewers append rows, only the stage's
author resolves. Row shape per `pre-execution-review/references/LEDGERS.md`:
`finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision`

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| F1 | spec | high | product | 605c8ee72203a3ad466af7d39f929cc32b3f58f802e4d2711849682800511ace | The SPEC defines the scripted scope as "SENSOR_CORE sequence (steps 1–7)" and marks that claim proven/current, but the published SENSOR_CORE.md defines nine numbered steps; steps 8 (pending quality gates) and 9 (fix-now fold ledger → `findings.fix_now[]`) are scope-defining envelope producers that in-scope item 2 would silently omit from the script | SPEC.md `#### In scope` item 2 + `### Product evidence` row 1 vs `skills/workflow-status/references/SENSOR_CORE.md:15-101` (numbered steps 1–9) and `:121` ("Steps 1-9 print these keys") | resolved | in-scope item 2 + evidence row 1 re-scoped/refreshed to the published SENSOR_CORE steps 1-9 (steps 8/9 named envelope producers) — mechanical intent-preserving; decisions.md 2026-09-09 | 38-73192450f7b4 |
| F2 | spec | medium | product | 605c8ee72203a3ad466af7d39f929cc32b3f58f802e4d2711849682800511ace | The SPEC pins the schema package at v3.4.0 in three places (Tooling + two evidence rows marked `proven`/`current`, mirrored in decisions.md), but the repository's schema package is 4.1.1 — the "current" freshness labels are drifted rows that C11 forbids surviving into a PASS | SPEC.md `### Tooling` + `### Product evidence` rows 2–3; decisions.md 2026-08-30 entry vs `packages/agentic-workflow-schema/package.json:3` (`"version": "4.1.1"`) | resolved | Tooling + evidence rows 2/4 refreshed to live v4.1.1 (`package.json`, `src/index.ts` verified 2026-09-09); NRS F002 drift recorded as a contradiction candidate, never silently edited — closure completion | 38-73192450f7b4 |
| F3 | spec | medium | product | 605c8ee72203a3ad466af7d39f929cc32b3f58f802e4d2711849682800511ace | Integration closure walks 10 of the inventory's 13 template subsystems and skips File/media storage, Feature flags, and Billing/payments by silence; its own spec-lint box claims "All 13 template subsystems are covered (8 with filled rows, 5 marked n/a)" while the section has 10 rows (4 filled, 6 n/a) | SPEC.md `### Capability closure` §2 (10 rows) + spec-lint integration box vs `docs/CAPABILITIES.md:36-53` (13 subsystem rows) | resolved | integration closure completed 13/13: File/media storage, Feature flags, Billing/payments rows added with explicit n/a reasons; lint box count corrected (13 rows: 8 filled resolutions + 5 n/a) — closure completion | 38-73192450f7b4 |
| F4 | spec | medium | product | 605c8ee72203a3ad466af7d39f929cc32b3f58f802e4d2711849682800511ace | Acceptance A:3's read-only check `grep -rE '(createBranch\|push\|label\|write)'` returning nothing is unsatisfiable together with A:7 (urgency labels read from the labels object) — the script must contain label-reading code, so A:3 can never pass as written | SPEC.md A:3 vs A:7; `skills/workflow-status/references/SENSOR_CORE.md:23-27` (labels-only scan is required behavior) | resolved | A:3 rewritten as a mutation-only grep (no branch/push/label-mutation/write calls) satisfiable together with A:7 labels-only reading; read-only invariant unchanged — mechanical | 38-73192450f7b4 |
| F5 | spec | low | product | 605c8ee72203a3ad466af7d39f929cc32b3f58f802e4d2711849682800511ace | The spec-lint box's mechanical counts contradict the tables it lints: expectations "in-scope (8) / deferred (3)" vs actual 9/2; "15 read-verified markers present" vs one `read-verified:`-labelled criterion; integration "8 with filled rows, 5 marked n/a" vs 4/6 — a mechanical gate that reports wrong counts fails its own fail-closed purpose | SPEC.md `### Spec-lint` product boxes (counts) vs `### Expectation sweep` (13 rows: 9 in-scope, 2 out, 2 deferred), `### Acceptance criteria` (1 read-verified label at A:16 tail), `### Capability closure` §2 | resolved | spec-lint mechanical counts recomputed and now verified by running the greps: sweep 11 in-scope / 2 out / 0 deferred; 16 runnable criteria + 1 read-verified; 13 integration rows — mechanical | 38-73192450f7b4 |
| F6 | spec | low | product | 605c8ee72203a3ad466af7d39f929cc32b3f58f802e4d2711849682800511ace | Expectation rows 5 and 13 resolve `deferred` with pointer A:10, but A:10 is an acceptance criterion of in-scope item 10 (--help/--version) and the `Deferred decisions` section does not contain these rows — they are in-scope work mislabelled as deferred, breaking the sweep's fixed three-way protocol | SPEC.md `### Expectation sweep` rows 5, 13 vs A:10 + `#### In scope` item 10 + `### Deferred decisions` | resolved | sweep rows 5/13 relabelled deferred -> in-scope, pointer A:10 (in-scope item 10 already owned the work) — mechanical | 38-73192450f7b4 |
| F7 | spec | medium | product | 605c8ee72203a3ad466af7d39f929cc32b3f58f802e4d2711849682800511ace | Deferred decision 1 (exact degradation-code vocabulary) sets decide-by "Implementation phase" — a machine-readable contract surface (`detail` codes consumers parse) handed to the implementer instead of the human/design owner, while Product decision 4 already names an example code (`"forge": "unavailable-no-network"`); the vocabulary is a bounded product choice that needs a human answer, not an implementer default | SPEC.md `### Deferred decisions` row 1 vs `### Product decisions` 4 + issue #185 body ("degrade to their declared machine-readable unavailability code") | resolved | F7 resolved: namespaced `unavailable-<source>-<cause>` selected by human design owner via bounded question (ask_user 2026-09-09) — folded into Product decisions §6, evidence row updated, deferred row 1 removed, spec-lint deferred box updated to 1 row | 615431bda699 |
| F8 | spec | low | product | 605c8ee72203a3ad466af7d39f929cc32b3f58f802e4d2711849682800511ace | Two affected surfaces from governing issue #185 are excluded by silence: `docs/workflow/ORCHESTRATION.md` (drivers consuming the script — business goal 3's consumer wiring) and `skills/workflow-status/references/ENVELOPE_CORE.md` (named for slimming); neither appears in In scope nor Out of scope | issue #185 body §Affected surfaces vs SPEC.md `#### In scope` (11 items) + `#### Out of scope / non-goals` (8 bullets) | resolved | ENVELOPE_CORE.md named inside in-scope item 7; ORCHESTRATION.md driver wiring added as in-scope item 12 + criterion A:16 (grep >= 1) — both implied by reviewed goal 3 + issue #185; dated scope re-entry note in decisions.md — closure completion | 38-73192450f7b4 |
| F9 | spec | low | product | 605c8ee72203a3ad466af7d39f929cc32b3f58f802e4d2711849682800511ace | A:5's objective check (`diff` of two runs returns empty) contradicts its own carve-out "byte-identical (modulo volatile timestamps)" — if any volatile field is emitted the literal command fails; A:14 and A:15 are the same check stated twice (budget re-base for the slimmed skill) | SPEC.md A:5, A:14, A:15 | resolved | A:5 carve-out removed (no volatile fields by construction; byte-identical verbatim); A:14/A:15 merged into one criterion, old A:16 renumbered A:15 — mechanical | 38-73192450f7b4 |
| F10 | spec | low | product | 605c8ee72203a3ad466af7d39f929cc32b3f58f802e4d2711849682800511ace | Entity closure's Delete row resolves UI (n/a) and API but leaves the test cell blank, and its resolution wording ("part of feature lifecycle, removed when feature is merged") does not describe a delete capability of the sensor entity — a non-`n/a` row without a filled test resolution | SPEC.md `### Capability closure` §1 Delete row | resolved | Delete row test cell filled: objective grep proving no runtime delete path (unlink/fs.rm/rmSync/writeFile); wording now describes the sensor (reads only; file removal is an ordinary PR) — mechanical | 38-73192450f7b4 |
| F11 | spec | low | product | 605c8ee72203a3ad466af7d39f929cc32b3f58f802e4d2711849682800511ace | Context claims "780 lines across SKILL.md + 7 reference files"; the skill has 8 reference files (the 780-line total is correct) — a small factual mismatch in the motivating context row | SPEC.md `### Context` first bullet vs `wc -l skills/workflow-status/SKILL.md skills/workflow-status/references/*.md` (149 + 8 files = 780) | resolved | Context corrected: SKILL.md + 8 reference files (780-line total kept) — mechanical | 38-73192450f7b4 |
| F12 | spec | medium | product | 711a06257dccf86505876a014549e43a2694545bb4fb6f2b3b3d303f8b08c870 | In-scope item 10 (pass-through of the existing `--json-only` and `--last-envelope` flags) maps to no acceptance criterion — A:10 covers only `--help`/`--version` (in-scope item 11), so the spec-lint claim "In-scope items 1–12 map to A:1 through A:16" is false for item 10 and the promised pass-through behaviour is unverifiable at acceptance | SPEC.md `#### In scope` item 10 (`--json-only`/`--last-envelope`) + `### Spec-lint` in-scope-mapping box + `### Acceptance criteria` A:1–A:16 (no json-only/last-envelope criterion; the flags appear only in Product decision 5) vs `skills/workflow-status/SKILL.md:7` (argument-hint `[--json-only] [--last-envelope <json|path>]`) | resolved | Acceptance set extended (instruction's primary path, human-selected over re-scoping): in-scope item 10 concretized + Product decision 7 + criteria A:17 (`--json-only` byte-identical no-op), A:18 (`--last-envelope <json|path>` computed by the script — stale hint fires the `workflow_observations` no-progress note, hint never overrides recomputed state, inline JSON + file path both accepted), A:19 (unreadable/malformed hint → `unavailable-hint-<cause>`, exit 0, fail-open); spec-lint mapping box corrected to A:1–A:19 — four bounded human answers 2026-09-09 (script-computes / no-op / fail-open / workflow_observations) | 38-e820dfebe700 |
| F14 | spec | low | product | 711a06257dccf86505876a014549e43a2694545bb4fb6f2b3b3d303f8b08c870 | Product evidence row "Slug is free: no existing folder at `docs/features/38-workflow-status-sensor-script`" is marked `proven`/`current` but is stale — the folder now exists and holds this unit's own artifacts, so a `current`-freshness row whose claim is false today survives into the half, which C11 forbids | SPEC.md `### Product evidence` row 8 ("Slug is free") vs `ls docs/features/38-workflow-status-sensor-script/` at source revision 212f7210 (SPEC.md, decisions.md, progress.md, planning-findings.md present) | resolved | Row 8 refreshed to the current fact: folder exists holding this unit's artifacts (SPEC.md, decisions.md, progress.md, planning-findings.md), re-verified 2026-09-09; authoring-time "slug is free" check retained as history only — mechanical | 38-e820dfebe700 |
| F15 | spec | medium | product | 21fadfc815161d1c6606c7524443b52a0186c85a46706fe9259efa90e4bff742 | Acceptance A:7's check (`grep -E 'title\|body\|comment' scripts/workflow-status.mjs` returns nothing) is unsatisfiable together with in-scope item 2: the published SENSOR_CORE sequence the script must execute mandates, at step 3, emitting `{number, title, label}` for each urgent-labeled issue, and the published envelope example carries `urgent.issues[].title` — the script must reference the string `title`, so the criterion can only pass by dropping required output or by obfuscating the key; it also contradicts A:3's own note that label *reading* stays (the invariant is labels-only scanning, not title-string absence) | SPEC.md A:7 + `#### In scope` item 2 + A:3 note vs `skills/workflow-status/references/SENSOR_CORE.md` step 3 (`emit {number, title, label}`) + `references/ENVELOPE_FIELDS.md:57-58` (`"urgent": {"issues": [{"number": 51, "title": …`) | resolved | A:7's check rewritten mechanical/intent-preserving: forbidden set is now body/comments only (`grep -nE '\b(body\|comment)' scripts/workflow-status.mjs` returns nothing — never fetched/read) plus a positive labels-path check (`grep -cE 'labels'` ≥ 1); `title` removed from the forbidden set (SENSOR_CORE step 3 emits `urgent.issues[].title` — carrying the fetched title through to the envelope is output, not a scan); the labels-only scanning invariant itself unchanged (in-scope item 6 + A:3 note + read-verified criterion) | 38-d04b301f28d6 |
| F16 | spec | low | product | 21fadfc815161d1c6606c7524443b52a0186c85a46706fe9259efa90e4bff742 | Acceptance A:8's check (`grep 'workflow-status' scripts/workflow-status.mjs \| grep -c 'schema'` ≥ 1) does not verify its claim "script imports the schema package's Envelope v2 vocabulary": the package name `@gtrabanco/agentic-workflow-schema` does not contain the substring `workflow-status` (…workflow-schema ≠ …workflow-status), so a genuine import line fails the count while a comment line containing both strings passes with no import at all — the criterion passes or fails independently of the property it asserts | SPEC.md A:8 vs `packages/agentic-workflow-schema/package.json:3` (package name) — substring check: `agentic-workflow-schema` lacks `workflow-status` | resolved | A:8's check replaced mechanical/intent-preserving: direct import grep `grep -nE "^import .*'@gtrabanco/agentic-workflow-schema'" scripts/workflow-status.mjs` returns a match — verifies the claim (a genuine import of the schema package) instead of a substring the package name cannot contain; the claim itself unchanged | 38-d04b301f28d6 |
| F17 | spec | medium | product | 4195c762f300577c2e2cfa592d3e8afe1be79595ec804e1cb20db5f3d7588f0a | Expectation-sweep row 2 resolves `in-scope` with pointer A:4, but A:4's criterion (offline fixture: no network → declared codes, exit 0, no hang; check: fixture-repo test with network severed) does not carry the row's promised behavior — non-zero exit on fatal errors (e.g. missing git) has no acceptance criterion anywhere in A:1–A:19; the pointer's own parenthetical ("fatal errors outside scope should still exit non-zero") is aspirational wording, not a checkable criterion, so per the sweep's own protocol ("an unresolved row fails the gate") the row is effectively unresolved — same defect class as F12 (in-scope behavior unverifiable at acceptance) | SPEC.md `### Expectation sweep` row 2 (line 226) vs A:4 (line 252) and the full A:1–A:19 set (no fatal-exit criterion); ledger precedent F12 (rp-38-20260909-002, medium) | resolved | A:20 added (closure completion): invalid invocation is the sensor's fatal class — unknown flag → non-zero exit + usage diagnostic on stderr, check form verified against the repo's own CLI convention (live: `scripts/ledger-provenance.mjs` unknown flag → usage + exit 2, `scripts/check-skill-context.mjs` → exit 1); sweep row 2 re-pointed to A:20 and its stale "like missing git" example corrected against reviewed Product decision 6 (`unavailable-git-missing` is a degradation code — environmental failures degrade with exit 0); row stays in-scope, scope unchanged — repair batch 2026-09-09 | 38-626571011339 |
| F18 | spec | info | product | 4195c762f300577c2e2cfa592d3e8afe1be79595ec804e1cb20db5f3d7588f0a | Sweep row 8's "byte-identical output (modulo volatile timestamps)" parenthetical is a stale echo of the carve-out the F9 repair removed from A:5 ("byte-identical verbatim — no volatile fields by construction"); the two sections describe the same guarantee at different strictness, so a reader cannot tell which binds; A:5 is the operative criterion and is verified sound (Envelope v2 declares no timestamp field), so this is wording drift, not behavioral contradiction | SPEC.md sweep row 8 (line 232) vs A:5; `packages/agentic-workflow-schema/src/index.ts:167-182` (Envelope fields — no timestamp) | resolved | sweep row 8 reworded to A:5's operative strictness (byte-identical verbatim — no volatile fields by construction; timestamp carve-out reference removed) — mechanical, intent-preserving; repair batch 2026-09-09 | 38-626571011339 |
| F19 | spec | info | product | 4195c762f300577c2e2cfa592d3e8afe1be79595ec804e1cb20db5f3d7588f0a | §2's capability-inventory preamble calls `docs/CAPABILITIES.md` "the maintained list of cross-cutting subsystems" and the spec-lint roles box counts 3 roles (consumer-processes, weak-executor-models, human-operators) as "the capability inventory", but at this revision the file is an unfilled seeded template — every Exists cell is a placeholder and the Roles table holds only the template row; the 13 walked subsystems are the template's floor set (the lint box itself says "template subsystems") and the 3 roles are derived, not inventory rows. Substance survives (each integration row is self-grounded with project-specific reasons), so non-material; provenance should be stated and inventory seeding proposed per the closure contract's fallback | `docs/CAPABILITIES.md:17-47` (template placeholders, empty roles) vs SPEC.md §2 preamble + spec-lint roles box | resolved | provenance stated in §2 preamble (file is the unfilled `init-workspace`-seeded template; the 13 subsystems are the template's fixed floor set, each row self-grounded) and in the spec-lint roles box (the 3 roles are derived from the sensor's consumer set, not inventory rows); new evidence row pins the template state at `docs/CAPABILITIES.md:17-47` (re-verified live 2026-09-09); seeding proposed to the design owner (decisions.md + closing block) — user confirmation pending, upsert-safe; non-material claim, substance survives — repair batch 2026-09-09 | 38-626571011339 |
| F20 | spec | medium | product | 4195c762f300577c2e2cfa592d3e8afe1be79595ec804e1cb20db5f3d7588f0a | A:11's check (`node --experimental-specifier-resolution=node -e "import('scripts/workflow-status.mjs')"`) is unsatisfiable as written: the dynamic-import specifier lacks the mandatory `./` prefix, so it is parsed as a bare package specifier and fails ERR_MODULE_NOT_FOUND ("Cannot find package 'scripts'") independent of the script's dependency graph — the criterion can never pass and therefore does not verify its claim ("no external dependencies beyond the schema package"); same root cause as F15/F16 (acceptance checks never executed against reality) | SPEC.md A:11 vs live run at this revision on Node v24.19.0: control `node -e "import('scripts/check-skill-context.mjs')"` → ERR_MODULE_NOT_FOUND; with `./` prefix → succeeds — the failure is the specifier form, not the dependency graph | resolved | A:11's check rewritten to the working form `node -e "import('./scripts/workflow-status.mjs')"` (claim unchanged: no external dependencies beyond the schema package); the mandatory `./` prefix verified live on Node v24.19.0 (bare specifier → ERR_MODULE_NOT_FOUND, `./`-prefixed → exit 0) and the no-op legacy flag `--experimental-specifier-resolution=node` dropped; new evidence row pins the live control — mechanical, intent-preserving; repair batch 2026-09-09 | 38-626571011339 |
| F21 | spec | medium | product | 38cfe90cc3780cf94f9837ef10b3371bbe2df554d5fb0e02ad4dc6bb7b3b5555 | The timed-out-forge degradation is promised in three places (Product decision 6 names `unavailable-forge-timeout`; expectation row 10 resolves in-scope "should not hang the script" pointing at A:4; A:4 promises "exit 0, no hang") but no acceptance check exercises it: A:4's check form is a fixture with the network severed, under which `gh` fails fast — a slow-but-alive forge (the hang scenario the row names) is never produced, so the criterion passes or fails independently of the timeout behavior it is pointed at for; same defect class as F17 (sweep pointer to a criterion whose check does not carry the row's claim) and the F15/F16/F20 root cause (checks not executed against the behavior they claim) | SPEC.md `### Expectation sweep` row 10 + `### Product decisions` 6 (`unavailable-forge-timeout`) + `### Acceptance criteria` A:4 (check: "fixture-repo test with network severed") — grep of A:1–A:20 finds no slow/hanging-forge fixture | resolved | A:21 added (closure completion): the timed-out-forge case is actually produced — fixture-repo test with a non-terminating `gh` shim → exit 0 within the forge timeout, `unavailable-forge-timeout` in `detail`; A:4 scoped by note to the severed-network (fail-fast) case and cross-references A:21; sweep row 10 re-pointed to A:21; Product decision 6 states the bounded forge timeout; decision evidence row added — repair batch 2026-09-09 | 38-6821d835490b |
| F22 | spec | low | product | 38cfe90cc3780cf94f9837ef10b3371bbe2df554d5fb0e02ad4dc6bb7b3b5555 | Expectation rows 3 ("No interactive prompts") and 6 ("Stdout for data, stderr for diagnostics") resolve in-scope with pointers to A:3, whose criterion and check form are a mutation-only grep (branch/push/label-mutation/write calls); the pointer cells' parentheticals ("script is deterministic and headless; no prompts in code path", "script prints JSON to stdout; diagnostics to stderr") describe behavior no A:1–A:20 check verifies, so both rows resolve to a criterion that does not carry their claim — same class as F17/F21 | SPEC.md `### Expectation sweep` rows 3 and 6 (pointers to A:3) vs A:3's text + check form (mutation grep only) | resolved | A:22 + A:23 added (closure completion): A:22 greps the no-prompts claim (no readline/createInterface/stdin-read/prompt-library call), A:23 checks stdout alone parses as one valid JSON document and the A:4 offline fixture asserts diagnostics land on stderr; sweep rows 3 and 6 re-pointed — repair batch 2026-09-09 | 38-6821d835490b |
| F24 | spec | info | product | 02c17f26c4580628d116ee43d9ef9e37422d6ab152c79b8a46402eca301f1b60 | Expectation-sweep row 13's pointer gloss says "A:10 (--version prints the schema package version; in-scope item 11)" but A:10's operative check text says only "`--version` exits 0 and prints version" — the equality source named by the gloss (schema package version) is not carried by the criterion text; the essential claim (a --version flag exists and prints a version) IS carried by A:10, and no repository surface defines a script "package version" to match (no root package.json — evidence row 15), so this is pointer-gloss wording drift, not an unverifiable behavior — same non-material class as F18 | SPEC.md `### Expectation sweep` row 13 pointer vs `### Acceptance criteria` A:10 text + `### Product evidence` row 15 (no root package.json, verified live 2026-09-09) | resolved | A:10's check text now carries the version source (the schema package's version, per E-38-4) that sweep row 13's gloss and the frozen AC-10 outcome cell already name — mechanical, intent-preserving, folded into the F31–F34 batch | 5eb9724bb44f |
| F23 | spec | medium | product | 38cfe90cc3780cf94f9837ef10b3371bbe2df554d5fb0e02ad4dc6bb7b3b5555 | The half claims the schema package "is already an existing dependency" (Product decision 2 + Tooling) and pins acceptance to that resolution: A:8 mandates a bare-specifier import line (`grep -nE "^import .*'@gtrabanco/agentic-workflow-schema'"` must match) and A:11 requires `node -e "import('./scripts/workflow-status.mjs')"` to exit 0 — but the repository provides no bare-specifier resolution for scripts/: there is no root package.json, no installed `@gtrabanco` in node_modules, and the repo's established mechanism (`scripts/schema-runtime.mjs`) loads the built local package by explicit path (`packages/agentic-workflow-schema/dist/index.js`, a gitignored build output) while documenting that it "deliberately does NOT fall back to an installed @gtrabanco/agentic-workflow-schema: a published build can be older than the source under review". As written, A:8 is satisfiable only by adding an undeclared repo surface (root package.json + dependency install), and A:11's exit-0 fails on a fresh clone regardless of the `./` prefix; A:11's F20 control validated the specifier form against a placeholder (check-skill-context.mjs, no schema import) and never exercised the schema-resolution precondition | SPEC.md `### Product decisions` 2 + `### Tooling` + A:8 + A:11 vs live checks at ae882179: no root package.json; `ls node_modules/@gtrabanco` empty; `git check-ignore packages/agentic-workflow-schema/dist` → ignored; `packages/agentic-workflow-schema/package.json:22` `"main": "./dist/index.js"`; `scripts/schema-runtime.mjs:5-20,47-49` (path-based load + deliberate no-published-name-fallback rationale) | resolved | mechanism corrected to the repo's established loader (closure completion + mechanical): A:8 now greps for the `./schema-runtime.mjs` import and forbids the bare-specifier import (no bare-specifier resolution exists for scripts/); A:11 split into built-dist import exit-0 + missing-dist named-precondition cases; Tooling, Product decision 2 and in-scope item 1 aligned (built local package, no dependency install, no published fallback — reviewed intent 'only the schema package' unchanged); new evidence row pins the live mechanism (re-verified 2026-09-09) — repair batch 2026-09-09 | 38-6821d835490b |
| F25 | plan | low | plan | 5cf504ef6be5d572c89d2289b86007403826149a6979c41d3e2e459624c79ca3 | The P1/P2 phase cut breaks the red-first discipline for A-17 and A-20: P1 task 2 already implements the closed flag set "unknown flag -> usage on stderr + non-zero exit" (A-20's behavior verbatim) and parses `--json-only` over the closed set (which, on a script whose only output is the envelope, is already a byte-identical no-op — A-17's behavior), while both pins are written "red-first" in P2 task 1 — so at the P1 commit the two behaviors ship with no validator, and at P2 the pins are born green and can never have failed, violating TASKS.md's own header rule ("Tests are written red-first inside each phase and made green by that phase's implementation") | TASKS.md P1 task 2 vs P2 task 1 + header red-first rule; ACCEPTANCE.md A-17/A-20 (both listed only in P2's pins and P2's done-when, never in P1's); SPEC `#### P1`/`#### P2` done-whens (A-10/A-17/A-20 named only under P2) | resolved | phase cut repaired mechanically, intent-preserving: A-17/A-20 pins moved into P1's red-first pin task + P1 done-when (behavior and pin in the same phase, red→green inside P1); P2 re-asserts them unchanged (never re-written red-first); P2 task 4 now only implements --help/--version; SPEC P1/P2 phase paragraphs + PLAN.md + testing.md ladder split Flag-contract-core (P1) / Flag-discoverability (P2) — operator-authorized batch 2026-09-09 (issue #205) | 38-c51c1416f9bf |
| F26 | plan | medium | product | 5cf504ef6be5d572c89d2289b86007403826149a6979c41d3e2e459624c79ca3 | In-scope item 7's `references/ENVELOPE_CORE.md` slimming maps to no acceptance criterion: A:9/A-09's checks read only SKILL.md + SENSOR_CORE.md (script-call presence + numbered-step count 0); no suite reads ENVELOPE_CORE.md (grep over `scripts/*.test.mjs` and `scripts/check-skill-context.mjs` returns nothing); the six-untouched-files check covers the OTHER references (ENVELOPE_CORE is correctly absent from that list, which proves nothing); and check-skill-context's re-based budget is measured-then-set, so it passes whatever shape the skill ends up in — P3 task 4 ("Slim references/ENVELOPE_CORE.md") could ship nothing (file untouched) and every frozen validator in ACCEPTANCE.md still exits 0, leaving the second-largest token surface unslimmed while the SPEC claims it in scope; same defect class as spec-stage F12/F17/F22 (in-scope behavior unverifiable at acceptance) | SPEC.md `#### In scope` item 7 + `### Spec-lint` in-scope-mapping box ("items 1–12 map to A:1 through A:23") vs A:9 + ACCEPTANCE.md A-09 (SKILL.md + SENSOR_CORE.md only); grep of the frozen ladder and four discipline suites for `ENVELOPE_CORE` → no reference; P3 task 4 + done-when | resolved | closure completion, operator-authorized (issue #205): criterion A:24 added to the Product half (ENVELOPE_CORE.md slimmed: script-backed reference ≥ 1 + assembly self-check prose gone) + ACCEPTANCE row A-24 + spec-lint counts 24 runnable + mapping box item 7 → A:24; the F12 resolution form — no new product choice needed, the criterion mirrors A:9's shape — batch 2026-09-09 | 38-c51c1416f9bf |
| F27 | plan | low | plan | 5cf504ef6be5d572c89d2289b86007403826149a6979c41d3e2e459624c79ca3 | E-38-1's declared failure path — `validateEnvelope` mismatch → stderr diagnostic, envelope still printed, exit 0 (never a gate) — has no scenario row, no pin, and no validator anywhere in the frozen ladder: the Dev scenarios table and testing.md's mandatory inventory cover offline/timeout/missing-git/hint/invalid-invocation/empty/caps/concurrency but not the mismatch path; an implementation that gates on mismatch (exits non-zero, suppressing the envelope) would violate A:20's restricted fatal class ("invalid invocation is the ONLY fatal class") and no frozen validator would notice; a feasible mechanism exists unassigned (swap the gitignored `dist/` build for a stub whose `validateEnvelope` always fails — the explicit-path loader makes this possible without a test seam) | SPEC.md Engineering Design assembly paragraph + `### Decisions to confirm` E-38-1; testing.md pin list (no mismatch entry); Dev scenarios table (no mismatch row); ACCEPTANCE.md A-20 (tests unknown flag only) | resolved | scenario+pin added, operator-authorized (issue #205): dev scenario sensor:envelope-mismatch (stub dist build whose validateEnvelope always fails, swapped via the explicit-path loader PE-001 → stderr diagnostic, envelope still printed, exit 0) added to SPEC Dev scenarios + testing.md inventory; P1 red-first mismatch pin added to TASKS P1 task 1 + P1 done-when; obligation O27 frozen in planning-obligations.md (P1, suite validator) — batch 2026-09-09 | 38-c51c1416f9bf |
| F28 | plan | info | product | 5cf504ef6be5d572c89d2289b86007403826149a6979c41d3e2e459624c79ca3 | A-07's check `grep -nE '\b(body|comment)'` returns-no-match also fails on innocuous occurrences — e.g. a source comment documenting the labels-only invariant ("bodies and comments are never fetched") turns the criterion red for the wrong reason; the invariant itself is correctly enforced in combination (A-11's runtime case catches any real body/comment fetch via the loader graph; A-RV read-verifies), so non-material — recorded so the implementer rewords documentation instead of silently weakening the grep (F15-family residual on the same criterion line) | ACCEPTANCE.md A-07 check text; SPEC.md A:7 | resolved | mechanical, intent-preserving, folded into the same operator-authorized batch (issue #205): A:7/A-07's forbidden set scoped to forge request field lists (`grep -nE '\-\-json[^|]*(body|comment)'` no match) — documenting the invariant in a source comment can no longer flip the check; the labels-only invariant itself unchanged (in-scope item 6 + A-RV read-verified) — batch 2026-09-09 | 38-c51c1416f9bf |
| F29 | spec | low | product | 22cba9ae43d09dfddf85a2a26f4d2dbb3da4e21af98c2ce4b7213595c7b846e5 | A:7's new check (`grep -nE '\-\-json[^|]*(body\|comment)'` returns nothing) does not catch a body/comment fetch made through a form other than an `--json` field list (e.g. a `gh api` endpoint whose response includes those fields), so the scoped grep could pass while the labels-only invariant is violated by another command shape; non-material under the operator's materiality bar — the invariant is independently guarded by A-RV's read-verification and the runtime loader checks, and no user-visible outcome of the frozen acceptance manifest is missed | SPEC.md A:7 (check text, this revision) vs ACCEPTANCE.md A-07 + A-RV | open | advisory only — no cycle restart; fold path: implementer should keep all forge reads on explicit `--json` field lists | 38-c51c1416f9bf |
| F30 | plan | info | plan | 6bb8b1dbe6c2a3e5f3c29e6983c2f09b51c1980f23a8b1f553a83d3611e271a3 | planning-obligations O7's validator cell still carries the older unscoped `\\b(body\\|comment)` grep instead of ACCEPTANCE.md A-07's F28-repaired scoped form (`\\-\\-json[^\\|]*(body\\|comment)`); the ledger rule says validators are copied from the frozen manifest — a stricter-than-frozen divergence, not a weakening, and the labels-only invariant is unaffected | planning-obligations.md O7 validator vs ACCEPTANCE.md A-07 check | superseded | F31–F33 (rp-38-20260909-010) re-verified the premise inverted after commit ed7aae98: ACCEPTANCE A-07 itself regressed to the unscoped form, so O7 and A-07 now agree with each other and both contradict SPEC A:7 — folded into F32's repair | — |
| F31 | plan | medium | product | 96025bc24a9075a8192fd3b1d7e5a723170d0a774567cab4ff9eec602f8c4d6b | Parent lineage stale: the operator-approved scope amendment (fix #209 fold, commits 32bb6434+1ce0bfae) moved the Product half after the newest SPEC-REVIEW-PASS receipt (rp-008) — spec-product-v1 projection e6d71d51 (43160 B) → 4f2c60ff (44201 B, A:25 added), stage snapshot 22cba9ae → 15875fe2, artifactRevisionId 38-c51c1416f9bf → 2bee477ba469, and the project-guide context moved with it (CLAUDE.md 9ae03966 → 89908a32; the fold pre-executed the release-policy line) — so this plan descends from a parent that no longer reviews its own governing SPEC; plan-stage bytes also moved after plan PASS rp-009 (ed7aae98 post-plan-review edits + 1ce0bfae), so rp-009 binds no current bytes either (sensor verify: current=false, structural.fresh=false, stale-context, observed 96025bc2 ≠ receipt 6bb8b1db) | builder re-derivations live at 1ce0bfae: spec-stage snapshot 15875fe2 ≠ rp-008 bound 22cba9ae; plan-stage snapshot 96025bc2 ≠ rp-009 bound 6bb8b1db; `git show --stat` for 32bb6434 (SPEC.md +31/-7) and ed7aae98 (7 plan artifacts); the fold batch's own progress note declares "bounded delta re-review required (spec first, then plan)" | open | route: /review-spec 38 (bounded delta over the fold amendment) first; the Plan receipt is then re-derived over the new snapshot — never repaired by re-copying digests — identity half resolved by this batch: in-file stamp claiming 2bee477ba469 = sha256 prefix removed, recomputation recorded (2e0ab6084f21… at 32bb6434/1ce0bfae, POLICY §7 claimed-beside-recomputed), lineage now decisions.md/progress.md only (rp-001…rp-009 convention), new revision 5eb9724bb44f | 5eb9724bb44f |
| F32 | plan | low | plan | 96025bc24a9075a8192fd3b1d7e5a723170d0a774567cab4ff9eec602f8c4d6b | ACCEPTANCE.md A-07's validator regressed to the pre-F28 unscoped form (`grep -nE '\b(body\|comment)' scripts/workflow-status.mjs` → nothing) in the post-plan-review rewrite (ed7aae98), contradicting governing SPEC A:7 which still carries the F28-repaired scoped form (`--json[^\|]*(body\|comment)` — scoped so a source comment documenting the invariant cannot flip the check); obligations O7 carries the same unscoped form, so both ledger copies now diverge from the SPEC criterion and the rp-009 delta-verification line "F28 ✓ (A-07 scoped to forge request field lists)" is no longer true of current bytes — the unscoped grep is the exact wrong-reason failure F28 evidenced | SPEC.md A:7 (scoped) vs ACCEPTANCE.md A-07 + planning-obligations.md O7 (unscoped); `git show ed7aae98 -- ACCEPTANCE.md` hunk replaces the scoped check; findings F28/F29/F30 lineage | resolved | ACCEPTANCE AC-07 + obligations O7 re-aligned to SPEC A:7's F28-scoped grep (forge request field lists) — validator cells copied from the governing criterion, invariant unchanged; operator-directed consolidated batch 2026-09-11 (F25–F28 precedent, recorded in decisions.md) | 5eb9724bb44f |
| F33 | plan | medium | plan | 96025bc24a9075a8192fd3b1d7e5a723170d0a774567cab4ff9eec602f8c4d6b | The post-plan-review rewrite (ed7aae98) dropped obligation rows O25 (bilingual-sync rule, P4), O26 (no-weakening pin rule, P3), and O27 (envelope-mismatch diagnostic-not-gate, P1 — F27's frozen resolution) from planning-obligations.md: the ledger at parent revision 2e34445b carried O1–O27; the current ledger carries a different 27 rows (O1–O23, O28, O-RV, O-01, O-02) whose added ids do not cover the dropped rows — PLAN.md:112 and TASKS.md:193 still cite the now-dangling `(O26)`, the F27-mandated mismatch obligation has no ledger row at all, and the bilingual-sync and pin-no-weakening obligations survive only as prose in PLAN/TASKS with no ledger row (L3/L4 would fail if swept) | `git show 2e34445b:docs/features/38-workflow-status-sensor-script/planning-obligations.md` rows O24–O27 vs current rows; `grep -n "O2[5-7]" TASKS.md PLAN.md` → TASKS.md:193, PLAN.md:112 (O26 dangling); current ledger id set from live grep | resolved | O25 (bilingual-sync, P4), O26 (no-weakening pins, P3), O27 (envelope-mismatch diagnostic-not-gate, P1 — F27's frozen resolution) restored verbatim from the parent 2e34445b ledger; dangling PLAN.md:112 / TASKS.md:193 (O26) citations resolve again; O24 covered by its rename O-RV (same obligation, AC-RV, P4) — operator-directed consolidated batch 2026-09-11 | 5eb9724bb44f |
| F34 | plan | low | plan | 96025bc24a9075a8192fd3b1d7e5a723170d0a774567cab4ff9eec602f8c4d6b | testing.md contradicts itself and the SPEC on where the envelope-mismatch pin lives: the mandatory-scenario inventory row says "sensor:envelope-mismatch … (P2, E-38-1)" while the validation-ladder row 1 lists envelope-mismatch under the P1 layer, and SPEC.md's scenario table says "(E-38-1; P1 mismatch pin — F27 repair)" with TASKS.md carrying the pin in P1 (lines 13, 44) — the F27 repair moved the pin to P1; testing.md's inventory row is stale | testing.md:19 (P2) vs testing.md:7 (P1 ladder) vs SPEC.md:788 (P1) vs TASKS.md:13,44 (P1) | resolved | testing.md inventory row sensor:envelope-mismatch re-pointed P2 → P1 — ladder row 1, TASKS P1 pins (lines 13, 44) and the SPEC scenario table now agree — mechanical, operator-directed batch 2026-09-11 | 5eb9724bb44f |
