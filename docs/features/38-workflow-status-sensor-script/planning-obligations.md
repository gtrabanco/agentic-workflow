# Planning obligations — 38-workflow-status-sensor-script

One row per phase slice of each acceptance criterion (AC-01…AC-23) from the
frozen `ACCEPTANCE.md`, plus IS-01 for the read-only invariant and IS-02 for
the injection-safety preservation. Row shape and column order per
`pre-execution-review/references/LEDGERS.md` §2. Status vocabulary:
`planned | in-progress | verified | n/a | deferred` — every row starts
`planned`; no row is `deferred` (no obligation is exported to a follow-up issue).

| obligation-id | Authority source | Affected use case or invariant | Phase | Task | Implementation owner | Validator | Required evidence | Status |
|---|---|---|---|---|---|---|---|---|
| O1 | AC-01 | The sensor script file exists at `scripts/workflow-status.mjs` | P1 | Create the module scaffold with shebang, loader import, main() entry | execute-phase | `test -f scripts/workflow-status.mjs` | exit 0; file present at expected path | planned |
| O2 | AC-02 | Script output is valid Envelope v2 — the complete envelope (all SENSOR_CORE steps implemented) validates against the schema package's envelope schema | P2 | Assemble the complete Envelope v2 with state, next, and detail from all steps; self-validate with validateEnvelope | execute-phase | fixture-repo property test: build git repo, run script, parse JSON, `validateEnvelope` → `valid` | `validateEnvelope(envelope)` returns `valid` on fixture output | planned |
| O3 | AC-03 | Script is read-only: no mutation calls exist in the source code | P1 | Implement read-only enforcement; the script never calls write/branch/push/label-mutation functions | execute-phase | `grep -nE '(createBranch|git push|gh pr (edit\|merge\|close\|create)|gh issue (edit\|close\|label\|create)|writeFile|fs\.write|unlink)' scripts/workflow-status.mjs` → nothing | grep returns no matches | planned |
| O4 | AC-04 | Offline fixture: severed network → forge sections degrade to declared codes, exit 0, no hang | P2 | Implement forge calls under bounded wall-clock timeout; degraded codes for no-network/fail-fast cases | execute-phase | fixture-repo test with `gh` shim failing fast → `unavailable-forge-no-network` codes in detail, exit 0, no hang | degradation codes present; exit 0; no hang within test timeout | planned |
| O5 | AC-05 | Idempotence: two consecutive runs on the same fixture tree → byte-identical output verbatim | P2 | The script emits no volatile fields (no clock reads, no timestamps); key order is fixed literal | execute-phase | `diff <(node scripts/workflow-status.mjs) <(node scripts/workflow-status.mjs)` → empty output | diff returns empty; byte-identical on consecutive runs | planned |
| O6 | AC-06 | Ambiguous roadmap row → mapped state + named degradation in output | P2 | Parse roadmap rows; non-standard status maps to nearest five-state value with `default: idea` and notes the raw string | execute-phase | fixture-repo test with a non-standard status row → `detail` contains the mapped state code and the raw string note in `workflow_observations` | mapped state code present; raw string in workflow_observations | planned |
| O7 | AC-07 | Urgency labels read from the labels object only — script never fetches/bodies/comments and always reads labels | P2 | Implement step 3: urgency labels-only scan of the step-2 issue list's labels array; bodies and comments never fetched | execute-phase | `grep -nE '\b(body\|comment)' scripts/workflow-status.mjs` → nothing AND `grep -cE 'labels' scripts/workflow-status.mjs` ≥ 1 | grep body/comment returns nothing; grep labels returns ≥ 1 | planned |
| O8 | AC-08 | Script consumes schema vocabulary through the repo's established loader | P1 | Import `loadSchemaRuntime` from `./schema-runtime.mjs` with the `./` prefix | execute-phase | `grep -nE "from ['\"]\\./schema-runtime\\.mjs['\"]" scripts/workflow-status.mjs` → match AND `grep -nE "^import .*'@gtrabanco/agentic-workflow-schema'" scripts/workflow-status.mjs` → nothing | first grep finds match; second grep finds nothing | planned |
| O9 | AC-09 | `skills/workflow-status/SKILL.md` slimmed: SENSOR_CORE numbered-command prose replaced by script call reference | P3 | Slim SKILL.md: replace the ~10-command assembly with a single script call; keep turn-contract boxes and structural sections | execute-phase | `git diff --cached -- skills/workflow-status/SKILL.md | grep -c 'workflow-status.mjs'` ≥ 1 AND the original numbered-command steps count is reduced | grep count ≥ 1 for script call; numbered-command count reduced | planned |
| O10 | AC-10 | `--help` exits 0 and prints usage; `--version` exits 0 and prints schema package version | P1 | Implement argv parsing: `--help` prints usage (exit 0), `--version` reads schema package version from package.json (exit 0) | execute-phase | `node scripts/workflow-status.mjs --help` → exit 0 with usage text; `node scripts/workflow-status.mjs --version` → exit 0 with version string | exit 0; usage text present for --help; version string present for --version | planned |
| O11 | AC-11 | No external dependencies beyond the schema package | P1 | The script imports only `loadSchemaRuntime` from `./schema-runtime.mjs` (built local package); no other imports | execute-phase | case (a) `node -e "import('./scripts/workflow-status.mjs')"` → exit 0 (with built dist); case (b) same import with `dist/` hidden → "schema runtime is not built" error | case a exit 0; case b named precondition error, not ERR_MODULE_NOT_FOUND | planned |
| O12 | AC-12 | `decideWorkflowAction()` is NOT referenced in the script | P2 | The script's envelope assembly does not call `decideWorkflowAction()`; that function stays consumer-side | execute-phase | `grep -c 'decideWorkflowAction' scripts/workflow-status.mjs` → 0 | grep count equals 0; the string is absent | planned |
| O13 | AC-13 | No change to the envelope vocabulary — the schema package is byte-untouched | P4 | The PR must not modify any file under `packages/agentic-workflow-schema/` | execute-phase | `git diff --name-only main...HEAD -- packages/agentic-workflow-schema` → empty | empty output; no files under the schema package changed | planned |
| O14 | AC-14 | Discipline-test pins and `check-skill-context` budgets re-based for the slimmed sensor route | P3 | Slim the skill, re-target discipline-test pins, re-measure and update the budget manifest | execute-phase | `node scripts/check-skill-context.mjs` → exit 0 with the updated `workflow-status` budget entry | suite exits 0; budget entry present in manifest | planned |
| O15 | AC-15 | Envelope v2 output includes `detail` section listing each degraded dimension (when offline) | P2 | Every environmental failure emits a namespaced code in `detail`; offline fixture output contains `detail` key with degradation entries | execute-phase | offline fixture output contains `detail` key with degradation entries (`forge`, `git` sources listed) | `detail` key present; degradation codes present in `detail` | planned |
| O16 | AC-16 | `docs/workflow/ORCHESTRATION.md` driver wiring points consumers at the script | P4 | Add the script path and invocation convention to the driver integration section; bilingual `.es.md` sibling updated | execute-phase | `grep -c 'workflow-status.mjs' docs/workflow/ORCHESTRATION.md` ≥ 1 | grep count ≥ 1; ES sibling has same wiring | planned |
| O17 | AC-17 | `--json-only` is an accepted no-op — output is identical with and without the flag | P1 | Implement `--json-only` as an accepted no-op; the script always prints only the envelope | execute-phase | `diff <(node scripts/workflow-status.mjs) <(node scripts/workflow-status.mjs --json-only)` → empty | diff returns empty; byte-identical output with/without flag | planned |
| O18 | AC-18 | `--last-envelope <json|path>` guard: stale hint's `next.recommended` targeted a unit still at pre-advance status → `detail.workflow_observations` contains the no-progress note | P2 | Implement the no-progress guard: load hint, diff against recomputed state, emit workflow_observations note when the unit's recomputed status is still at the pre-advance state | execute-phase | fixture-repo test with stale hint → `workflow_observations` contains the no-progress divergence note; recomputed `state`/`next` unchanged | note present in workflow_observations; state/next unchanged | planned |
| O19 | AC-19 | Unreadable/malformed hint degrades without failing — exit 0 with `unavailable-hint-<cause>` note | P2 | Degrade-to-namespaced-codes: missing path → `unavailable-hint-missing-path`, invalid JSON → `unavailable-hint-invalid-json`; both with exit 0 | execute-phase | fixture-repo test with missing path → `unavailable-hint-missing-path` note, exit 0; invalid JSON hint → `unavailable-hint-invalid-json` note, exit 0 | note present; exit 0; recomputed envelope unaffected | planned |
| O20 | AC-20 | Invalid invocation is the only fatal exit — unknown flag → non-zero exit with usage diagnostic on stderr | P1 | Implement argv parsing: unknown flag → usage diagnostic on stderr + non-zero exit (value: 1) | execute-phase | `node scripts/workflow-status.mjs --not-a-real-flag` → non-zero exit code; stderr contains usage diagnostic | exit code non-zero; stderr contains usage text | planned |
| O21 | AC-21 | Slow-but-alive forge cannot hang the script — bounded wall-clock timeout degrades to `unavailable-forge-timeout` | P2 | Implement bounded wall-clock timeout for all forge calls; non-terminating `gh` shim → `unavailable-forge-timeout` within the bound | execute-phase | fixture-repo test with `gh` shim that accepts but never terminates → script exits 0 within the timeout bound, `unavailable-forge-timeout` in detail | `unavailable-forge-timeout` present in detail; exit 0; no hang beyond bound | planned |
| O22 | AC-22 | No interactive prompts — the script is headless by construction | P1 | The script never calls readline, createInterface, stdin-read, or prompt library functions | execute-phase | `grep -nE '(readline|createInterface|process\.stdin\.(read\|setRawMode)|@clack|inquirer|prompts?\(|confirm\()' scripts/workflow-status.mjs` → nothing | grep returns no matches | planned |
| O23 | AC-23 | Stdout for data, stderr for diagnostics — stdout alone parses as one valid JSON document | P1 | The envelope JSON goes to stdout only; all diagnostics go to stderr | execute-phase | `node scripts/workflow-status.mjs 2>/dev/null | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{JSON.parse(s)})'` → exit 0 | stdout alone parses as valid JSON | planned |
| O-RV | AC-RV | Feature 15's injection-safety invariant (urgency from labels only) preserved in the new script | P4 | Read-verified: code review against feature 15 (PR #47) merge commit; urgency labels-only path preserved verbatim | execute-phase | read-verified at PR time: code review against feature 15 (PR #47) merge commit; the labels-only path preserved verbatim in the script's step 3 implementation | PR diff shows the labels-only path in the script matches the original invariant | planned |
| O-01 | A-03 + A-07 (invariant) | Read-only by construction: the script performs no write action — structural proof via greps, not prose promises | P1 | Implement read-only enforcement; structural proof greps in the test suite | execute-phase | grep mutation calls returns nothing; grep no-prompt calls returns nothing | all greps return no matches; structural proof present | planned |
| O-02 | A-13 (invariant) | Envelope vocabulary unchanged: the schema package is byte-untouched; `detail` is schema-unconstrained | P4 | Verify the PR does not modify any file under `packages/agentic-workflow-schema/` | execute-phase | `git diff --name-only main...HEAD -- packages/agentic-workflow-schema` → empty | empty output; no schema package files changed | planned |

## Closure

- Every acceptance criterion AC-01…AC-23 maps to obligation rows with exactly one
  phase each: AC-01→O1(P1), AC-02→O2(P2), AC-03→O3(P1), AC-04→O4(P2), AC-05→O5(P2),
  AC-06→O6(P2), AC-07→O7(P2), AC-08→O8(P1), AC-09→O9(P3), AC-10→O10(P1),
  AC-11→O11(P1), AC-12→O12(P2), AC-13→O13(P4), AC-14→O14(P3), AC-15→O15(P2),
  AC-16→O16(P4), AC-17→O17(P1), AC-18→O18(P2), AC-19→O19(P2), AC-20→O20(P1),
  AC-21→O21(P2), AC-22→O22(P1), AC-23→O23(P1), AC-RV→O-RV(P4).
- No obligation is `deferred` or exported.
- One behaviour appears once: the read-only invariant is O3+O-01 (the structural proof
  is the obligation; A-03 and A-22 are separate checks of different greps).