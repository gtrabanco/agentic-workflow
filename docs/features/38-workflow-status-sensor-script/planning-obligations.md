# Planning obligations — 38-workflow-status-sensor-script

One row per normative behaviour, applicable invariant, affected use case, and
required failure state. Column order per `pre-execution-review/LEDGERS.md` §2.
Frozen by `plan-feature-scaffold` on 2026-09-09; only the `Status` column
changes during execution. Validators are copied from `ACCEPTANCE.md`
(rows `A-01`…`A-23`, `A-RV`) or the owning phase's done-when.

| obligation-id | Authority source | Affected use case or invariant | Phase | Task | Implementation owner | Validator | Required evidence | Status |
|---|---|---|---|---|---|---|---|---|
| O1 | SPEC A:1 | The deterministic producer exists as a repo script | P1 | Create `scripts/workflow-status.mjs` (ESM, argv parsing, schema-runtime consumption) | execute-phase | `test -f scripts/workflow-status.mjs` → exit 0 | pin output in progress.md (A-01) | planned |
| O2 | SPEC A:2 | Drivers and the skill consume one valid Envelope v2 JSON built from the schema package's vocabulary (steps 1–9 incl. 6a) | P1 | Envelope assembly + `validateEnvelope` self-check + steps 1–9 field population | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0 (schema + field-presence pins, fixture repo) | suite output pasted in progress.md (A-02) | planned |
| O3 | SPEC A:3 | Read-only invariant: no mutation action of any kind | P1 | Implement collection without any write path; labels read, never written | execute-phase | mutation grep (`grep -nE '(createBranch\|git push\|gh pr (edit\|merge\|close\|create)\|gh issue (edit\|close\|label\|create)\|writeFile\|fs\.write\|unlink)' scripts/workflow-status.mjs`) → no match | grep output in progress.md (A-03) | planned |
| O4 | SPEC A:4 | Failure state: offline environment degrades, never fails | P2 | Severed-network degradation → declared codes, exit 0 | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0 (offline pin section) | suite output in progress.md (A-04) | planned |
| O5 | SPEC A:5 | Deterministic producer: byte-identical output on the same tree | P1 | No volatile fields; fixed key order; no clock reads in the output path | execute-phase | `diff <(node scripts/workflow-status.mjs) <(node scripts/workflow-status.mjs)` → empty (fixture run) | diff output in progress.md (A-05) | planned |
| O6 | SPEC A:6 | Failure state: ambiguous roadmap row maps safely to the five-state machine | P1 | Five-state parser with nearest-value + `default: idea` mapping and `workflow_observations` note | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0 (ambiguous-row pin, fixture with non-standard status) | suite output in progress.md (A-06) | planned |
| O7 | SPEC A:7 | Feature 15 injection-safety: urgency labels read from the labels object only | P1 | Labels-only urgency scan (never fetch/read issue bodies or comments) | execute-phase | `grep -nE '\b(body\|comment)' scripts/workflow-status.mjs` → no match AND `grep -cE 'labels' scripts/workflow-status.mjs` ≥ 1 | grep outputs in progress.md (A-07) | planned |
| O8 | SPEC A:8 | Schema vocabulary flows through the established loader, never a bare specifier | P1 | `import { loadSchemaRuntime } from "./schema-runtime.mjs"`; no bare-specifier import | execute-phase | `grep -nE "from ['\"]\\./schema-runtime\\.mjs['\"]" scripts/workflow-status.mjs` → match AND `grep -nE "^import .*'@gtrabanco/agentic-workflow-schema'" scripts/workflow-status.mjs` → no match | grep outputs in progress.md (A-08) | planned |
| O9 | SPEC A:9 | The skill slims to interpret-and-recommend (run script, read JSON, interpret, print report) | P3 | Slim SKILL.md + SENSOR_CORE.md + ENVELOPE_CORE.md fixed-sequence prose to the script call | execute-phase | `grep -c 'scripts/workflow-status.mjs' skills/workflow-status/SKILL.md skills/workflow-status/references/SENSOR_CORE.md` → ≥ 1 each AND `grep -cE '^[0-9]+\. \*\*' skills/workflow-status/references/SENSOR_CORE.md` → 0 | grep outputs in progress.md (A-09) | planned |
| O10 | SPEC A:10 | Script-level discoverability flags | P2 | `--help` (usage, exit 0) and `--version` (schema package version, exit 0) | execute-phase | `node scripts/workflow-status.mjs --help` → exit 0 + usage; `--version` → exit 0 + version | command outputs in progress.md (A-10) | planned |
| O11 | SPEC A:11 | No external dependency beyond the schema runtime; missing build names its precondition | P1 | Loader-path consumption with named fail-fast precondition (E-38-1) | execute-phase | (a) `node -e "import('./scripts/workflow-status.mjs')"` → exit 0 with dist built; (b) with `dist/` hidden → named precondition error, never bare `ERR_MODULE_NOT_FOUND` | both outputs in progress.md (A-11) | planned |
| O12 | SPEC A:12 | Decision logic stays consumer-side | P1 | Script never references `decideWorkflowAction` | execute-phase | `grep -c 'decideWorkflowAction' scripts/workflow-status.mjs` → 0 | grep output in progress.md (A-12) | planned |
| O13 | SPEC A:13 | Envelope vocabulary invariant: schema package byte-untouched | P4 | Close-out validation of the untouched surface | execute-phase | `git diff --name-only main...HEAD -- packages/agentic-workflow-schema` → empty; `cd packages/agentic-workflow-schema && npm test` → exit 0 | diff + suite output in progress.md (A-13) | planned |
| O14 | SPEC A:14 | Slimmed skill's pins and budgets re-based, never weakened | P3 | Re-base `SKILL_CONTEXT_BUDGETS.json` + re-target discipline pins | execute-phase | `node scripts/check-skill-context.mjs` → exit 0 with the updated `workflow-status` entry | tool output in progress.md (A-14) | planned |
| O15 | SPEC A:15 | Degraded dimensions are visible in `detail` | P2 | Degradation entries land in `detail` under the namespaced vocabulary | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0 (offline pin asserts `detail` carries the degradation entries) | suite output in progress.md (A-15) | planned |
| O16 | SPEC A:16 | Driver consumers wired to the deterministic producer | P4 | ORCHESTRATION.md names the script as the envelope's producer | execute-phase | `grep -c 'workflow-status.mjs' docs/workflow/ORCHESTRATION.md` ≥ 1 | grep output in progress.md (A-16) | planned |
| O17 | SPEC A:17 | Argv parity: `--json-only` is an accepted no-op | P2 | Flag accepted; output byte-identical | execute-phase | `diff <(node scripts/workflow-status.mjs --json-only) <(node scripts/workflow-status.mjs)` → empty | diff output in progress.md (A-17) | planned |
| O18 | SPEC A:18 | No-progress guard computed by the script from the hint | P2 | `--last-envelope <json\|path>` hint diff + guard note in `detail.workflow_observations`; hint never overrides recomputed state | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0 (stale-hint pin: inline-JSON + file-path variants, note shape per ENVELOPE_FIELDS.md) | suite output in progress.md (A-18) | planned |
| O19 | SPEC A:19 | Failure state: bad caller input degrades fail-open | P2 | Unreadable/malformed hint → `unavailable-hint-<cause>` note, exit 0 | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0 (missing-path + invalid-JSON pins) | suite output in progress.md (A-19) | planned |
| O20 | SPEC A:20 | Fatal class = invalid invocation only | P2 | Unknown flag → non-zero exit + stderr usage (repo convention, PE-006) | execute-phase | `node scripts/workflow-status.mjs --not-a-real-flag` → non-zero exit, usage on stderr | command output + exit code in progress.md (A-20) | planned |
| O21 | SPEC A:21 | Failure state: slow-but-alive forge cannot hang the sensor | P2 | Bounded wall-clock forge timeout → `unavailable-forge-timeout`, exit 0 | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0 (non-terminating `gh` shim pin) | suite output in progress.md (A-21) | planned |
| O22 | SPEC A:22 | Headless by construction | P1 | No interactive prompt path | execute-phase | `grep -nE '(readline\|createInterface\|process\\.stdin\\.(read\|setRawMode)\|@clack\|inquirer\|prompts?\\(\|confirm\\()' scripts/workflow-status.mjs` → no match | grep output in progress.md (A-22) | planned |
| O23 | SPEC A:23 | Stream separation: stdout = one JSON document, stderr = diagnostics | P2 | Diagnostics never leak into the data stream | execute-phase | `node scripts/workflow-status.mjs 2>/dev/null \| node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{JSON.parse(s)})'` → exit 0; offline fixture asserts diagnostics on stderr | command outputs in progress.md (A-23) | planned |
| O24 | SPEC read-verified criterion | Feature 15 injection-safety preserved in the new script | P4 | Injection-safety read-verified code-review pass over the urgency path | execute-phase | read-verified: reviewer confirms the labels-only scan against feature 15 (#47) in the PR review | review note in progress.md | planned |
| O25 | Repo bilingual-sync rule (CLAUDE.md working rules) | EN/ES sibling pairs move together | P4 | ORCHESTRATION.es.md gains the same wiring in the same change | execute-phase | read-verified at PR time: PR diff shows both siblings, reciprocal switcher links intact | PR diff check noted in progress.md | planned |
| O26 | Repo no-weakening pin rule (feature 30 precedent) | Discipline pins keep behavior or gain strictly stronger assertions | P3 | Pin re-targeting from prose-presence to script-behavior form | execute-phase | `node --test scripts/bounded-delivery-loops.test.mjs scripts/pre-execution-quality.test.mjs scripts/workflow-status-pre-execution.test.mjs scripts/normative-drift.test.mjs` → exit 0 with re-targeted pins | suite output in progress.md | planned |
| O27 | SPEC E-38-1 (required failure state, F27) | validateEnvelope mismatch is a diagnostic, never a gate: stderr diagnostic, envelope still printed, exit 0 | P1 | Envelope-mismatch pin: forced mismatch via a stub schema build whose `validateEnvelope` always fails, swapped in through the explicit-path loader (PE-001) | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0 (envelope-mismatch pin section) | suite output in progress.md (P1 done-when) | planned |
