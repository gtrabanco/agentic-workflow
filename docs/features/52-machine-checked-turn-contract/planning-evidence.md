# Planning evidence — 52-machine-checked-turn-contract

One row per Engineering claim (ledger contract:
`pre-execution-review/references/LEDGERS.md` §1; row shape:
`evidence-grounding/references/ROWS.md` §Plan-stage table). Artifact revision:
`52-plan-1`.

| id | claim-or-obligation | authority-kind | source-and-location | observed-revision | affected-decision-or-obligation | freshness | status | owner-or-next-evidence |
|---|---|---|---|---|---|---|---|---|
| PE-001 | The phase-lint prefix table maps `template/`, `skills/`, `docs/` → `docs` and `scripts/`, `packages/`, `.github/`, `.agentic-workflow/` → `config/infra`; one phase cannot carry both the shim (`template/`) and the engine (`packages/`), so the 3-phase cut groups shim + grammar + profile (docs) in P1 and engine + suites (config/infra) in P2 | repository | `scripts/phase-lint.mjs` `layerForTarget()` | main @ plan turn | ED-52-1 (phase cut satisfies phase-lint box 2) | current | proven | — |
| PE-002 | The sensor envelope already emits `next` with `recommended`/`alternatives` — the echo source exists; no sensor change needed | repository | `scripts/workflow-status.mjs:922` (`hint?.next?.recommended`), `:1270` (envelope `next`) | main @ plan turn | D-52-8 / AC10 | current | proven | — |
| PE-003 | `gh pr view <n> --json state,headRefOid` returns both fields (sampled: PR 212 → `{"headRefOid":"b0a423ba…","state":"MERGED"}`) — box4's mechanical probe is implementable exactly as specified | repository | `gh pr view 212 --json state,headRefOid` (sampled 2026-09-16) | gh CLI @ plan turn | box4 semantics / AC5 | current | proven | — |
| PE-004 | `git cat-file -e HEAD:<path>`, `git rev-list --count <ref>..<ref>`, `git symbolic-ref refs/remotes/origin/HEAD` (→ `refs/remotes/origin/main` here), `git status --porcelain`, `git branch --show-current` cover boxes 1/2/3/5 and default-branch resolution (sampled 2026-09-16) | repository | git plumbing sampled at `f38f9bb8` | main @ plan turn | box 1/2/3/5 Design | current | proven | — |
| PE-005 | node v22.23.1 (`.node-version`) supports `node --test <dir>` directory mode — AC2's invocation runs every suite file in the crate test directory | repository | `.node-version`; node CLI docs | main @ plan turn | AC2 | current | proven | — |
| PE-006 | The producer crate is dependency-free (`package.json` has no `dependencies`, no lockfile); the engine must stay node-stdlib-only so no lockfile-policy surface is touched | repository | `packages/agentic-workflow/package.json` | main @ plan turn | D-52-2 / lockfile policy | current | proven | — |
| PE-007 | `guard-command.sh` is the house hook pattern: `set -u`, flag loop, unknown argument → stderr + exit 2, fixed output lines; the shim mirrors this shape | repository | `template/.agentic-workflow/hooks/guard-command.sh` | main @ plan turn | D-52-6 / AC6 / AC12 | current | proven | — |
| PE-008 | The house bash-test pattern (`expect_allow`/`expect_block` helpers, failure tally, exit-code verdict) is `test-command-guard.sh`; the hook suite follows it with throwaway `git init` fixture repos | repository | `template/.agentic-workflow/hooks/tests/test-command-guard.sh` | main @ plan turn | AC13 | current | proven | — |
| PE-009 | `normative-drift.test.mjs` parses `CLAUDE.md`'s normative-surfaces table rows; a `machine: n/a` cell is accepted (filtered at row parse) with standing precedent rows (`ledger-ownership@1`, `review-mark@1`) — the `turn-contract-receipt` row registers cleanly | repository | `scripts/normative-drift.test.mjs` (row parse + `NA` filter); `CLAUDE.md` §Normative surfaces | main @ plan turn | D-52-5 / AC8 | current | proven | — |
| PE-010 | Mirror parity is enforced byte-identically over the whole `skills/` tree (references included); the bundle command is `npm run bundle:skills` in `packages/pi-agentic-workflow` — any `skills/` edit, including `references/`, requires the re-bundle | repository | `packages/pi-agentic-workflow/test/skill-parity.test.mjs`; `packages/pi-agentic-workflow/package.json` (`bundle:skills`) | main @ plan turn | AC11 / P1 bump+bundle task | current | proven | — |
| PE-011 | `bump-skill` handles internal skills (precedent: `phase-contract` 1.0.1 → 1.0.2 in feature 37 P1); `orchestration-envelope` bumps 2.0.2 → 2.1.0 (minor — backward-compatible capability); `CHANGELOG.md` is single-language (English-only interim) | repository + ledger | `skills/bump-skill/SKILL.md`; `docs/features/37-phase-lint-script/TASKS.md` P1; `REPOSITORY_STATE.md` F011 | main @ plan turn | P1 bump task / AC11 | current | proven | — |
| PE-012 | CI node-compat jobs run only the packages' own suites (`bun run test:node` in `packages/pi-agentic-workflow`); no job runs `scripts/` suites today — the integration-closure row's substance is met by the conformance test's node-first invocation (AC8's command is `node --test …`), and CI wiring is disclosed in `known-issues.md` for #198 triage, not silently dropped | repository | `.github/workflows/publish-pi-package.yml` (node-compat job); `packages/pi-agentic-workflow/package.json` (`test:node`) | main @ plan turn | ED-52-5 / integration-closure row | current | proven | — |
| PE-013 | `scripts/phase-lint.mjs` (+ test) exists at main (feature 37, merged PR #212); the engine's box2 phase-lint check targets a real script, and the lint target shape is the per-phase `## P<n> —` heading + `Layer:`/`Done-when:` lines + checkbox tasks (regex `PHASE_HEADING`) | repository | `scripts/phase-lint.mjs`; `docs/features/37-phase-lint-script/TASKS.md` | main @ plan turn | box2 semantics / this plan's lint run | current | proven | — |
| PE-014 | No architectural-invariants document exists and the NRS ledger status is `frozen` (not draft/contradicted/resolved) — planning may proceed and Stage 2 classification is n/a | ledger | `REPOSITORY_STATE.md` (Snapshot status; F010) | snapshot 2026-08-30 | planning preflight Stage 1/2 | current | proven | — |
| PE-015 | Six skills load `TURN_CONTRACT.md` (design-feature, execute-phase, orchestration-envelope, review-change, review-plan, review-spec) — the profile section must stay compact; AC11's budget check re-runs after the edit | repository | `grep -c TURN_CONTRACT skills/*/SKILL.md` (2026-09-16) | main @ plan turn | profile section size / AC11 | current | proven | — |
| PE-016 | The receipt tokens (`TURN-CONTRACT ok`, `TURN-CONTRACT fail box<N>: <code>`), the closed reason-code vocabulary, and the exit contract are frozen in the reviewed Product half — engines implement, never redefine them | document | `docs/features/52-machine-checked-turn-contract/SPEC.md` §Acceptance criteria (grammar block) | SPEC @ `fc9bfa01` (receipt-bound `a768d95c…`) | AC2–AC5 / AC8 / D-52-6 | current | proven | — |
