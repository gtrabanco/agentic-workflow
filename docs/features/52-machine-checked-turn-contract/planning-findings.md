# Planning findings — 52-machine-checked-turn-contract

One stage-aware table. Reviewers append rows; nobody edits a reviewed artifact
to make a row disappear. Contract:
`pre-execution-review/references/LEDGERS.md` §Findings.

2026-09-16 author resolution (plan-feature, plan-author route): plan-class
rows **PLAN52-F1�\|F5 resolved as one root-caused batch** at artifact revision
`52-plan-2` — one batch because the rows share two causes: a validator whose
shell syntax was neutered by markdown-table cell-escaping (F1's escaped-pipe
grep can never match), and owned criteria/invariants whose carrying row or
task never landed (F2, F5) or landed only as prose without an executor
checklist entry (F3, F4). Product-class **PLAN52-F6 stays with its owner**
(`design-feature`): its repair rides the next SPEC-touching design turn, since
an edit now would rotate the parent snapshot the plan receipt binds (the row
itself records this). No claim, evidence, severity or class cell was edited;
only `status`, `resolution-evidence` and `resolving-artifact-revision` moved.

```text
finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision
```

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| SPEC52-F1 | spec | info | product | a768d95cb498656aeea9d46c7b9bf41110a3bdb6732ed69ee068abb2b850536b | Capability closure row E2 cites the house bash-test pattern as `tests/test-command-guard.sh style`, but no top-level `tests/` directory exists — the canonical file is `template/.agentic-workflow/hooks/tests/test-command-guard.sh`. The claim's substance holds (the house pattern exists); the path prefix is shorthand. No downstream effect: AC13's own target path `template/.agentic-workflow/hooks/tests/test-turn-contract.sh` is already correct. | `find . -name test-command-guard.sh` → single hit at `template/.agentic-workflow/hooks/tests/test-command-guard.sh`; `ls tests/` → no such directory | open | — | — |
| PLAN52-F1 | plan | medium | plan | 53a3868b57598ff65bd03b686ef792e3bf13d5a584d6eb22d349a70169daf9f3 | AC12's frozen validator cannot fail: ACCEPTANCE.md AC12 and obligations O1 freeze the `grep -nE` pattern with backslash-escaped pipes over node/bun/npm/npx, which GNU grep -E reads as the literal string "node\|bun\|npm\|npx" — a shim containing the token `node` (or bun/npm/npx) passes AC12 as written (no-op pass, validator that cannot fail). SPEC AC12 carries the correct unescaped alternation, so the manifest and the ledger copied from it deviate from their own source; testing.md's hook-suite "AC12 grep guard" would copy the neutered pattern into the suite. | repro: escaped-pipe form run on a fixture containing `node`/`bun` lines → exit 1 (no match); unescaped form on the same fixture → exit 0, both lines matched; ACCEPTANCE.md AC12 row vs SPEC.md §Acceptance criteria AC12; planning-obligations.md O1 validator cell | resolved | Validator re-issued as repeated `-e` patterns — `grep -n -e node -e bun -e npm -e npx template/.agentic-workflow/hooks/turn-contract.sh` → no matches — in ACCEPTANCE.md AC12 and obligations O1; root cause: the `node\|bun\|npm\|npx` alternation was backslash-escaped to survive the markdown cell, which grep reads as a literal string that can never match (verified live: escaped form exits 1 on a token-carrying fixture, `-e` form matches). testing.md's hook-suite guard now pins the executable form verbatim | 52-plan-2 |
| PLAN52-F2 | plan | medium | plan | 53a3868b57598ff65bd03b686ef792e3bf13d5a584d6eb22d349a70169daf9f3 | AC10 is orphaned — no obligation row carries the profile's echo clause (SPEC §Scope item 4, use case C4, D-52-8) nor AC10's sensor-suite command: O3 covers only AC9's four clauses and O12's gate names AC11 only. An acceptance criterion with no owning obligation can silently die in the execution sweep (the echo rule survives only as TASKS.md P1 task prose). | planning-obligations.md O1–O14 authority/validator cells contain no AC10 reference; SPEC.md §Acceptance criteria AC10; PLAN.md P1 bullet 3 (echo clause present as prose) | resolved | Obligation O15 added (P1, profile-section task) owning AC10 end to end: the sensor-suite command and the read-verified echo rule copied verbatim from ACCEPTANCE.md AC10, with D-52-8 and use case C4 named in its authority and use-case cells | 52-plan-2 |
| PLAN52-F3 | plan | low | plan | 53a3868b57598ff65bd03b686ef792e3bf13d5a584d6eb22d349a70169daf9f3 | Budget-check drift: SPEC §Open questions claims the AC11 mitigation re-runs `check-skill-context.mjs` "in P1/P3", but no P1 task or obligation carries it — the ≤ 20-line profile section lands in P1 and a context-budget breach would surface only at P3's gate (O12). Detection delayed, not lost. | SPEC.md §Open questions risk 1 vs TASKS.md P1 tasks (no budget check) and planning-obligations.md O5/O12 validators | resolved | O3's validator now runs `bun scripts/check-skill-context.mjs` after the profile section lands (AC11's P1 leg); TASKS.md P1 gains the regression task (task count 6 → 7) and its done-when carries both regression commands; PLAN.md P1 narrates them; SPEC §Phase-lint re-synced (P1:docs:7, fingerprint 2c7d8179…) | 52-plan-2 |
| PLAN52-F4 | plan | info | plan | 53a3868b57598ff65bd03b686ef792e3bf13d5a584d6eb22d349a70169daf9f3 | box2's reason codes (`acceptance-missing`, `phase-lint-failed`) appear in no named acceptance case — AC4 enumerates only box1/box3 codes and no dev-scenario row exercises box2; coverage rides O7's "each code asserted once pass/fail" prose and the per-box suite tasks, not a pinned AC case. | SPEC.md §Acceptance criteria AC2–AC6 and §Dev scenarios; planning-obligations.md O7 | resolved | O7's validator now names box2's `acceptance-missing` and `phase-lint-failed` as asserted suite cases (the phase-lint clause is engine-only, ED-52-3); TASKS.md P2 engine-suite bullet and testing.md's coverage line name them too | 52-plan-2 |
| PLAN52-F5 | plan | low | plan | 53a3868b57598ff65bd03b686ef792e3bf13d5a584d6eb22d349a70169daf9f3 | D-52-2's "crate stays `private: true` / zero dependencies" compatibility invariant has no obligation row or validator — O13 guards only the schema package, so nothing frozen fails if P2 added a dependency or flipped privacy on `packages/agentic-workflow`. | planning-obligations.md O1–O14 (no row); packages/agentic-workflow/package.json (no deps keys today — PE-006) | resolved | Obligation O16 added (P3 gate re-run): `grep -ic 'dependenc' packages/agentic-workflow/package.json` → 0 (no dependency-family key) and `grep -c '"private": true' packages/agentic-workflow/package.json` → 1 — both verified against the current package.json before the row was frozen | 52-plan-2 |
| PLAN52-F6 | product | info | product | 53a3868b57598ff65bd03b686ef792e3bf13d5a584d6eb22d349a70169daf9f3 | SPEC §Design status still reads "Awaiting independent review by `review-spec`." although spec-review-52-20260916-1 returned spec-review-pass — stale status prose in the reviewed Product half (cosmetic; no AC or obligation points at it; repair rides the next SPEC-touching design turn, since an edit now would rotate the parent snapshot). | progress.md spec receipt vs SPEC.md §Design status; SPEC Product half byte-identical fc9bfa01 → HEAD | open | — | — |
| PLAN52-F7 | plan | info | product | 1a929fa77447b16d362a9cadd512d0e8d683df05c5a10a042a64484508fa923f | SPEC Product half §Dependencies cites `48 binary-validated-continuations (#215)` and §Post-merge cites Phase-0 rows `55 (#230) · 48 (#215)`; the owner-approved 2026-09-16 consolidation folds 48+55 into new feature 59 (#215+#230). At source revision `83789d48` the references still match the committed roadmap (no drift at the reviewed revision — the fold lives in uncommitted `ROADMAP.md` bytes from unit 59's design turn); the prose goes stale the moment that fold commits. No AC or obligation binds to these references ("no coupling now", SPEC §Dependencies); repair rides the next SPEC-touching design turn (same vehicle as PLAN52-F6). | `git diff docs/features/ROADMAP.md` (uncommitted: rows 48/55 folded → 59); ROADMAP.md rows 48/55/59 at HEAD vs working tree; SPEC.md §Dependencies + §Post-merge | open | — | — |
| PLAN52-F8 | plan | info | product | 1a929fa77447b16d362a9cadd512d0e8d683df05c5a10a042a64484508fa923f | SPEC Product half §Size still sketches the design-turn phase order "engines → profile/registration → hardening & PR", which the frozen plan reverses (P1 docs surface → P2 engine). The reversal itself is disclosed and recorded (ED-52-1 in `decisions.md`, SPEC §Phases, PLAN.md header note), so this is superseded prose, not a hidden contradiction — but the §Size sentence alone states the superseded order. Cosmetic; no AC or obligation binds to it; repair rides the next SPEC-touching design turn (same vehicle as PLAN52-F6/PLAN52-F7). | SPEC.md §Size vs SPEC §Phases + decisions.md ED-52-1 | open | — | — |
| PLAN52-F9 | plan | high | plan | 1a929fa77447b16d362a9cadd512d0e8d683df05c5a10a042a64484508fa923f | AC2 and AC7 (and TASKS.md P2's done-when) invoke `node --test packages/agentic-workflow/test/` in **directory mode**, but Node.js ≥ 22 — the repository's `.node-version` is `v22.23.1` — no longer accepts a directory as a `--test` positional argument: it resolves the directory as a module and exits with `Cannot find module '…/packages/agentic-workflow/test'`. Planning-evidence PE-005 ("node v22.23.1 supports `node --test <dir>` directory mode") is false. The documented interface is one or more **glob patterns** ("Alternatively, one or more glob patterns can be provided as the final argument(s)", nodejs.org `api/test.html` v22), which is also the form the repo already uses (`test:node`: `node --test test/*.test.mjs`). The frozen validator is therefore unsatisfiable on the pinned runtime; it passes only on node ≤ 20. Discovered at the P2 gate; no source or test is wrong — only the validator's invocation form. | node v22.23.1: `node --test packages/agentic-workflow/test/` → `Error: Cannot find module '/home/agent/projects/agentic-workflow/packages/agentic-workflow/test'`, nonzero. node v20.20.2: same command → `pass 41 · fail 0`. node v24.19.0 (environment default): same failure. `node --test packages/agentic-workflow/test/*.test.mjs` → 40/40 pass on node 22 and 24. nodejs.org v22 `api/test.html` "Running tests from the command line" — glob patterns, no directory form. | resolved | User-approved amendment (SPEC §Amendments 2026-09-16, decisions.md ED-52-8): AC2/AC7 validators become `bun test packages/agentic-workflow/test/` (bun-first) with a Node-24 glob fallback `node --test packages/agentic-workflow/test/*.test.mjs`; replacement manifest re-frozen blob c088a621b794fe6b9d4bf3c138406c0c488c4889; identical suite, no assertion narrowed. | 52-amend-1 |
