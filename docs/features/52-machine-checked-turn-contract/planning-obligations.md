# Planning obligations — 52-machine-checked-turn-contract

One row per normative behaviour, applicable compatibility invariant, affected
use case, and required failure state (ledger contract:
`pre-execution-review/references/LEDGERS.md` §2). Artifact revision:
`52-plan-2`.

| obligation-id | Authority source | Affected use case or invariant | Phase | Task | Implementation owner | Validator | Required evidence | Status |
|---|---|---|---|---|---|---|---|---|
| O1 | SPEC §Scope item 2; AC7, AC12, AC13 | Target projects verify the contract with bash+git+gh only (C1 in the shim's environment) | P1 | shim + hook suite | execute-phase | `bash template/.agentic-workflow/hooks/tests/test-turn-contract.sh` → exit 0; `grep -n -e node -e bun -e npm -e npx template/.agentic-workflow/hooks/turn-contract.sh` → no matches (repeated `-e` patterns — the SPEC AC12 alternation without the escaped-pipe form that cannot fail; PLAN52-F1) | hook-suite output + grep output in TASKS.md ticks | planned |
| O2 | SPEC §Scope item 3; AC8, D-52-5 | The receipt grammar is declared once and normatively registered (machine `n/a`) | P1 | grammar block + CLAUDE.md row | execute-phase | fenced `turn-contract-receipt@1` present in `skills/orchestration-envelope/references/TURN_CONTRACT.md`; `node --test scripts/normative-drift.test.mjs` → exit 0 | block grep + test output in TASKS.md tick | planned |
| O3 | SPEC §Scope item 4; AC9 (read-verified); AC11 (P1 leg) | Machine-check profile: paste-receipt demonstrates boxes 1–5; recitation not required when the verifier ran; fallback stated; boxes 6–11 unchanged | P1 | profile section | execute-phase | read-verified: the section satisfies AC9's four clauses (reviewer checklist); `bun scripts/check-skill-context.mjs` → exit 0 after the section lands (context budgets — SPEC §Open questions risk 1; PLAN52-F3) | profile text in TURN_CONTRACT.md + context-budget output in TASKS.md tick | planned |
| O4 | SPEC §Scope item 7; AC14 (read-verified) | One pointer per workflow doc; the grammar is restated nowhere else | P1 | docs pointers | execute-phase | `grep -c "machine-check" docs/workflow/ORCHESTRATION.md docs/workflow/FEATURE_WORKFLOW.md` → `1` per file | grep output in TASKS.md tick | planned |
| O5 | Repo rule (Version every change); AC11 | Skill versioned + mirror byte-identical | P1 | bump-skill + bundle | execute-phase | `orchestration-envelope` at 2.1.0; `node --test packages/pi-agentic-workflow/test/skill-parity.test.mjs` → exit 0 | version grep + suite output in TASKS.md tick | planned |
| O6 | SPEC §Scope item 1; AC1, AC6 | Engine CLI: `--help` exits 0 naming both flags; unknown flag → usage on stderr, exit 2 | P2 | engine CLI | execute-phase | engine suite `--help`/unknown-flag cases green (`bun test packages/agentic-workflow/test/` → exit 0; Node 24 fallback: `node --test packages/agentic-workflow/test/*.test.mjs`) | suite output in TASKS.md tick | planned |
| O7 | SPEC §Scope item 5; AC3, AC4, AC5 | Box semantics: reason codes, order 1→5 first-failure-wins, box4 n/a without `--finished`, gh failure fail-closed | P2 | engine box checks | execute-phase | engine suite per-box cases green — every reason code asserted once pass/fail, with box2's `acceptance-missing` and `phase-lint-failed` named among the cases (the phase-lint clause is engine-only, ED-52-3; PLAN52-F4) | suite output in TASKS.md tick | planned |
| O8 | SPEC §Scope item 1; AC2 | Verifier is read-only, works from any cwd inside the repo, prints exactly one receipt line | P2 | engine suite fixtures | execute-phase | engine suite clean-branch + subdirectory + no-tree-mutation cases green | suite output in TASKS.md tick | planned |
| O9 | SPEC §Scope item 2; AC7 | Engine parity: byte-identical stdout lines and exit codes across both engines | P2 | parity suite | execute-phase | parity suite green (`bun test packages/agentic-workflow/test/turn-contract.parity.test.mjs` → exit 0; Node 24 fallback: `node --test packages/agentic-workflow/test/turn-contract.parity.test.mjs`) | parity output in TASKS.md tick | planned |
| O10 | SPEC §Scope item 3; AC8 | Grammar conformance pinned by a repo test; normative-drift stays green | P2 | conformance test | execute-phase | `node --test scripts/turn-contract-grammar.test.mjs` → exit 0; `node --test scripts/normative-drift.test.mjs` → exit 0 | test outputs in TASKS.md tick | planned |
| O11 | SPEC §Dev scenarios; §Scope item 6 | Failure scenarios reproducible in local dev (empty repo, unreadable .git, gh outage, dirty-tree ordering) | P3 | edge-corpus task | execute-phase | edge fixtures in engine + hook suites green | suite output in TASKS.md tick | planned |
| O12 | Repo verification gate; AC11 | Full gate green before the PR | P3 | gate re-run | execute-phase | AC11 commands + all new suites exit 0 | gate commands + exit codes pasted in TASKS.md tick | planned |
| O13 | SPEC §Dependencies; D-52-5 | Schema package untouched | P3 | gate re-run | execute-phase | `git diff --name-only main...HEAD -- packages/agentic-workflow-schema` → empty | diff output in TASKS.md tick | planned |
| O14 | Turn contract boxes 3–5; issue #226 | PR open with `Closes #226`; roadmap row `done · [#<pr>]`; clean tree | P3 | close-out chain | execute-phase | PR URL printed in chat; `git status --porcelain` → empty | PR URL + status output in TASKS.md tick | planned |
| O15 | SPEC §Scope item 4; AC10; D-52-8 | Profile echo rule maps the closing `→ Next:` block from the sensor envelope's `next.recommended` + `next.alternatives` (use case C4); sensor suite stays green | P1 | profile section (echo clause) + sensor-suite regression | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0; read-verified: echo rule in `TURN_CONTRACT.md` maps both fields | sensor-suite output + echo-clause text in TASKS.md tick | planned |
| O16 | SPEC §Dependencies; D-52-2 | Producer crate stays `private: true` with zero dependencies (no lockfile — PE-006) | P3 | gate re-run | execute-phase | `grep -ic 'dependenc' packages/agentic-workflow/package.json` → 0 (no dependencies/devDependencies/peerDependencies key); `grep -c '"private": true' packages/agentic-workflow/package.json` → 1 | both grep outputs in TASKS.md tick | planned |
