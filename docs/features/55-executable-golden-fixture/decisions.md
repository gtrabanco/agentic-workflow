# decisions — 55-executable-golden-fixture

> Product-stage decision log and evidence rows. Written by `design-feature`
> (2026-09-17). The SPEC carries the decisions in summary; this file carries
> the full rows with their evidence.

## Decision log

| ID | Decision | Alternatives considered | Rationale |
|---|---|---|---|
| D-55-1 | Fixture home: `scripts/fixtures/golden-fixture/` | `tests/fixtures/golden-fixture/` (new top-level dir) | Matches the `scripts/fixtures/unit-route/` committed-toy-repo precedent; no new top-level dir; keeps the fixture out of the human-docs surface (#230 item 1). User-selected 2026-09-17. |
| D-55-2 | `docs/workflow/GOLDEN_FIXTURE.md` slims to purpose + ~100-word judgment protocol + run log + pointers | Keep the 383-line doc intact and add the fixture dir beside it (duplication) | #230's "short manual protocol in the run-log doc"; a second live copy of the toy SPEC would fork the fixture — the committed files become the single source. User-selected 2026-09-17. |
| D-55-3 | Both toy trees committed: executor CSV fixture AND audit-evidence target | Executor CSV only (audit target stays described in doc prose) | The four trap invariants (worklist lag, ADR terminal item, prior finding, aggregate tail) are deterministic file facts; committing the tree makes the manual product-audit run replayable from bytes. User-selected 2026-09-17. |
| D-55-4 | Run-log Result field: test-enforced closed grammar | Free-text Result column | #230 absorbs #183's "machine-comparable result field"; without enforcement, run-over-run comparison stays qualitative — the exact gap #183 names. User-selected 2026-09-17. |
| D-55-5 | Traceability: #230 closed in error (never absorbed by 59); this SPEC is #230's product definition under roadmap row 55; no forge-state action taken by the design turn | Reopen #230 in this turn; post a corrective comment | Forge state is owner-owned; the roadmap row summary + this file carry the correction. Owner statement 2026-09-17: "#230 wasn't absorbed by feature 59". |
| D-55-6 | Grammar `exact <n>/<n> · invented none|<k> · shape ok|<fail-code>`; cutoff date 2026-09-18; rows dated earlier are grandfathered verbatim | Enforce on all rows (would fail on 30+ historical free-text verdicts); migrate history (rewrites evidence) | Historical rows are evidence, not data to migrate; a dated cutoff is deterministic where a content heuristic is not. |
| D-55-7 | Suite determinism constraints: no network, no wall-clock, no randomness, no model in the mechanical loop | Allow bounded network/time | Golden-master practice (ApprovalTests) + Fowler's non-determinism quarantine rule; #183's anti-flakiness grounding (arXiv:2505.06177) documents harness degradation under drift — a fixture that flakes re-fails frozen criteria with zero changes. |
| D-55-8 | Repair batch SPEC55-F1…F5 applied 2026-09-17 in one batch: F1 lineage corrected (schema runtime → feature 28; contracts → 25; `next.continuation` → 59), F2 AC6 + E2 paths qualified with the fixture root, F3 envelope samples added to E1's closure and located in AC3(b), F4 AC2 token renamed to the tool's actual `box-<n>` finding form, F5 AC5 extended with grep-verified survival of the smoke-test section and fixed pass criteria | Split the batch (rejected: one review's findings are one batch); dismiss any row (rejected: each reviewer claim was re-verified against the repository and confirmed) | All five findings are `class: product` from spec-review-fail 2026-09-16. Repair classes: F1/F2/F4 mechanical intent-preserving (factual/locatability slips — producer attribution, path qualification, token naming); F3/F5 closure completion (samples and expectation survival were already in scope via AC3(b)/In-scope item 5 but unverified or unclosed). No scope, role, authority, or user-outcome change — reviewed intent unchanged, so the batch runs autonomously. Per-finding resolution evidence recorded in `planning-findings.md`. |
| D-55-9 | Repair batch SPEC55-F6…F10 applied 2026-09-17 in one batch: F6 AC5's zero-match grep narrowed to the live docs surface (dated run-log rows and append-only unit ledgers exempt per E4's preservation rule), F7 `check-skill-context.mjs` re-attributed to feature 20, F8 `scripts/fixtures/unit-route/` re-attributed to fix #224, F9 Expectation 2's diagnostic criterion frozen in AC4, F10 PE-5/6/7 `authority-kind` corrected to `document` | Split the batch (rejected: one review's findings are one batch); keep `web` as a kind (rejected: outside the closed vocabulary in ROWS.md); rewrite the run-log rows instead of exempting them (rejected: violates E4 and the out-of-scope rewrite ban) | All five findings are `class: product` from spec-review-fail 2026-09-17 (second cycle). Repair classes: F7/F8/F10 mechanical intent-preserving (producer-attribution slips + vocabulary value); F6/F9 closure completion (criterion made satisfiable / promised criterion frozen). No scope, role, authority, or user-outcome change — reviewed intent unchanged, so the batch runs autonomously. Per-finding resolution evidence in `planning-findings.md`; evidence rows PE-13/14/15 added. |

## Evidence rows (product stage)

| id | claim | authority-kind | source-and-location | observed-revision | freshness | status | owner-or-next-evidence |
|---|---|---|---|---|---|---|---|
| PE-1 | The golden fixture is a test, not human-readable documentation; the owner approved superseding feature 12's docs-only "no runnable script" constraint | forge | https://github.com/gtrabanco/agentic-workflow/issues/230 (fetched 2026-09-17) | issue body @ 2026-09-16 | current | proven | — |
| PE-2 | #230 was closed as "absorbed into feature 59" but the merged roadmap kept row 55 at `idea`; 59's decisions record the owner restoring the executable-fixture scope to row 55 | repository + forge | `docs/features/59-executable-continuations/decisions.md` (scope-reconciliation rows); `gh api issues/230` close comment; `ROADMAP.md` row 55 | main @ `2264b454` (2026-09-16) + owner statement 2026-09-17 | current | proven | — |
| PE-3 | #183's item 3 (machine-comparable run-log result field) routes to this feature; items 1–2 stay with feature 36 | forge | https://github.com/gtrabanco/agentic-workflow/issues/183 (fetched 2026-09-17) | issue body @ 2026-09-07 | current | proven | — |
| PE-4 | The mechanical halves asserted here are real, merged tools: `phase-lint.mjs` (byte-stable stdout, exit semantics), schema runtime loader, committed-fixture test precedent | repository | `scripts/phase-lint.mjs` header (usage/exit contract); `scripts/schema-runtime.mjs` exports; `scripts/fixtures/unit-route/` + `scripts/continuation-discipline.test.mjs` | main @ `2264b454` | current | proven | — |
| PE-5 | Snapshot/golden-master discipline: expected outputs are committed artifacts reviewed in code review; a mismatch means fix-or-deliberate-re-pin, never silent regeneration | document | https://jestjs.io/docs/snapshot-testing (fetched 2026-09-17) | Jest 30.5 docs | current | proven | — |
| PE-6 | Approval tests = "Golden Master Tests", an alternative to asserts, suited to structured output with many fields | document | https://raw.githubusercontent.com/approvals/ApprovalTests.cpp/master/README.md (fetched 2026-09-17) | README @ master, 2026-09-17 | current | proven | — |
| PE-7 | Non-deterministic checks must be quarantined; regression suites must be deterministic (isolation, no time/random/remote causes) | document | https://martinfowler.com/articles/nonDeterminism.html (fetched 2026-09-17, HTTP 200) | article @ 2026-09-17 | current | proven | — |
| PE-8 | The current doc is 383 lines with the toy SPEC/manifest/audit-target embedded; run log holds 30+ dated rows incl. FAILs that drove wording fixes | repository | `docs/workflow/GOLDEN_FIXTURE.md` (heading map + run log) | main @ `2264b454` | current | proven | — |
| PE-9 | `scripts/schema-runtime.mjs` was added by feature 28, not feature 25/59 | repository | `git log --diff-filter=A --oneline -- scripts/schema-runtime.mjs` → `1951b717 fix(28): fold RS3(b)+RS13+RS14…`; `ROADMAP.md` rows 25 (schema contracts: `CandidateSnapshot v1` + `ReviewReceipt v1`), 28 (evidence-grounded review), 59 (`next.continuation` in Envelope v2) | HEAD @ `83726ab3` (2026-09-17) | current | proven | — |
| PE-10 | The repository root has no `docs/adr/` or `docs/audits/`; the audit-target tree E2 describes exists only under `scripts/fixtures/golden-fixture/audit-target/`, whose shape gives the worklist as `docs/fix/README.md`, the ADR tree terminal as `docs/adr/0047-transport.md`, and the newest prior audit as `docs/audits/3-<earlier-date>.md` | repository | `ls docs/` at HEAD (no `adr`/`audits`); `docs/workflow/GOLDEN_FIXTURE.md` audit-target section (worklist index, T2/T3/T4 traps, prior-audit mapping `3 F2`) | HEAD @ `83726ab3` (2026-09-17) | current | proven | — |
| PE-11 | `phase-lint.mjs` finding lines emit `P<n> box-<n>: <reason>`; the machine-matchable finding token is `box-<n>`, and no "rule id" token exists | repository | `scripts/phase-lint.mjs:698` (finding-line emission loop); `skills/phase-contract/SKILL.md:72-75` (fixed output names "box <n>") | HEAD @ `83726ab3` (2026-09-17) | current | proven | — |
| PE-12 | Envelope valid/invalid samples are consumed read-only by the suite via `loadSchemaRuntime()`; static committed sample files under `scripts/fixtures/` follow the unit-route precedent | repository | `scripts/continuation-discipline.test.mjs:40-53` (`loadSchemaRuntime()` consumption precedent); `scripts/fixtures/unit-route/` committed-fixture layout | HEAD @ `83726ab3` (2026-09-17) | current | proven | — |
| PE-13 | `csv-export-command` occurs in `docs/workflow/GOLDEN_FIXTURE.md` only in the embedded toy blocks (lines 45, 54, 122 — removed by the slim) and in five dated run-log rows (278, 279, 280, 298, 383) that the half forbids rewriting; the token also appears as quoted history in unit ledgers under `docs/features/` (e.g. feature 17's `progress.md`, this unit's own finding row and AC5 criterion) | repository | `grep -n "csv-export-command" docs/workflow/GOLDEN_FIXTURE.md` → 8 matches (run-log section from line 269); the AC5 exemption pipeline (exact filter string frozen in SPEC AC5) run pre-slim → only the 3 embedded-block lines remain, every other match exempted | HEAD @ `42d094ab` (2026-09-17) | current | proven | — |
| PE-14 | `scripts/check-skill-context.mjs` was introduced by feature 20, not 38 (PR #116, not #213): added in `635168ce feat(distribution): add grouped catalog and context budgets` (2026-07-31); feature 38 is `workflow-status-sensor-script` delivering `scripts/workflow-status.mjs` | repository | `git log --diff-filter=A --oneline -- scripts/check-skill-context.mjs` → `635168ce`; `ROADMAP.md` rows 20/38 | HEAD @ `42d094ab` (2026-09-17) | current | proven | — |
| PE-15 | `scripts/fixtures/unit-route/` was committed by fix #224 P1 (`8cdf9548 feat(scripts): deterministic unit router with bounded read set (#224)`, 2026-09-15, body "P1 of fix-224… the committed unit-37 dogfood fixture"); feature 59's `scripts/continuation-discipline.test.mjs` builds `mkdtempSync` throwaway repos and never references `fixtures/unit-route` | repository | `git log --diff-filter=A --oneline -- scripts/fixtures/unit-route` → `8cdf9548`; commit body; `scripts/continuation-discipline.test.mjs:57`; `docs/fix/224-deterministic-replan-routing/` | HEAD @ `42d094ab` (2026-09-17) | current | proven | — |

## Open offers

- Seed `docs/CAPABILITIES.md` from the derived inventory recorded in the SPEC's
  Integration closure (deferred; see SPEC Deferred decisions).

## Opportunistic fixes (this unit's PR)

| Date | Finding | Class | Fix |
|---|---|---|---|
| 2026-09-17 | `ROADMAP.md` row 40 linked `pull/240` as feature 40's PR — a dangling forward reference (no PR #240 existed; feature 40's real PR is #235, merged). This unit's own PR then took number 240, making the stale link actively misleading. | Opportunistic Fix (1 line, same file the unit already touches, verified via `gh pr view 235`) | Row 40 status cell now links `#235`. |
