# Planning findings — 55-executable-golden-fixture

One stage-aware table per unit (ledger contract:
`skills/pre-execution-review/references/LEDGERS.md` § 3). Reviewers append rows;
only the owning skill resolves them (`design-feature` for `class: product`).
Column order is fixed:

```text
finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision
```

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| SPEC55-F1 | spec | low | product | 48f3977ae8ff4fb85ec489ed1b6b54b5b6fef9dadb78ef3793c5af1ecccb4cb7 | `## Dependencies` and `### Context` credit "feature 25/59" with the `schema-runtime` producer, but `scripts/schema-runtime.mjs` was added by feature 28 — a claim about an existing surface whose lineage the repository contradicts (C10) | `docs/features/55-executable-golden-fixture/SPEC.md:43` ("feature 25/59 (schema runtime + `next.continuation`…)") and `:66-67` ("feature 25/59 gave the repo a schema runtime"); `git log --diff-filter=A --oneline -- scripts/schema-runtime.mjs` → `1951b717 fix(28): fold RS3(b)+RS13+RS14 — content-bound snapshot identity, named drift, fix units bind no parent`; `docs/features/28-evidence-grounded-spec-plan-review/progress.md:530` records "Added `scripts/schema-runtime.mjs` (guard + loader…)"; `grep -rn "schema-runtime" docs/features/25-content-bound-review-receipts/` → no matches; `ROADMAP.md` row 25 describes schema contracts (`CandidateSnapshot v1` + `ReviewReceipt v1`), not a runtime | resolved | SPEC `## Dependencies` + `### Context` re-attributed: schema runtime to feature 28 (`scripts/schema-runtime.mjs`, added in `1951b717 fix(28)…`; decisions.md evidence row PE-9), schema-package contracts to 25, `next.continuation` to 59. Producer lineage is the only change — the set of merged producers the suite reuses is untouched; mechanical, intent-preserving class | 98d8e6a4b66b27a43c14ffe2675f22528fad3434 |
| SPEC55-F2 | spec | low | product | 48f3977ae8ff4fb85ec489ed1b6b54b5b6fef9dadb78ef3793c5af1ecccb4cb7 | AC6 locates the audit-target invariants at bare `docs/adr/`, `docs/audits/3-*.md`, and "the worklist", but E2 places that tree under `scripts/fixtures/golden-fixture/audit-target/` and the repository root has neither `docs/adr/` nor `docs/audits/` — the criterion is not objectively located from the half alone (C8) | `docs/features/55-executable-golden-fixture/SPEC.md:322-324` (AC6: "the worklist shows row `9 — stale-cache`…; `docs/adr/` ends at `0047-transport.md`; `docs/audits/3-*.md` exists and mentions finding `F2`"); `:184-190` (E2 — "`scripts/fixtures/golden-fixture/audit-target/`: README, worklist index, ADR tree ending at `0047-transport.md`, prior audit file carrying `F2`"; "API: files under `audit-target/`"); `:98-100` (In-scope item 2, same fixture-root path); `ls docs/` at the reviewed revision lists `features`, `fix`, `workflow`, `design`, `assets`, `research`, `site` — no `adr` or `audits` directory | resolved | AC6 now locates every invariant under `scripts/fixtures/golden-fixture/audit-target/` (worklist index `docs/fix/README.md`, `docs/adr/` terminal `0047-transport.md`, `docs/audits/3-*.md`), and E2's enumeration carries the same fixture-root-relative paths (PE-10: repo root has neither `docs/adr/` nor `docs/audits/`). Location qualification only — the asserted invariants and fixture tree are unchanged; mechanical, intent-preserving class | 98d8e6a4b66b27a43c14ffe2675f22528fad3434 |
| SPEC55-F3 | spec | low | product | 48f3977ae8ff4fb85ec489ed1b6b54b5b6fef9dadb78ef3793c5af1ecccb4cb7 | AC3(b) requires a committed valid and a committed invalid envelope sample (also referenced in In-scope item 3 and the schema-package Integration-closure row), but the entity closure's E1 row enumerates the executor fixture tree as plan / non-atomic variant / toy SPEC / acceptance manifest / expected-output snapshots with no entity row for the sample files — a committed fixture entity with no CRUD + state closure (C3) | `docs/features/55-executable-golden-fixture/SPEC.md:310` (AC3b: "envelope schema validity — committed valid sample passes and committed invalid sample is rejected via `scripts/schema-runtime.mjs`"); `:105` (In-scope item 3) and `:248` (Integration closure, schema package: "for the valid/invalid sample cases"); entity-closure E1 block "**E1 — executor fixture tree** (`scripts/fixtures/golden-fixture/` toy CSV files: plan, non-atomic plan variant, toy SPEC, acceptance manifest, expected output snapshots)" enumerates no sample file; `grep -n -i "sample" SPEC.md` matches only lines 105, 248, 310 — none inside the E1 block | resolved | E1's enumeration extended with the committed envelope samples `envelope/valid.json` + `envelope/invalid.json`, so E1's existing Create/Read/Update/Delete/state rows close them; AC3(b) names the sample paths (PE-12: read-only consumption via `loadSchemaRuntime()`, unit-route committed-fixture precedent). The samples were already in scope (AC3(b), In-scope item 3) — closure completion, no scope change | 98d8e6a4b66b27a43c14ffe2675f22528fad3434 |
| SPEC55-F4 | spec | info | product | 48f3977ae8ff4fb85ec489ed1b6b54b5b6fef9dadb78ef3793c5af1ecccb4cb7 | AC2 passes only when `phase-lint.mjs` "names at least one phase-lint rule id in its finding lines", but the tool's finding lines emit `P<n> box-<n>: <reason>` and its own contract calls the eight checks "boxes", so "rule id" names no token a suite or reader can match unambiguously (C8) | `docs/features/55-executable-golden-fixture/SPEC.md:307-309` (AC2); `scripts/phase-lint.mjs:699-700` (`for (const finding of findings) lines.push(\`P${phase.number} box-${finding.box}: ${finding.reason}\`)`); `skills/phase-contract/SKILL.md:72-75` fixed output block "`Phase-lint: BLOCKED — box <n>: <one-line reason>`" | resolved | AC2 now requires the finding lines to emit at least one phase-lint finding token `box-<n>`, explicitly anchored to the tool's fixed `P<n> box-<n>: <reason>` finding-line form (`scripts/phase-lint.mjs:698`; PE-11) — the same assertion with a machine-matchable token instead of the unmatchable "rule id"; mechanical, intent-preserving class | 98d8e6a4b66b27a43c14ffe2675f22528fad3434 |
| SPEC55-F5 | spec | info | product | 48f3977ae8ff4fb85ec489ed1b6b54b5b6fef9dadb78ef3793c5af1ecccb4cb7 | Expectation rows 3–4 resolve in-scope ("the manual judgment protocol survives…", "the tool-calling smoke test stays reachable") but no acceptance criterion verifies either survives the doc slim: AC5 checks only ≤ 150 lines, the judgment protocol and run log, and a fixture pointer | `docs/features/55-executable-golden-fixture/SPEC.md` "### Expectation sweep" rows 3 and 4 (pointers "AC5; In-scope item 5" and "In-scope item 5 (protocol section)"); AC5 `:316-319` ("`docs/workflow/GOLDEN_FIXTURE.md` is ≤ 150 lines, contains the judgment protocol and run log, and points at `scripts/fixtures/golden-fixture/`"); In-scope item 5 `:113-117` promises the smoke precondition and the "no invented steps" pass criteria stay | resolved | AC5 extended: `grep -n "Tool-calling smoke test"` and `grep -n "Fixed pass criteria"` must each match the slimmed doc, and the "No invented steps" rule must appear in the retained pass criteria — Expectations 3–4 are now criterion-verified against the doc slim; their sweep pointers updated to the extended AC5. Retention was already promised (In-scope item 5) — criterion completion, no scope change | 98d8e6a4b66b27a43c14ffe2675f22528fad3434 |
| SPEC55-F6 | spec | medium | product | 20b088bc4b28997a2ade6fb700c78e08284b610c43b392121f53f4e41a11aa0e | AC5's first clause (`grep -rn "csv-export-command" docs/` returns no matches) is unsatisfiable while the run log is preserved verbatim: five grandfathered run-log rows in `docs/workflow/GOLDEN_FIXTURE.md` contain the token, and the half forbids rewriting them (C9) | `docs/features/55-executable-golden-fixture/SPEC.md:325-326` (AC5 requires a zero-match grep over `docs/`); `:80-81` ("run-log history is load-bearing evidence and is preserved verbatim; nothing in this feature rewrites past rows"); `:149-150` ("Rewriting or pruning historical run-log rows — append-only; grandfathered rows are read-only evidence"); `:223-224` and `:235-236` (E4/E5 Update append-only); `docs/workflow/GOLDEN_FIXTURE.md:278,279,280,298,383` (run-log rows carrying `docs/features/99-csv-export-command`); `grep -c "csv-export-command" docs/workflow/GOLDEN_FIXTURE.md` → 8, of which 5 are run-log rows (lines > 269) | resolved | AC5's first clause narrowed to the live docs surface: a zero-match pipeline over `grep -rn "csv-export-command" docs/` whose filter exempts exactly two preserved-evidence shapes — the doc's dated run-log rows (E4: append-only, preserved verbatim) and the append-only unit ledgers under `docs/features/` (which quote the token as history — including this finding's own row and the criterion itself, a self-void the original docs-wide grep also carried). The exact filter string is frozen in AC5 and verified: pre-slim it leaves exactly the 3 embedded-block lines (PE-13: 8 doc matches = 3 embedded-block lines removed by the slim + 5 grandfathered rows). The embedded toy blocks the clause was written against still must vanish (In-scope item 5). Criterion made satisfiable — no scope change (closure completion class) | c5032f6ebb316ad912601692c414c51b943d03fd |
| SPEC55-F7 | spec | low | product | 20b088bc4b28997a2ade6fb700c78e08284b610c43b392121f53f4e41a11aa0e | `## Dependencies` and `### Context` attribute `scripts/check-skill-context.mjs` to feature 38, but feature 38 is `workflow-status-sensor-script` (`scripts/workflow-status.mjs`, PR #213); the checker was introduced by feature 20 (commit `635168ce`, PR #116) — a producer-lineage claim about an existing surface the repository contradicts (C10) | `docs/features/55-executable-golden-fixture/SPEC.md:42-43` ("feature 38 (`scripts/check-skill-context.mjs`)"); `:67-68` ("feature 37/38 landed deterministic checkers (`phase-lint.mjs`, `check-skill-context.mjs`)"); `docs/features/ROADMAP.md:48` row 38 = `workflow-status-sensor-script` · PR #213, delivering `scripts/workflow-status.mjs`; `git log --diff-filter=A --oneline -- scripts/check-skill-context.mjs` → `635168ce feat(distribution): add grouped catalog and context budgets` (2026-07-31); `git show --stat 635168ce` touches `docs/features/20-runtime-guardrails-progressive-skills/{TASKS,progress,testing}.md`; that unit's `progress.md:23` lists `scripts/check-skill-context.mjs` among its files; `docs/features/ROADMAP.md:30` row 20 = `runtime-guardrails-progressive-skills` · PR #116 | resolved | Producer lineage corrected: `scripts/check-skill-context.mjs` re-attributed to feature 20 in `## Dependencies` and `### Context` (`635168ce feat(distribution): add grouped catalog and context budgets`, 2026-07-31; roadmap row 20 · PR #116; feature 38 delivers `scripts/workflow-status.mjs`, PR #213 — PE-14). Attribution slip only — the producer set and the unaffected-tooling statement are unchanged; mechanical, intent-preserving class | c5032f6ebb316ad912601692c414c51b943d03fd |
| SPEC55-F8 | spec | low | product | 20b088bc4b28997a2ade6fb700c78e08284b610c43b392121f53f4e41a11aa0e | `### Context` says "feature 59 established the committed-toy-repo fixture pattern (`scripts/fixtures/unit-route/` + `scripts/continuation-discipline.test.mjs`)", but `scripts/fixtures/unit-route/` was committed by fix #224 P1 (commit `8cdf9548`), and feature 59's `continuation-discipline.test.mjs` builds throwaway `mkdtempSync` repos — not a committed fixture (C10) | `docs/features/55-executable-golden-fixture/SPEC.md:70-72`; `git log --diff-filter=A --oneline -- scripts/fixtures/unit-route` → `8cdf9548 feat(scripts): deterministic unit router with bounded read set (#224)`, body "the committed unit-37 dogfood fixture copied verbatim from feat/37-phase-lint-script@e1e282c5"; `docs/fix/224-deterministic-replan-routing/`; `scripts/continuation-discipline.test.mjs:53` (`fs.mkdtempSync(...)`) and `grep -n "fixtures/unit-route" scripts/continuation-discipline.test.mjs` → no matches; `git log --diff-filter=A --oneline -- scripts/continuation-discipline.test.mjs` → `ee1008ab feat(workflow-status): emit next.continuation with fail-closed refusals (59 P2)` | resolved | `### Context` re-attributed: the committed-toy-repo fixture pattern (`scripts/fixtures/unit-route/`) to fix #224 P1 (`8cdf9548`, body "P1 of fix-224… the committed unit-37 dogfood fixture"); feature 59 keeps only `next.continuation` + `scripts/continuation-discipline.test.mjs`, which builds `mkdtempSync` throwaway repos and never references the committed fixture (PE-15). Attribution slip only; mechanical, intent-preserving class | c5032f6ebb316ad912601692c414c51b943d03fd |
| SPEC55-F9 | spec | info | product | 20b088bc4b28997a2ade6fb700c78e08284b610c43b392121f53f4e41a11aa0e | Expectation 2 resolves `in-scope` ("a failing mechanical assertion names which fixture and which assertion failed"), but its pointer AC3 lists only coverage items (a)–(e) and carries no diagnostic-output requirement; the promised criterion is deferred to the empty Engineering half, so no frozen Product criterion verifies it (C8) | `docs/features/55-executable-golden-fixture/SPEC.md:291` (Expectation 2, pointer "AC3 (suite output contract in Engineering half)"); `:314-318` (AC3 a–e: no output/diagnosability clause); the `## Engineering half` at `:399`+ is empty at this revision | resolved | Expectation 2's criterion frozen in the Product half: AC4's tamper case now additionally asserts the failing check's message names the mutated fixture path and the violated assertion — diagnosability is verified by the one always-exercisable failing scenario on every suite run instead of being deferred to the empty Engineering half; sweep row 2's pointer moved to `AC4 (tamper-case diagnostic assertion)`. Criterion completion — no scope change (closure completion class) | c5032f6ebb316ad912601692c414c51b943d03fd |
| SPEC55-F10 | spec | low | product | 20b088bc4b28997a2ade6fb700c78e08284b610c43b392121f53f4e41a11aa0e | The Product evidence rows PE-5/PE-6/PE-7 carry `authority-kind: web`, a value outside the closed vocabulary (`repository` / `document` / `ledger` / `forge` / `user` / `derived`) — the fetched-source kind is `document` — so the frozen evidence ledger violates its own row contract (C11) | `docs/features/55-executable-golden-fixture/decisions.md:28-30` (PE-5/6/7 rows with `web`); `skills/evidence-grounding/references/ROWS.md:26-36` ("`authority-kind` — exactly one", six values, `document` covers "fetched external documentation"); `grep -rn "web" docs/features/*/decisions.md docs/features/*/planning-evidence.md` → only these three rows | resolved | PE-5/PE-6/PE-7 `authority-kind` set to `document` per the closed vocabulary (`evidence-grounding/references/ROWS.md`: `document` covers fetched external documentation; `web` is not a value). Vocabulary fix only — claims, sources, access dates, freshness, status unchanged; mechanical, intent-preserving class | c5032f6ebb316ad912601692c414c51b943d03fd |
| PF-55-01 | plan | high | plan | 01ede4dfadf0da465c21741a5a22440104be4d59d389c435718108c2bfe74eff | ACCEPTANCE.md AC5's slug-grep validator is malformed (both alternation pipes markdown-escaped) and contradicts SPEC AC5; it returns no output on the current pre-slim doc where the embedded toy blocks still carry the slug, so it passes for the wrong reason, and in its raw-source form exempts nothing and falsely fails on the five grandfathered run-log rows — the frozen finish-line check cannot isolate the intended exemption (P10, L5) | `docs/features/55-executable-golden-fixture/ACCEPTANCE.md:15` (`'^(docs/workflow/GOLDEN_FIXTURE\.md:[0-9]+:\| 20\|docs/features/)'`) vs `docs/features/55-executable-golden-fixture/SPEC.md:330-331` (`'^(docs/workflow/GOLDEN_FIXTURE\.md:[0-9]+:\| 20|docs/features/)'`); run at `438c7782` pre-slim: SPEC-intent pipeline → `docs/workflow/GOLDEN_FIXTURE.md:45`, `:54`, `:122` (non-empty, correctly fails); ACCEPTANCE-rendered pipeline → no output (vacuous pass while the embedded blocks are still present) | open | — | — |
| PF-55-02 | plan | medium | plan | 01ede4dfadf0da465c21741a5a22440104be4d59d389c435718108c2bfe74eff | The doc-slim budget arithmetic is wrong and P2's ≤150-line done-when is unreachable by the plan's enumerated removals: the listed ranges (44–115, 121–134, 204–268, 312–317, 333–356, 357–380) total 205 lines and leave 178, while the plan's own claim (173 embedded + 54 prose = 227 removed) leaves 156 — still above 150; even also dropping `### Form-turn shape` (188–203) and 116–120 leaves 157 (P9, P10) | `docs/features/55-executable-golden-fixture/SPEC.md` §Design ("the measured budget: 383 lines today; 173 embedded lines leave, 54 prose lines move … caps at 150 lines"); PE-006; simulation at `438c7782`: `wc -l` = 383, removals = 205 → 178 remaining | open | — | — |
| PF-55-03 | plan | low | plan | 01ede4dfadf0da465c21741a5a22440104be4d59d389c435718108c2bfe74eff | O2 is mis-phased and duplicates O7: both cite SPEC §Scope item 2 + AC6 with the same validator (`suite auditTargetTraps`) and the same required evidence ("three invariant assertions pass"), but O2's phase is P1 while the suite it names is authored in P3 — its validator cannot run in the phase that owns it (L3, L4) | `docs/features/55-executable-golden-fixture/SPEC.md` `### Obligations` rows O2 (P1, tasks 6–7) and O7 (P3, task 6); `### Phases` P1 (fixture bytes) vs P3 (suite) | open | — | — |
| PF-55-04 | plan | info | plan | 01ede4dfadf0da465c21741a5a22440104be4d59d389c435718108c2bfe74eff | O12 states the roadmap row reads "planned then done", but the status machine requires execute-phase P1 to set `in-progress` (planned → in-progress → done); the row summary also still ends with the stale routing note "next: /review-spec 55" although review-spec passed and the plan is under review (L4) | `docs/features/55-executable-golden-fixture/SPEC.md` O12; `docs/features/ROADMAP.md:96-98,117` (legend: "execute-phase P1 (branch open; row → in-progress)"); `docs/features/ROADMAP.md:65` tail "next: `/review-spec 55`" | open | — | — |
