# Planning evidence — 59-executable-continuations

Compact source-backed conclusions used to cut this Engineering plan. This is not an
exploration transcript. Row shape and column order per
`evidence-grounding/references/ROWS.md` (§ Plan-stage table). All rows observed at
HEAD `b228ff95d924d4fc7cb2de4605fd5be9e98394a2` (branch
`feat/59-executable-continuations-fixture`) unless a row says otherwise.

Planning baseline: HEAD `b228ff95` + live forge state verified 2026-09-16 (governing
issue #237 OPEN; absorbed #215/#231 closed as absorbed — receipt SPEC-REVIEW-59-2).

| id | claim-or-obligation | authority-kind | source-and-location | observed-revision | affected-decision-or-obligation | freshness | status | owner-or-next-evidence |
|---|---|---|---|---|---|---|---|---|
| PE-001 | The schema package is `@gtrabanco/agentic-workflow-schema@4.1.2`; its test script is `tsc && tsc -p tsconfig.test.json && bun test test/*.test.mjs`, so `bun run test` (npm test fallback) is the AC1 runner and the next additive minor is 4.2.0 | repository | `packages/agentic-workflow-schema/package.json:2-3,62` | b228ff95 | O1, O2; AC1, AC7 | current | proven | re-verify version before the P1 bump |
| PE-002 | `EnvelopeNext` is prose-only today — `{ recommended, alternatives, tier, suggested? }` — and `Envelope.next` is a required field; there is no `continuation` member anywhere in the package | repository | `packages/agentic-workflow-schema/src/index.ts:159-167` (`EnvelopeNext`), `:168-184` (`Envelope`) | b228ff95 | O1; AC1 | current | proven | P1 extends this interface additively |
| PE-003 | The envelope JSON Schema declares `next` with `additionalProperties: false` and per-member schemas at `envelope.schema.json:161-182` — the optional `continuation` property must be added to this projection too, or schema-validated envelopes with the field would refuse | repository | `packages/agentic-workflow-schema/envelope.schema.json:161-182` | b228ff95 | O1; AC1 | current | proven | add the property, keep it optional |
| PE-004 | `WORKFLOW_TRANSITION_TABLE` (:780) and `decideWorkflowAction()` (:1042) exist and stay consumer-side; the SPEC pins the emitter as a projector of the already-computed decision, never a second decider | repository + SPEC product half | `packages/agentic-workflow-schema/src/index.ts:780,:1042`; SPEC `## Dependencies` (24 `workflow-transition-decider`: MERGED) | b228ff95 | O3; E-59-2 | current | proven | emitter input is the resolved command string |
| PE-005 | Feature 38's frozen acceptance pins `grep -c 'decideWorkflowAction' scripts/workflow-status.mjs` → 0 — the sensor must not start calling the decider; the emitter therefore consumes the sensor's own `resolveNext()` output | ledger | `docs/features/38-workflow-status-sensor-script/ACCEPTANCE.md` AC-12 (frozen manifest) | b228ff95 | O3; E-59-2 | current | proven | never import `decideWorkflowAction` into the sensor |
| PE-006 | The sensor's `next` assembly is `resolveNext()` at `scripts/workflow-status.mjs:961-1004` (returns `{ recommended, alternatives, tier }`), the envelope attaches `next.suggested` at `:1192`, and the step-13 class router `readSuggestions()` sits at `:759` — these are the emission hook points | repository | `scripts/workflow-status.mjs:961-1004,:1192,:759` | b228ff95 | O3, O5; AC2 | current | proven | re-verify line numbers at P2 edit time |
| PE-007 | Digest helpers for the receiver-verifiable evidence token already ship: `sha256HexSync` (sync), `sha256Hex`, `sha256Bytes` in the schema package | repository | `packages/agentic-workflow-schema/src/sha256.ts:51,:175,:190` | b228ff95 | O4, O5; AC4 | current | proven | token digest = sha256 of the referenced artifact bytes |
| PE-008 | The `normative-surfaces@1` table lives in `CLAUDE.md` inside a fenced block at :311-335; the `turn-contract-fields` row (:331) binds `hand-off-fields@1` with machine `envelope-field:next` and `must-name: yes` — the machine→text closed set that a new `next.continuation` field and a new refusal vocabulary must join | repository | `CLAUDE.md:311-335` (row `turn-contract-fields` at :331) | b228ff95 | O8; AC6 | current | proven | add rows; never widen the closed set silently |
| PE-009 | The `hand-off-fields@1` block sits at `skills/orchestration-envelope/references/TURN_CONTRACT.md:47-54` declaring `next \| recommended / alternatives / tier / suggested` — AC6's `next \| continuation` row is appended there and mirrored by `npm run bundle:skills` | repository | `skills/orchestration-envelope/references/TURN_CONTRACT.md:47-54` | b228ff95 | O8; AC6 | current | proven | same-PR re-bundle keeps parity green |
| PE-010 | The drift gate reads `normative-surfaces@1` from `CLAUDE.md`, refuses a row whose `grammar` cell does not resolve, and parses machine vocabularies out of committed schema source with fixed-shape extractors — a new `schema-export:CONTINUATION_REFUSALS` grammar kind and the `continuation-refusal-type` must-name vocabulary need a declared extractor, not prose | repository | `scripts/normative-drift.test.mjs:11-33` (scope + extractor contract) | b228ff95 | O8; AC6 | current | proven | extend the extractor set in the same phase as the rows |
| PE-011 | The five quote surfaces exist at the exact paths and lines the reviewed Product half pins: `skills/workflow-status/SKILL.md:121` (next-command echo), `skills/workflow-status/references/PRE_EXECUTION.md:44,52` (`stale` label row + re-run sentence), `skills/review-spec/references/OUTPUT.md:3`, `skills/review-plan/references/OUTPUT.md:3`, `skills/review-change/references/PERSIST_AND_DECIDE.md:3` | repository | the five files at the pinned lines, re-verified at HEAD | b228ff95 | O9; AC10 | current | proven | quote rule lands at each pinned surface in P3 |
| PE-012 | `INTERVIEW.md` carries the one-question-per-turn rule at :19 inside §3 (`## Process` step 3, the raw-idea interview with the six fixed slots, defaults, mandatory-question rule, `NEEDS_INPUT` escalation), and `design-feature/SKILL.md`'s progressive-loading hard stop (:91-95) says "Ask its one next question" — both are the text AC8/AC9 replace | repository | `skills/design-feature/references/INTERVIEW.md:17-19,21-45`; `skills/design-feature/SKILL.md:85-99` | b228ff95 | O10, O11; AC8, AC9 | current | proven | batch the ask protocol; rubric verbatim |
| PE-013 | The golden fixture's fixed pass criteria are at `docs/workflow/GOLDEN_FIXTURE.md:168` with the expected-report extension rule at :230-232 ("Add these boxes to the fixed pass criteria above; never replace them") — the form-turn shape is added as boxes, the run-log table untouched | repository | `docs/workflow/GOLDEN_FIXTURE.md:168,:230-232` | b228ff95 | O12; AC11 | current | proven | additive boxes only |
| PE-014 | Every touched skill has a context-budget entry in the manifest (`design-feature`, `workflow-status`, `review-change`, `review-plan` per-skill + `review-spec:default` route at :416); the tool's own re-basis convention is `ceil(measured × 1.10)` with the growth source named in `policy.declared` | repository | `docs/workflow/SKILL_CONTEXT_BUDGETS.json` (entries + `policy.declared` history) | b228ff95 | O13; AC12 | current | proven | re-base after ALL skill text is final (end of P4) |
| PE-015 | The pi mirror parity suite requires `packages/pi-agentic-workflow/skills/` byte-identical to the source skill tree; the bundle is regenerated by the package's own `bundle:skills` script — never hand-edited | repository | `packages/pi-agentic-workflow/test/skill-parity.test.mjs:4-40`; `packages/pi-agentic-workflow/package.json` `bundle:skills` | b228ff95 | O14; AC12 | current | proven | re-bundle in the same PR as the skill edits |
| PE-016 | The schema package's published vector pattern is a frozen exported const array of canonical objects with digests (see `src/pre-execution-vectors.ts`, `PRE_EXECUTION_CANONICAL_VECTORS`); continuation vectors follow the same shape so the suite can pin byte-stable canonical forms | repository | `packages/agentic-workflow-schema/src/pre-execution-vectors.ts` | b228ff95 | O1; AC1 | current | proven | vectors frozen in P1 |
| PE-017 | The Normalized Repository State is `frozen` and records that no project architectural-invariants document exists — the plan-stage invariant classification is `n/a: no project invariants declared` | ledger | `docs/workflow/REPOSITORY_STATE.md` Snapshot table (Status `frozen`) + fact F010 | b228ff95 | preflight Stage 2 | current | proven | re-check if an invariants doc appears before execution |
| PE-018 | `docs/architecture/ARCHITECTURAL_INVARIANTS.md` is absent and the documentation map declares no other invariants document; `docs/workflow/WORKFLOW_INVARIANTS.md` is the portable workflow contract, not a project rule set | repository + ledger | directory absence at HEAD; NRS F010 wording | b228ff95 | preflight Stage 2 | current | proven | none — recorded n/a |
| PE-019 | Roadmap row 59 has `Depends on: —` (no hard dependencies); the fix index's only `pending` row (#179) is dependency-gated on features 30/31/32 and touches execution-ledger amendment classes, disjoint from the continuation surfaces; no open issue is a fix-now for the schema package, the sensor, or the five quote surfaces | forge + document | `docs/features/ROADMAP.md` row 59; `docs/fix/README.md` rows (#179 pending, all others done); `gh issue list --state open` (2026-09-16: #237 is this unit's governing issue; no fix-now issues) | b228ff95 | dependency & blocker check | current | proven | re-verify forge state at execution |
| PE-020 | The phase-lint script's frozen target→layer table maps `scripts/*` and `packages/*` to `config/infra` and `skills/*`/`docs/*`/`*.md` to `docs`, and box 2 judges each task's first path-like target against the phase's one declared layer — the product sketch's "P2 emission + quote surfaces" spans both layers and cannot pass as one phase | repository | rule stated by `scripts/phase-lint.mjs` `layerForTarget()`/box-2 (pinned by `scripts/phase-lint.test.mjs`); product sketch in SPEC `## Size` | b228ff95 | E-59-1 (phase cut) | current | proven | split recorded as an engineering decision, product bytes untouched |
