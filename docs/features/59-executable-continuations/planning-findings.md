# Planning findings — 59-executable-continuations

One stage-aware table per unit (ledger contract:
`pre-execution-review/references/LEDGERS.md` § 3). Reviewers append rows; only the
owning skill resolves them (`design-feature` for `class: product`). Column order is
fixed:

```text
finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision
```

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| F1 | spec | low | product | 55a70fd35d884ef88c3bb2c8a170ebb7a775bf6e9ef4b0b85098131db944721e | Expectation-sweep row 15 resolves "A token-savings measurement protocol for both capabilities" as `out-of-scope` with pointer "Out-of-scope bullet", but no bullet in `#### Out of scope / non-goals` covers a measurement protocol — the exclusion exists only inside the row's own pointer text, so the scope disposition is recorded nowhere else in the half | `docs/features/59-executable-continuations/SPEC.md` "### Expectation sweep" row 15; `grep -in measurement docs/features/59-executable-continuations/SPEC.md` matches only line 311 (the row itself); `#### Out of scope / non-goals` holds 10 bullets (model-emitted tokens, new verbs, verdict rules, AWL, extra classes, interview semantics, prose removal, continuation store, majors, #230 fixture) — none mentions a measurement protocol; decisions.md D-59-1…D-59-10 record no such disposition either | resolved | 11th out-of-scope bullet added (`A token-savings measurement protocol` — deliberately dropped, no owner, same disposition as #226's audit, pointing at Expectation-sweep row 15); the disposition is the one row 15 already recorded, so scope and intent are unchanged — mechanical class; spec-lint bullet count restated 10 → 11 | b7a405c2239ff3f43eff3187 |
| F2 | spec | low | product | 55a70fd35d884ef88c3bb2c8a170ebb7a775bf6e9ef4b0b85098131db944721e | In-scope item 4 and AC10 name the quote-rule consuming surfaces as "`workflow-status` skill text and the gate-re-run/review-receipt-refresh routes", but no repository file or route carries either route name — the concrete surfaces exist under other names, so the read-verified criterion AC10 cannot be located from the half alone and a plan/audit cannot bind it without guessing | `docs/features/59-executable-continuations/SPEC.md` "#### In scope" item 4 and "### Acceptance criteria" AC10; repo-wide grep for `gate-re-run` / `review-receipt-refresh` matches only this unit's own artifacts and roadmap row 59 (which quotes #215's class labels verbatim); the concrete surfaces live at `skills/workflow-status/references/PRE_EXECUTION.md:44,52` (stale receipt → re-run that stage's review) and the review-receipt persistence/refresh surfaces `skills/review-spec/references/OUTPUT.md`, `skills/review-plan/references/OUTPUT.md`, `skills/review-change/references/PERSIST_AND_DECIDE.md` | resolved | in-scope item 4 and AC10 now pin concrete files, all verified in the working tree this turn: status refresh → `skills/workflow-status/SKILL.md`; planning-gate re-run → `skills/workflow-status/references/PRE_EXECUTION.md` (`stale` label row + re-run sentence); review-receipt refresh → `skills/review-spec/references/OUTPUT.md`, `skills/review-plan/references/OUTPUT.md`, `skills/review-change/references/PERSIST_AND_DECIDE.md`; the class→surface mapping stays 1:1 with D-59-5, no surface added or removed — mechanical class; adjacent slip fixed in the same sentence: item 4's AC pointer listed AC9 (the interview-protocol criterion), the quote rule is AC10, and AC9 is owned by item 6 (→ AC8, AC9) | b7a405c2239ff3f43eff3187 |
| F3 | spec | info | product | 55a70fd35d884ef88c3bb2c8a170ebb7a775bf6e9ef4b0b85098131db944721e | Role-matrix preamble says the derived roles are "recorded in `decisions.md`", but decisions.md contains no role list — the five-role derived set (executor-agent, authoring-agent, review-agent, driver, human-owner) exists only in the SPEC's own matrix, so the provenance pointer does not resolve | `docs/features/59-executable-continuations/SPEC.md` "### Capability closure" § 3 preamble; `grep -n "executor-agent\|authoring-agent" docs/features/59-executable-continuations/decisions.md` matches no role-list row (only D-59-3's consumer sentence); the matrix itself is complete (5 roles × 5 capabilities, explicit allowed/denied) | resolved | pointer now resolves inside the SPEC: the role-matrix preamble and the capability-closure intro state the matrix below is the unit's only role list (`decisions.md` carries only the open CAPABILITIES-seeding offer); the five-role derived set is unchanged — mechanical class | b7a405c2239ff3f43eff3187 |
| F4 | plan | low | plan | edf2fb501a456f623e6f63420501d63fd6b2e279a0c8d15be78f91621809917d | PE-020's `status` cell reads `derived`, which is not in the closed Plan-stage status vocabulary (`proven \| decision \| unknown` — `derived` is an authority-kind, not a status), and its `authority-kind` cell reads the compound `derived + repository` where the set is exactly one; the SPEC's `### Planning evidence` restatement ("all `current` + `proven`/`derived`") repeats the off-vocabulary status | `docs/features/59-executable-continuations/planning-evidence.md` PE-020 row; `skills/evidence-grounding/references/ROWS.md` § Closed vocabularies (both the repository copy and the installed release copy read this turn — status closes at `proven\|decision\|unknown`, authority-kind at exactly one); `docs/features/59-executable-continuations/SPEC.md` "### Planning evidence" line | resolved | PE-020 relabelled to the closed vocabularies: `authority-kind` `derived + repository` → `repository` (exactly one; the row's source cell already cites the rule by path + symbol/test — `scripts/phase-lint.mjs` `layerForTarget()`/box-2 pinned by `scripts/phase-lint.test.mjs` — repository's acceptable form), `status` `derived` → `proven` (the cited rule was actually read and answers the claim; re-confirmed by the phase-lint re-run at review and again at this repair, verdict PASS); the derived phase-cut consequence stays owned by E-59-1 in `affected-decision-or-obligation`, claim/source/freshness cells unchanged; SPEC `### Planning evidence` restatement now reads "all `current` + `proven`" — mechanical class | 59-plan-2 |
| F5 | plan | low | plan | edf2fb501a456f623e6f63420501d63fd6b2e279a0c8d15be78f91621809917d | O9's `validator` is not copied from its authority AC-10: it substitutes `grep -c 'quote' docs/workflow/FEATURE_WORKFLOW.md ≥ 1` for AC-10's frozen `grep -c 'next.continuation' docs/workflow/FEATURE_WORKFLOW.md → 1`, so the obligation could read verified while the single-pointer requirement (exactly one `next.continuation` pointer in the hand-off section) is never checked | `docs/features/59-executable-continuations/planning-obligations.md` O9 (Validator column) vs `docs/features/59-executable-continuations/ACCEPTANCE.md` AC-10 (Validator column), read side by side this turn; LEDGERS §2: validator is "copied from `ACCEPTANCE.md` or the phase's done-when" | resolved | O9's Validator re-copied verbatim from ACCEPTANCE.md AC-10's Validator cell: "read-verified: per-file quote sentences quoted in the phase handoff; `grep -c 'next.continuation' docs/workflow/FEATURE_WORKFLOW.md` → 1" — the substituted `grep -c 'quote' … ≥ 1` is gone, so the obligation can no longer read verified while the exactly-one-pointer requirement goes unchecked; the per-file sentence detail stays in O9's Required-evidence cell, no other O9 cell touched — mechanical class | 59-plan-2 |
| F6 | plan | low | plan | edf2fb501a456f623e6f63420501d63fd6b2e279a0c8d15be78f91621809917d | Dev scenarios `continuation:empty-state` and `continuation:concurrent-emit` resolve to "(P2 pins)" in the SPEC's scenario table and testing.md's mandatory inventory, but no P2 task, no P2 done-when pin family, and no obligation row creates those pins — P2 task 5 enumerates only non-terminal emission, refusal, and offline fixtures, the P2 done-when names emission/refusal/offline/digest/discipline/rendering, and O3–O7/O17 carry neither pin; executed as written, both scenarios (stateless byte-identical envelopes / no-store idempotence per D-59-9, and empty-state no-emission) are never exercised while the phase reads green | `docs/features/59-executable-continuations/SPEC.md` "### Dev scenarios" rows 1 and 4 (both "(P2 pins)"); `docs/features/59-executable-continuations/testing.md` "Mandatory scenario inventory" rows 1 and 4; `docs/features/59-executable-continuations/PLAN.md` and `TASKS.md` P2 task list (8 tasks enumerated) + done-when; `docs/features/59-executable-continuations/planning-obligations.md` (no row carries either pin) | resolved | P2 gained the missing pin surface for both scenarios: the sensor-suite emission-pins task in PLAN.md + TASKS.md now also pins `continuation:empty-state` (fixture with no startable unit → no `continuation` field, exit 0) and `continuation:concurrent-emit` (two consecutive runs → byte-identical envelopes, no continuation store, D-59-9) — box 3 caps a config/infra phase at 8 tasks, so the pins join the suite's existing emission-pins task rather than a ninth task (a first-cut ninth task was rejected by the phase-linter at this repair and folded into task 5); the P2 done-when now names the empty-state and concurrent-emit idempotence pins; obligation rows O18 (empty-state) and O19 (concurrent-emit) added — one row per normative behaviour, both P2, validator = the sensor-suite command including the pin; SPEC restatements aligned (`### Obligations` O1…O17 → O1…O19 naming the two pins; `### Phases` P2 bullet names the scenario pins); phase-lint re-run at this repair: verdict PASS (8/8 × 5), whole-set fingerprint unchanged `1a3bf1481e923fa03b105fa9b133b4a0be1182862f2a0c3d5ade6cec063583dc` (phase shape is untouched — the pin joins task 5), P2 fingerprint `P2:config/infra:8:sensor-continuation-emission` — mechanical class | 59-plan-2 |
| F7 | plan | low | plan | 8237e6c2bcf4e14996a4fcf2fc66069483c873934eeac4fe693ce457fd64ab5d | The F4–F6 repair batch rotated the plan set to `59-plan-2` but left two Engineering-half restatements in `SPEC.md` unaligned: (a) the `## Engineering half` intro asserts in the present tense that "the artifact revision of this plan set is `59-plan-1`" while `PLAN.md` and the planner handoff record `59-plan-2` — per POLICY §7 the identity pairing is claimed `59-plan-1` / recomputed `59-plan-2`, a recorded value no recomputation supports is a defect in the artifact that recorded it; (b) the `## Artifacts created by plan-feature` list still records `planning-obligations.md — O1…O17` while the ledger carries O1…O19 and the SPEC's own `### Obligations` restatement — aligned by this same repair — says O1…O19, so the SPEC contradicts itself on the obligations count | `docs/features/59-executable-continuations/SPEC.md:482` ("bytes are untouched; the artifact revision of this plan set is `59-plan-1`.") and `:754` (`planning-obligations.md — O1…O17`) vs `docs/features/59-executable-continuations/PLAN.md:9-10` (`59-plan-2` with the rotation note) and the 19-row ledger (`grep -c '^\| O' planning-obligations.md` → 19); `grep -rn "O1…O17"` over the unit dir matches only SPEC.md:754 outside finding rows; adjacent stamps verified accurate and excluded as defects: SPEC.md:745 section header, decisions.md:210 header, progress.md receipt blocks, PLAN.md:10 — historical records of the `59-plan-1` cut, not present-tense claims | resolved | SPEC.md `## Engineering half` intro now states the plan set's revision as `59-plan-3` in PLAN.md's own rotation-note form (rotated on 2026-09-16 by the F4–F6 repair batch, re-rotated on 2026-09-16 by the F7 repair batch; initial cut `59-plan-1`), and PLAN.md's revision line was rotated to `59-plan-3` in the same batch — the POLICY §7 identity pairing now holds, claimed = recomputed = `59-plan-3` in both artifacts; the artifacts list reads `planning-obligations.md — O1…O19`, matching the 19-row ledger (`grep -c '^\| O' planning-obligations.md` → 19) and the SPEC's own `### Obligations` restatement (SPEC.md:596), so the SPEC no longer contradicts itself; wording-only determination (POLICY §3): intent, obligation identity (O1–O19 unchanged, all `planned`), phase topology (P1–P5), validators, and authority all unchanged — no replan; phase-lint re-run at this repair: verdict PASS (8/8 × 5), whole-set fingerprint unchanged `1a3bf1481e923fa03b105fa9b133b4a0be1182862f2a0c3d5ade6cec063583dc`; Product half verified untouched — `selectSpecProduct` on the post-repair SPEC = 26199 bytes, sha256 `15f99f5e4295cecefb5dbc40f0dd89fe941bc603809e5ebd64b3f8282a11470e`, equal to the SPEC-REVIEW-59-2 pin (parent Product receipt stays current); historical stamps left byte-identical per the finding's own evidence (SPEC.md:745 header, decisions.md:210 header, progress.md receipt blocks, PLAN.md initial-cut note) — mechanical class | 59-plan-3 |
| F8 | plan | info | plan | 6da9bdd824a9636050851ab4237b59d89a6bec0c3165a08fd4ca2abab23e5664 | Validator cells of O1, O3, and O8 abbreviate their authority criterion's validator text instead of copying it: O1 drops AC-01's `vector` pin name and the node-fallback note; O3 drops AC-02's `offline` pin name; O8 drops AC-06's two row-presence greps (`grep -c 'continuation-refusal-type' CLAUDE.md` ≥ 1 and `grep -c 'next \| continuation' skills/orchestration-envelope/references/TURN_CONTRACT.md` ≥ 1). Each obligation keeps the identical executable command, so this is not the F5 substitution class — no check is weakened or replaced — but the cells understate their own gates and a reader of the ledger alone would not see the greps/pins | `docs/features/59-executable-continuations/planning-obligations.md` O1/O3/O8 validator cells read side by side with `docs/features/59-executable-continuations/ACCEPTANCE.md` AC-01/AC-02/AC-06 validator cells this turn; the full pin/grep sets remain carried by the frozen manifest (never moved without amendment), the phase done-whens in PLAN.md P1/P2/P3, and testing.md's ladder rows | open | — | — |
