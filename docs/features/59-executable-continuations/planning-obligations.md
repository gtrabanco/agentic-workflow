# Planning obligations — 59-executable-continuations

One row per normative behaviour, applicable compatibility invariant, affected use
case, and required failure state. Row shape and column order per
`pre-execution-review/references/LEDGERS.md` §2. Status vocabulary:
`planned | in-progress | verified | n/a | deferred` — every row starts `planned`;
no row is `deferred` (no obligation is exported to a follow-up issue).

| obligation-id | Authority source | Affected use case or invariant | Phase | Task | Implementation owner | Validator | Required evidence | Status |
|---|---|---|---|---|---|---|---|---|
| O1 | AC-01 | `next.continuation` is an optional, schema-validated Envelope v2 member — backward compatible (envelopes without it stay valid), invalid shapes fail closed with typed errors, canonical vectors published | P1 | Extend `EnvelopeNext` + JSON Schema with the optional object; validator + vectors in the package suite | execute-phase | `cd packages/agentic-workflow-schema && bun run test` → exit 0 with the continuation, backward-compat, and fail-closed pins green | schema-suite output recorded in the phase handoff (`progress.md`) | planned |
| O2 | AC-07 | Additive-minor release evidence: version is the next minor (4.1.2 → 4.2.0, no major), `CHANGELOG.md` carries the row, the package policy docs state the additive guarantee | P1 | Bump `packages/agentic-workflow-schema/package.json`, add the CHANGELOG row, state the guarantee | execute-phase | `node -p "require('./packages/agentic-workflow-schema/package.json').version"` prints 4.2.0; read-verified: CHANGELOG row + policy sentence present | version output + CHANGELOG row quoted in the phase handoff | planned |
| O3 | AC-02 | Emission/refusal contract: a non-terminal fixture unit yields `next.continuation` with well-formed argv; an uncheckable precondition or a rendering failure yields NO continuation field plus a closed-vocabulary refusal; offline forge state yields the refusal, never a guessed command | P2 | Wire the emitter into `resolveNext()` output; implement the four fail-closed refusal paths; extend the sensor suite | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0 with the emission/refusal pins green | suite names of the new pins recorded in the phase handoff | planned |
| O4 | AC-04 (schema side) | Evidence-token digest equality is testable in the package suite: the token's hash equals the referenced artifact digest; a mismatched digest fails verification; nothing is persisted | P1 | Add the evidence-token digest cases to the package suite next to the vectors | execute-phase | schema suite green including the digest-equality and mismatch-fails cases | case names recorded in the phase handoff | planned |
| O5 | AC-04 (receiver side) | The sensor binds the evidence token via the schema package's sha256 helpers; the receiver re-derives the digest and treats a mismatch as verification failure | P2 | Bind the token in the sensor emission path using the shipped helpers | execute-phase | discipline/emission suites green including the receiver-check case | case name recorded in the phase handoff | planned |
| O6 | AC-03 | Per-class discipline proof: for each of the 3 classes (status refresh · planning-gate re-run · review-receipt refresh) a fixture repo shows the emitted command parses, preconditions are checkable at emit time, and executing it exactly as emitted advances the named `convergence` field | P2 | Create `scripts/continuation-discipline.test.mjs` with one fixture repo per class | execute-phase | `node --test scripts/continuation-discipline.test.mjs` → exit 0 (3 classes × parse/precondition/convergence) | per-class case names recorded in the phase handoff | planned |
| O7 | AC-05 | Rendering is derivable from argv per platform family; argv is never altered by rendering; a forced divergence fails the suite | P2 | Pin the rendering-derivation cases in the discipline suite | execute-phase | discipline suite green including the divergence-fails case | case name recorded in the phase handoff | planned |
| O8 | AC-06 | Normative surfaces: the refusal-vocabulary row (grammar `schema-export:CONTINUATION_REFUSALS`, machine `continuation-refusal-type`, must-name `yes`) is in `CLAUDE.md`'s `normative-surfaces@1`, `hand-off-fields@1` gains `next \| continuation`, and the drift gate resolves both | P3 | Add both rows + the `schema-export:` grammar extractor in the same phase | execute-phase | `node --test scripts/normative-drift.test.mjs` → exit 0 | drift suite green at head; rows quoted in the phase handoff | planned |
| O9 | AC-10 | Quote-not-author adoption at the pinned surfaces: workflow-status skill text, the PRE_EXECUTION stale re-run route, the three receipt-persistence surfaces, and exactly one FEATURE_WORKFLOW pointer; prose `→ Next:` blocks remain for humans | P3 | State the quote rule at each pinned surface | execute-phase | read-verified: per-file quote sentences quoted in the phase handoff; `grep -c 'next.continuation' docs/workflow/FEATURE_WORKFLOW.md` → 1 | per-file quote sentences quoted in the phase handoff | planned |
| O10 | AC-08 | Bounded form protocol in `INTERVIEW.md` §3: one form-turn over the ≤ 6 fixed slots with recommended defaults, ≤ 2 ambiguity follow-up turns; vagueness rubric, mandatory-question rule, ask-nothing rule restated unchanged; the one-question-per-turn rule is gone (no second live protocol) | P4 | Rewrite §3; delete the superseded rule | execute-phase | read-verified: §3 carries the form protocol, the rubric/mandatory/ask-nothing rules verbatim, and `grep -c 'One question per turn' skills/design-feature/references/INTERVIEW.md` → 0 | §3 text quoted in the phase handoff | planned |
| O11 | AC-09 | `design-feature/SKILL.md` step 3 and the progressive-loading table describe the form protocol (batch turn + ≤ 2 follow-ups); the upsert mode and review mode are untouched | P4 | Update SKILL.md step 3 + progressive loading | execute-phase | read-verified: both sections describe the batched protocol; upsert/review mode text unchanged | diff excerpt recorded in the phase handoff | planned |
| O12 | AC-11 | Golden-fixture expectations updated: the fixed pass criteria gain the form-turn shape boxes; the run-log table unchanged | P4 | Add the boxes to `docs/workflow/GOLDEN_FIXTURE.md` | execute-phase | read-verified: form-turn boxes present in the fixed pass criteria; run-log table untouched | diff excerpt recorded in the phase handoff | planned |
| O13 | AC-12 (budgets) | Context budgets re-based for every touched skill via the tool's own declared re-basis, growth source named | P4 | Re-measure and re-base the manifest entries at end of P4 (all skill text final) | execute-phase | `node scripts/check-skill-context.mjs` → exit 0 with the touched entries re-based | checker output + `policy.declared` entry recorded in the phase handoff | planned |
| O14 | AC-12 (mirror) | Pi mirror re-bundled byte-identically in the same PR | P5 | Re-bundle via the package's own `bundle:skills`, run the parity suite | execute-phase | `cd packages/pi-agentic-workflow && npm run bundle:skills && npm test` → exit 0 (parity green) | parity suite output recorded in the phase handoff | planned |
| O15 | Sensor read-only invariant (SPEC E3; feature 38 discipline) | The sensor stays read-only — emission adds fields, never mutations; the existing read-only greps keep passing | P2 | Emission path only reads and emits; no write/forge-mutation call introduced | execute-phase | feature 38's read-only greps still return nothing; emission suites green | grep result recorded in the phase handoff | planned |
| O16 | Refusal-vocabulary closure (SPEC D-59-5, AC6) | The refusal vocabulary stays the closed ≤ 4-code set declared by the schema package's exported const; no code added outside a SPEC change | P1 | Export `CONTINUATION_REFUSALS` with exactly the four frozen codes | execute-phase | `node -p "require('./packages/agentic-workflow-schema/dist/index.js').CONTINUATION_REFUSALS.length"` prints 4 (after build); drift gate names the vocabulary | const dump recorded in the phase handoff | planned |
| O17 | Refusal is terminal at the driver layer (SPEC D-59-4, C5) | When a refusal is emitted the driver stops; skills keep prose when nothing is emitted; no continuation field accompanies a refusal | P2 | Refusal paths emit no `continuation` field; sensor suite pins the absence | execute-phase | emission suite: refusal fixtures show no `continuation` key and a closed-vocabulary code | case names recorded in the phase handoff | planned |
| O18 | SPEC dev scenario `continuation:empty-state` (AC-02 suite) | Terminal/empty roadmap state — nothing to emit: a fixture repo with no startable unit yields an envelope with no `continuation` field, exit 0 | P2 | Pin the empty-state fixture in the sensor emission suite | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0 including the empty-state pin (no startable unit → no `continuation` key, exit 0) | case name recorded in the phase handoff | planned |
| O19 | SPEC dev scenario `continuation:concurrent-emit` (D-59-9, AC-02 suite) | Two consecutive sensor runs on the same tree — stateless projection: byte-identical envelopes, no continuation store exists | P2 | Pin the concurrent-emit idempotence cases in the sensor emission suite | execute-phase | `node --test scripts/workflow-status-sensor.test.mjs` → exit 0 including the idempotence pin (two consecutive runs → byte-identical envelopes; no continuation store exists) | case name recorded in the phase handoff | planned |
