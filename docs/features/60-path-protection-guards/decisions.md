# decisions — 60-path-protection-guards

> Product-stage decision log and evidence rows. Written by `design-feature`
> (2026-09-17). The SPEC carries the decisions in summary; this file carries
> the full rows with their evidence.

## Decision log

| ID | Decision | Alternatives considered | Rationale |
|---|---|---|---|
| D-60-1 | Feature identity: roadmap row 60, slug `path-protection-guards`, tracks issue #220; PR will close the issue | A different number/slug (rejected by user answer: accepted as proposed) | User-confirmed 2026-09-17 interview (identity slot). Next free roadmap slot after row 59. |
| D-60-2 | Size `M` — full artifact set | `S` SPEC-only | User-selected 2026-09-17. Surfaces span the crate gate, pi extension, shipped policy, and wording across skills + template — phased work, not a single-commit slice. |
| D-60-3 | Tests freeze = plan-declared: the plan declares which tests are needed, each test justified (done / not done / ignored); test-authoring phases first; freeze point = where the declared test set is complete; from the freeze point the policy tightens | ACCEPTANCE.md flag (rejected: ties freeze to the delivery contract instead of the plan the gate parses); automatic-from-last-phase (rejected: inferred, not declared) | User decision 2026-09-17 (freeze slot, custom): "Plan declares which tests deserve to be done, all of them are done in first phases; when finish all of them are freeze and you begin to make all the code to satisfy tests and accomplish the feature goal. Bug on test can be fixed but tests are the expected behaviour & feature and can not be modified to finish before the feature unless justification. Every test need a justification to be done or not be done or ignored." Deterministic: the plan grammar is already parsed by `scripts/phase-lint.mjs` (feature 37). |
| D-60-4 | Docs-only work justifies no tests; every test decision is evaluated and justified | Tests always required (rejected: docs-only units need none) | User decision 2026-09-17 (globs slot): "if no tests are needed just don't create them. Don't make too much tests just necessary tests, evaluate and justify every decision." |
| D-60-5 | Policy location: doc-config canonical in the target repo beside the feature 20 hooks, shipped via `template/`; pi settings may only tighten | pi settings only (rejected: non-pi projects lose config); pi may diverge freely (rejected: silent loosening) | User-selected 2026-09-17 ("both"). Keeps Tier 1 portable; the tighten-only override cannot weaken the owner's policy. |
| D-60-6 | Absent/malformed config → shipped defaults + reported fallback | Fail closed (block all protected classes; rejected: contradicts the zero-config promise the defaults exist to serve); fail open silently (rejected: the guard would vanish without a trace) | User decision 2026-09-17 (config-failure slot: "provide default values") + issue #220's never-silent rule. |
| D-60-7 | Default protected set: `tests/**`, `e2e/**`, `*.test.*`, `fixtures/**` + the policy config itself; creation ≠ modification | Tighter (new test files also need a marker; rejected: conflicts with the TDD authoring phase the plan declares) | Issue #220 default globs confirmed 2026-09-17. The policy config itself is protected at all times — a guard that can edit its own policy is no guard. |
| D-60-8 | Owner approval recorded as append-only rows in the unit's review-findings ledger (`finding-mark` schema), gate-verified | decisions.md row (rejected: not machine-checkable against a diff); commit trailer (rejected: weaker audit surface, forge-native but not gate-verifiable pre-merge) | User-selected 2026-09-17 ("ledger"). Reuses the feature 17 ledger convention; append-only evidence survives the unit. |
| D-60-9 | Non-goals accepted as stated in issue #220; no lockfile class, no separate delete class, no forge-side enforcement | Adding lockfile/deps protection (declined: out of this unit's scope) | User-confirmed 2026-09-17 (nongoals slot). |
| D-60-10 | Success criteria = the issue's tests-first list, frozen as AC1–AC6 discipline tests + extension unit test | Free-prose criteria (rejected: not machine-checkable) | User-confirmed 2026-09-17 (success slot). |
| D-60-11 | Architectural invariants: `n/a` — no project invariants doc exists (NRS F010); workflow-level invariants unaffected | Treat `docs/workflow/WORKFLOW_INVARIANTS.md` as the invariant source (rejected: it is workflow-level, not the declared architecture-invariants document) | Standard classification record; NRS F010 frozen fact. |
| D-60-12 | Derived subsystem inventory recorded in the SPEC (Integration closure); offer to seed `docs/CAPABILITIES.md` from the template | Skip closure (rejected: the gate never opens); seed the file in this turn (rejected: substrate changes are `init-workspace`'s surface and need user confirmation) | `docs/CAPABILITIES.md` is unseeded (template placeholders at HEAD); the template's closure contract requires the derived inventory to be walked and recorded. Seeding offer rides this file. |
| D-60-13 | No `NEEDS_INPUT` escalation: all 10 interview slots resolved in the first form turn; no deferred product decisions beyond the two engineering-owned rows | Additional follow-up turns (not needed) | Feature 59's batched form protocol; 10/10 answered 2026-09-17. |
| D-60-14 | Repair batch SPEC60-F1…F3 applied 2026-09-18 in one batch: F1 supersedes D-60-8's surface choice and moves the owner-approval record off the review-findings `finding-mark@1` row (single writer `review-change`, `route`/`folded` fixed to `n/a`, no approval column) onto the unit's `decisions.md` using its already-declared `human-owner:ratified-verdicts` (owner approval) and `execute-phase:phase-decisions` (agent justification) column sets (`ledger-ownership@1` `decisions` row); F2 normalizes the product-stage evidence rows to the closed `authority-kind`/`freshness` vocabularies; F3 freezes AC10 for the tighten-only pi override | Split the batch (rejected: one review's findings are one batch); keep `finding-mark@1` and extend the ledger-ownership map with an approval column set (rejected: a workflow-wide ledger-contract change is not a Product-half repair and is not this unit's scope) | All three findings are `class: product` from spec-review-fail SPEC-REVIEW-60-1 (2026-09-18). F1 is a user-directed product change (instruction 2026-09-18: "move D5's approval record off finding-mark@1 onto a ledger-sanctioned column set/writer"), recorded as the `user` authority row PE-13; F2 is mechanical intent-preserving (vocabulary only); F3 is closure completion (the expectation was already in scope, only its criterion was missing). No scope, role, authority, or user-outcome change beyond the corrected approval surface. Per-finding resolution evidence recorded in `planning-findings.md`. |

## Evidence rows (product stage)

| id | claim | authority-kind | source-and-location | observed-revision | freshness | status | owner-or-next-evidence |
|---|---|---|---|---|---|---|---|
| PE-1 | The repo mandates "never change a test to pass it" and "do tests before implementation" — the prose rule this feature determinizes | document | `CLAUDE.md` working rules; `AGENTS.md` | HEAD `4b3a56b7` | current | proven | — |
| PE-2 | TDD practice: tests document intended behaviour before implementation; deterministic test-immutability enforcement in agent rules is an established pattern | document | qaskills.sh "TDD with AI Agents — Best Practices for 2026"; heyclau.de "Test-Driven Development Enforcer" (both fetched 2026-09-17) | accessed 2026-09-17 | current | proven | — |
| PE-3 | Owner-approval-on-protected-paths precedent: CODEOWNERS + branch protection make owners approve changes to designated paths | document | GitHub Docs "About code owners"; GitLab Docs "Code Owners" (both fetched 2026-09-17) | accessed 2026-09-17 | current | proven | — |
| PE-4 | pi's `tool_call` extension event supports blocking with `{ block: true, reason }` — the Tier 2 prevention surface | document | pi-coding-agent `docs/extensions.md` tool_call section | docs at install time | current | proven | — |
| PE-5 | Feature 20 shipped the runtime-tripwire family and the `.agentic-workflow/hooks` convention with platform adapters — the precedent and packaging home this feature extends | repository | feature 20 SPEC; `template/.agentic-workflow/hooks/guard-command.sh`; hooks README | HEAD `4b3a56b7` | current | proven | — |
| PE-6 | The phase fingerprint and plan-grammar parser exist (`scripts/phase-lint.mjs`); feature 37 is `done` (PR #212) — the hard dependency is satisfied | repository | `scripts/phase-lint.mjs`; `docs/features/ROADMAP.md` row 37 | HEAD `4b3a56b7` | current | proven | NRS snapshot (2026-08-30) predates row 37; inspected directly (allowed for absent facts) |
| PE-7 | The ledger-ownership map sanctions the unit's `decisions.md` for owner verdicts (`human-owner:ratified-verdicts`) and agent justification rows (`execute-phase:phase-decisions`); the `review-findings` `finding-mark@1` row has the single writer `review-change`, fixes `route`/`folded` to `n/a`, and carries no owner-approval column | repository | `skills/pre-execution-review/references/LEDGERS.md` § "Durable ledger write ownership" (`ledger-ownership@1` `decisions` row) and § "The durable finding mark" | HEAD `4b3a56b7` | current | proven | — |
| PE-8 | Producer vehicle rule: producers land as subcommands of `packages/agentic-workflow` (crate exists since row 37) | repository | `packages/agentic-workflow/package.json`; `docs/features/ROADMAP.md` rows 37/43 | HEAD `4b3a56b7` | current | proven | — |
| PE-9 | Issue #220 requirements: shipped deterministic defaults, Tier 1/Tier 2 split, escape hatch, non-goals, affected surfaces, depends-on 37 | forge | https://github.com/gtrabanco/agentic-workflow/issues/220 (fetched 2026-09-17) | issue body @ 2026-09 | current | proven | — |
| PE-10 | Freeze definition, per-test justification, docs-only n/a, config fallback to defaults, ledger approval — all user-decided | user | `decisions.md` D-60-3…D-60-11 (user decisions, 2026-09-17) | turn of 2026-09-17 | not-applicable | decision | — |
| PE-11 | No project invariants doc → `n/a` classification | repository | absence of `docs/architecture/ARCHITECTURAL_INVARIANTS.md`; NRS F010 | NRS 2026-08-30 | current | proven | — |
| PE-12 | `docs/CAPABILITIES.md` unseeded (template placeholders) → derived inventory recorded in the SPEC | repository | `docs/CAPABILITIES.md` at HEAD `4b3a56b7` | HEAD `4b3a56b7` | current | proven | Seed the file from the template (user confirms; `init-workspace` substrate) |
| PE-13 | The 2026-09-18 user instruction fixes the owner-approval record surface: the unit's `decisions.md` under a `ledger-ownership@1`-sanctioned column set, not a `finding-mark@1` row (SPEC60-F1) | user | `decisions.md` D-60-14 (2026-09-18) | turn of 2026-09-18 | not-applicable | decision | — |
| PE-14 | The tighten-only pi override is the owner's decision: a pi settings override may narrow but never widen the shipped policy (AC10) | user | `decisions.md` D-60-5 (2026-09-17) | turn of 2026-09-17 | not-applicable | decision | — |

## Engineering decision log (plan stage)

| ID | Decision | Alternatives considered | Rationale |
|---|---|---|---|
| E-60-1 | Canonical shipped defaults live in the crate module `packages/agentic-workflow/src/path-policy.mjs`; `template/.agentic-workflow/path-policy.json` and the pi package's `src/config/path-policy.ts` are copies, each pinned by a parity check | A single shared runtime file read by both packages (rejected: the two packages are independent bun islands with no cross-dependency, and the template is not shipped in either) | The pi package cannot import the private crate; the repo's existing shipped-copy pattern (the pi `skills/` mirror pinned by `skill-parity.test.mjs`) is the precedent. Three surfaces, two pins — drift is a test failure, never a silent divergence. |
| E-60-2 | The freeze point is the plan-declared `freeze-after` phase in the `path-protection-plan@1` block; pre-freeze and post-freeze are computed from the phase ordinal the checkpoint passes | Infer it from commit timestamps (rejected: nondeterministic); derive it from the ACCEPTANCE manifest (rejected: D-60-3 ties the freeze to the plan, not the delivery contract) | D-60-3's user decision: "the freeze point is where the declared test set is complete". A declared phase ordinal is deterministic and already parsed from the plan. |
| E-60-3 | Justification and approval rows are append-only rows of the unit's `decisions.md` under its sanctioned `execute-phase:phase-decisions` (agent) and `human-owner:ratified-verdicts` (owner) column sets; the gate validates `authority`, so an approval can only come from a human-owner row | Extend the `review-findings` ledger with an approval column set (rejected: D-60-14 recorded that a workflow-wide ledger-contract change is out of this unit's scope) | D5 as repaired (SPEC60-F1). The gate is a read-only consumer; no ledger map changes and no writer is added. |
| E-60-4 | An absent policy yields `missing-config`; an unreadable, invalid, or over-256-KiB policy yields `malformed-config`; both fall back to the shipped defaults and print the degradation line, and a clean diff still exits 0 | Fail closed on a malformed policy (rejected: D-60-6's user decision is "provide default values"); fail open silently (rejected: the guard would vanish unannounced) | D-60-6 + In scope 6. The bound keeps a pathological file from hanging the checkpoint. |
| E-60-5 | Tier 2 blocks a `write` / `edit` call to an **existing** protected path without a matching justification record; new-file creates and read-only calls pass; post-freeze approval stays the Tier 1 checkpoint duty | Require an approval at Tier 2 too (rejected: the extension has no phase state, so it would over-block pre-freeze authoring); block every protected write including creates (rejected: breaks the TDD authoring phase, expectation 2) | The pi extension sees one tool call, not a phase. Its floor is "never weaker than the shipped requirement for modification"; the phase-sensitive approval is enforced where the phase is known. |
| E-60-6 | `guard-command.sh` keeps its command-only policy; the shell hook family is not extended with a path check | Add a path check to `guard-command.sh` (rejected: the normalized payload carries only `(command, path)` — no write intent — so the check would block reads of protected files and break expectation 8) | PE-016. Product half out-of-scope 3 keeps prevention pi-only; other hosts get the Tier 1 checkpoint gate. |
| E-60-7 | The pi `pathProtection` override is tighten-only: globs are unioned and each requirement takes the maximum, and a removal or a lowering is ignored and reported | A replace-style override (rejected: AC10 requires a removal attempt to be rejected, not applied) | AC10. Union + maximum makes loosening structurally impossible while the degradation report keeps the ignored attempt visible. |
| E-60-8 | The path-protection checkpoint has no `--force` bypass; the escape hatch is the recorded justification/approval | Reuse the ordering-stop `--force` escape (rejected: a deterministic safety gate that a flag can bypass is the prose rule it replaced) | The pre-execution review gate's no-force precedent. `--force` overrides ordering, never a verdict only a record can produce. |
| E-60-9 | Five phases, one layer each: P1 crate gate (config/infra), P2 template ship (docs), P3 checkpoint contract (docs), P4 pi guard (config/infra), P5 hardening. The Product sketch's two config/infra and two docs concerns are separated so no phase spans the `layerForTarget()` table | One combined config/infra phase (rejected: box 2 blocks a phase whose tasks target two layers); more than five phases (rejected: the Product half's M split rule fires only above about five) | `scripts/phase-lint.mjs:232-237` maps `template/` and `skills/` to `docs` and `packages/` and `scripts/` to `config/infra`, so the template seed cannot share a phase with the crate tests. Product bytes untouched. |
| E-60-10 | Every crate-suite validator uses the runnable form `bun test packages/agentic-workflow/test/` with the Node 24 fallback `node --test packages/agentic-workflow/test/*.test.mjs`; the manifest's AC-01/03/05/06 validators freeze that form, and AC1's inline `node --test packages/agentic-workflow` shorthand is materialized by them (Product bytes untouched) | Keep the directory positional `node --test packages/agentic-workflow` (rejected: not a Node ≥ 22 `--test` interface — it resolves the directory as a module and exits `MODULE_NOT_FOUND`); change the Product criterion (rejected: a Product amendment is not a plan repair) | PLAN60-F1; PE-023. The assertion is unchanged, only the invocation form; feature 52's `PLAN52-F9` is the precedent. |
| E-60-11 | `PATH_GUARD_REASONS` is published as a normative surface by a `schema-export:PATH_GUARD_REASONS` row (file `packages/agentic-workflow/src/path-policy.mjs`, machine `path-protection-reason`, must-name `no`); the `block:path-protection@1` row declares the block grammar against the same vocabulary, also must-name `no` | `must-name: yes` with the block grammar alone (rejected: the drift gate's `unpublished-vocabulary` check refuses a machine no surface exports, and its must-name closed set is the fixed four — a fifth set has no text-ordering source); drop the machine cell entirely (rejected: publishing the vocabulary keeps the closure machine-checked) | PLAN60-F2; PE-024. The crate module exports the frozen literal array the `schema-export:` reader requires; the reason closure stays enforced by O12. |
| E-60-12 | The Tier 1 checkpoint observes the phase diff: each phase entry records its base ref in the phase handoff, and the preflight runs the gate over the just-closed committed range (`path-guard --phase P<n-1> --base <P<n-1> base ref>`), with P1 using the planning-artifacts range and the final phase's range checked by the close-out gate | Keep the pre-edit working-tree-only invocation (rejected: a fresh pre-edit tree is clean, so the gate always returns `pass — clean` and never sees a phase's protected changes) | PLAN60-F3; PE-004. The preflight stays the checkpoint's home, satisfying AC8; a committed-range fixture pin is added to P1. |
| E-60-13 | Tier 2's record source: the pi guard resolves the repo root from `ctx.cwd`, reads the `path-protection-records@1` block of every `docs/features/*/decisions.md` and `docs/fix/*/decisions.md`, and permits the write when a `justification` row (authority `execute-phase`) matches the target; approvals are never consulted | Give the extension unit/phase state (rejected: pi exposes one tool call, no phase); consult approvals (rejected: over-blocks pre-freeze authoring) | PLAN60-F6; PE-025. P4's suite adds the matching-justification-permits-write positive case beside the block case. |
| E-60-14 | The template-seed parity is a P2 done-when byte-diff command against the crate's `serializeShippedPolicy()` output; it is not a committed test file (P2 is a docs phase, so a `scripts/` test file would break the one-layer rule), and the plan declaration's `not-created` row names that command | Rely on AC-09's existence+grep validator (rejected: it proves existence, never parity — the F5 defect); add a `scripts/` parity file (rejected: box 2 layer violation) | PLAN60-F5; PE-026. The pi-mirror pin stays a committed P4 suite case (O18). |
| E-60-15 | P5 runs the close-out path gate over its own committed range (`path-guard --unit docs/features/60-path-protection-guards --phase P5 --base <P5 base ref>`), records the P5 base ref at phase entry, and adds the invocation to the P5 done-when; a fail stops the close-out before the PR and there is no `--force` bypass | Rely on the P<n-1> checkpoint only (rejected: no phase follows P5, so its committed range would never be observed — the PLAN60-F3 repair named the close-out gate but no task ran it); give the close-out a `--force` escape (rejected: D-60-8/E-60-8 — the escape hatch is a recorded justification/approval) | PLAN60-F9; PE-028. Completes the SPEC Checkpoint wiring's final-phase sentence; O19 carries the obligation. |
| E-60-16 | The same-PR release mechanic lands in P5: the six touched skills take a `bump-skill` **minor** bump (each with its CHANGELOG row), and the pi package takes a **minor** bump to `0.11.0` with its Companion npm packages CHANGELOG row, before the Pi mirror re-bundle | Defer the bumps to a follow-up PR (rejected: `CLAUDE.md` requires version bumps same-PR, and CI publishes the pi package only when its version differs — a silent non-publish); leave the private `packages/agentic-workflow` crate unbumped (accepted: `private: true`, version `0.0.0`, nothing published, and the version-tables check covers only the pi and schema packages) | PLAN60-F10; PE-027. Minor, not patch: the touched skills gain new contract text (the checkpoint step, the rejection type, the grammar block, the declaration instruction, the pointer) and the pi package gains the `pathProtection` capability. Majors stay frozen until #176 merges. O20/O21 carry the obligations. |
| E-60-17 | The canonical shipped-default `test-file` class protects `**/*.test.*` alone; the Engineering-invented `**/*.spec.*` entry is dropped so `SHIPPED_PATH_POLICY` (E-60-1) projects D4 / AC-01 / issue #220's four default glob families exactly | Record a decision authorizing the spec variant (rejected: the operator decided no such family, and PLAN60-F11's whole point is that no Product row or E-60 row supports it); keep the glob and widen the Product half (rejected: a governing-SPEC amendment for a family nobody asked for) | PLAN60-F11; PE-029. The Product projection is byte-identical, so the change is Engineering-half only; the template seed and the pi mirror inherit the correction through their parity pins (O15/O18), and no acceptance criterion or obligation changes because AC-01 and D4 already enumerate the four families without a spec variant. `**/*.spec.*` was the tree's only occurrence (`grep -rn '\.spec\.' docs/features/60-path-protection-guards/` matched `SPEC.md` alone). |
| E-60-18 | PLAN60-F12 — the `undeclared-test` failure reason has no obligation row, no dev scenario, and no P1 validator — is **recorded as an advisory** under the operator's materiality bar, not repaired in this batch: the ledger row stays `open` with its fold path named, and it does not restart a plan cycle | Add the `undeclared-test` obligation plus a P1 case now (the reviewer's suggested repair — deferred by the operator, not rejected; the fold path carries it); dismiss the row (rejected: no counter-evidence falsifies it); add a P1 task (rejected by the same operator instruction) | Operator instruction 2026-09-18 for this batch (`drop **/*.spec.*; F12 recorded advisory`); the open-advisory precedent under the same bar is feature 38's rp-008/rp-011/rp-012 and F42/F43 (`decisions.md` 2026-09-09/2026-09-11). `undeclared-test` stays a declared Product behaviour (`SPEC.md:515`, `:547`) closed in the reason vocabulary (O12); the residual verification gap is recorded in `planning-findings.md` with its fold path (the next plan-side write, or ridden by the P1 phase's progress receipt), severity `low` and non-cycle-restarting. |
| E-60-19 | The evaluator scopes escape records to the evaluated phase: only rows whose `phase` column equals the checkpoint's `--phase` participate in justification/approval satisfaction **and** in the `unmatched-record` check | Evaluate every record row regardless of its `phase` (rejected: a checkpoint checks one phase's committed range, so a prior phase's still-valid record would be `unmatched-record` at every later checkpoint and re-fail a finished phase) | `PLAN-REVIEW-60-4` non-blocking observation (b): the record grammar's `phase` column implies this scoping but the SPEC prose did not state it. The parser still validates every row's `phase`/`date`/`authority`, so a malformed prior-phase row is still `malformed-declaration`; only the matching and the void-record check are phase-scoped. |
