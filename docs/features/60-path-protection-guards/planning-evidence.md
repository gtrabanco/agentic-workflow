# Planning evidence — 60-path-protection-guards

Compact source-backed conclusions used to cut this Engineering plan. This is not
an exploration transcript. Row shape and column order per
`evidence-grounding/references/ROWS.md` (§ Plan-stage table). All rows observed at
HEAD `44732c8b` (branch `feat/220-path-protection-guards`) unless a row says
otherwise; PE-023…PE-026 were added by the PLAN60-F1…F8 repair, PE-027/PE-028 by
the PLAN60-F9…F10 repair, and PE-029 by the PLAN60-F11…F12 repair.

Planning baseline: HEAD `44732c8b` + live forge state verified 2026-09-18
(governing issue #220 OPEN; dependency feature 37 `done` via PR #212; receipt
SPEC-REVIEW-60-2).

| id | claim-or-obligation | authority-kind | source-and-location | observed-revision | affected-decision-or-obligation | freshness | status | owner-or-next-evidence |
|---|---|---|---|---|---|---|---|---|
| PE-001 | The producer crate exists at `packages/agentic-workflow` (private, version 0.0.0, no `bin`/`scripts` field) and already carries a subcommand precedent, `bin/turn-contract.mjs`; the Tier 1 gate lands as a sibling node-only CLI | repository | `packages/agentic-workflow/package.json:1-12`; `packages/agentic-workflow/bin/turn-contract.mjs:1-12` | 44732c8b | O1, O2; AC1, AC2 | current | proven | add `bin/path-guard.mjs` beside the precedent |
| PE-002 | `scripts/phase-lint.mjs` maps `skills/`/`docs/`/`template/`/`*.md` to `docs` and `scripts/`/`packages/`/`.github/`/`.agentic-workflow/` to `config/infra`, and box 2 judges the first path-like task target against the phase's one declared layer | repository | `scripts/phase-lint.mjs:232-237` (`layerForTarget`), `:477-487` (box 2) | 44732c8b | E-60-9 (phase cut) | current | proven | the five-phase cut follows the table |
| PE-003 | Box 4 counts a task's path tokens when a creation verb is present (so a slash-joined word list reads as several created files) and box 5 rejects a standalone `or`; the PLAN task wording avoids both | repository | `scripts/phase-lint.mjs:402-405,498-517,520-533` | 44732c8b | phase-lint PASS (8/8) | current | proven | re-run the linter at every plan edit |
| PE-004 | The `execute-phase` preflight runs phase-lint at `:154` after the dependency and own-status gates; the path-protection checkpoint lands after it and before any edit | repository | `skills/execute-phase/references/PREFLIGHT.md:154-190` | 44732c8b | O10; AC8 | current | proven | wire the step in P3 |
| PE-005 | The gate-rejection vocabulary is a versioned `type \| printed-by` block at `:188`; a new `path-protection \| PREFLIGHT.md` row extends the closed set | repository | `skills/pre-execution-review/references/POLICY.md:185-199` | 44732c8b | O10; AC8 | current | proven | add the row in P3 |
| PE-006 | The `hand-off-fields@1` block sits at `:78` in the canonical turn contract; the file is the checkpoint contract's home and the drift gate reads its fenced blocks by marker | repository | `skills/orchestration-envelope/references/TURN_CONTRACT.md:47-84` | 44732c8b | O9; AC8 | current | proven | add the `path-protection@1` block in P3 |
| PE-007 | `CLAUDE.md:314` declares `normative-surfaces@1`; the drift gate refuses a row whose `grammar` cell does not resolve and supports `block:`, `fenced:`, `table:`, `schema-export:`, and frontmatter grammars | repository | `CLAUDE.md:314-336`; `scripts/normative-drift.test.mjs:374-405` | 44732c8b | O9; AC8 | current | proven | declare the new row + grammar in the same phase |
| PE-008 | The drift gate's machine→text check iterates the `must-name: yes` surfaces, and `gate-rejection-type` is one of them; adding `path-protection` to that block keeps the closed set complete | repository | `scripts/normative-drift.test.mjs:630,894-900` | 44732c8b | O9, O10; AC8 | current | proven | extend both surfaces together |
| PE-009 | `packages/pi-agentic-workflow/src/extension/index.ts:163-165` registers `model_select` / `thinking_level_select` / `agent_settled`; the `tool_call` registration is additive beside them | repository | `packages/pi-agentic-workflow/src/extension/index.ts:160-166` | 44732c8b | O13; AC4 | current | proven | register the guard in P4 |
| PE-010 | Pi documents `tool_call` blocking via `{ block: true, reason?, terminate? }` and ships the canonical protected-paths example; the Tier 2 guard uses exactly that contract | document | `/home/agent/.bun/install/global/node_modules/@earendil-works/pi-coding-agent/docs/extensions.md` §tool_call (`:778-800`); `examples/extensions/protected-paths.ts` | pi docs @ 2026-09-18 | O13; AC4 | current | proven | no re-fetch needed |
| PE-011 | The pi config validator is strict: `ROOT_KEYS` at `schema.ts:14` rejects an unknown root key, so adding `pathProtection` is a validator change and never a permissive one | repository | `packages/pi-agentic-workflow/src/config/schema.ts:14,138-147` | 44732c8b | O14; AC10 | current | proven | extend `ROOT_KEYS` + the shape check in P4 |
| PE-012 | `ConfigFile` / `EffectiveConfig` live at `types.ts:44,72`; the two config files are `<agentDir>/pi-agentic-workflow.json` (global) and `<cwd>/.pi/pi-agentic-workflow.json` (project), and the project file is read only when the project is trusted | repository | `packages/pi-agentic-workflow/src/config/types.ts:44-96`; `src/config/load.ts:28-40,93-99` | 44732c8b | O14; AC10 | current | proven | thread `pathProtection` through both shapes |
| PE-013 | `mergeConfigs` merges project-over-global per key; the tighten-only intersection for `pathProtection` belongs at that seam | repository | `packages/pi-agentic-workflow/src/config/merge.ts:26-49` | 44732c8b | O14; AC10 | current | proven | resolve in P4 |
| PE-014 | The repo's shipped-copy pattern is a mirror pinned by its own test (`test/skill-parity.test.mjs`); the same pattern pins the policy default mirror | repository | `packages/pi-agentic-workflow/test/skill-parity.test.mjs:1-40` | 44732c8b | O15; AC10 | current | proven | pin the mirror in the pi suite |
| PE-015 | The template guard family ships `guard-command.sh`, its adapters, and a README in one directory; the policy seed and doc page land there | repository | `template/.agentic-workflow/hooks/README.md`; `template/.agentic-workflow/hooks/guard-command.sh` | 44732c8b | O11; AC9 | current | proven | ship in P2 |
| PE-016 | The shell adapters normalize a hook payload to `(command, path)` only — no write-intent field exists — so a path check in `guard-command.sh` would block reads of protected files too | repository | `template/.agentic-workflow/hooks/adapters/normalize-hook-payload.sh:1-30` | 44732c8b | E-60-6; AC7 | current | proven | keep the guard command-only |
| PE-017 | `init-workspace` install and upgrade write the template scaffold additively and keep `.agentic-workflow/hooks/`; the policy seed follows that path | repository | `skills/init-workspace/references/BOOTSTRAP_WRITE.md:1-30`; `skills/init-workspace/references/UPGRADE.md` | 44732c8b | O11; AC9 | current | proven | seed in P2 |
| PE-018 | Context budgets are enforced by `scripts/check-skill-context.mjs` over `docs/workflow/SKILL_CONTEXT_BUDGETS.json`; the declared re-basis is `ceil(measured × 1.10)` with the growth source in `policy.declared` | repository | `docs/workflow/SKILL_CONTEXT_BUDGETS.json` (`policy` block); `scripts/check-skill-context.mjs` | 44732c8b | O12; AC7 | current | proven | re-base after all skill text is final (P3) |
| PE-019 | The unit's `decisions.md` is a durable ledger under `ledger-ownership@1` whose writers are `plan-feature-scaffold:create` and `human-owner:ratified-verdicts` (plus `execute-phase:phase-decisions`); the gate reads it and never writes | ledger + repository | `docs/features/_TEMPLATE/LEDGERS.md:18`; `skills/pre-execution-review/references/LEDGERS.md:141-145` | 44732c8b | O6; AC5 | current | proven | read-only consumer, unchanged map |
| PE-020 | `scripts/ledger-ownership.test.mjs` scans only `scripts/` and `packages/<name>/scripts/` for undeclared writers, so a read-only `bin/path-guard.mjs` and a `scripts/*.test.mjs` suite stay out of its write-scan scope | repository | `scripts/ledger-ownership.test.mjs:421-439` | 44732c8b | O16 | current | proven | no ledger map change |
| PE-021 | The Normalized Repository State is `frozen` and records that no project architectural-invariants document exists; the plan-stage invariant classification is `n/a` | ledger | `docs/workflow/REPOSITORY_STATE.md` Snapshot table (Status `frozen`) + fact F010 | 44732c8b | preflight Stage 2 | current | proven | re-check if an invariants doc appears |
| PE-022 | Roadmap row 60 is `defined` with hard dependency 37 (`done`, PR #212); no fix-now row touches the crate, the pi config, the template hooks, or the execute-phase preflight; governing issue #220 is OPEN and matches the SPEC | forge + document | `docs/features/ROADMAP.md:47,70`; `docs/fix/README.md`; `gh issue view 220` (2026-09-18) | 44732c8b | dependency & blocker check | current | proven | re-verify forge state at execution |
| PE-023 | `node --test <dir>` is not a Node ≥ 22 `--test` interface: the repository's pinned runtime (`.node-version` v22.23.1) and the observed v24.21.0 both exit 1 with `Cannot find module .../packages/agentic-workflow`, while the glob form `node --test packages/agentic-workflow/test/*.test.mjs` and `bun test packages/agentic-workflow/test/` both exit 0 (2 files, 56 tests); feature 52's `PLAN52-F9` is the identical defect fixed the same way | repository | `.node-version`; live runs at HEAD `a504b203`; `docs/features/52-machine-checked-turn-contract/progress.md:181`; `docs/features/52-machine-checked-turn-contract/planning-findings.md` (PLAN52-F9) | a504b203 | O1, O3, O6, O11, O16; AC-01, AC-03, AC-05, AC-06 | current | proven | E-60-10 fixes every validator form |
| PE-024 | A normative-surface `schema-export:NAME` row (`scripts/normative-drift.test.mjs:400-419`) is the only grammar that registers a machine vocabulary from committed source (it reads a frozen `export const NAME = [...]`); the `must-name: yes` surfaces are the fixed four (`:894-896`), and a `block:` row registers no vocabulary, so a `block:` row naming an unpublished machine is refused with `unpublished-vocabulary` (`:536-546`) | repository | `scripts/normative-drift.test.mjs:400-419,536-546,894-896`; `CLAUDE.md:334` (`schema-export:CONTINUATION_REFUSALS`) | a504b203 | O9, O10; PLAN60-F2 | current | proven | E-60-11 publishes `PATH_GUARD_REASONS` with must-name `no` |
| PE-025 | Pi's `tool_call` event exposes `event.toolName` (`write` / `edit`) and `event.input.path` (the target file), and the handler receives `ctx.cwd`; the existing extension already reads `ctx.cwd` for config resolution, so the Tier 2 guard resolves the repository root and the unit decision ledgers from it without new state | document | pi `docs/extensions.md` §tool_call (`:799-814`) and §Typing custom tool input (`:2043-2047`, `edit` input `{ path, edits }`) (fetched 2026-09-18); corroborated by `packages/pi-agentic-workflow/src/extension/index.ts:140-166` | a504b203 | O4; PLAN60-F6 | current | proven | E-60-13 fixes the record source and the positive case |
| PE-026 | `scripts/phase-lint.mjs` fingerprints each phase as `P<n>:<layer>:<task-count>:<slug>` and digests the newline-joined list, so a repair that keeps each phase's task count and declared layer leaves the plan digest unchanged and the recorded fingerprints valid | repository | `scripts/phase-lint.mjs:680-703` | a504b203 | phase-lint PASS | current | proven | re-run the linter after every plan edit |
| PE-027 | The same-PR version rule is mandatory and mechanical: `CLAUDE.md` requires a `version:` bump plus a `CHANGELOG.md` line for every changed skill, and a `package.json` version bump plus a Companion npm packages row for a touched published package; the repo's internal `bump-skill` performs the skill half; the version-tables drift check compares an existing CHANGELOG cell to the frontmatter/package version, so it cannot see an omitted bump | repository + document | `CLAUDE.md:172-178,383-390,421-428`; `skills/bump-skill/SKILL.md`; `scripts/normative-drift.test.mjs:1017-1027` | 1a12784d | O20, O21; PLAN60-F10 | current | proven | E-60-16 schedules the sweep and O20/O21 carry the validators |
| PE-028 | The SPEC's Checkpoint wiring already names the final phase's own committed range as the close-out gate's job (`path-guard --phase P<n> --base <P<n> base ref>`), but the pre-repair P5 task list ran no such invocation — the residual PLAN60-F3 gap | repository | `docs/features/60-path-protection-guards/SPEC.md:591`; pre-repair `PLAN.md` P5 and `TASKS.md` P5; `planning-findings.md` PLAN60-F9 | 1a12784d | O19; PLAN60-F9 | current | proven | E-60-15 adds the P5 task and done-when validator |
| PE-029 | The Product's default protected set is D4's four glob families (`tests/**`, `e2e/**`, `*.test.*`, `fixtures/**`) plus the policy config itself; no Product decision or E-60 row authorizes a `*.spec.*` variant, so the canonical shipped-default `test-file` class protects `**/*.test.*` alone | repository | `docs/features/60-path-protection-guards/SPEC.md:336` (D4), `:277-280` (AC-01 families); `docs/features/ROADMAP.md` row 60 + issue #220 (four default globs) | e411326b | E-60-1; PLAN60-F11 | current | proven | E-60-17 aligns `SHIPPED_PATH_POLICY` at P1; the template seed and pi mirror inherit it through O15/O18 |
