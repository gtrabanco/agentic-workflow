# Planning findings — 60-path-protection-guards

One stage-aware table per unit (ledger contract:
`skills/pre-execution-review/references/LEDGERS.md` § 3). Reviewers append rows;
only the owning skill resolves them (`design-feature` for `class: product`).
Column order is fixed:

```text
finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision
```

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| SPEC60-F1 | spec | medium | product | e48019513667407db008d313039d7a6a1800708ced1d8582ea9aa9a313b53dae | D5 and the Integration-closure row record owner approvals and justifications as `finding-mark`-schema rows in the unit `review-findings.md`, but that row's ledger contract gives it exactly one writer (`review-change`), fixes its `route` and `folded` cells to `n/a`, and carries no owner-approval field; the `ledger-ownership@1` block declares no writer or column set for owner approvals, so the chosen surface cannot host the record as specified (C10) | `docs/features/60-path-protection-guards/SPEC.md:324` (D5 "Append-only justification/approval rows in the unit's review-findings ledger (`finding-mark` schema)"), `:215` (Integration closure "Review-findings ledger (`finding-mark` schema) — owner approvals and justifications are recorded as append-only rows the gate verifies"), `:172-181` (entity "Justification & approval record"); `docs/features/60-path-protection-guards/decisions.md:35` (PE-7 "the append-only record surface approvals reuse"); `skills/pre-execution-review/references/LEDGERS.md:184` ("The single writer of every finding mark is `review-change`; nothing else appends one"), `:187-189` (row shape `VF-<n> \| <file:line> · reviewer review-change · HEAD <sha> · recheck … \| <axis> \| confirmed \| finding-mark \| n/a \| n/a`), `:141` (`ledger-ownership@1`: the review-findings owner cell names `review-change:finding-mark` and no approval/human-owner column set) | resolved | D5, the entity-closure `Justification & approval record` Create row, the Integration-closure row, and evidence row PE-7 now name the unit's `decisions` ledger (the `ledger-ownership@1` `decisions` row) as the record home — owner approval under its already-declared `human-owner:ratified-verdicts` column set, agent justification under `execute-phase:phase-decisions`; the `review-findings` `finding-mark@1` schema is no longer referenced anywhere in the Product half. Dated `decisions.md` D-60-14 records the 2026-09-18 user instruction that fixed the surface ("move D5's approval record off finding-mark@1 onto a ledger-sanctioned column set/writer") and PE-13 is its `user` authority row. Product-change class, user-directed surface correction — the intent (append-only, gate-verified owner approval) is unchanged; no scope, role, authority, or user-outcome change. | 7e9f6f543719ded87d9da03385b7ba99bd2ab93d |
| SPEC60-F2 | spec | low | product | e48019513667407db008d313039d7a6a1800708ced1d8582ea9aa9a313b53dae | The Product-stage evidence rows in `decisions.md` use `authority-kind`/`freshness` values outside the closed vocabularies: PE-1 `repository doc`, PE-2/PE-3 `web`, PE-4 `platform doc (authoritative, local)`, PE-9 `forge (open)`, PE-10 `user decision` (authority-kind) and PE-10 `freshness: decision` — the frozen evidence ledger violates its own row contract (C11) | `docs/features/60-path-protection-guards/decisions.md:29-40` (PE-1…PE-12 rows); `skills/evidence-grounding/references/ROWS.md` § Closed vocabularies: `authority-kind` closes at `repository \| document \| ledger \| forge \| user \| derived` (exactly one), and `freshness` closes at `current \| drifted \| stale \| not-applicable` (`not-applicable` is the value for a `status: decision` row that cannot drift) | resolved | Every product-stage evidence row now uses the closed vocabularies of `skills/evidence-grounding/references/ROWS.md`: PE-1 `document` (was `repository doc`), PE-2 and PE-3 `document` (was `web`), PE-4 `document` (was `platform doc (authoritative, local)`), PE-9 `forge` (was `forge (open)`), PE-10 `user` (was `user decision`) with `freshness: not-applicable` (was `current`, since a `status: decision` row cannot drift); new rows PE-13 and PE-14 are `user`/`decision`/`not-applicable`. Claims, sources, observed-revisions, and statuses unchanged. Mechanical, intent-preserving class. | 7e9f6f543719ded87d9da03385b7ba99bd2ab93d |
| SPEC60-F3 | spec | medium | product | e48019513667407db008d313039d7a6a1800708ced1d8582ea9aa9a313b53dae | Expectation-sweep row 15, D2, and the Integration-closure pi row promise a pi settings override that may only tighten and never loosen the shipped policy, but no acceptance criterion verifies it: AC3 tests absent/malformed-config fallback and AC4 tests edit/write blocking by tool type; no criterion exercises an attempted loosening (or an accepted tightening override), so the expectation's own pointer to AC3 does not resolve (C8) | `docs/features/60-path-protection-guards/SPEC.md:268` (Expectation 15 "The shipped defaults can be tightened but never silently loosened \| in-scope \| AC3 + pi settings tighten-only; In scope 1"), `:207` ("settings may only tighten the shipped defaults, never loosen"), `:321` (D2 "pi settings may only tighten"); AC3 `:281-283` (fallback only); AC4 `:284-286` (block/allow by tool type only); `grep -niE 'loosen\|override' docs/features/60-path-protection-guards/SPEC.md` matches only lines 82, 95, 166, 207, 268, 320-321 — none inside `### Acceptance criteria` (`:270-304`) | resolved | AC10 (`Tighten-only pi override`) freezes the expectation: the `packages/pi-agentic-workflow` extension unit test proves the effective policy is the shipped defaults **intersected** with any pi settings override — a tightening override (added protected glob, or a phase made stricter) is honored, a loosening override (removing a shipped protected glob, or relaxing a phase) is rejected with the degradation report while the shipped protection stays in force. Expectation row 15's pointer now reads `AC10 (tighten-only pi override) + AC3 (fallback)`, and the Spec-lint In-scope mapping box adds AC10 to items 1 and 3; PE-14 records the tighten-only owner decision. Criterion completion — the expectation was already in scope (In scope 1 / D2), only its criterion was missing; no scope change. | 7e9f6f543719ded87d9da03385b7ba99bd2ab93d |
| PLAN60-F1 | plan | high | plan | 40626cd083e620763d791c8b1408d248c610a67846b380242836a14e5c1b0ad8 | Every crate-suite validator this plan freezes uses the directory form `node --test packages/agentic-workflow`, which is not a Node ≥ 22 interface and fails at the reviewed revision with `Cannot find module '.../packages/agentic-workflow'` (MODULE_NOT_FOUND); P1's done-when and closing task, P5's ladder, and ACCEPTANCE AC-01/AC-03/AC-05/AC-06 plus its Commands list therefore cannot exit 0 as written. | `PLAN.md:49,51` (P1 task and done-when), `PLAN.md:126` (P5 ladder); `ACCEPTANCE.md:14,16,18,19,34`; reproduced this turn: `node --test packages/agentic-workflow` → exit 1 with MODULE_NOT_FOUND while `node --test packages/agentic-workflow/test/*.test.mjs` → 56 pass (exit 0); repo precedent `docs/features/52-machine-checked-turn-contract/progress.md:181` ("`node --test <dir>` is not a Node ≥ 22 interface — use `bun test <dir>` or the glob") | resolved | Every frozen crate-suite validator now uses the runnable form `bun test packages/agentic-workflow/test/` (Node 24 fallback `node --test packages/agentic-workflow/test/*.test.mjs`): `PLAN.md` P1 closing task + done-when and P5 ladder, `TASKS.md` P1, `ACCEPTANCE.md` AC-01/AC-03/AC-05/AC-06 + its Commands list, `planning-obligations.md` O1/O3/O6/O11/O16, `testing.md`, and the SPEC Engineering half's Testing requirements + P1 phase. E-60-10 records the materialization. Verified this turn: `bun test packages/agentic-workflow/test/` → 56 pass; `node --test packages/agentic-workflow/test/*.test.mjs` → 56 pass; the directory positional still exits 1 (`MODULE_NOT_FOUND`). Invocation form only — the assertions are unchanged, and the Product projection is byte-identical (24443 B, sha256 `50b33e5d…920e`), so AC1's Product-half shorthand is untouched. PE-023 records the evidence. | 513e8c171fe2fc14ea987a1f419b72c9e7d2285f |
| PLAN60-F2 | plan | high | plan | 40626cd083e620763d791c8b1408d248c610a67846b380242836a14e5c1b0ad8 | P3 adds a `path-protection-contract` normative-surface row with grammar `block:path-protection@1`, machine `path-protection-reason`, `must-name: yes`, but no task makes that vocabulary published: the drift gate refuses the row (`unpublished-vocabulary`) and its must-name closed-set assertion is hardcoded to four values, so P3's done-when `node --test scripts/normative-drift.test.mjs` → exit 0 is unreachable as written. | `PLAN.md:88` (P3 task), `PLAN.md:94` (P3 done-when); `TASKS.md` P3 task 4; `scripts/normative-drift.test.mjs:536-546` (4a `unpublished-vocabulary`), `:894-896` (asserted must-name closed set), `:405-419` (only a `schema-export:` row registers a vocabulary), `:452-490` (a `block:` row registers none); pinned this turn in a patched throwaway tree via `NORMATIVE_DRIFT_REPO`: 2 failures, `[unpublished-vocabulary] path-protection-contract: the inventory names machine vocabulary path-protection-reason, which no published surface exports` | resolved | E-60-11 publishes the vocabulary: a `schema-export:PATH_GUARD_REASONS` normative-surface row (file `packages/agentic-workflow/src/path-policy.mjs`, machine `path-protection-reason`, must-name `no`) registers it, and the `block:path-protection@1` row declares the block grammar against the same machine. `must-name: yes` is dropped: the drift gate's closed set stays the fixed four (a fifth set has no text-ordering source), while publishing keeps the closure machine-checked. P1 task 1 now freezes `PATH_GUARD_REASONS` as a literal array the `schema-export:` reader parses, and P3 task 4 names both rows. Verified this turn by simulation in a throwaway worktree (`NORMATIVE_DRIFT_REPO=<patched tree> node --test scripts/normative-drift.test.mjs`): 16 pass, 0 fail, including `text → machine` and `machine → text`. PE-024 records the mechanism. | 513e8c171fe2fc14ea987a1f419b72c9e7d2285f |
| PLAN60-F3 | plan | medium | plan | 40626cd083e620763d791c8b1408d248c610a67846b380242836a14e5c1b0ad8 | The Tier 1 checkpoint is placed "before any edit" in the execute-phase preflight, but the gate's only changed-path input is the mutable working tree (or a `--base` ref the wiring never passes) and every phase commits before the next preflight, so at the checkpoint the tree is clean and the gate returns `pass — clean` — Tier 1 never observes the phase's protected changes and no scenario or validator exercises the checkpoint catching a real violation. | `SPEC.md:572-575` (checkpoint wiring, "before any edit"), `SPEC.md:486-491` ("a checkpoint sees the dirty tree"), `SPEC.md:669` (`path-guard:empty-diff`); `PLAN.md` P3 task 2; `TASKS.md` P3 task 2; `skills/execute-phase/references/PREFLIGHT.md:154-190` (guard runs before any edit) with `skills/execute-phase/references/WORKFLOWS_FEATURE.md:6-7,18-28` (preflight pre-edit, one commit per phase); the CLI contract at `SPEC.md:554` is `--unit <unit-dir> --phase <P<n>> [--base <ref>]` but the wiring task names no argument | resolved | E-60-12 moves the checkpoint's changed-path input to the just-closed phase's recorded committed range: each phase entry records its base ref, and the preflight runs `path-guard --unit <unit-dir> --phase P<n-1> --base <P<n-1> base ref>` (P1 uses the planning-artifacts range; the final phase's range is checked by the close-out gate). The SPEC Engineering half's Changed-path derivation and Checkpoint wiring now state the `--base` requirement, P3 task 2 names the base-ref record and the argument, a new `path-guard:committed-range` dev scenario + P1 discipline-suite pin exercise the checkpoint catching a committed violation, and O8's validator names the committed-range fail case. The checkpoint stays in the preflight (AC8), and the Product projection is unchanged. | 513e8c171fe2fc14ea987a1f419b72c9e7d2285f |
| PLAN60-F4 | plan | medium | plan | 40626cd083e620763d791c8b1408d248c610a67846b380242836a14e5c1b0ad8 | Obligation O15 names two phases (`P2, P4`) and a compound task; the frozen ledger contract requires exactly one phase and one task and states that a row needing two phases means the phase cut is wrong. | `planning-obligations.md:25` (O15 phase cell "P2, P4"); `skills/pre-execution-review/references/LEDGERS.md` §2 row `phase` / `task` — "exactly one phase and one task; a row needing two phases means the phase cut is wrong"; `PLAN.md` P2 and P4 split the two pins | resolved | O15 is now one phase and one task (P2, template ship); the pi-mirror parity is a distinct O18 (P4, config resolution + package suite). Every obligation row carries exactly one phase. Split verified against `LEDGERS.md` §2's `phase`/`task` contract. | 513e8c171fe2fc14ea987a1f419b72c9e7d2285f |
| PLAN60-F5 | plan | medium | plan | 40626cd083e620763d791c8b1408d248c610a67846b380242836a14e5c1b0ad8 | The "single source + two pins" promise (E-60-1: template seed and pi mirror each parity-pinned to the crate default) has only one enforcement — the pi suite parity case; the template-seed pin has no failing validator: AC-09 checks only file existence plus a README grep, P2's done-when is a state not a command, O15's validator names an undefined "template mirror diff", and the plan's own declaration block asserts the template mirror is checked by "the AC-09 command validator (a diff)", which AC-09 is not. | `PLAN.md:29` (declaration row `not-created \| template seed parity test file \| the template mirror is checked by the AC-09 command validator (a diff)`), `PLAN.md:69,71` (P2 check and done-when); `ACCEPTANCE.md:22` (AC-09 = `test -f … && test -f … && grep -q …`); `planning-obligations.md:25` (O15 validator); `decisions.md` E-60-1; `SPEC.md:593-597` ("three surfaces, two pins") | resolved | E-60-14 gives the template-seed pin its own failing validator: P2's done-when is now the byte-diff command `diff <(node -e "import('./packages/agentic-workflow/src/path-policy.mjs').then(m=>process.stdout.write(m.serializeShippedPolicy()))") template/.agentic-workflow/path-policy.json` → empty (plus the seeding greps), O15's validator names it, `testing.md` names it, and the plan declaration's `not-created \| template seed parity test file` justification now names that command instead of attributing a diff check to AC-09. P1 task 1 exports `serializeShippedPolicy()`. AC-09 keeps the Product existence+grep validator; the parity is the Engineering pin. | 513e8c171fe2fc14ea987a1f419b72c9e7d2285f |
| PLAN60-F6 | plan | medium | plan | 40626cd083e620763d791c8b1408d248c610a67846b380242836a14e5c1b0ad8 | The Tier 2 guard's block condition "carries no matching justification record" has no specified record source and no positive scenario: the pi extension has no phase or unit state, the records live in the unit's `decisions.md`, and neither AC-04 nor P4's test list includes a case where a matching record permits the write, so the escape branch is neither definable nor verifiable. | `SPEC.md:580-587` (Tier 2 design, "existing" protected path, "no matching `justification` record", "the extension has no phase state"), `SPEC.md:184-187` and `SPEC.md:516-518` (records home = unit `decisions.md`); `ACCEPTANCE.md:17` (AC-04 block and read only); `PLAN.md:112` (P4 test list), `PLAN.md:115` (P4 done-when); `TASKS.md` P4 task 6 | resolved | E-60-13 fixes the record source and adds the positive case: the pi guard resolves the repo root from `ctx.cwd`, reads the `path-protection-records@1` block of every `docs/features/*/decisions.md` and `docs/fix/*/decisions.md`, and permits a write/edit to an existing protected path when a `justification` row (authority `execute-phase`) matches the target; `approval` rows are never consulted (no phase state). P4 task 1 adds `matchingJustification`, task 5 names the ledger source, task 6 adds the matching-justification-permits-write positive case, and task 5/6 + O4 + the P4 done-when name it. PE-025 records `event.input.path` + `ctx.cwd`. | 513e8c171fe2fc14ea987a1f419b72c9e7d2285f |
| PLAN60-F7 | plan | low | plan | 40626cd083e620763d791c8b1408d248c610a67846b380242836a14e5c1b0ad8 | `path-guard:empty-diff` and `path-guard:two-runs` are asserted as P1 pins by the scenario table, `testing.md`, and O16/O17, but no P1 task and no P1 done-when names either pin, so the executor's TASKS checklist never produces them. | `SPEC.md:669,674` (`(P1 pins)`); `testing.md:20,30`; `planning-obligations.md:26-27` (O16/O17); `PLAN.md:47-51` (P1 tasks and done-when enumerate matrix, fallback, record, and CLI exit-code pins only); `TASKS.md` P1 | resolved | Both pins are now P1 tasks: `path-guard:empty-diff` in the engine-suite task (task 5) and `path-guard:two-runs` in the discipline-suite task (task 6); P1's done-when lists them (plus the new committed-range pin), matching the SPEC scenario table, `testing.md`, and O11/O16/O17. | 513e8c171fe2fc14ea987a1f419b72c9e7d2285f |
| PLAN60-F8 | plan | low | plan | 40626cd083e620763d791c8b1408d248c610a67846b380242836a14e5c1b0ad8 | P3 adds `path-protection` as a fifth `gate-rejection-vocabulary@1` type but schedules no edit to POLICY §8's sentence "A gate rejection is typed, from a closed set of four", so the skill text contradicts the block the same phase edits. | `skills/pre-execution-review/references/POLICY.md:173` ("closed set of four"); `PLAN.md:87` (P3 task 3); `TASKS.md` P3 task 3 (no prose update) | resolved | P3 task 3 now edits POLICY §8's prose sentence ("from a closed set of four") together with the `gate-rejection-vocabulary@1` row, so the five-member block and the sentence cannot contradict each other; `architecture-notes.md` records the pairing. The `path-protection` gate type is ordered by the new `GATE REJECTION — path-protection` trace in PREFLIGHT.md (P3 task 2), so the drift gate's must-name direction stays satisfied. | 513e8c171fe2fc14ea987a1f419b72c9e7d2285f |

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| PLAN60-F9 | plan | medium | plan | 4fd9b2e12fb702d91265774201366aa833ff4aeed58295ef3f02d464b5d41989 | The SPEC's Checkpoint wiring, P3 task 2, E-60-12 and the PLAN60-F3 resolution all state the final phase's own committed range is checked by the close-out gate (`path-guard --unit <unit-dir> --phase P5 --base <P5 base ref>`, no `--force` bypass), but no P5 task and no P5 done-when — nor any other phase — runs that invocation, so the hardening phase's protected changes are never observed; a frozen test edited during P5 to make the ladder pass would go undetected, which is the exact gap PLAN60-F3's repair claimed to close (P10, P11) | `docs/features/60-path-protection-guards/SPEC.md:591` ("the final phase's own range is checked by the close-out gate (P5's ladder) with `--phase P<n> --base <P<n> base ref>`"); `PLAN.md:86` and `TASKS.md:41` (P3 task 2 parenthetical "(the final phase's range is checked by P5's close-out gate)"); `decisions.md` E-60-12; `planning-findings.md` PLAN60-F3 resolution ("the final phase's range is checked by the close-out gate"); `grep -rn "path-guard --" docs/features/60-path-protection-guards/` → only the P1 discipline pin (`--phase P1`) and the P3 PREFLIGHT wiring (`--phase P<n-1>`), none at P5; `PLAN.md:126-134` and the P5 `TASKS.md` list enumerate no gate invocation and the P5 done-when runs only `node --test scripts/path-protection.test.mjs`; `planning-obligations.md:18` (O8) covers only the per-phase-entry checkpoint | resolved | P5 now runs the close-out gate: task 7 records P5's base ref (`git rev-parse HEAD` at phase entry) and runs `bun packages/agentic-workflow/bin/path-guard.mjs --unit docs/features/60-path-protection-guards --phase P5 --base <P5 base ref>` → exit 0 with the `PATH-GUARD` block pasted, and the P5 done-when names the same invocation; a fail stops the close-out before the PR and there is no `--force` bypass. O19 records the obligation; the SPEC Phases P5 bullet and done-when name the gate; E-60-15 is the decision and PE-028 the evidence. Phase-lint PASS at the new P5 fingerprint `P5:hardening:10:hardening-pr` (whole-set digest `381019477bdf277c455ec78b11d05c10a01542db75161a4f737a0fb9add85e0a`). | 812035ec31a16720f6c466d7dbb96a719aec9463 |
| PLAN60-F10 | plan | low | plan | 4fd9b2e12fb702d91265774201366aa833ff4aeed58295ef3f02d464b5d41989 | The plan edits five skills in P3 (orchestration-envelope, execute-phase, pre-execution-review, verification-contract, plan-feature-scaffold) and the pi package in P4, and `architecture-notes.md` claims the pi package "gains a minor version bump … (recorded at execution with its CHANGELOG row)", but no task in any phase bumps a `version:` or adds a `CHANGELOG.md` row and no validator checks it — the repo's same-PR version rule is silently unmet, the version-tables drift check passes whether or not a bump is made (it only compares an existing CHANGELOG cell to the frontmatter/package version), and CI would skip publishing the pi package because the version did not change (P8, P10) | `CLAUDE.md:172-178` ("Version every change … bump its `version:` … and add a line to CHANGELOG.md") and `CLAUDE.md:383` ("Version bumps are manual and same-PR. Bump `version:` in the touched package's `package.json` and add a row … in the same PR"); `architecture-notes.md:59-61`; `grep -rn -i "version\|changelog\|bump" docs/features/60-path-protection-guards/{PLAN.md,TASKS.md,ACCEPTANCE.md,SPEC.md}` → no bump/CHANGELOG task; `scripts/normative-drift.test.mjs:1019-1027` (version-tables compares the newest CHANGELOG cell to the frontmatter/package.json version, so an omitted bump is invisible); repo precedents `docs/fix/224-deterministic-replan-routing/planning-findings.md:14` (RP-224-6, a reference-only edit still needs a same-skill bump) and `docs/features/37-phase-lint-script/planning-findings.md:33` (F9, version bumps with no CHANGELOG rows) | resolved | P5 now schedules the same-PR release sweep: task 2 bumps the six touched skills minor with `bump-skill` (each with its CHANGELOG row), and task 3 bumps the pi package to `0.11.0` with its Companion npm packages row, both before the Pi mirror re-bundle and verified by `node --test scripts/normative-drift.test.mjs` → exit 0. O20/O21 record the obligations; `architecture-notes.md` names the scheduled mechanic; E-60-16 is the decision and PE-027 the evidence. The private `packages/agentic-workflow` crate is deliberately unbumped (`private: true`, nothing published). Phase-lint PASS at the new P5 fingerprint `P5:hardening:10:hardening-pr`. | 812035ec31a16720f6c466d7dbb96a719aec9463 |

<!-- PLAN60-F9 + PLAN60-F10 appended 2026-09-18 by `/review-plan 60-path-protection-guards` (PLAN-REVIEW-60-2, snapshot `4fd9b2e1…`) — the two findings of that review. -->

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| PLAN60-F11 | plan | medium | plan | 7f66d179b929ca7cc2279843c4ce28f9c1e24d496f5134acbcd0767c15fb5047 | The Engineering half's shipped-default `test-file` class protects `**/*.spec.*` (`SPEC.md:460`), a glob the Product's frozen default set never authorizes: D4 lists `tests/**`, `e2e/**`, `*.test.*`, `fixtures/**`, plus the policy config itself (`SPEC.md:336`), AC-01 enumerates the same families without any spec variant (`SPEC.md:277-280`), issue #220 names the same four globs, and no E-60 decision or planning-evidence row records the addition. The canonical shipped default (E-60-1) therefore protects a file family the Product never decided, no acceptance criterion tests it, and the repo contains no `.spec.` file (grep: the only `*.spec` occurrence in the tree is `SPEC.md:460`). | `docs/features/60-path-protection-guards/SPEC.md:460` (`"test-file": { "globs": ["**/*.test.*", "**/*.spec.*"], "freeze": true }`); `SPEC.md:336` (D4 default protected set); `SPEC.md:277-280` (AC-01 families); `SPEC.md:207`/`:320-321` (D2 the shipped defaults are the floor); `docs/features/ROADMAP.md` row 60 and `gh issue view 220 --json body` (issue #220: `tests/**`, `e2e/**`, `*.test.*`, fixtures); `grep -rn '\*\.spec\|\.spec\.\*' --include=*.md --include=*.json --include=*.mjs --include=*.sh .` → only `SPEC.md:460`; `decisions.md` E-60-1…E-60-16 (no decision records the addition) | resolved | E-60-17 drops the Engineering-invented `**/*.spec.*` glob from the canonical shipped-default `test-file` class (SPEC Design block), so `SHIPPED_PATH_POLICY` (E-60-1) projects D4 (`SPEC.md:336`) and AC-01 (`SPEC.md:277-280`) exactly; PE-029 sources the claim. The glob is pruned repo-wide: `grep -rn '\*\.spec\|\.spec\.\*' --include=*.md --include=*.json --include=*.mjs --include=*.ts --include=*.sh .` matches no live surface — the remaining hits are this row's own claim, the repair records, and `progress.md` review notes. Plan-owner repair batch `f9015324`; the Product projection is byte-identical (24443 B, sha256 `50b33e5d…920e`), the phase fingerprints and whole-set digest are unchanged (`381019477bdf…e0a`, `node scripts/phase-lint.mjs` → `verdict PASS`, exit 0), and no phase, task, acceptance criterion, or obligation changed. Same class as recorded (`plan`). | f901532493ca96cb293704352ce4af73150ff5c7 |
| PLAN60-F12 | plan | low | plan | 7f66d179b929ca7cc2279843c4ce28f9c1e24d496f5134acbcd0767c15fb5047 | The `undeclared-test` failure reason — a created protected path no `created` row covers (`SPEC.md:515`, `:547`, the enforcement of the Product's "every test justified up front", In scope 5 / D-60-3) — has no obligation row, no dev scenario, and no P1 task or validator: the P1 evaluator task enumerates only `unmatched-record` (`PLAN.md:46`), `testing.md`'s Tier 1 layer lists justification / approval / unmatched-record / malformed-declaration but not `undeclared-test` (`testing.md:8`), `planning-obligations.md` has no row for it (O12 covers vocabulary closure only, `:22`), and the P1 scenario pins cover empty-diff, malformed-config, missing-approval, unmatched-record, freeze-boundary, two-runs and committed-range. A regression in the plan-declaration enforcement would pass every P1 gate (P10, P11, L3, L5). | `docs/features/60-path-protection-guards/SPEC.md:515` ("a created protected path no `created` row covers is `undeclared-test`") and `:547` (reason table row); `PLAN.md:45-46` (parser + evaluator tasks name only `malformed-declaration`/`unmatched-record`); `TASKS.md:14-16`; `testing.md:8` (Tier 1 layer enumeration), `testing.md:18-39` (scenario inventory); `planning-obligations.md:22` (O12, vocabulary closure); `grep -rn undeclared-test docs/features/60-path-protection-guards/` → only `SPEC.md:515` and `:547` | open | advisory only — no cycle restart, operator materiality bar (feature 38's rp-008/rp-011/rp-012 + F42/F43 precedent; E-60-18, operator instruction 2026-09-18 for this batch: `drop **/*.spec.*; F12 recorded advisory`). `undeclared-test` stays a declared Product failure state (`SPEC.md:515`, `:547`) closed in the reason vocabulary (O12) but keeps no obligation row, dev scenario, or P1 validator; the residual verification gap is accepted, not falsified (no counter-evidence), so the row stays `open`. Fold path: add the `undeclared-test` obligation + P1 case at the next plan-side write, or ride the P1 phase's progress receipt. Severity `low` — non-material under the recorded bar, so the re-review binds it without a third repair cycle. | f901532493ca96cb293704352ce4af73150ff5c7 |

<!-- PLAN60-F11 + PLAN60-F12 appended 2026-09-18 by `/review-plan 60-path-protection-guards` (PLAN-REVIEW-60-3, snapshot `7f66d179…`) — the two findings of that review. -->
