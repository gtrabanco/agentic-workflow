# Planning obligations — 60-path-protection-guards

One row per normative behaviour, applicable compatibility invariant, affected use
case, and required failure state. Row shape and column order per
`pre-execution-review/references/LEDGERS.md` §2. Status vocabulary:
`planned | in-progress | verified | n/a | deferred` — every row starts `planned`;
no row is `deferred` (no obligation is exported to a follow-up issue).

| obligation-id | Authority source | Affected use case or invariant | Phase | Task | Implementation owner | Validator | Required evidence | Status |
|---|---|---|---|---|---|---|---|---|
| O1 | AC-01 | The phase × path-class matrix is deterministic: create/modify/delete/rename rows for the tests class, the e2e class, the test-file class, the fixtures class, and the policy-config class resolve to the frozen requirement | P1 | engine suite | execute-phase | `node --test packages/agentic-workflow` → exit 0 with the matrix cases green | suite case names recorded in the phase handoff (`progress.md`) | planned |
| O2 | AC-02 | The Tier 1 gate exits non-zero with a closed reason on a dirty protected path without a justification record, and exits 0 with the record verified against the changed paths | P1 | gate CLI + discipline suite | execute-phase | `node --test scripts/path-protection.test.mjs` → exit 0 with the pass/fail exit-code and closed-reason pins | CLI exit codes and block pasted in the phase handoff | planned |
| O3 | AC-03 | An absent, malformed, or oversized config falls back to the shipped defaults and emits the degradation line; the gate never fails open and never disappears | P1 | policy resolution + engine suite | execute-phase | `node --test packages/agentic-workflow` → exit 0 including the `missing-config` and `malformed-config` cases | case names recorded in the phase handoff | planned |
| O4 | AC-04 | The pi `tool_call` guard blocks a `write` / `edit` call to an existing protected path with `{ block: true, reason }`, and a read-only call is unaffected | P4 | `tool_call` registration | execute-phase | `cd packages/pi-agentic-workflow && bun run test` → exit 0 including the block, reason, and read-passthrough cases | case names recorded in the phase handoff | planned |
| O5 | AC-05 | A post-freeze change with a justification but no recorded owner approval fails the gate; adding the recorded approval makes the identical diff pass; no code path grants an approval | P1 | evaluator + discipline suite | execute-phase | `node --test scripts/path-protection.test.mjs` → exit 0 including the `approval-required` and no-auto-approval negative cases | case names recorded in the phase handoff | planned |
| O6 | AC-06 | Pre-freeze creation of a declared test passes without a record; modify/delete of an already committed test file requires one | P1 | engine suite | execute-phase | `node --test packages/agentic-workflow` → exit 0 including the create-versus-modify cases | case names recorded in the phase handoff | planned |
| O7 | AC-07 | Skills carry no project-specific policy data inline — the globs live in the crate module, the template seed, and the pi mirror only | P3 | contract adoption | execute-phase | `grep -nE 'tests/\*\*|e2e/\*\*' skills/` → no output | grep output recorded in the phase handoff | planned |
| O8 | AC-08 | The `execute-phase` preflight runs the Tier 1 gate at the phase fingerprint checkpoint and a gate fail blocks the phase with the typed rejection trace | P3 | checkpoint wiring | execute-phase | read-verified: the PREFLIGHT step text quoted in the phase handoff; command-verified by O2's suite | quoted PREFLIGHT text recorded in the phase handoff | planned |
| O9 | AC-09 | The shipped policy default and its doc page exist under `template/.agentic-workflow/` and the hooks README documents the guard | P2 | template ship | execute-phase | `test -f template/.agentic-workflow/path-policy.json && test -f template/.agentic-workflow/path-protection.md && grep -q 'path-protection' template/.agentic-workflow/hooks/README.md` → exit 0 | command output recorded in the phase handoff | planned |
| O10 | AC-10 | The pi override is tighten-only: an added protected glob and a raised requirement are honored; a removal and a lowering are rejected with the shipped protection in force and the degradation report emitted | P4 | config resolution + package suite | execute-phase | `cd packages/pi-agentic-workflow && bun run test` → exit 0 including the tighten-honored and loosen-rejected cases | case names recorded in the phase handoff | planned |
| O11 | SPEC Design (read-only gate) | The gate writes nothing: it reads the policy, the declaration, the records, and git state, and prints; two consecutive runs leave the tree byte-identical | P1 | gate CLI | execute-phase | `node --test packages/agentic-workflow` → exit 0 including the two-runs idempotence pin; the CLI's only spawned processes are `git status` and `git diff` | case name recorded in the phase handoff | planned |
| O12 | SPEC Design (closed vocabulary) | `PATH_GUARD_REASONS` is the closed set declared by the `path-protection@1` block; no reason is emitted outside it | P1 | policy module | execute-phase | `node --test scripts/path-protection.test.mjs` → exit 0 including the closed-reason closure case | vocabulary dump recorded in the phase handoff | planned |
| O13 | SPEC D5 / D-60-3 (no auto-approval) | An approval row's `authority` is `human-owner`; any other authority is `malformed-declaration`, so no unattended path can grant an approval | P1 | record parser + evaluator | execute-phase | `node --test scripts/path-protection.test.mjs` → exit 0 including the no-auto-approval negative case | case name recorded in the phase handoff | planned |
| O14 | SPEC Design (unavailable-gate disclosure) | Where the crate is unavailable, the checkpoint discloses the missing gate and records it; it never silently skips it | P3 | checkpoint wiring | execute-phase | read-verified: the disclose-and-degrade sentence quoted in the phase handoff | quoted PREFLIGHT text recorded in the phase handoff | planned |
| O15 | SPEC Design (single source + two pins) | The template seed and the pi mirror match the crate's `SHIPPED_PATH_POLICY`; a drift is a test failure | P2, P4 | template mirror check + pi parity case | execute-phase | the template mirror diff is empty and `cd packages/pi-agentic-workflow && bun run test` → exit 0 including the cross-package parity case | diff output + parity case name recorded in the phase handoffs | planned |
| O16 | SPEC dev scenario `path-guard:empty-diff` | A clean tree at a checkpoint yields `pass — clean`, exit 0 | P1 | engine/discipline suite | execute-phase | `node --test packages/agentic-workflow` → exit 0 including the empty-diff pin | case name recorded in the phase handoff | planned |
| O17 | SPEC dev scenario `path-guard:two-runs` | Two consecutive runs on the same tree produce byte-identical blocks and leave the tree unchanged | P1 | discipline suite | execute-phase | `node --test scripts/path-protection.test.mjs` → exit 0 including the idempotence pin | case name recorded in the phase handoff | planned |
