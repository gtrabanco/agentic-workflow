# Roadmap

The single source of truth for feature **numbering, ordering, and dependencies**.
Every feature folder under `docs/features/<NN>-<slug>/` must have a row here, and
every row must have a folder (or be explicitly marked "scheduled").

## Features

| NN | Slug | Status | Depends on | Summary |
|----|------|--------|------------|---------|
| 01 | `generate-docs` | done · [#8](https://github.com/gtrabanco/agentic-workflow/pull/8) | — | New user-invocable skill: incremental, diff-driven developer docs into the target project's docs site (Starlight MDX first-class adapter), deterministic knowledge/call map, optional review-report export, drift protection via execute-phase and audit-docs |
| 02 | `measured-perf-review` | done · [#9](https://github.com/gtrabanco/agentic-workflow/pull/9) | — | init-workspace discovers/installs performance tooling (lint complexity rules, benchmark harness, profiler) and registers its commands; review-perf runs them when declared so perf findings cite real measurements |
| 03 | `orchestrator-crash-recovery` | done · [#10](https://github.com/gtrabanco/agentic-workflow/pull/10) | — | workflow-status gains a crash-recovery reconcile section (dirty tree, half-closed phase, stale envelope) so an external driver (opencode REST server) can restart safely from ground truth |
| 04 | `running-economically` | done · [#22](https://github.com/gtrabanco/agentic-workflow/pull/22) | — | Docs-only cost pack (backlog U1, closes #11): a Context hygiene & cost section (clear + log-session over compaction, with the compaction cost mechanism stated) in FEATURE_WORKFLOW/template; a cross-family review line extending the "never review with a weaker model" invariant to prefer a different model *family*; driver prompt-cache guidance (byte-stable prefixes, short window, no mid-unit model switch) in ORCHESTRATION |
| 05 | `adversarial-context-clean-review` | done · [#23](https://github.com/gtrabanco/agentic-workflow/pull/23) | — | Backlog U2 (closes #12): harden the mandatory end-of-unit review. `review-implementation`'s find phase becomes adversarial by default ("assume the diff is WRONG; prove it does not work"); `review-change` gains a mandatory context-clean turn-contract box (the end review MUST run in a conversation that did NOT write the diff — else STOP and hand off). References feature 04's cross-family preference; does NOT build `--adversarial N` (that is U8/#18, which depends on this). Minor bumps for both skills |
| 06 | `design-feature` | done · [#24](https://github.com/gtrabanco/agentic-workflow/pull/24) | — | Backlog U3 (closes #13, major): new user-facing `design-feature` skill owning product definition via **capability closure** (per entity → CRUD + state transitions + UI entry + API + test, or explicit `n/a`; per capability → entry point + ACL; per role → assign/revoke/view) → exhaustive acceptance criteria — the fix for weak executor models omitting implicit work. **One SPEC in two halves** (design writes product half, plan writes engineering half; `## Design status` marker). `plan-feature` slims to engineering planning (**major**) and STOPS+redirects to `/design-feature` when a feature is undesigned (no bypass flag); `plan-feature-interview` is retired into `design-feature`. Roadmap `defined` state + workflow-status/ship-roadmap wiring are **U4/#14** (depends on this) |
| 07 | `roadmap-status-machine` | done · [#25](https://github.com/gtrabanco/agentic-workflow/pull/25) | 06 | Backlog U4 (closes #14, M): promote the roadmap status column to the pipeline's state machine `idea → defined → planned → in-progress → done` (repo + `template/` legend). `workflow-status` reports `idea` rows as **design candidates** and only calls `defined`+ units startable (new `design_candidates` envelope field); `execute-phase`'s gate redirects a sub-`planned` unit (`idea`→`/design-feature`, `defined`→`/plan-feature`); the authoring skills **set** the statuses (`defined` by design-feature/from-issue, `planned` by scaffold) and `plan-feature`'s gate keys on the roadmap status (SPEC `## Design status` retained as legacy fallback); `ship-roadmap` complies via **batch design (founding) + JIT design from locked decisions (mid-run, no new questions)**, undesignable → `NEEDS_INPUT`+park. Legacy `planned`-with-designed-SPEC = `defined`+`planned` (MIGRATION.md). Minor bumps across the touched skills |
| 08 | `phase-economics` | done · [#26](https://github.com/gtrabanco/agentic-workflow/pull/26) | — | Backlog U5 (closes #15, M): move cost from execution to planning so a **weak model** can execute a phase. `plan-feature-scaffold` gains a **hard split rule** (mandatory `Depends on:`-chained split on >~5 phases OR a multi-layer/concern phase OR an unresolved design decision — replaces the soft "consider splitting"), a **per-phase cheap-executability checklist** (independently checkable · zero open decisions · one concern · gate runs locally), and **acceptance criteria emitted as runnable commands** in TASKS/testing. `execute-phase` + `FEATURE_WORKFLOW` state the **one-phase-one-session** rule. SPEC template hardened (repo + `template/`). Soft dep on 06/07 (both merged). Rejected: dynamic model self-selection. Golden-fixture enforcement deferred to U9. Minor bumps to `plan-feature-scaffold` + `execute-phase` |
| 09 | `product-audit-tooling-sweep` | done · [#27](https://github.com/gtrabanco/agentic-workflow/pull/27) | — | Backlog U6 (closes [#16](https://github.com/gtrabanco/agentic-workflow/issues/16), S): give `product-audit` an **installed-tooling sweep** — inventory installed skills + connected MCP servers, cross them against the project's applicable review axes and roadmap features, and PROPOSE (never auto-apply) either **registering** a useful one in the project's `CLAUDE.md` (zero-discovery-cost reuse by `review-change`/`execute-phase`) or **re-designing** a feature a discovered skill/MCP would rescope (routes to `/design-feature <slug>`). New dimension + process step + a fourth proposal stream in `skills/product-audit/SKILL.md`, keeping its proposes-only contract. Placement invariant: heavy discovery lives only in `design-feature` (per feature) and here (product-wide); the execution path stays read-only. Soft pair with 06 (merged). Single-pass; minor bump to `product-audit` |
| 10 | `envelope-orchestrator-only` | done · [#28](https://github.com/gtrabanco/agentic-workflow/pull/28) | — (driver-gated) | Backlog U7 (closes [#17](https://github.com/gtrabanco/agentic-workflow/issues/17), M, **major**): move the machine turn-envelope out of the per-skill contract and into the orchestration layer that consumes it. Remove the `## Machine envelope` section **and** its turn-contract box from **14 user-facing skills** (`audit-docs, audit-pr, bump-skill, design-feature, execute-phase, generate-docs, init-workspace, log-session, plan-feature, plan-fix, product-audit, review-change, ship-roadmap, triage-issue`); **`workflow-status` keeps it** (emitting the envelope IS the sensor's function). `orchestration-envelope` + `ORCHESTRATION.md`/`PORTABLE_PROMPT.md` gain the canonical **driver-injected system-prompt snippet** + a **repair loop** (parse-fail → re-invoke "Emit only the machine envelope for the turn above"). MAJOR bump chain × 14; MIGRATION.md note; schema/npm package **unchanged**. **Hard external gate (not a roadmap dep): the driver must ship the repair loop before this merges** — blocks execution, not planning |
| 11 | `adversarial-multi-reviewer` | done · [#29](https://github.com/gtrabanco/agentic-workflow/pull/29) | 05 | Backlog U8 (closes [#18](https://github.com/gtrabanco/agentic-workflow/issues/18), M): opt-in `review-change --adversarial N` — N independent, **context-clean**, diff-only reviewers carrying U2's adversarial stance run in **parallel** (Claude Code subagents / headless invocations / sequential-fresh-conversation fallback), preferring model-family diversity; findings **merged + deduped by `file:line`** into the one decision table, inclusion threshold **≥1** reviewer (no quorum). **Default OFF**; auto-recommended (never forced) for `L`/sensitive changes. `ship-roadmap` enables `--adversarial 2` as an **unattended hard floor** for L/sensitive checkpoints (deliberately NOT aligned with the interactive advisory). MINOR bumps for `review-change` + `ship-roadmap`; `REVIEW_AND_CLASSIFY.md` + MIGRATION.md notes. Depends on 05 (U2), merged |
| 12 | `golden-fixture-procedure` | done · [#30](https://github.com/gtrabanco/agentic-workflow/pull/30) | — | Backlog U9 (closes [#19](https://github.com/gtrabanco/agentic-workflow/issues/19), S): a manual, infrastructure-free **golden-fixture smoke test for skill wording** — new `docs/workflow/GOLDEN_FIXTURE.md` with an **embedded fixture** (a fixed toy "add a CSV export command" feature + toy SPEC, copy-pasteable, **not** a committed feature folder), a repeatable **procedure** (after editing any executor-path skill, run the fixture through it following its `SKILL.md` literally, **with the weakest fleet model** — Qwen3.6 35B / Gemma4 26B), **fixed pass criteria** (fixed output blocks print exactly · branch/commit discipline held · no invented steps · closing `→ Next:` printed), and a **run-log table** (date · model · skill+version · result). Closes the enforcement gap feature 08 deferred here. Docs-only, single-pass; **no CI, no `template/` mirror, no skill edits** (this unit tests skills, it does not reword them — a wording fix a run reveals is a separate change). Discoverable from `docs/workflow/README.md`'s index |
| 13 | `init-workspace-upgrade-mode` | done · [#31](https://github.com/gtrabanco/agentic-workflow/pull/31) | 06 07 08 | Backlog U10 (closes [#20](https://github.com/gtrabanco/agentic-workflow/issues/20), M): give `init-workspace` an **upgrade mode** — on a repo that already has the scaffold, diff its `CLAUDE.md`/`docs` substrate against the **current** `template/`, read `docs/workflow/MIGRATION.md`, and propose **only the new/missing blocks** (Docs site, Performance commands, five-state roadmap statuses, whatever U1–U5 added) via a short, discovery-defaulted interview — **never clobbering an existing decision** (additive-only). Plus a documented "updating an existing install" recommendation in `README`/`README.es`/`MIGRATION.md` (update skills → read MIGRATION → `init-workspace` upgrade → optional `product-audit`). Closes the substrate-migration gap `product-audit` only detects. Docs/wording-only; no `template/` change; minor bump to `init-workspace`. Deliberately late (U10) — migrates blocks earlier units introduced. Deps 06/07/08 (U3/U4/U5) all merged |
| 14 | `final-docs-batch` | done · [#32](https://github.com/gtrabanco/agentic-workflow/pull/32) | 04 05 06 07 08 09 10 11 12 13 | Backlog U11 (closes [#21](https://github.com/gtrabanco/agentic-workflow/issues/21), S, **always last**): the final documentation sweep, run once after U1–U10 merged instead of churning the same tables per-unit. Corrects **skill counts everywhere** (READMEs EN/ES + `docs/workflow/SKILLS.md` — computed at execution time, never hardcoded; found: SKILLS.md said 12+4, README said 14+14 *and* 15+13; frontmatter-verified reality is **15 user-facing + 13 internal**, not the SPEC's assumed 16+12 — its `N_UF` grep was unanchored and false-matched a prose mention in `orchestration-envelope/SKILL.md`, see feature 14's `CHECKLIST.md`); adds the **missing `workflow-status` + review-pack entries** to `SKILLS.md` (resolves feature 01 known-issue #4); adds the **design stage** to the README EN/ES pipeline diagrams; **verifies/refreshes** the model + equivalence tables and `model-routing.yml` coverage (found and added a missing `generate-docs` entry); **consolidates `MIGRATION.md`** into one upgrade narrative over the backlog's majors (`plan-feature` slimming, envelope removal), keeping the dated notes. Docs-only, single-pass; **no `bump-skill`** (no SKILL.md touched), no `template/` change. Deps 04–13 (U1–U10) all merged |
| 15 | `injection-safe-urgency` | done · [#47](https://github.com/gtrabanco/agentic-workflow/pull/47) | — (soft: 03 07 10, all merged) | Issue [#42](https://github.com/gtrabanco/agentic-workflow/issues/42) (M): an injection-safe urgency channel — urgency derived **only** from capability-gated GitHub labels (triage+ can apply; issue title/body/comments never), never attacker text. `triage-issue` owns + applies two labels (`urgent` = evaluate for interrupt-now → judge; `fix-next` = head of queue, no interrupt) on a fix-now/high-severity verdict, creating them via `gh label create` if missing. `workflow-status` reports urgency (`detail.urgent`, presence-only) + the in-flight unit's interruptibility facts (phase · dirty/clean · distance to commit boundary) — never decides. The pause-vs-finish decision is a **bounded, tool-less micro-judge** in the consumer (deterministic short-circuit first; then cheap-tier clean-context classifier → closed `FINISH_FIRST | INTERRUPT_NOW` + repair loop; rubric-as-system-prompt; fail-safe `FINISH_FIRST`), canonical in `ORCHESTRATION.md`, referenced by `ship-roadmap` SELECT. Interrupt/park reuses `RESUMABLE` + `execute-phase` idempotent re-entry. `init-workspace` seeds both labels (scaffold + upgrade mode). Minor bumps across `triage-issue`, `workflow-status`, `ship-roadmap`, `init-workspace` |
| 16 | `nan-model-guidance` | done · [#56](https://github.com/gtrabanco/agentic-workflow/pull/56) | — (docs-only touch on 04 10 12, all merged) | Opportunistic (not backlog-chained, S): corrects the README's NaN.builders/OpenAI-compatible provider guidance against that provider's own API reference — the prior "1:1 uniform effort dial" claim was false. Adds a per-model reasoning-control matrix + `effort:` mapping, demotes the model with unvalidated/XML-format tool calling out of the agentic-execution ladder, adds a GLM-5.2 catalog-verification caveat and per-key rate/concurrency limits. `orchestration-envelope` (1.2.0, minor) + `ORCHESTRATION.md` gain a structured-outputs shortcut for forcing the driver-injected envelope where the provider supports strict JSON schema, repair loop as fallback. `ship-roadmap` (2.2.1, patch) gains a Portability concurrency guardrail. `GOLDEN_FIXTURE.md` gains a tool-calling smoke test as a model precondition for the executor path |
| 17 | `finding-severity-routing` | done · [#57](https://github.com/gtrabanco/agentic-workflow/pull/57) | — (soft: 37 docs the manual ladder) | Issue [#49](https://github.com/gtrabanco/agentic-workflow/issues/49) (M): surface per-finding severity so the fold cycle can route the fixing model per finding. `review-change`/`audit-pr` compute a `Sev` per finding but persist nothing; this adds a per-unit **fix-now fold ledger** (`review-findings.md`, schema `\| id \| file:line \| axis \| severity \| class \| route \| folded \|`) the writer skills append to (fix-now only; non-fix-now stays with `triage-issue`), `execute-phase`'s fold cycle ticks (`folded: no→yes`), and `workflow-status` reads (read-only) to emit `findings.fix_now[]` items carrying `severity` + a derived `suggested_tier` (severity `high` OR subtle axis → `strong`; else `cheap`, reusing `next.tier`'s vocabulary). `next.tier` unchanged. Schema package mirrored (item shape + version bump) same PR; `template/` mirrors the ledger convention. Resolves #49's two scope questions (D1 new ledger; D2 verbatim severity + derived tier) |
| 18 | `normalized-repository-state` | done · [#114](https://github.com/gtrabanco/agentic-workflow/pull/114) | — | Issue [#110](https://github.com/gtrabanco/agentic-workflow/issues/110) (L): introduce an evidence-backed, frozen Normalized Repository State artifact and discovery/resolution skills. Planning, implementation, review, audit, and orchestration consume it rather than independently reconstructing facts; contradictions are explicit and only the resolver can update facts or accepted decisions. |
| 19 | `architectural-invariants` | done · [#115](https://github.com/gtrabanco/agentic-workflow/pull/115) | 18 | Issue [#109](https://github.com/gtrabanco/agentic-workflow/issues/109) (L): introduce a portable architectural-invariants contract and optional project document. Planning, execution, review, and audit classify each applicable invariant with evidence; violations and new/changed rules stop for an explicit architectural decision. Frozen normalized state is consumed when present but never required or substituted for repository truth. |
| 20 | `runtime-guardrails-progressive-skills` | done · [#116](https://github.com/gtrabanco/agentic-workflow/pull/116) | — | User-requested (L): add portable command guards for environment disclosure and direct merges; make `ship-roadmap --fullauto` the sole transient automated-merge authority with a PR audit comment; add grouped distribution metadata; and progressively segment oversized skills under machine-checked context budgets without weakening small-model contracts. |
| 21 | `workflow-contract-consolidation` | done · [#121](https://github.com/gtrabanco/agentic-workflow/pull/121) | — | Consolidate the hot planning, execution, review, and PR-audit contracts so each check has one owner, route-specific context is loaded only when needed, in-scope review findings stay in the current unit, and only `audit-pr` can emit `MERGE-READY`. Complete canonical Turn Contract ownership through semantic per-skill additions without changing the fixed eleven-box behavior. |
| 22 | `bounded-delivery-loops` | done · [#122](https://github.com/gtrabanco/agentic-workflow/pull/122) | 21 | Make unattended delivery the efficient default: freeze a compact acceptance manifest, execute all remaining phases from one command, group fix issues and review findings by one verifiable outcome, remove automatic backlog creation, and add a bounded context-clean review/fold loop with explicit no-progress stops. |
| 23 | `workflow-skill-capability-profiles` | done · [#140](https://github.com/gtrabanco/agentic-workflow/pull/140) | — | Issue [#136](https://github.com/gtrabanco/agentic-workflow/issues/136) (S): extend `WORKFLOW_SKILL_PROFILES` with optional, closed-vocabulary capability metadata for maximum effects, reasoning, context sources, and required evidence; populate every built-in profile, fail closed on absence, preserve existing machine-contract behavior, and ship synchronized package references. |
| 24 | `workflow-transition-decider` | done · [#143](https://github.com/gtrabanco/agentic-workflow/pull/143) | 23 | Issue [#137](https://github.com/gtrabanco/agentic-workflow/issues/137) (S): export a pure, deterministic `decideWorkflowAction()` from the schema package — a closed 13-row direct-invocation transition table over `WorkflowSnapshot v1` + the last `SkillOutcome v1` + #136 capability profiles + caller policy decides invoke (proven transition) / sense (refresh with `workflow-status`) / stop, with the 12 closed reason codes, the exact evidence rules (frozen repository state for the eight planning/execution/review intents, current receipts for review/audit/merge), per-intent target contracts, and fail-closed sensor points; property/fuzz + determinism tests; bilingual reference with the elision and fallback examples; minor release 3.2.0. |
| 25 | `content-bound-review-receipts` | done · [#144](https://github.com/gtrabanco/agentic-workflow/pull/144) | — | Issue [#138](https://github.com/gtrabanco/agentic-workflow/issues/138) (M): publish strict content-bound `CandidateSnapshot v1` + `ReviewReceipt v1` contracts from the schema package (`GitObjectId` sha1/sha256 uniform per snapshot, path-byte-ordered manifest with status/mode/null-applicability matrix, acceptance fingerprint over ordered `{id, blobSha256}` inputs, closed review kinds/severities, opaque identities, policy version); canonical serialization + lowercase SHA-256 digests with published test vectors; pure freshness predicate returning one stable code per stale dimension (`stale-base-tree \| stale-candidate-tree \| stale-manifest \| stale-acceptance-fingerprint \| stale-review-policy`); red-first validation suites + edge matrix (>32 paths, >4 MiB, binary, rename/copy/type-changed, base/candidate/acceptance/policy mutation, full revert, empty diff); two new JSON Schema files rejecting undeclared fields; bilingual package docs stating validity ≠ correctness; additive minor release 3.3.0. Runs parallel to #136/#137 (both merged). |
| 26 | `staged-verification-contracts` | done · [#145](https://github.com/gtrabanco/agentic-workflow/pull/145) | 25 | Issue [#139](https://github.com/gtrabanco/agentic-workflow/issues/139) (L after approved replan): publish bounded staged `VerificationPlan v1` + `VerificationReceipt v1` contracts from the schema package — exactly two authoritative validation entries backed by one canonical internal definition; generated Draft-07 structural projections (not a second semantic authority); ≤128 commands/results, bounded args/strings/payloads/diagnostics and fast/full timeout budgets; per-command status and evidence; verdict `pass \| fail \| incomplete`; canonical digests + readonly vectors; six reachable freshness codes bound to #138 content; only a current complete `full` receipt satisfies delivery; bilingual docs, deterministic projection/benchmark gates, additive minor release 3.4.0. AWL dialect/runner adoption remains user-routed future consumer work. |
| 27 | `pre-execution-plan-review` | idea · scheduled | 26 | Issue [#146](https://github.com/gtrabanco/agentic-workflow/issues/146): add one portable read-only `review-plan --product \| --engineering \| --full` gate plus separate `fold-plan-findings` repair; bind every verdict to an exact planning-artifact snapshot, invalidate stale evidence deterministically, close at the terminal PASS without post-PASS artifact writes, preserve bounded independent/adversarial review, and keep implementation mapping, research authoring, provider routing, and durable execution outside this planning authority. |
| 28 | `bounded-implementation-discovery` | idea · scheduled | 27 | Issue [#149](https://github.com/gtrabanco/agentic-workflow/issues/149): add a portable read-only pre-write mapping step inside `execute-phase` with an inline fast path and fresh-mapper route; prove entry points, current behavior, reuse patterns, expected writes, tests, plan assumptions, contradictions, and unknowns against the exact source revision before returning `READY \| REPLAN \| NEEDS-DECISION`, without creating a committed map, public schema, issue, or runtime/provider dependency by default. |

## Status legend

The pipeline's single ground-truth state machine — every sensor and executor
reads this column, not a SPEC-local marker:

```
idea ──design-feature / plan-feature-from-issue──▶ defined
        (stamps ## Design status: designed)
                                                     │
                        plan-feature-scaffold        │
             (fills engineering half + artifacts)    ▼
                                                   planned
                                                     │
                     execute-phase P1                │
              (branch open; row → in-progress)       ▼
                                                 in-progress
                                                     │
                        PR-open step                 │
              (row → done; merge state in forge)     ▼
                                                    done
```

- `idea` — a roadmap row exists (the wishlist); no completed product design.
  **No new file** — a thin row *is* the idea. Next action: `/design-feature
  <slug>`. Set by whoever adds the row (human or `ship-roadmap` founding).
- `defined` — `SPEC.md` exists with the **product half complete** (`## Design
  status: designed`, capability closure filled). Next action: `/plan-feature
  <slug>`. Set by `design-feature` or `plan-feature-from-issue`.
- `planned` — full SPEC (**engineering half filled**) + planning artifacts
  exist. Next action: `/execute-phase <NN>`. Set by `plan-feature-scaffold`
  (XS/S SPEC-only sizes included — scaffold still runs and lands here).
- `in-progress` — branch open, phases executing. Set by `execute-phase` P1.
- `done` — built and its PR open (the last step opened the PR); **merge state
  lives in the forge**, not the status — a `done` row may still be awaiting a
  human merge. Set by the PR-open step.

Each transition is owned by exactly one skill (a write) — no status is ever
inferred, and no second skill writes the same edge.

## Conventions

- Numbers are assigned in order and never reused.
- A feature that depends on another cannot start until its dependency is **merged**
  (not merely `done` — a `done` dep with an open PR isn't on `main` yet).
- A unit is **executable only when `planned`** (or above). `execute-phase`'s
  dependency gate STOPs and redirects a sub-`planned` unit: `idea` →
  `/design-feature <slug>`, `defined` → `/plan-feature <slug>`.
- **Legacy compat:** a pre-U4 roadmap row still reading a plain `planned` with
  no five-state history, whose SPEC's product half is complete, is treated as
  `defined`+`planned` (no redirect) — see `docs/workflow/MIGRATION.md`.
- Keep this table consistent with the feature folders (the `audit-docs` skill
  checks for drift).
