# Planning findings — fix-154-settings-picker-search-bulk-chain

Reviewers append rows; only the stage's author resolves them (LEDGERS contract).
`info` is the only immaterial severity. `dismissed` requires counter-evidence
that falsifies the finding; `resolved` names the closing artifact revision.

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| RP1-F1 | plan | medium | plan | a0ed8357905ab4763fe15273ebfd086a7fd5b1e4116261dab8fff3e8e4cb70a6 | Every scoped test validator — OB-1…OB-14, the P1–P6 done-when commands, and the mirrored AC1–AC12 validator cells in ACCEPTANCE.md — uses the shape `bun run test test/<file>`. The package's test script is `tsc && bun test test/*.test.mjs`; `bun run` appends the positional arg after the whole script, so the arg never scopes the run. Every scoped invocation executed the full suite (140 tests / 14 files) and exited 0, and the named file need not even exist: `bun run test test/picker-filter.test.mjs` exits 0 today with the file absent (P4 not executed), and a direct `bun test test/zzz-missing.test.mjs` also exits 0 (bun treats an unmatched path as a filter and exits 0). Result: each implementation phase's validator currently passes on a no-op of that phase's work — the L5 fail-ability property is violated for 14 of 16 obligations and 6 phase done-whens. Repair is mechanical: scope with a direct `bun test test/<file>` (scopes correctly once the file exists — 9 tests / 1 file demonstrated for config-merge) paired with the file-creating task ordering + required-evidence paste to cover the missing-file case, or keep the full-suite command and scope via a pinned test-name grep in `required-evidence`. ACCEPTANCE.md inherits the same shape and is repaired in the same batch (pre-execution, by the author). | Review-run evidence in receipt rp-fix154-20260908-001: `bun run test test/picker-filter.test.mjs` → `140 pass / 0 fail, 14 files, exit 0` (file absent); `bun run test test/config-merge.test.mjs` → full suite; `bun test test/zzz-missing.test.mjs` → exit 0; `bun test test/config-merge.test.mjs` → `9 pass, 1 file` (direct form scopes) | resolved | Re-run by the author at repair revision `7aeab75d` confirms both claims stand (140/14 full-suite run under `bun run test`; direct `bun test test/config-merge.test.mjs` → `9 pass, 1 file`; direct `bun test test/zzz-missing.test.mjs` → exit 0 with only a note). Repair adopted the directed option A in one batch: every scoped validator — OB-1…OB-14 (SPEC `### Obligations`), P1–P6 done-whens (SPEC `## Phases`), and AC1–AC12 validator cells (ACCEPTANCE.md) — now uses direct `cd packages/pi-agentic-workflow && bun test test/<file>…` with the required-evidence cell demanding the pasted summary line `Ran N tests across K file(s)` (an absent file prints only a note and cannot produce it), and the file-creating task ordering is stated explicitly in the SPEC's scoped-validator-shape note and each done-when. The full gate (OB-16 / AC13) keeps `bun run test` (tsc + whole suite); no validator weakened. Verified: zero residual `bun run test test/` cells in SPEC/ACCEPTANCE | `7aeab75d` |
| RP1-F2 | plan | low | plan | a0ed8357905ab4763fe15273ebfd086a7fd5b1e4116261dab8fff3e8e4cb70a6 | PE-011 cites `src/extension/index.ts:96-104` as where `agentic-workflow-settings` is registered. The registration is `registrar.registerCommand(SETTINGS_COMMAND, …)` in `src/extension/factory.ts:92`, with the `knownCommands` guard at `factory.ts:79`; `index.ts:96-104` is the console wiring (`runSettingsConsole({…})` inside the `settings:` surface callback). The row's substantive claim is verified — commands register by name, and a second `aw-settings` registration with the same handler is a pointer that leaves config keys and `routePath` unaffected — the source-and-location cell is imprecise by one hop. | `grep -n "SETTINGS_COMMAND\|registerCommand\|knownCommands" src/extension/factory.ts` → :79, :92; `index.ts:96-104` shows the `runSettingsConsole` wiring; registration path `index.ts:64-71 → factory.ts:92` | resolved | Re-verified by the author at repair revision `7aeab75d`: `factory.ts:92` = `registrar.registerCommand(SETTINGS_COMMAND, {`, `factory.ts:79` = the `knownCommands` set, `index.ts:96-104` = the `runSettingsConsole` wiring. PE-011's claim and source-and-location cells corrected in SPEC `### Planning evidence` | `7aeab75d` |
