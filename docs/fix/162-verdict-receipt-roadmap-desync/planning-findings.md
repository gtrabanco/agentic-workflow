# Planning findings — fix-162 (plan stage)

Ledger contract: `skills/pre-execution-review/references/LEDGERS.md` §3. Reviewers
append; the author (`plan-fix`) resolves. Every row below is bound to plan snapshot
`ae671fc6954fff1c20f34c3a7fc2430585afaef532f2ea93f1f1f03c22e29d5f`
(source revision `6efe9b54690148245d603203afd06688fd27c8af`, receipt
`rp-fix162-20260906-001`). `route:` inside the claim cell records the review's
classification (fix-now / replan-in-unit / decision-required / proposal / ignore).

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| PF-1 | plan | medium | plan | ae671fc6954fff1c20f34c3a7fc2430585afaef532f2ea93f1f1f03c22e29d5f | PE-008's route-ceiling enumeration is false at its own bound revision: `node scripts/check-skill-context.mjs --routes` reports ten exceedance lines across **five** routes (`design-feature:repair`, `plan-feature:scaffold`, `plan-fix:issue`, `review-plan:default`, `review-spec:default`), not the two PE-008 names (`review-plan:default`, `review-spec:default`). O9 ("the two route ceilings this unit touches return to green"), Decision 7, and P5 task 8's re-basis instruction ("growth source named (`fix/162`)") are cut on that false baseline, while O9's and AC8's validator (`--routes` → exit 0) requires all five green. The unit also grows the shared `POLICY.md` reference (§4/§5/§8 additions), a component of four of the five red routes, so "the two routes it touches" understates the unit's own blast radius. An executor honouring Decision 7 cannot reach exit 0 (three routes left red, root suite `check-skill-context.test.mjs` stays red, AC8 fails at P6); one honouring P5 task 8 literally would declare `fix/162` the growth source of pre-existing red it did not cause — a false declaration under the budgets file's own "name the growth source" rule. The decision the plan never cuts: which of the three pre-existing red routes are resolved by trim and which by a declared re-basis naming their **true** growth source. Checks hit: L2, P12, P10 (uncut mid-execution decision on the unit's own gate). route: fix-now (re-cite PE-008 with the true five-route baseline, split grown vs pre-existing red; widen O9/Decision 7/Affected docs to own the exit-0 endpoint explicitly with the honest per-route mechanism) | `node scripts/check-skill-context.mjs --routes` at a `b18fb612` worktree AND at HEAD → 10 exceedance lines / 5 routes, measured values identical at both revisions (plan-feature:scaffold 20994/1491, plan-fix:issue 23646/1712, review-plan:default 15661/1009, review-spec:default 14101/955, design-feature:repair 23894/1614); route component tables list `pre-execution-review` in plan-feature:scaffold, plan-fix:issue, review-plan:default, review-spec:default; SPEC.md PE-008 + O9 + Decision 7 + P5 task 8 + Affected docs; `docs/workflow/SKILL_CONTEXT_BUDGETS.json` re-basis rule; `node --test scripts/check-skill-context.test.mjs` → fail 1 at HEAD | resolved | Owner instruction (2026-09-06): the pre-existing route-ceiling policy — slim the skills or refactor them, and where ceilings should sit long-term — is #176's scope, so the plan need not own it; the factual defect is repaired at root cause: PE-008 re-cited with the true five-route baseline (re-run at `b18fb612` worktree + HEAD), O9 and P5 task 8 reworded so the two grown routes return green by trim and the three pre-existing shared-component ceilings rise via the budgets file's own declared re-basis naming `fix/162` (the unit grows the shared `pre-execution-review` reference component of those routes), Decision 12 records the routing | ar-162-2 |
| PF-2 | plan | medium | plan | ae671fc6954fff1c20f34c3a7fc2430585afaef532f2ea93f1f1f03c22e29d5f | Symptom (c) is re-homed in prose only while the machine contract keeps sanctioning plan-stage `needs-design`: `VERDICTS_BY_STAGE.plan = ["plan-review-pass", "plan-review-fail", "needs-design"]` (`packages/agentic-workflow-schema/src/pre-execution-contract.ts:127-131`), enforced by the `verdict-stage-matrix` projection rule (`pre-execution-contract.ts:608-618`) that validates a PLAN-stage receipt carrying `needs-design` as well-formed, and pinned by the schema suite (`test/pre-execution-receipt.test.mjs:47` pins the list; `:90`/`:95` pin `["plan", "needs-design", true]`). The SPEC never mentions the surface (grep `VERDICTS_BY_STAGE\|verdict-stage-matrix\|pre-execution-contract` over SPEC.md → 0); no phase, task, or obligation owns it; AC3's validator stops at `skills/review-plan/`, so execution passes green with the contradiction live — against the unit's own thesis ("the persisted state machine is the only arbiter") and its own standard for (a), where prose-only enforcement is exactly the defect being fixed. PE-011 covers only the flat `PRE_EXECUTION_VERDICTS` enum via `review-spec`'s must-name surface, not the stage map. The SPEC's Out-of-scope sentence ("the verdict enum (`needs-design` stays, emitted by `review-spec`)") and Decision 4 argue the flat enum only. No historical plan-stage `needs-design` receipt exists in this repository (`grep -rn "Verdict: needs-design" docs/` → 0), so aligning the map breaks no persisted receipt — but the choice (align `VERDICTS_BY_STAGE` + its red-first pins, or keep it with the two-layer emission/validation semantics argued in the evidence) is an uncut engineering decision. Failure state "machine-valid plan-stage needs-design receipt" has no scenario and no validator. Checks hit: L5, P11, P12. route: fix-now (SPEC amendment: name the surface, cut the decision explicitly, and if aligning — add the schema edit + `pre-execution-receipt.test.mjs` pin updates to P1 red-first with an owning obligation) | `packages/agentic-workflow-schema/src/pre-execution-contract.ts:127-131` and `:608-618` (read this review); `packages/agentic-workflow-schema/test/pre-execution-receipt.test.mjs:47,90,95`; `grep -c "VERDICTS_BY_STAGE\|verdict-stage-matrix\|pre-execution-contract" docs/fix/162-verdict-receipt-roadmap-desync/SPEC.md` → 0; `grep -rn "Verdict: needs-design" docs/` → 0 hits; SPEC.md Out of scope + Decision 4 + PE-011 + AC3 path list | resolved | Root-cause repair (ar-162-2): P1 gains the schema task — `VERDICTS_BY_STAGE.plan` drops `needs-design` (stays in the `spec` row + flat `PRE_EXECUTION_VERDICTS`), `pre-execution-receipt.test.mjs` pins flipped red-first (list `:47`, matrix `:95`), new obligation O11 + frozen AC11 own it with a fail-capable validator, Out of scope + Decision 11 record the deliberate machine-grammar change, PE-013 evidences the map and the zero-legacy-receipts grep, Rollback updated (a revert restores the permissive map; nothing historical breaks) | ar-162-2 |
| PF-3 | plan | medium | plan | f42bd42edb36b810a89284bf778dfef4de0146c74d8d0f5246e7e67e02165329 | P1 task 4 and the Testing section cite a red-first "stage-map list" pin at `packages/agentic-workflow-schema/test/pre-execution-receipt.test.mjs:47` that does not exist: `:47` is the line `"plan-review-pass", "plan-review-fail", "needs-design",` inside the **flat `PRE_EXECUTION_VERDICTS`** deepEqual (assert opens `:45`), which the same task declares immutable ("it stays in the … flat `PRE_EXECUTION_VERDICTS`"; Testing: `pre-execution-docs`/`pre-execution-quality` "stay untouched (the flat enum … keep[s] the token)"). No suite asserts `VERDICTS_BY_STAGE` directly anywhere — the map's only pins are behavioral (matrix rows `:88–:97`; the plan-stage case at `:95`) plus `normative-drift`'s adaptive src-parsing — so an executor following the pointer literally edits the one assertion the task forbids touching (the flat enum that PE-011's must-name coverage and two untouched suites protect), discoverable only after a confusing red suite. PE-013's citation cell repeats `:47` among "the schema suite pins it" without the flat-enum qualifier (L2 note). Same batch should tighten PE-013's literal grep: `grep -rn "Verdict: needs-design" docs/` now returns 2 at HEAD — both self-quotes from this unit's own artifacts (PF-2's row, PE-013's citation cell); the substantive claim re-verified true (all 15 receipt verdict lines in `docs/` are spec-review-*/plan-review-*, zero needs-design). route: fix-now (re-point the red-first flip at the matrix row `:95` — ideally adding a NEW explicit `VERDICTS_BY_STAGE.plan` deepEqual pin red-first, the durable list pin the plan currently lacks — and relabel/drop `:47` in P1 task 4, Testing, and PE-013's cell; tighten PE-013's grep to a receipt-discriminating form in the same edit) | `sed -n '40,50p' packages/agentic-workflow-schema/test/pre-execution-receipt.test.mjs` → flat-enum assert `:45-:48`, needs-design at `:47`; `grep -rn "VERDICTS_BY_STAGE" packages/agentic-workflow-schema/test/ scripts/` → only `scripts/normative-drift.test.mjs:159-166,545,1118` (parses the map from src, adaptive); matrix rows `:88-:97` with `["plan", "needs-design", true]` at `:95`; SPEC.md P1 task 4 + Testing §Verdict-stage map + PE-013 cell; `grep -rn "Verdict: needs-design" docs/` → 2 self-quote hits vs 15 receipt verdict lines enumerated → 0 receipts (rp-fix162-20260906-002 at `81ebdd7d`) | open | — | — |
| PF-4 | plan | medium | plan | f42bd42edb36b810a89284bf778dfef4de0146c74d8d0f5246e7e67e02165329 | AC10's changelog sub-check (`grep -c "4\.1\.0" CHANGELOG.md CHANGELOG.es.md` → each ≥ 1) already passes at the pre-fix bytes: both changelogs carry two historical `4.1.0` rows for other packages (CHANGELOG.md `:211` 2026-09-03, `:442` 2026-07-31; CHANGELOG.es.md `:213`/`:444`), none for the schema package — the check passes on no-op and cannot fail, so it cannot prove the schema package's 4.1.0 row landed. L5: "a validator that passes on no-op is a finding"; review-plan guardrail: "a validator that cannot fail" blocks PASS. The real requirement lives in P1 task 6 (one row each in the "Companion npm packages" tables) and AC10's read-verified half — but the frozen manifest's runnable bar is already green before any work, so a missing schema changelog row (the exact bilingual/versioning hygiene this repo hard-rules) sails through AC10. route: fix-now (ACCEPTANCE.md AC10 + SPEC O10 validator cell: anchor the grep to the schema row's discriminating context — e.g. `grep -c "agentic-workflow-schema.*4\.1\.0"` or scope it to the Companion-npm-packages table — so it is red at pre-fix bytes and green only after the row lands; re-run every cited validator against pre-fix bytes in the same batch to confirm fail-capability) | `grep -n "4\.1\.0" CHANGELOG.md CHANGELOG.es.md` → 4 historical rows, 2 each, none schema-package; `grep '"version"' packages/agentic-workflow-schema/package.json` → 4.0.1; ACCEPTANCE.md AC10 row; SPEC.md O10 row (rp-fix162-20260906-002 at `81ebdd7d`) | open | — | — |
