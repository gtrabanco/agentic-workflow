# Planning findings — fix-166 (plan stage)

Ledger contract: `skills/pre-execution-review/references/LEDGERS.md` §3. Reviewers
append; the author (`plan-fix`) resolves. Every row below is bound to plan snapshot
`debd8046aaec7bca56df0c18e5a12b98d1d2211b617575bb704f9dd8507f606a`
(source revision `74e3adc3f3ede536b1ca1415fac0bbb59a704127`, receipt
`rp-fix166-20260904-001`). `route:` inside the claim cell records the review's
classification (fix-now / replan-in-unit / decision-required / proposal / ignore).

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| PR-1 | plan | low | plan | debd8046aaec7bca56df0c18e5a12b98d1d2211b617575bb704f9dd8507f606a | AC6/O11's flip validator can only pass on an **unbackticked** status cell: the frozen regex `grep -cE "\[#166\]\(…issues/166\) \| pi-0850-baseline-refresh \| done · \[#" docs/fix/README.md` matches literal `\| done · \[#` only, but the index's modern-format done rows (#159, line 17; #161, line 19) write the backticked `` `done` · [#pr] ``, and the row being flipped currently carries backticked `` `pending` ``. Following the index's own convention fails the frozen AC at P5 close-out (mid-execution amendment ceremony, fix/157 precedent); satisfying the validator lands a cell inconsistent with both sibling done rows. The SPEC's P5 task-8 inline-code target (`done · [#<pr>](<pr-url>)`) prescribes the deviating bytes, so the plan is internally consistent but externally conflicting. route: fix-now (amend AC6 in SPEC.md + ACCEPTANCE.md and O11's validator to a backtick-tolerant pattern, e.g. `\| \`?done\`? · \[#`, or explicitly sanction the unbackticked cell as this row's convention) | `grep -cE "\[#161\]\(https://github.com/gtrabanco/agentic-workflow/issues/161\) \| finding-verification-loop-removal \| done · \[#" docs/fix/README.md` → 0; raw bytes `docs/fix/README.md:17,19` = `` `done` · [#160/#163] ``, `:20` = `` `pending` ``; ACCEPTANCE.md AC6 + Commands; SPEC.md O11 row + P5 task 8 | resolved | AC6's frozen regex made backtick-tolerant in both artifacts (SPEC Acceptance AC6 row + ACCEPTANCE.md AC6 row + Commands): the done cell now matches `` `?done`? `` — probed live: 1 match on #161's backticked done row (`docs/fix/README.md:19`), 2 on synthesized backticked + unbackticked post-flip rows, 0 on the current `` `pending` `` row; SPEC P5 task 8 now prescribes the backticked `` `done` · [#<pr>](<pr-url>) `` cell (the #159/#161 sibling convention); O11's claim names the convention | ar-166-2 |
| PR-2 | plan | low | plan | debd8046aaec7bca56df0c18e5a12b98d1d2211b617575bb704f9dd8507f606a | The required-failure-state ledger covers **suite-red only**: O12/PE-012 stop the unit before P3 when `bun run test` is red, but a **failed manual smoke observation** (any of the six `- SMOKE` rows recording a failing surface on pi 0.85.x) has no failure state, no stop rule, and no validator — P3's note refresh is conditional only in P2's layer prose ("the P3 note edit depends on this outcome"), and neither O7, PE-005's binding, nor `## Rules that must never be violated` forbids refreshing the note when the suite is green but the smoke is red. A green-suite/red-smoke run ships a note whose parenthetical claims surfaces the smoke refuted (violating PE-005's trust contract) with no obligation breached. route: fix-now (extend O12 + PE-012 to "suite red **or any smoke observation failed**" with the same stop-before-P3 rule; state the failure semantics in O6/AC2's outcome requirement) | SPEC.md O12 row + PE-012 row (suite-scoped text); P2 layer line (prose-only dependency); `## Rules that must never be violated` bullet 1 (suite-scoped); ACCEPTANCE.md quality floor bullet 3 (suite-scoped); AC2 counts rows, not outcomes | resolved | O12 re-anchored P1/task-2 → P2/task-3 (the last pre-P3 checkpoint) and extended to "a red suite on 0.85.x or any failed smoke observation" with the same stop-before-P3 rule; PE-012 extended identically; failure semantics stated in O6 + AC2 (SPEC + ACCEPTANCE) with the `outcome: pass` / `outcome: fail` row grammar ("a fail row is a required stop state, never a sanitizable detail"); SPEC Rules bullet 1, ACCEPTANCE quality-floor bullet 3, P2 layer line + task 3, and Operational risks bullet extended to match | ar-166-2 |
| PR-3 | plan | info | plan | debd8046aaec7bca56df0c18e5a12b98d1d2211b617575bb704f9dd8507f606a | The root cause (nothing ties the baseline note's refresh to pi releases) is evidenced and disclosed but its systemic repair is owned by nobody: #164's proposed gate covers package version bumps, not baseline refreshes, so after this fix the drift recurs at the next pi release. route: proposal (extend #164's gate scope or open a small issue for a pi-release baseline trigger; owner decision — the SPEC discloses the gap honestly in Root cause + Out of scope + Observability, so no SPEC change is required in this unit) | SPEC.md `## Root cause` ("the gap stays open until a future unit owns it"), `### Out of scope` (#164 bullet), `## Observability` ("the next baseline re-verification re-reads the note"); issue #166 fetched live (`Dependencies: None blocking; pairs naturally with the next package release`); issue #164 open | resolved | Carried as proposal per the review's own route ("no SPEC change is required in this unit"): the SPEC discloses the gap in Root cause / Out of scope / Observability; a systemic-repair entry (extend #164's gate scope or a new tracking issue) is an owner decision — planning performs no forge writes (evidence-grounding guardrail: no issue creation, ever) | ar-166-2 |

Repair cycle 2 (2026-09-04, author `plan-fix`): PR-1 + PR-2 repaired as one
batch and PR-3's routing recorded at artifact revision `ar-166-2`. Rows keep
their original `snapshot-digest` cells (all bound to snapshot
`debd8046aaec7bca56df0c18e5a12b98d1d2211b617575bb704f9dd8507f606a`); the next
`review-plan` re-review binds a fresh snapshot over the repaired bytes.

Re-review cycle 2 (2026-09-04, receipt `rp-fix166-20260904-002`, snapshot
`5b7e8d84efd21c668621fa7b32e3131b40d20893be72853ccca73ef953bc40c9`, source
revision `24c20e23a3431729d4a6a3428f5faac41cfabd14`, verdict
`plan-review-pass`): no new findings opened. PR-1 and PR-2 resolutions verified
against the repaired bytes — PR-1: AC6 validator backtick-tolerant in both
artifacts, P5 task 8 prescribes the backticked cell (probes: 1 on #161's real
backticked done row, 1 on each synthesized backticked/unbackticked post-flip
row, 0 on the current `pending` row); PR-2: O12 re-anchored (P2/task 3) and
extended to any failed smoke observation with the failure semantics in O6/AC2
and Rules/quality-floor/P2-layer/Operational-risks aligned. PR-3 stays a
resolved proposal. Ledger clean: no open rows, no dismissed rows. Rows above
keep their original snapshot-digest cells (bound to
`debd8046aaec7bca56df0c18e5a12b98d1d2211b617575bb704f9dd8507f606a`).
