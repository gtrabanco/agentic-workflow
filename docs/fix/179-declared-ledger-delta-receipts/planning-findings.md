# Planning findings — fix-179 (plan stage)

Ledger contract: `skills/pre-execution-review/references/LEDGERS.md` §3. Reviewers
append; the author (`plan-fix`) resolves. Every row below is bound to plan snapshot
`bb685fa872cb78591894a7f0e9b41a75ed1af90d10931465b0a63cb5140cb95b`
(source revision `ec59c8fb877fa9ef7a0bd995df2d7fe32d4a565c`, receipt
`rp-fix179-20260907-001`). `route:` inside the claim cell records the review's
classification (fix-now / replan-in-unit / decision-required / proposal / ignore).

Author resolution (plan-fix repair batch 1, 2026-09-07): all five rows are
`resolved` by the SPEC/ACCEPTANCE rewrite on `fix/179-declared-ledger-delta-receipts`;
every `resolving-artifact-revision` cell names the declared id `fix179-rev2` of
that write. Claim and evidence cells are untouched — only the three resolution
columns changed.

Author resolution (plan-fix repair batch 2 — PF-6 only, 2026-09-07): the PF-6 row
is `resolved` by the SPEC/ACCEPTANCE rewrite on
`fix/179-declared-ledger-delta-receipts`; its `resolving-artifact-revision` cell
names the declared id `fix179-rev3` of that write. FY-1 (info, class
environment) is untouched — its route is the user's environment decision, not
this author's (LEDGERS.md §3), and the user's fix landed in commit `bb524559`.

Cycle-3 review (2026-09-07, receipt `rp-fix179-20260907-003`, plan snapshot
`0ed772ce775faec8f5180b4f26249579c5b53186f0bfa7ae5caaee82e379be0f`): no new
finding rows — the review returned `plan-review-pass` with an empty findings set
(0 findings, 0 material open). PF-1…PF-6 remain `resolved` as recorded above;
FY-1 remains `open` at `info` (immaterial; the reviewer does not resolve rows).

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| PF-1 | plan | medium | plan | bb685fa872cb78591894a7f0e9b41a75ed1af90d10931465b0a63cb5140cb95b | Three planning-evidence rows cite `source-and-location` pointers that do not match the repository at `ec59c8fb`, although every claim's substance verifies true at the actual locations: **PE-002** cites `scripts/pre-execution-snapshot.mjs:117-131` for "`contentRevision` over newest touching commit" — actual: the RS3(b) docstring is `:168-171`, `contentRevision()` is defined at `:182` and used at `:258` (the cited range is the context list + `parseArgs`); **PE-005** cites `scripts/pre-execution-snapshot.mjs:247-253` for "the sensor refuses invented codes (guard on `PRE_EXECUTION_FRESHNESS_CODES`)" — actual: the guard is `:377` (`if (!PRE_EXECUTION_FRESHNESS_CODES.includes(reasonCode))`; import `:71`, publish `:471`; the cited range is the RS14 feature-parent guard); **PE-006** cites `packages/agentic-workflow-schema/test/pre-execution-docs.test.mjs:106-113` for "the docs test derives the README table from the schema" — actual: `test("AC8: every freshness and vocabulary code is documented")` at `:161-164` derives the guarded set from `schema.PRE_EXECUTION_FRESHNESS_CODES` and asserts both READMEs document each code (the cited range is the export-pattern test). L2 requires every Engineering claim to resolve to its evidence row; P12 requires cited `path:line` to match the repository. An executor or reviewer re-validating on the cited lines reads the wrong code and "verifies" the wrong thing. Checks hit: L2, P12. route: fix-now (correct the three pointer cells; the claims stand unchanged — each verified true at its corrected location) | `sed -n '117,131p' scripts/pre-execution-snapshot.mjs` (contexts + parseArgs, not contentRevision) vs `grep -n contentRevision` → `:168` docstring, `:182` def, `:258` use; `sed -n '247,254p'` (RS14 parent guard) vs `grep -n PRE_EXECUTION_FRESHNESS_CODES` → `:71/:377/:471`; `sed -n '106,113p' test/pre-execution-docs.test.mjs` (AC8 export-pattern comment) vs `grep -n FRESHNESS` → `:161-164` derivation test; same results at `a22130ba` (PE's observed revision) and HEAD — `ec59c8fb` touched only the new unit folder + fix-index row | resolved | PE-002/PE-005/PE-006 pointers corrected to the verified locations (`contentRevision` docstring `:168-171` · def `:182` · use `:258`; freshness guard `:377` with import `:71` + publish `:471`; docs-test AC8 derivation `:161-164`) and the Root-cause §1 + Rules §1 pointers corrected with them (both cited `:117-131`/`:247-253` too); claims unchanged — each re-verified at HEAD `25a0764c` before the edit; corrected rows' observed-revision rotated to `25a0764c` | fix179-rev2 |
| PF-2 | plan | medium | plan | bb685fa872cb78591894a7f0e9b41a75ed1af90d10931465b0a63cb5140cb95b | P3's change set cannot reach its own done-when: it amends only `skills/pre-execution-review/references/LEDGERS.md` (adding `execute-phase:amendment-rows + fold-findings:amendment-rows` to the `progress` ownership row), but `scripts/ledger-ownership.test.mjs` requires the map **and both template projections** to agree in both directions (`:16-18` "the map and each template projection must agree"; `:146-151` `declaredOwnerScan` over both templates; `:446` loads `docs/features/_TEMPLATE/LEDGERS.md` + `docs/fix/_TEMPLATE/LEDGERS.md`), and both templates carry a `progress` row with the ownership cell P3 amends (feature template `:16`; fix template's `progress` row). With the two template files absent from P3's task list and from Impact/Modules, the ledger-ownership suite stays red and P3's done-when (`→ exit 0, fail 0`) is unreachable as written; an executor honouring "keep tasks literally" stalls, one improvising works outside the frozen plan. Checks hit: P9 (phase cut incomplete — the task set does not cover the phase's own done-when). route: fix-now (add both template-projection edits to P3's tasks and to Impact/Modules) | `scripts/ledger-ownership.test.mjs:8-18,55-57,146-151,220,446` (two-direction agreement, both template paths, both-direction failure modes); `docs/features/_TEMPLATE/LEDGERS.md:16` (progress row); `docs/fix/_TEMPLATE/LEDGERS.md` (progress row, same ownership cell); fix-179 SPEC P3 bullets (only `skills/pre-execution-review/references/LEDGERS.md` named) + Impact/Modules list | resolved | P3's LEDGERS task now mirrors the ownership-cell change into `docs/features/_TEMPLATE/LEDGERS.md` + `docs/fix/_TEMPLATE/LEDGERS.md` (their `progress` rows) and both files joined Impact/Modules — the ledger-ownership two-direction agreement is reachable and P3's done-when (`→ exit 0, fail 0`) is coverable by its own task set | fix179-rev2 |
| PF-3 | plan | low | plan | bb685fa872cb78591894a7f0e9b41a75ed1af90d10931465b0a63cb5140cb95b | The sensor test matrix is internally inconsistent and its forged-declaration case is under-bound in the frozen acceptance: the P2 red-first bullet, In-scope §2, and the Testing section all say "**six**-case matrix" while the same P2 bullet enumerates **seven** cases (a)–(g), and Impact's "Detection lead time" says "**seven**-case"; AC2's validator enumerates fail-closed cases "(b)–(e)" and omits **(g)** (forged declaration row naming a commit that never touched the path → `stale-source-revision`), which is bound only by AC2's "any out-of-class delta" clause and the P2 task bullet — no acceptance criterion names it, and nothing pins the case into the suite beyond that bullet. The scenario exists and is mapped (P2 task → sensor suite), so there is no coverage hole for a faithful executor; the defect is wording drift plus a frozen-manifest enumeration an executor reading only ACCEPTANCE could follow literally and drop (g) — a no-weakening case then left unpinned. Checks hit: L5, P11. route: fix-now (normalize the count to seven everywhere; add (g) to AC2's enumeration — `plan-fix` re-freezes ACCEPTANCE in the repair batch) | fix-179 SPEC: In-scope §2 ("six-case matrix"), P2 red-first bullet ("six-case matrix" over (a)–(g)), Testing ("six-case matrix", six labels listed, forged omitted), Impact ("seven-case matrix"); ACCEPTANCE.md AC1 ("declared cases (a)/(f)") + AC2 ("fail-closed cases (b)–(e)"); P2 bullet case (g) text | resolved | Count normalized to seven in In-scope §8, the P2 red-first bullet and the Testing section (label list gains the forged case); AC2's outcome enumerates the forged declaration row naming a commit that never touched the path and its validator names fail-closed cases (b)–(e), (g) — `plan-fix` re-froze ACCEPTANCE in this repair batch as the finding's route directs | fix179-rev2 |
| PF-4 | plan | low | plan | bb685fa872cb78591894a7f0e9b41a75ed1af90d10931465b0a63cb5140cb95b | The declaration-row format P2's parser consumes and P3's `LEDGERS.md` freeze documents are never cross-validated: P3's done-when (ledger-ownership + bounded-delivery suites + POLICY/LEDGERS/mandate greps) does not re-run the sensor suite, and no test asserts that the frozen row shape (`- <ISO-8601 date> · commit <40-hex> · <bound path> · <tick-flip \| obligation-status> · <changed-line-count> · by <execute-phase P<k> \| fold-findings F<k>>`) is the shape `classifyLedgerStateDelta` actually parses. Both derive from the same plan text, so a faithful executor aligns them; a drifted executor ships a parser and a documented format that disagree, and the failure surfaces only when a real unit's declaration goes unparsed (fail-closed → `stale-source-revision`) at first use — the failure state "parser ↔ frozen format drift" has no scenario and no validator that runs it. Checks hit: L5, P11. route: fix-now (cheap pin: P3's done-when re-runs the sensor suite, or the P2 suite asserts the exact frozen row shape byte-for-byte) | fix-179 SPEC: P2 bullet ("parse the unit's `## Ledger-state amendments` rows from `progress.md`… format frozen in `LEDGERS.md`, P3"), P3 bullet (row shape), P3 done-when command list (no sensor suite); LEDGERS.md §3 (row shapes are frozen contracts) | resolved | P2's red-first bullet pins the suite's declaration-row fixtures byte-for-byte to the frozen row shape the plan freezes (P3 → `LEDGERS.md`) and P3's red-first pins bullet adds the byte-exact `LEDGERS.md` shape pin — parser ↔ frozen-format agreement now has both a scenario and a validator in both directions | fix179-rev2 |
| PF-5 | plan | low | plan | bb685fa872cb78591894a7f0e9b41a75ed1af90d10931465b0a63cb5140cb95b | O6's mapping deviates from the one-phase/one-task rule: Phase cell `P2+P3+P4`, Task cell "all text + tests". No work is orphaned — the row's five-suite validator decomposes exactly onto the P2 (sensor/attribution/quality), P3 (ledger-ownership/bounded-delivery) and P4 (discipline/audit-pr) done-whens plus the P6 full gate — but L4 requires exactly one phase and one task, and the span leaves ambiguous which phase owns flipping O6's `status` to `verified` (an obligation-status cell — exactly the amendment class this unit defines, so the plan's own class depends on unambiguous ownership). Checks hit: L4. route: fix-now (name the single accountable phase for the status flip — P6's full gate — or split O6 into per-phase rows) | fix-179 SPEC Obligations O6 row (`P2+P3+P4`, "all text + tests"); AC4; P2/P3/P4/P6 done-whens (the exact suite decomposition); LEDGERS.md:61 (status vocabulary) | resolved | O6 normalized to one accountable phase/task: Phase `P6`, Task `full verification gate re-run (Hardening & PR)`; validator unchanged — the per-phase pin updates stay P2/P3/P4 red-first tasks and the status flip owns exactly one phase | fix179-rev2 |
| PF-6 | plan | medium | plan | 18da59f645be6273ac001647cbf8d86d37f30902f5c6efc44f1e48b1627a0558 | The empty-diff refusal Rules §2 names has **no scenario and no validator**: Rules §2 (`:210-213`) and In-scope §2 (`:104`) say "an empty-diff revision movement (bytes reverted, a commit between) is never `declared-delta` — the class requires actual in-class byte deltas, so an older PASS still cannot resurrect", but P2's seven-case matrix (a)–(g) never exercises it, no existing pin covers a **commit-touched** bound path with identical bytes at the CLI level (the closest case, `RS13: a rotated authoring revision with identical bytes is its own dimension` at `scripts/pre-execution-sensor.test.mjs:308-314`, rotates via the explicit `--artifact-revision` override, lands at precedence 7 `stale-artifact-revision` with `changedPaths: []`, and never enters the new classification branch, which runs only at precedence 6 — `scripts/pre-execution-snapshot.mjs:430-434`, fired precisely when a commit moved `contentRevision`, diff-blind); `pre-execution-quality.test.mjs:328` pins the void-staleness answer, not the new branch's reasonCode; the parity test's sole divergence case is `impossible-timeline` (`pre-execution-attribution.test.mjs:160`). A vacuous classification (`every()` over zero hunks reads as "all tick-flip") plus declaration rows naming both the tick commit and the revert commit answers `declared-delta` — the frozen row shape has no net-effect field to refuse a revert and a revert's own diff is tick-flip class — and P4 then treats that answer as a **non-blocking audit warning**, so the older PASS resurrects through the warning path, exactly what Rules §2 forbids. A faithful implementation of the Rule text passes every validator, and a no-op implementation of the refusal also passes every validator — a validator that cannot fail for a failure state the SPEC names. Checks hit: L5, P11. route: fix-now (add matrix case (h) — declared tick flips + declared revert commits, total diff empty → `stale-source-revision`; pin the non-vacuity — ≥ 1 in-class hunk — in the P2 task wording alongside the Rule) | fix-179 SPEC: Rules §2 `:210-213`, In-scope §2 `:104`, P2 matrix (a)–(g) `:362-370`, Testing rows (no empty-diff case); ACCEPTANCE AC2 "fail-closed cases (b)–(e), (g)"; `scripts/pre-execution-sensor.test.mjs:308-314`; `scripts/pre-execution-snapshot.mjs:428-434`; `scripts/pre-execution-quality.test.mjs:328,143-149`; `scripts/pre-execution-attribution.test.mjs:160`; frozen row shape (P3 task) | resolved | P2's matrix gains case (h) — declared tick flips + declared revert commits (both named by declaration rows), total diff since the recorded revision empty → `stale-source-revision`, never `declared-delta` — and the count is normalized to eight everywhere (In-scope §8, Impact detection-lead-time, P2 red-first bullet, Testing); the P2 `classifyLedgerStateDelta` wording pins the non-vacuity — ≥ 1 in-class hunk, Rules §2's mutate-and-revert sentinel, a vacuous `every()` over zero hunks refused — so a no-op implementation of the refusal now fails case (h) while a faithful one stays green; O2's affected cell names the empty-diff movement and its validator notes case (h); AC2's outcome + validator enumeration gain the empty-diff revert movement + case (h) — ACCEPTANCE re-frozen by plan-fix per the route; PE-014 added with the four verified code locations, observed-revision rotated to HEAD `bb524559` | fix179-rev3 |
| FY-1 | plan | info | environment | 18da59f645be6273ac001647cbf8d86d37f30902f5c6efc44f1e48b1627a0558 | Unrelated uncommitted worktree dirt (not one of this unit's bound artifacts), both pre-existing this review: **(1)** `docs/features/ROADMAP.md` carries rows 34–36 added/renumbered **and the worktree copy is truncated** — 24 lines of committed legend content are absent (the `done` status bullet, the single-owner transition paragraph, the entire `## Conventions` section; file ends without a trailing newline: 80 worktree lines vs 104 at HEAD) — the same edit the prior receipt disclosed as "rows 34–36 renumbering + status-legend trim", verified more extensive than that note; **(2)** `docs/LOGS.md` carries the authoring session's uncommitted log entry for the draft commit `ec59c8fb` (mtime 11:20:06, written after this review's session started). Neither touches fix-179's plan bytes, but fix-179's P6 pending-docs check (`git status --porcelain -- docs/` → empty) cannot pass while they sit unresolved — the gate stops execution and the owner decides (restore the truncated ROADMAP content and commit rows 34–36 properly or revert; commit or discard the LOGS entry). Not material to the plan verdict; recorded for visibility. route: user decision (environment) | `git diff docs/features/ROADMAP.md` at review time; `git show HEAD:docs/features/ROADMAP.md | wc -l` → 104 vs worktree 80; HEAD retains `## Conventions` (grep → 1), worktree ends at the `in-progress` bullet; `git diff docs/LOGS.md` (append-only log entry, content matches the draft commit); prior receipt rp-fix179-20260907-001 environment note | open | — | — |
