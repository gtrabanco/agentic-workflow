# Planning findings — fix/182-deterministic-receipts-and-pr-hygiene

Ledger contract: `pre-execution-review/references/LEDGERS.md` §3. Reviewers append
rows; the stage author resolves them.

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| PF-1 | plan | medium | plan | 2f2471eeb5a2d49c93e8076bdcd7960378bd9462d93f1e0801e392ab8fb9b4d0 | The SPEC claims the `emit` moved-head refusal is covered by an "`emit` refusal test in `scripts/review-receipt.test.mjs`" (`### Failure scenarios` and `## Testing`), and AC1 requires `emit` to refuse a PR head that differs from the reviewed head — but no test in the suite (or anywhere under `scripts/`/`packages/`) invokes the `emit` command or its head-comparison path. AC1's validator (`node --test scripts/review-receipt.test.mjs` → exit 0) therefore passes while a declared required outcome is unexercised: a validator that passes for the wrong reason. | `docs/fix/182-deterministic-receipts-and-pr-hygiene/SPEC.md:319` (failure-scenario row names the non-existent test), `SPEC.md:298` (`## Testing` claims "moved-head refusal" coverage), `docs/fix/182-deterministic-receipts-and-pr-hygiene/ACCEPTANCE.md:7` (AC1 outcome), `scripts/review-receipt.test.mjs` (20 `test(` cases; zero `"emit"` invocations — the only emit-adjacent assertions are `validateEmitOptions` malformed-args and the pure `shouldPost` decision), `scripts/review-receipt.mjs:314-327` (the refusal is an `emit`-branch `throw` behind `forgePr` → `gh`, with no `--comments-json`-style fixture seam) | resolved | This write adds P1 task 3 — `scripts/review-receipt.test.mjs` pins the `emit` moved-head refusal through a fake `gh` on `PATH` — and re-points the `### Failure scenarios` row and `## Testing` at it; O1's required evidence names the test. The row was supplied with evidence, never narrowed. | ar-fix182-20260917-plan-2 |
| PF-2 | plan | low | plan | 2f2471eeb5a2d49c93e8076bdcd7960378bd9462d93f1e0801e392ab8fb9b4d0 | The SPEC's failure-scenario row for "A draft PR at the merge gate" names "AC4 (`hygiene --apply` runs the PR-ready repair only)", and AC4 requires `--apply` to run only the `gh pr ready` repair — but no test invokes the `hygiene --apply` CLI branch. The pure `hygieneFromState` producing the single repair is tested, so the behavior is structurally pinned by the underlying function; the scenario still names a validator that does not exercise the CLI path it cites. | `docs/fix/182-deterministic-receipts-and-pr-hygiene/SPEC.md:319` (row), `docs/fix/182-deterministic-receipts-and-pr-hygiene/ACCEPTANCE.md:10` (AC4 outcome), `scripts/audit-pr-receipt.test.mjs:210-216` (only `hygieneFromState` for the draft case), `scripts/audit-pr-gate.mjs:257-268` (the untested `--apply` repair loop) | resolved | This write adds P1 task 7 — `scripts/audit-pr-receipt.test.mjs` pins the `hygiene --apply` CLI on a fixture repo with a fake `gh` — and AC4's validator plus the `### Failure scenarios` row now name that test; O4's validator and required evidence were strengthened. | ar-fix182-20260917-plan-2 |
| PF-3 | plan | medium | plan | 5a9c450f79c9b74a45d92c4de5681f3e74ce1473d38bb8afb08d38ee614886af | The F3 outcome is not validated. O10 and `### P5` require the unit's plan-stage preflight to be `current` again at the merged head and the superseded `Pre-execution gate` line (`progress.md:36`) to stop presenting its `current: true` as standing, but the validator asserts `verdictIsPass` — the newest receipt's verdict, independent of currency (`scripts/pre-execution-snapshot.mjs:483`: `receipt.verdict === "plan-review-pass"`) — and neither the P5 done-when nor the `### Failure scenarios` row 11 checks that the line was corrected. A live `verify` already prints `verdictIsPass: true` with `current: false, digestMatches: false`, so the phase can pass while the stale claim stands — the same validator-passes-for-the-wrong-reason class as PF-1/PF-2. | `docs/fix/182-deterministic-receipts-and-pr-hygiene/SPEC.md` `### Obligations` O10 validator + `### P5` done-when + `### Failure scenarios` row 11; `scripts/pre-execution-snapshot.mjs:483`; live `node scripts/pre-execution-snapshot.mjs verify --stage plan --unit fix-182 --dir docs/fix/182-deterministic-receipts-and-pr-hygiene --unit-kind fix` → exit 4, `current:false`, `digestMatches:false`, `verdictIsPass:true`; `docs/fix/182-deterministic-receipts-and-pr-hygiene/progress.md:36` (the stale line O10/P5 must correct). | open | — | — |
| PF-4 | plan | low | plan | 5a9c450f79c9b74a45d92c4de5681f3e74ce1473d38bbafb08d38ee614886af | The F1 outcome is only half-validated. `### Failure scenarios` row 8, O11 and `### P6`'s done-when assert the new token is present (`grep -c "audit-pr-gate.mjs comment" skills/audit-pr/SKILL.md` → ≥ 1) but never that the hand-assembled `gh pr comment --body-file` instruction was removed, so the scenario the row names — "a merge-ready box that still instructs the hand-assembled comment path" — is not exercised: a half-done P6 (add the runtime token, leave the old line) passes the validator while the exact F1 defect survives in the box. | `docs/fix/182-deterministic-receipts-and-pr-hygiene/SPEC.md` `### Failure scenarios` row 8 + `### Obligations` O11 validator + `### P6` done-when; `docs/fix/182-deterministic-receipts-and-pr-hygiene/ACCEPTANCE.md` AC7 ("no prose receipt/comment path remains as the box"); `skills/audit-pr/SKILL.md:46-48` — at `c3649687`, `grep -c "audit-pr-gate.mjs comment"` → 0 and `grep -c "gh pr comment"` → 1. | open | — | — |
