# Planning findings — fix-200-over-budget-fixture-stale-guard

Reviewers append rows; only the stage's author resolves them (LEDGERS contract).
`info` is the only immaterial severity. `dismissed` requires counter-evidence
that falsifies the finding; `resolved` names the closing artifact revision.

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| RP1-F1 | plan | high | plan | 04092de89c9bb4a3505c748ab9d02995e75d03601ee19e6b99066ec6ad6c608f | P1's done-when and OB-1/AC1's validator (`bun scripts/check-skill-context.test.mjs` → exit 0, final line `PASS context checker: …`) are **unreachable at the draft revision**: a second, currently-masked red assertion exists in the same test file — `assert.equal(routeJson.status, 0, …)` at `scripts/check-skill-context.test.mjs:112` fails because 15 route-budget exceedances (7 `execute-phase:*` routes; `review-change:adversarial/default-backend/default-web/synthesize` × estimate+lines) exist at `7fa68074`. The suite stops at the over-budget fixture assertion (line 35) first, so the route red was invisible to the planner's reproduction; the plan's scope explicitly excludes route re-basis/trimming, so **no phase of this unit can make AC1 pass as written**. | `bun scripts/check-skill-context.mjs --routes` → exit 1, 15 `^- ` exceedance lines (`execute-phase`, `review-change` only; `skills/` + `docs/workflow/SKILL_CONTEXT_BUDGETS.json` byte-clean at `7fa68074`); `bun`/`node scripts/check-skill-context.test.mjs` both exit 1 at `test.mjs:112` once the fixture assertion is unmasked (observed 2026-09-08) | resolved | Cycle-2 repair re-scopes the validator to the reachable fixture pin exactly as this receipt prescribed: AC1/OB-1/P1 done-when is now `grep -cF "over-budget reference should fail closed"` → `0` with the pasted suite tail showing the remaining failure at the route red (`test.mjs:112`), and the route red is declared out of scope with owner + disposition paths (PE-009/PE-010, AC6). Reachability re-verified at the repair revision: the suite aborts at `test.mjs:112` once the fixture assertion passes, so the grep is satisfiable and falsifiable (a fixture regression re-emits the pinned line) | c1ff890e469a4514c4a96698300b947800c50949 |
| RP1-F2 | plan | medium | plan | 04092de89c9bb4a3505c748ab9d02995e75d03601ee19e6b99066ec6ad6c608f | PE-004's regression-scope claim is falsified: "the remaining nine+ failure fixtures … all pass" and "every currently-passing assertion in the suite keeps its exact input" — the routes block (`test.mjs:107–112`) does **not** pass at the draft revision. The SPEC's single-red-cause framing (Goal, Detected in, Impact: "the suite is red on main" at the over-budget fixture) is true but incomplete: the suite is red for **two independent reasons**, and the route red's root cause is unattributed (suspected growth landed with #199's skill rewrites; no CI runs the gate — PE-008 — so nothing caught it). | PE-004 row vs `test.mjs:112` failure reproduced at `7fa68074`; `--routes` exit 1 output; RP1-F1 | resolved | PE-004 re-cited honestly in the cycle-2 repair: Goal, Detected in, PE-004 and Impact now frame **two independent reds** (stale fixture + pre-existing route red at `test.mjs:112`), the single-red-cause framing is gone, and PE-009/PE-010 record the route red with its owner (repo owner via triage/#176) and both disposition paths. Exceedance count re-verified at the repair revision: `bun scripts/check-skill-context.mjs --routes` → exit 1, 15 exceedance lines (7 `execute-phase:*` estimate; 8 `review-change:*` estimate+lines) | c1ff890e469a4514c4a96698300b947800c50949 |
| RP1-F3 | plan | medium | environment | 04092de89c9bb4a3505c748ab9d02995e75d03601ee19e6b99066ec6ad6c608f | Unsanctioned implementation edit appeared **mid-review** (unstaged, `scripts/check-skill-context.test.mjs` mtime 2026-09-08T20:46:39Z): the over-budget fixture body was changed from `"x".repeat(10_000)` to a hard-coded `"x".repeat(22_400)` plus a comment. This pre-executes P1 task 1 **without a current plan PASS** (the gate this skill owns) and does **not** satisfy OB-2/AC2 — the size is a constant, not derived from `docs/workflow/SKILL_CONTEXT_BUDGETS.json` at runtime; the added comment also misattributes 2800 as "the default ceiling" (defaults is 2200; 2800 is the `review-change` override). Committed bytes at `7fa68074` are untouched (P12 binds at sourceRevision). | `git status --porcelain` shows ` M scripts/check-skill-context.test.mjs` (not staged, not committed); `git diff` shows only the fixture-body swap; file mtime 20:46:39Z inside this review's window (started 20:44Z) | resolved | Resolved source-side in the cycle-2 repair, as this receipt directed (never in the plan): the unstaged edit was reverted via `git checkout`, restoring `scripts/check-skill-context.test.mjs` to its `7fa68074` bytes (PE-012); re-verified at the repair revision: `git status --porcelain -- scripts/` → empty and the fixture still reads `"x".repeat(10_000)`, so `execute-phase` implements P1 task 1 from the manifest | c1ff890e469a4514c4a96698300b947800c50949 |
