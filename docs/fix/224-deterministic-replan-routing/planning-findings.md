# planning-findings — fix-224 (deterministic-replan-routing)

One stage-aware table; rows are appended by reviewers and resolved only by the
stage's author through its own route. Shape:
`finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision`.

| finding-id | stage | severity | class | snapshot-digest | claim | evidence | status | resolution-evidence | resolving-artifact-revision |
|---|---|---|---|---|---|---|---|---|---|
| RP-224-1 | plan | high | plan | 843327b1a50c1359308efe8182638166eb8a15020aa8d451e69ad3c8710be566 | AC5's dogfood target does not exist at the bound revision: `docs/features/37-phase-lint-script/` is absent from HEAD `56355c2c` and from `b5358666`, the 1065-line ledger lives only on unmerged `feat/37-phase-lint-script`, and roadmap row 37 at HEAD is `idea` — so `node scripts/unit-route.mjs 37-phase-lint-script` → `route: replan` (ACCEPTANCE AC5, the Observability silent-failure alarm, the S1 dogfood leg) cannot pass where acceptance is verified; `Depends on: none` hides a real ordering dependency on unit 37's merge | `git ls-tree HEAD docs/features/ --name-only \| grep -c 37` → 0; `git ls-tree b5358666 …` → 0; `git show feat/37-phase-lint-script:docs/features/37-phase-lint-script/review-findings.md \| wc -l` → 1065; `docs/features/ROADMAP.md:47` row 37 = `idea` | resolved | AC5 re-targeted to a committed fixture copied from `feat/37-phase-lint-script@e1e282c5`'s ledger (SPEC AC5 + PE-013, validator `node --test scripts/unit-route.test.mjs`); live variant recorded post-merge/informational in `### Testing` and `Depends on`; Observability alarm re-worded to the fixture (SPEC `## Observability`) | fix-224-artrev-0002 |
| RP-224-2 | plan | medium | plan | 843327b1a50c1359308efe8182638166eb8a15020aa8d451e69ad3c8710be566 | PE-010's `observed-revision: b5358666` misattributes the `wc -l → 1065` cost measurement — the cited path does not exist at that revision; the observation is reproducible only against `feat/37-phase-lint-script`'s tree, so a `proven` row's provenance is wrong (L2 evidence integrity) | SPEC `### Planning evidence` PE-010 vs `git ls-tree b5358666 docs/features/37-phase-lint-script` → empty; PE-011 in the same table honestly discloses the cross-branch linter read, proving the cross-branch context was known but not recorded on PE-010 | resolved | PE-010's source now cites `git show feat/37-phase-lint-script:…\| wc -l → 1065` with `observed-revision: feat/37-phase-lint-script@e1e282c5` and an explicit cross-branch disclosure mirrored from PE-011; re-verified at the repair revision (`git show feat/37-phase-lint-script:docs/features/37-phase-lint-script/review-findings.md \| wc -l` → 1065) | fix-224-artrev-0002 |
| RP-224-3 | plan | medium | plan | 843327b1a50c1359308efe8182638166eb8a15020aa8d451e69ad3c8710be566 | The replan destination survives outside the converged set: `skills/review-implementation/references/CLASSIFY.md:96-98`, `skills/review-implementation/SKILL.md:100-101` and `skills/review-change/references/OUTPUT_AND_GUARDRAILS.md:27-29` each send `replan-in-unit` to `execute-phase` with no planner and no router, and `docs/workflow/FEATURE_WORKFLOW.md:280-282` + `PORTABLE_PROMPT.md:135-136` (+ ES siblings) repeat it — AC7's unqualified clause ("no surface sends a replan row to the executor or the fold") is false at HEAD while its validator (grep over four files) passes, and PE-007's own regression-scope grep returns ~45 hits that the fold does not cover (L5 closure + F3 scope) | `sed -n '96,98p' skills/review-implementation/references/CLASSIFY.md` → "…then `execute-phase` on the same branch"; `sed -n '100,101p' skills/review-implementation/SKILL.md`; `sed -n '27,29p' skills/review-change/references/OUTPUT_AND_GUARDRAILS.md`; `sed -n '280,282p' docs/workflow/FEATURE_WORKFLOW.md`; `sed -n '135,136p' docs/workflow/PORTABLE_PROMPT.md`; `grep -rn "replan-in-unit" skills/ docs/workflow/ --include="*.md"` → ~45 hits | resolved | Convergence widened to the full census (PE-007 re-measured at `48aac035`: 43 hits / 21 files, 14 routing files enumerated in Scope): P4 now converges the eight skill files (incl. `triage-issue/SKILL.md:118`, added beyond the enumeration as it carries the same planner-only sentence as its own reference), new P5 converges the three tutorial pairs, and AC7/OB-4's validator greps all 14 files (SPEC Scope, P4/P5, AC7) | fix-224-artrev-0002 |
| RP-224-4 | plan | low | plan | 843327b1a50c1359308efe8182638166eb8a15020aa8d451e69ad3c8710be566 | The promised sanitizer has no execution owner: Security risks commits the router to "echoes … through a single sanitizer, truncates long cells, and never emits a verbatim ledger line" and S7 points at "the sanitizer pin", but no obligation row, no acceptance criterion and no P1 task implements or pins it — a promised normative behavior that dies silently in the execution ledger (L3/L4) | SPEC `## Security risks` bullet 2 + `### Scenario matrix` S7 vs `### Obligations` OB-1…OB-9 (none names the sanitizer) and `### P1` task list (five tasks, none writes the sanitizer or its pin) | resolved | The sanitizer now has all three owners: OB-10 (authority PE-014 = `pre-execution-review/references/POLICY.md` §7 + Security risks, validator `node --test scripts/unit-route.test.mjs`), AC12 (sanitizer: no verbatim ledger line, long cells truncated), and P1 task 6 writing the sanitizer; S7's validator now names AC12/OB-10 (SPEC Obligations, Acceptance, P1, S7) | fix-224-artrev-0002 |
| RP-224-5 | plan | info | plan | 843327b1a50c1359308efe8182638166eb8a15020aa8d451e69ad3c8710be566 | Citation window drift on the sensor evidence: PE-005 records `grep -n suggested → only :729` (actual `:735`) and PE-006's window `:708-729` (actual `readFixNow` at `:713`, projection push `:735`) — substance identical, same genus as fix-221's PL-1; advisory, no repair required beyond the touch of the next edit | `grep -n "function readFixNow\|suggested_tier" scripts/workflow-status.mjs` at `56355c2c` → `:713`, `:735` | resolved | Touched by this repair as the finding prescribed: PE-005 → `:735`, PE-006 → `:713,735`, and the root-cause prose citation `:708-729` → `:713,735`; re-verified at `48aac035` (`grep -n "function readFixNow\|suggested_tier" scripts/workflow-status.mjs` → `:713`, `:735`) | fix-224-artrev-0002 |
| RP-224-6 | plan | low | plan | 605f53068f5e0d43509750aecfb752fd5afe7d98cc3dbff08b1ae871a7424b35 | The version-bump sweep in the phase ledger is uneven: P4 edits two `review-change` reference files (`PERSIST_AND_DECIDE.md`, `OUTPUT_AND_GUARDRAILS.md`) but no task anywhere in P4/P6 bumps `review-change`'s `version:` or adds its per-skill changelog cell, while the same genus of reference-only change gets a scheduled bump for `workflow-status` (P6 task 6) — CLAUDE.md's "Version every change" rule and the CHANGELOG policy ("per-skill tables are the source of truth") make a reference edit version-worthy (repo precedent: review-change 3.2.1 bumped for prose renumbering; fix-165 patch), so a pinned consumer at `review-change` 3.5.0 would receive changed routing prose with no version signal | SPEC `### P4` tasks 1–2 + `### P6` task list vs `grep -n "^version:" skills/review-change/SKILL.md` → 3.5.0 (no bump task names it); `CLAUDE.md` "Version every change"; CHANGELOG.md versioning policy + 0.4.0 entry (`review-change 3.2.1` for process-step renumbering); contrast P6 task 6 | resolved | resolved — P4's two review-change reference tasks merged into one same-skill task (box 3's ≤8 tasks preserved; P4's fingerprint string unchanged, `P4:docs:8:skills-replan-destination`); new P4 task 8 bumps `skills/review-change/SKILL.md` 3.5.0 → 3.5.1 (patch) and adds its per-skill changelog cell to `CHANGELOG.md` + `CHANGELOG.es.md`; grounded as PE-015 (rule + 3.2.1 precedent + the `version-tables` drift check re-verified at `0bea68fa`), owned by OB-11 + AC13, and P4's done-when extended with the version-signal grep; phases re-linted → P4 PASS (8/8) | fix-224-artrev-0003 |
| RP-224-7 | plan | medium | plan | bcde3ca15fa0f63fd16166833a3674668654715c8a89a6c13796362bbd610065 | OB-15's second half has no executing task: the executed-phase exemption "in the phase contract" is owned by a row whose named task (P11.3) adds only the `replan-findings` changelog cell, and no phase edits `skills/phase-contract/SKILL.md` — so the sole rule owner (rule 3's budget, rule 7's gate, and its "Never relax a rule to let a phase pass" guardrail) never states the exemption that P10 makes `scripts/phase-lint.mjs` skip for ticked phases; the required evidence ("the drift verdict plus the two version cells") is unreachable through P11.3, and because the exemption is keyed on the author-controlled tick marks, a plan that pre-ticks its tasks dodges boxes 3 and 7 with no owner-side rule authorizing it | SPEC `### Obligations` OB-15 (`SPEC.md:198`) vs `### P10`/`### P11` task lists (`SPEC.md:650-672`); `grep -n "phase-contract" docs/fix/224-deterministic-replan-routing/SPEC.md` over the P9–P12 bodies → no task; `skills/phase-contract/SKILL.md:4` (`version: 1.0.3`), `:17` ("Nothing else may define what a valid phase is"), `:43` (rule 3: ≤8 tasks, final 1–10), `:51` (rule 7: no external/manual gates outside hardening), `:85` (guardrail); `scripts/phase-lint.mjs:489-493` (box 3), `:580-590` (box 7 `gh pr`), `:651-657` (`finalCloseOut`); OB-12 (`SPEC.md:195`) repeats the documented+released behaviour while naming only P9.3 | resolved | P11 gained a task for the sole rule owner: `skills/phase-contract/SKILL.md` now states the executed-phase exemption (boxes 3 and 7 only, every other box armed for it, pre-ticking to dodge a check is a defect) and takes its own patch bump; OB-15 was narrowed to the replan contract's documented token and new OB-16 owns the phase-contract rule statement plus the corpus triple whose third case pins the pre-tick dodge; OB-12 dropped its duplicated documented-and-released half; P11 re-linted to PASS (8/8) at `P11:docs:5:replan-path-contract-docs` | fix-224-artrev-0005 |
| RP-224-8 | plan | medium | plan | bcde3ca15fa0f63fd16166833a3674668654715c8a89a6c13796362bbd610065 | `## Depends on` and PE-011/PE-013 assert a repository state that does not hold at the bound revision: unit `37-phase-lint-script` is merged (roadmap row 37 = `done · [#212]`), its directory and `scripts/phase-lint.mjs` exist on the bound tree, and its ledger is 1153 lines — while the section still reads "unmerged at the bound revision … row 37 = `idea` … the unit directory exists only on `feat/37-phase-lint-script`", the live dogfood note still calls the invocation "runnable only after that unit merges", and both PE rows keep `freshness: current`; the same amendment row states "unit 37 is now on `main`" and "`Depends on` unchanged", so the artifact contradicts itself at its own bound revision (P12's dependency-status claim) | `git show 2f801ad3:docs/features/ROADMAP.md \| sed -n 47p` → `done · [#212]`; `git ls-tree 2f801ad3 docs/features/ --name-only \| grep 37` → `docs/features/37-phase-lint-script`; `git cat-file -e 2f801ad3:scripts/phase-lint.mjs` → present; `git show 2f801ad3:docs/features/37-phase-lint-script/review-findings.md \| wc -l` → 1153; SPEC `## Depends on` (`SPEC.md:34-43`), PE-011 (`:174`), PE-013 (`:176`), `## Testing` (`:474`), `## Amendments` artrev-0004 (`:718`) | resolved | `## Depends on` now records that unit 37 is merged (row 37 = `done · [#212]`), that AC5 keeps its committed fixture as the accepted evidence and that the live variant was executed as the informational check; PE-011 re-observed as present on the bound revision through the merge and PE-013 as directory-present with a 1153-line live ledger, both at `0e35b189`, so the artifact no longer contradicts its own amendment row | fix-224-artrev-0005 |
| RP-224-9 | plan | medium | plan | bcde3ca15fa0f63fd16166833a3674668654715c8a89a6c13796362bbd610065 | The re-opened `P8` is not executable or resumable at this branch state, and the receipt closure `P12` is ordered before it: PR #225 is already open and the fix-index row already reads `done · [#225]`, so `P8` task 5 (`gh pr create`) cannot succeed and tasks 3/6/7 have nothing to commit; if they do move the head, `P12`'s end-review and merge-audit receipts (tasks 4 and 6, ordered before `P8` because the ledger's order is the execution order) then describe a non-terminal head — re-opening F21's "no `review-change` receipt at the close-out head". The plan states no idempotent re-entry for the re-opened phase (P6), and P9's "no phase builds a later phase's deliverable early" is strained by closing out receipts before the phase that finalizes the PR | `docs/fix/README.md:30` → `done · [#225](https://github.com/gtrabanco/agentic-workflow/pull/225)`; SPEC re-opened `### P8` tasks 3/5/6/7 (`SPEC.md:693-701`) and `### P12` tasks 4/6 (`SPEC.md:682-687`); ledger order P9–P12 then P8 (`SPEC.md:488-508`); `progress.md` P8 close-out ("PR opened and printed — https://github.com/gtrabanco/agentic-workflow/pull/225", head `a74ee351`); `review-findings.md` F21 | resolved | The re-opened P8 is explicitly idempotent: a re-entry paragraph plus reworded tasks 3/5/6/7 verify the live PR #225 and the already-linked fix-index row and never re-create either; the three receipt tasks (plan-review currency, end review, merge audit) moved from P12 into P8 as tasks 8-10, ordered to run after the ledger is fully ticked so no later SPEC write voids the plan receipt they produce; P8 re-linted at `P8:hardening:10:hardening-pr` (final close-out budget 1-10) and P12 reduced to the ledger work | fix-224-artrev-0005 |
| RP-224-10 | plan | medium | plan | bcde3ca15fa0f63fd16166833a3674668654715c8a89a6c13796362bbd610065 | P10 edits `scripts/phase-lint.mjs` and `scripts/phase-lint.test.mjs` — the shared linter every unit's planning and execution pre-flight runs — but the SPEC's declared surface was not extended: `### In scope` names neither file, `## Impact` lists "the router script, the sensor, their tests" plus docs and `Blast radius` claims only open-row units and the envelope, and `## Rollback` still says the change is "confined to skills prose, one new script with its tests, one sensor emission and documentation" — so the regression scope (F3) and the un-ship accounting (P7/F4) omit the one edit that changes a validator shared by every unit | `grep -n "phase-lint" docs/fix/224-deterministic-replan-routing/SPEC.md` → `:140-150` (Out of scope: root-file layer) and `:650-659` (P10 targets) only, no Scope/Impact/Rollback row; `SPEC.md:105-137` (`### In scope`), `:279-304` (`## Impact`), `:703-710` (`## Rollback`) | resolved | `### In scope` now names `scripts/phase-lint.mjs` and `scripts/phase-lint.test.mjs`; `## Impact` adds the linter to the touched layers and modules and states the narrowed blast radius (boxes 3 and 7 only, and only for a fully-ticked phase; every box armed for every unemitted phase; pre-ticking cannot dodge a box-2 or box-4..8 defect); `## Rollback` names the linter edit and the dead ends a revert restores | fix-224-artrev-0005 |
| RP-224-11 | plan | low | plan | bcde3ca15fa0f63fd16166833a3674668654715c8a89a6c13796362bbd610065 | The scenario matrix was not extended for the replan's new failure categories: a `done` unit mis-routed to the executor (F24), an executed phase retro-blocked (F25) and the bare `status` token (F23) have no scenario row in S1–S8, and S4's validator ("the execute fixture") now covers only the `planned` half of the no-open-row case; P11's named validator (`node --test scripts/normative-drift.test.mjs`) can also pass while OB-15's phase-contract half stays undelivered — it compares newest changelog cell to frontmatter only, i.e. it can pass for the wrong reason | `### Scenario matrix` S1–S8 (`SPEC.md:239-250`); `### P9`–`### P11` (`SPEC.md:636-672`); `ACCEPTANCE.md` AC1 (`:19`) and AC14 (`:32`); `scripts/normative-drift.test.mjs:677-683` (`version-tables`: `newestVersionCell(text, skill)` vs `meta.version`) | resolved | The scenario matrix gained S9 (a finished unit handed to the executor), S10 (a replan re-judging an already-executed phase) and S11 (a status cell carrying its markdown decoration), and S4 now points at S9 for the `done` half; OB-15/OB-16 evidence was made reachable (the drift verdict alone could pass for the wrong reason) and AC14 was extended to cover the owner-side rule statement and re-frozen in `fix-224-artrev-0005` | fix-224-artrev-0005 |
| RP-224-12 | plan | medium | plan | 6394ed8cb51cb15df0bba56eeb5e3af7df7fa2b4df7e74abd565354f053a13bc | The SPEC's `## Acceptance` mirror was never brought along with the frozen manifest, so two bound authorities disagree on the finish line: the SPEC's AC1 still enumerates only the five pre-replan routes with no `close-out` token and no bare-`status` clause, and the SPEC has no AC14, while `ACCEPTANCE.md` AC1 requires six routes (`close-out` for a finished unit) plus the bare status token and AC14 carries the executed-phase exemption. The SPEC therefore contradicts the manifest whose blob it records, and contradicts its own terminal-route behaviour (OB-12, P9 task 3, S9); the `## Status` note also lists re-freezes only through `fix-224-artrev-0004` while recording artrev-0005's blob, and the `## Amendments` table orders `0005` above `0004` | `SPEC.md:214` (AC1, five routes), `SPEC.md:225-226` (table ends at AC13, no AC14), `ACCEPTANCE.md:20` (AC1, six routes plus bare status), `ACCEPTANCE.md:34` (AC14), `ACCEPTANCE.md:36` (owner map says AC14 to OB-14 only, while AC14's own validator also covers the phase-contract rule OB-16 owns), `SPEC.md:740-745` (Status re-freeze list vs recorded blob), `SPEC.md:752-755` (amendment order 0002, 0003, 0005, 0004); `diff` of the `## Acceptance` section between `2f801ad3` and `9cc6bf90` is empty although `ACCEPTANCE.md` was re-frozen (`b89e915f…` to `15e9661f…`); `skills/verification-contract/SKILL.md` "one stable ID per SPEC criterion" | resolved | The SPEC's `## Acceptance` mirror now rotates with the frozen manifest (AC1 lists the six routes and the bare `status`, AC14 exists in both files); the `## Status` re-freeze list names `fix-224-artrev-0005`; and the `## Amendments` rows read chronologically (`0002` -> `0003` -> `0004` -> `0005`) | fix-224-artrev-0006 |
| RP-224-13 | plan | medium | plan | 6394ed8cb51cb15df0bba56eeb5e3af7df7fa2b4df7e74abd565354f053a13bc | The terminal route `P9` must add is a new token (`close-out`, the frozen manifest's AC1) in the router's closed `ROUTES` set, but P9's own gate task cannot pass without editing `scripts/normative-drift.test.mjs` — the test that pins the five-token vocabulary — and no phase task names that file, under an acceptance quality floor that forbids rewriting a validator to manufacture PASS | `scripts/unit-route.mjs:36` (five-token `ROUTES`), `scripts/unit-route.mjs:371` (a route outside the set fails), `scripts/normative-drift.test.mjs:1086-1093` (`assert.deepEqual(names, ["replan", "decision", "fold", "execute", "plan-from-issue"])`), `SPEC.md ### P9` 4 tasks all naming `scripts/unit-route.test.mjs` plus task 4 "re-run `node --test scripts/*.test.mjs` … to exit 0", `ACCEPTANCE.md:20` names `close-out`, `ACCEPTANCE.md` quality floor; reproduced on an exported copy of the bound bytes (`git archive 9cc6bf90` into a temp tree): baseline 17 pass / 0 fail, after adding `"close-out"` to `ROUTES` 15 pass / 2 fail with `AssertionError: the route vocabulary is closed` | resolved | P9 gained a task that extends `scripts/normative-drift.test.mjs`'s closed-vocabulary pin to the six published route tokens, so the drift gate and the router's `ROUTES` agree; P9 re-linted at `P9:config/infra:5:terminal-route-for-finished-unit` | fix-224-artrev-0006 |
| RP-224-14 | plan | low | plan | 6394ed8cb51cb15df0bba56eeb5e3af7df7fa2b4df7e74abd565354f053a13bc | `OB-15`'s documentation half has no failing validator: the row requires that the terminal route token and the bare `status` field be documented in the replan contract, yet its validator is the version-tables drift test only and `### P11`'s done-when greps nothing in `skills/replan-findings/SKILL.md`, so P11 task 1 can be skipped while the row is still recorded `verified` — the residual half of the genus `RP-224-11` raised (its phase-contract half is correctly owned by OB-16's grep) | `OB-15` row (`SPEC.md:204`) behaviour vs its validator, `### P11` done-when (`SPEC.md:689`), `OB-16` (`SPEC.md:205`) carries `grep -q "fully-ticked phase is historical" skills/phase-contract/SKILL.md`, `grep -rn replan-findings scripts/*.test.mjs` gives 0 hits, P4/P5 done-whens grep their named target files; `scripts/normative-drift.test.mjs` version-tables compares the newest per-skill changelog cell against the frontmatter and never reads the contract text | resolved | OB-15's validator is now `grep -q "close-out" skills/replan-findings/SKILL.md && node --test scripts/normative-drift.test.mjs` -> exit 0, a check that fails if the documented block is skipped, and P11's done-when runs the same grep | fix-224-artrev-0006 |
| RP-224-15 | plan | info | plan | 6394ed8cb51cb15df0bba56eeb5e3af7df7fa2b4df7e74abd565354f053a13bc | `PE-005` and `PE-006` keep `freshness: current` while their cited windows and PE-005's "never emitted" assertion no longer hold at the bound revision, because this unit's own executed phases P3 and P6 moved those bytes; the `## Root cause` item-4 window carries the same stale citation | `PE-005` (`SPEC.md:178`) and `PE-006` (`SPEC.md:179`) plus the `## Root cause` item-4 prose (both cite `scripts/workflow-status.mjs:713,735`); at the bound revision `grep -n suggested scripts/workflow-status.mjs` gives `:753`, `:759`, `:1192` and `function readFixNow` is at `:742` | resolved | PE-005 re-observed as the doc/code surface at the bound revision (`next.suggested` assigned at `scripts/workflow-status.mjs:1192`, the pre-fix gap closed by P3's execution) and PE-006's window corrected to `:742,753`; both cite `9cc6bf90` with `freshness: current` | fix-224-artrev-0006 |
| RP-224-16 | plan | medium | plan | 5286729185a902f0315236766f8eca3781d786fdddfbc5961d228a8e9901de02 | `P13`'s obligation `OB-17` has no phase left that can move it from `planned` to `verified`: the replan appended `P13` and a fresh final `P14`, but `P14` carries only the canonical close-out chain (gate, pending-docs, fix-index/PR link, the three receipts) and `P13`'s four tasks never touch the ledger — while `P12`, whose task 1 owned the reconciliation, is already executed and historical, so `audit-pr`'s obligation-closure gate blocks the terminal head on a `planned` row | `SPEC.md:206` (OB-17 `status: planned`); `SPEC.md:757-769` (`P13` tasks); `SPEC.md:771-792` (`P14` tasks); `SPEC.md:723-732` (`P12` done-when "every `OB-` row … carries `verified` or `n/a`" + task 1 the reconciliation); `skills/audit-pr/references/02_CLOSURE_AND_SCOPE_GATES.md:101-105` ("Any `planned`, `in-progress`, blank, or `deferred` row is BLOCKED, naming the ids"); `git show 0e35b189:…/SPEC.md` → OB-1…OB-11 still `planned` after P1–P7 executed, flipped only by `P12`'s explicit task at `da9d0df2` | resolved | `P13` gained task 5, which reconciles the `OB-17` row to `verified` with its validator's evidence in the phase receipt, so the terminal obligation-closure gate has an owner for the row this replan added; P13 re-linted at `P13:config/infra:5:archived-unit-state` | fix-224-artrev-0009 |
| RP-224-17 | plan | medium | plan | 5286729185a902f0315236766f8eca3781d786fdddfbc5961d228a8e9901de02 | `## Impact`'s blast-radius statement was not rotated by the replan and is now false: it reads "The router is additive and read-only; the envelope gains one optional array. No existing route changes for a unit with no plan-routed row", while `P13` changes exactly that route — a known unit with no open rows and a gone status source moves from `route: execute` to `route: historical` (OB-17, AC1, `P13` fixture pins), so the regression-scope disclosure denies the behaviour the replan adds | `SPEC.md:310-315` (the blast-radius sentence) vs `SPEC.md:757-769` (`P13`) and `SPEC.md:206` (OB-17); `scripts/unit-route.mjs:384` (`status` computed before the route decision) and `:403-409` (the `status === "done"`/fall-through `execute` branch); `grep -n historical scripts/unit-route.mjs` → 0 hits at the bound revision | resolved | `## Impact` now states that `P13` changes one route for a unit with no plan-routed row (`execute` becomes `historical` when the status source is gone), names it as the archived state `F32` requires, and keeps `execute` for an unfinished unit | fix-224-artrev-0009 |
| RP-224-18 | plan | low | plan | 5286729185a902f0315236766f8eca3781d786fdddfbc5961d228a8e9901de02 | The replan added a new failure category — a known unit with no open row whose status source is gone, mis-routed to the executor — without a scenario row: the matrix still ends at S11, while the equivalent F23/F24/F25 categories each gained a row (S9/S10/S11) in the previous cycle, and `S4`'s "the unit exists with no open row" validator covers only the `planned`/`done` halves | `SPEC.md:248-266` (S1–S11, `S4`, no archived row); `SPEC.md:757-769` (`P13`); `SPEC.md:206` (`OB-17`); `SPEC.md:215` (AC1's `historical`); `RP-224-11` resolution ("The scenario matrix gained S9 …, S10 … and S11") | resolved | The scenario matrix gained `S12` (a unit whose status source is gone and which carries no open row is sent to the executor instead of the archived state), pointing at the `P13` pins | fix-224-artrev-0009 |
| RP-224-19 | plan | low | plan | 5286729185a902f0315236766f8eca3781d786fdddfbc5961d228a8e9901de02 | The manifest half of the AC1 vocabulary amendment did not land: `ACCEPTANCE.md` AC1 still enumerates six route tokens (`replan`, `decision`, `fold`, `execute`, `close-out` and `plan-from-issue`) with `historical` appearing only in the trailing `where` clause, while the SPEC's `## Acceptance` mirror enumerates seven and the manifest's own freeze note claims "AC1 gains the seventh route token (`historical`, F32)" | `ACCEPTANCE.md:16-18` (freeze note) and `:23` (AC1, six-token list) vs `SPEC.md:215` (AC1, seven-token list); `diff` of the two AC1 rows → only the `` `close-out` `` vs `` `close-out`, `` + `` `historical` `` difference; both rows carry `node --test scripts/unit-route.test.mjs` | resolved | The manifest's `AC1` now enumerates the seven route tokens, matching the SPEC mirror and the freeze note; the manifest was re-frozen (blob `13580b459b1d93e8e80176ecf47311ca165b68fa`) | fix-224-artrev-0009 |
| RP-224-20 | plan | low | plan | 5286729185a902f0315236766f8eca3781d786fdddfbc5961d228a8e9901de02 | `planning-findings.md`'s `RP-224-6` row violates the ledger's own shape: it carries 11 cells against the 10-column definition, so its `status` cell is the reviewer's `verified: P4/P6 contain no review-change bump task…` prose, its `resolution-evidence` cell is the bare word `resolved` and its `resolving-artifact-revision` cell is the resolution prose — the row's status is outside the closed `open \| resolved \| dismissed` vocabulary the ledger contract and the plan-review L6 check require | `planning-findings.md:14` header; the `RP-224-6` row (11 cells; all 14 other rows 10); `skills/pre-execution-review/references/LEDGERS.md:97`; `skills/review-plan/references/CHECKS.md` L6; `scripts/pre-execution-quality.test.mjs:562` (a material row whose `status !== "resolved"` → `PLAN-REVIEW-FAIL` in the pure model) | resolved | The `RP-224-6` row was rebuilt to the canonical ten-field shape: its status cell is `resolved` and the verification prose moved into the resolution-evidence cell | fix-224-artrev-0009 |
