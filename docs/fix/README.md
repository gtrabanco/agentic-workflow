# Active fixes

Index of in-progress and pending fixes. Merged fixes are removed from
this table — history lives in git log + closed issues.

## Status legend

- `pending` — SPEC drafted, branch not yet open
- `in-progress` — branch open, work ongoing
- `done` — built, PR open, awaiting merge (merge state lives in the forge — same
  meaning as the roadmap's `done`); the entry is removed only **after** the PR merges

## Active

| Folder | Topic | Status | Depends on | Issue |
| ------ | ----- | ------ | ---------- | ----- |
| `38-schema-package-republish` | Republish schema package (bump 1.0.1 → 1.0.2 so the stranded #44 README reaches npm) | done · [#48](https://github.com/gtrabanco/agentic-workflow/pull/48) | — | [#38](https://github.com/gtrabanco/agentic-workflow/issues/38) |
| `37-bilingual-human-docs` | Spanish `.es.md` siblings for all human-readable docs (`docs/workflow/*.md` + schema package README) + on-next-touch sync convention; also folds in the model-routing recommendation revision (GLM-5.2 → €200 plan, quota-aware per-task ladders) | done · [#50](https://github.com/gtrabanco/agentic-workflow/pull/50) | — | [#37](https://github.com/gtrabanco/agentic-workflow/issues/37) |
| `52-workflow-status-envelope-hardening` | Emit-time hardening of the `workflow-status` envelope: turn-contract assertions for a non-bare, correctly-staged `next.recommended` + `next.tier` derivation + always-emit on follow-ups; schema shape reminders (`blockers[].scope`, `dependencies.unmet`) so it passes `validateEnvelope()`; mechanical (exception-proof) `product_audit` trigger; unknown-status → `idea` mapping; **new** untriaged open-issue backlog field (`detail.untriaged_issues`) | done · [#53](https://github.com/gtrabanco/agentic-workflow/pull/53) | — | [#52](https://github.com/gtrabanco/agentic-workflow/issues/52) |
| `51-plan-feature-replan-loop` | Break the infinite re-plan loop: `plan-feature` already-planned short-circuit (`planned`/`in-progress`/`done` → hand off, never re-scaffold) + `--next` retargets to `defined` + turn-contract assertion the `defined→planned` write landed; `plan-feature-scaffold` re-reads the row to confirm the write; `workflow-status` no-progress guard (hint recommended `/plan-feature <slug>` yet `<slug>` still `defined` → `workflow_observations` note) | done · [#55](https://github.com/gtrabanco/agentic-workflow/pull/55) | — | [#51](https://github.com/gtrabanco/agentic-workflow/issues/51) |
| `54-triage-disposition-labels` | Close the untriaged-detection spoof gap: make `triage-issue` the sole owner/writer of the `postponed` / `promoted` / `wontfix` disposition labels (applied on verdict, triage+-permission-gated, same injection-safety invariant as its urgency labels), and reframe `workflow-status` step 11 so the label is the authoritative triaged signal with the `VERDICT:` comment kept as a backward-compat legacy fallback | done · [#58](https://github.com/gtrabanco/agentic-workflow/pull/58) | — | [#54](https://github.com/gtrabanco/agentic-workflow/issues/54) |
| `63-next-block-verdict-branching` | Branch `review-change` step 11's `→ Next:` block on the `Decision` verdict: a FAIL block recommends folding the fix-now findings (gate green, commit + push, re-review) with `/audit-pr` demoted to a table-clean sub-bullet, a PASS block keeps `/audit-pr — merge gate`; restore the fixed-output contract weak models copy verbatim (multi-line, no `·`-joined prose) per CLAUDE.md "Checklists over heuristics" | done · [#68](https://github.com/gtrabanco/agentic-workflow/pull/68) | — | [#63](https://github.com/gtrabanco/agentic-workflow/issues/63) |
| `67-nan-ladder-design-feature-row` | Add the missing **Product definition** row for `design-feature` to the README "Running on NaN.builders" *Preference ladders per task* table (Mimo-first, merge-gate-class ladder) + a condensed rationale paragraph; mirror both in `README.es.md` (bilingual sync) | done · [#69](https://github.com/gtrabanco/agentic-workflow/pull/69) | — | [#67](https://github.com/gtrabanco/agentic-workflow/issues/67) |
| `65-fold-findings-skill` | New strict skill `fold-findings`: takes a `review-change`/`audit-pr` findings ledger and truly fixes each fix-now finding (frozen classification, definition-of-fixed `✓`-list, forbidden `✗`-list closing every escape hatch, one-commit-per-finding, `FOLDED`/`DISPUTED→triage`/`BLOCKED` verdicts) + minimal hand-off wiring (`review-change` FAIL `→ Next`, `execute-phase` fold-cycle reference) + registration (model-routing, SKILLS EN/ES, README tables, CHANGELOG) | done · [#70](https://github.com/gtrabanco/agentic-workflow/pull/70) | — | [#65](https://github.com/gtrabanco/agentic-workflow/issues/65) |
| `64-phase-atomicity-lint` | Mechanical phase-atomicity lint (fixed 8-box checklist) authored once in the fix SPEC template and quoted in the feature template + both emitters (`plan-feature-scaffold`, `plan-fix`) + a pre-flight guard in the consumer (`execute-phase`) that STOPS on a non-atomic phase (`--force`-overridable, logged); turns the existing "one layer/concern, no open decisions" heuristic into binary boxes weak models can't misread | done · [#75](https://github.com/gtrabanco/agentic-workflow/pull/75) | — | [#64](https://github.com/gtrabanco/agentic-workflow/issues/64) |
| `81-legacy-spec-phase-lint-carveout` | Restore fix #64's backward-compat promise: add a legacy-SPEC carve-out to `execute-phase`'s phase-lint pre-flight guard so a SPEC with no `## Phases` section skips the guard entirely (no lint, no STOP) and runs the legacy single-pass flow — mirroring the exemption already stated in the *Workflows* section; the guard's behavior for phased SPECs is unchanged | done · [#83](https://github.com/gtrabanco/agentic-workflow/pull/83) | — | [#81](https://github.com/gtrabanco/agentic-workflow/issues/81) |
| `80-plan-fix-multi-issue-semantics` | Define `plan-fix`'s multi-issue contract: `/plan-fix <n> [<n2> …]` — single-number unchanged; multiple numbers run a fixed 4-box shared-root-cause checklist → all tick = ONE unit (primary = lowest number, `Closes #<n>` per issue), any fail = verbatim refusal + split, invalid input = usage error; plus argument-shape doc parity (SKILLS EN/ES, README) and `bump-skill` | done · [#84](https://github.com/gtrabanco/agentic-workflow/pull/84) | — | [#80](https://github.com/gtrabanco/agentic-workflow/issues/80) |
| `78-audit-pr-closure-integrity` | Add a mechanical **closure-integrity gate** to `audit-pr`'s merge-readiness contract: three grep-level boxes (closure block present · zero blank rows · every non-`n/a` row maps to an acceptance criterion) with a trigger-based legacy carve-out — block present → blocker; block absent → dated `design-debt: closure absent` warning (never blocks); feature PRs only; `n/a: <reason>` always passes. Retrofit trigger cross-referenced from `design-feature` upsert; `→ Next:` routes to `/design-feature <slug>`; contract restated in README/SKILLS/FEATURE_WORKFLOW EN+ES; `bump-skill`; GOLDEN_FIXTURE smoke | done · [#85](https://github.com/gtrabanco/agentic-workflow/pull/85) | — | [#78](https://github.com/gtrabanco/agentic-workflow/issues/78) |
| `66-scope-bleed-guardrail` | Close the scope-export laziness hatch across three skills via one **descope amendment** invariant: `execute-phase` descope guard (discovered-vs-descoped test → descope STOPs for a user-approved dated `## Amendments` entry before any follow-up issue) · `audit-pr` scope-bleed gate (issues born during the unit that map to an unmet criterion with no amendment → BLOCKER) · `product-audit` recurrence signal (≥ 2 consecutive units exporting scope → planning-quality finding routed to #64). Docs parity (README/SKILLS EN+ES); `bump-skill`; GOLDEN_FIXTURE smoke. Guardrail #4 (`workflow-status` envelope) deferred to #79 | done · [#88](https://github.com/gtrabanco/agentic-workflow/pull/88) | — | [#66](https://github.com/gtrabanco/agentic-workflow/issues/66) |
| `76-adversarial-weak-fleet-usability` | Make `review-change --adversarial N` executable by hand-orchestrated weak-model fleets: model-condition recommendation checklist (replaces the L/sensitive-only trigger), fixed N ladder (2 default · 3 on security/single-family · >3 discouraged), index-assigned reviewer roles (R1 correctness · R2 security · R3 SPEC-coverage) with a role-is-priority-not-scope guardrail, a `--merge` fusion mode + single-source reviewer/merge contracts with a forbidden-drop list (≥1 threshold restated), Portability paste blocks, a once-per-unit cadence anchor (explicit `#77` boundary), `→ Next:` wiring (`review-change` step 11 + `execute-phase` end-of-unit hand-off), and a NaN adversarial ladder row (EN+ES). Envelope exposure deferred to `#79`; `bump-skill`; GOLDEN_FIXTURE smoke | done · [#91](https://github.com/gtrabanco/agentic-workflow/pull/91) | — | [#76](https://github.com/gtrabanco/agentic-workflow/issues/76) |
| `86-triage-open-unit-awareness` | Give `triage-issue` **open-unit awareness** (consumer-side complement of #66). Adds a scope-membership step (before classification, fixed checklist: enumerate open units → both-sides-quoted SPEC/phase comparison) and a fifth verdict **`fix-in-unit <unit>`** resolving member issues on the open unit's own branch — via a provenance-marked `review-findings.md` ledger append so `fold-findings` runs one queue (#86), or an incremental replan naming the exact command (`design-feature`/`plan-feature`/#66 `## Amendments`) or a scope-bleed restore (#87). Non-member issues route byte-for-byte unchanged. `fold-findings`/`workflow-status` verified as read-only consumers (marker inside `route`, envelope/npm untouched). Docs parity (ISSUE_WORKFLOW/SKILLS/README EN+ES); `bump-skill`. **Shared unit for #86 + #87** | done · [#90](https://github.com/gtrabanco/agentic-workflow/pull/90) | — | [#86](https://github.com/gtrabanco/agentic-workflow/issues/86), [#87](https://github.com/gtrabanco/agentic-workflow/issues/87) |
| `77-review-checkpoint-cadence-triggers` | Replace `execute-phase`'s every-2-phases review-checkpoint rule (miscalibrated once #64 makes phases atomic) with three mechanical, independently-checkable triggers — **layer boundary** (next phase declares a different `Layer:`), **accumulation** (`git diff --stat <baseline>..HEAD` > 400 lines OR > 8 files since the last-reviewed SHA), **sensitivity** (auth/payments/destructive-migration/secrets/CI phase). Specifies the `Last reviewed: <sha>` marker in `progress.md` (sole writer `execute-phase`, absent → `git merge-base` fallback). Checkpoints stay single-reviewer + skippable; the mandatory terminal review and its adversarial hook are untouched (boundary reserved by #76). Cadence docs parity (SKILLS/FEATURE_WORKFLOW/PORTABLE_PROMPT/MIGRATION/README EN+ES); `bump-skill`; GOLDEN_FIXTURE smoke. Envelope exposure deferred to #79 | done · [#92](https://github.com/gtrabanco/agentic-workflow/pull/92) | [#64](https://github.com/gtrabanco/agentic-workflow/pull/75) (merged) | [#77](https://github.com/gtrabanco/agentic-workflow/issues/77) |
| `93-ship-roadmap-cadence-triggers` | Cross-reference `ship-roadmap`'s autopilot L/sensitive review-floor cadence to `execute-phase`'s three named triggers (layer boundary / accumulation / sensitivity) instead of the stale "every 2 phases" phase-counter #77 already replaced elsewhere; the `--adversarial 2` hard floor and the deliberate non-alignment with `review-change`'s own advisory cadence stay untouched | done · [#94](https://github.com/gtrabanco/agentic-workflow/pull/94) | [#77](https://github.com/gtrabanco/agentic-workflow/issues/77) | [#93](https://github.com/gtrabanco/agentic-workflow/issues/93) |
| `71-skill-registration-parity` | Close PR #70's registration/documentation-parity drift and make it non-recurring: register `fold-findings` in `.claude-plugin/plugin.json` (installs under `Agentic Workflow`, not `General`); name `fold-findings` (primary) + `execute-phase`'s fold cycle (fallback) in both READMEs' non-Claude equivalence ladder and drop the stale "GLM-5.2 everywhere" tier (EN+ES); restore `model-routing.yml` to alphabetical; declare per-surface ordering (machine → alphabetical, narrative → flow order) in `CLAUDE.md`; add two `bump-skill` lint rules (`plugin.json` ↔ `skills/` parity for `user-invocable` skills + machine-surface alphabetical). Shared unit for #71 + #72 + #73; #74 (discovery-exclusion) stays separate | done · [#95](https://github.com/gtrabanco/agentic-workflow/pull/95) | — | [#71](https://github.com/gtrabanco/agentic-workflow/issues/71), [#72](https://github.com/gtrabanco/agentic-workflow/issues/72), [#73](https://github.com/gtrabanco/agentic-workflow/issues/73) |
| `79-workflow-status-driver-signals` | Expose the 2026-07-17 design round's sensor side in the `workflow-status` envelope: per-unit `review` (`last_checkpoint_sha`/`unreviewed_diff`/`terminal_done`/`adversarial`), `closure.state`, `issues_born{n,with_descope_amendment}` under schema-opaque `detail`, plus a typed `next.suggested[]` (`{command,trigger,source_skill}`, single-sourced from each owning skill's trigger — #77/#76/#78/#65). Same-PR schema mirror (`next.suggested` typed + `envelope.schema.json` + 2.0.0→2.1.0 + `npm test`); `orchestration-envelope` snippet. **Folds in #89**: widen `audit-pr`'s descope gate to also match issues linked from any `## Amendments` row (closes the no-slug/no-number backstop miss) — same `## Amendments`-log provenance the `issues_born` counter needs. `bump-skill`; bilingual READMEs | done · [#98](https://github.com/gtrabanco/agentic-workflow/pull/98) | [#66](https://github.com/gtrabanco/agentic-workflow/pull/88), [#76](https://github.com/gtrabanco/agentic-workflow/pull/91), [#77](https://github.com/gtrabanco/agentic-workflow/pull/92), [#78](https://github.com/gtrabanco/agentic-workflow/pull/85) (all merged) | [#79](https://github.com/gtrabanco/agentic-workflow/issues/79), [#89](https://github.com/gtrabanco/agentic-workflow/issues/89) |
| `82-phase-lint-heading-wrap` | Join the two wrapped `Phase-lint` sub-section headings in `docs/fix/_TEMPLATE/SPEC.md` and `docs/features/_TEMPLATE/SPEC.md` onto single physical lines so CommonMark renders them as headings, not heading + orphaned paragraph (introduced by fix #64/PR #75; folded into a normal template touch per the `postpone` verdict). Docs-only, XS, no skill/schema/test | done · [#99](https://github.com/gtrabanco/agentic-workflow/pull/99) | — | [#82](https://github.com/gtrabanco/agentic-workflow/issues/82) |
| `74-bump-skill-discovery-exclusion` | Stop `npx skills add` from discovering/offering repo-internal `bump-skill` (fix #40 only changed `user-invocable`/manifest, which gate the post-install menu, not discovery). Primary-source finding: the `skills` CLI honours `metadata.internal: true` in frontmatter (verified in `dist/cli.mjs` 1.5.16 + 1.5.19; manifest is additive, no `.skillsignore`) — mooting the relocate/hook options. Add `metadata.internal: true` to `bump-skill` only (the sole `user-invocable: false` skill absent from `plugin.json`; the 13 shipped review-pack sub-skills stay discoverable); record the finding + option-2 decision in `decisions.md`; add a 7th `bump-skill` lint (conjunction rule) + `CLAUDE.md` note; `bump-skill` self-bump. No path change (REPLICATE/model-routing untouched) | done · [#96](https://github.com/gtrabanco/agentic-workflow/pull/96) | — | [#74](https://github.com/gtrabanco/agentic-workflow/issues/74) |

---

## Conventions

- Folder: `docs/fix/<issue-number>-<topic>/`
- Branch: `fix/<issue-number>-<topic>`
- Every fix has a GitHub issue; PR closes it with `Closes #<n>`.
- Entry is removed from this table on merge — do not maintain history
  here.
- See `_TEMPLATE/SPEC.md` for the spec format.
- Workflow rules: `.claude/skills/execute-phase/SKILL.md` (`--fix`
  mode).
