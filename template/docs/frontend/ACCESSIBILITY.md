# Accessibility *(optional)*

> Stub — keep only if the project has a UI. Delete `docs/frontend/` otherwise.

## Standard

The accessibility standard you target (e.g. WCAG level) and the checklist applied
to every user-facing change: semantics, keyboard navigation, contrast, focus,
screen-reader behavior.
