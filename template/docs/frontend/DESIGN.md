# Design / visual system *(optional)*

> Stub — keep only if the project has a UI. Delete `docs/frontend/` otherwise.

## Principles

How the product should feel and the UI values it optimizes for (e.g. clarity,
speed, trust). What to avoid.

## Tokens

Where design tokens live and the rule to use them (no hardcoded colors/spacing).

## Components & patterns

Pointers to the component library and the reusable layout/interaction patterns.
