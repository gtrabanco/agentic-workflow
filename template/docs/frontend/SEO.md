# SEO *(optional)*

> Stub — keep only if the project has a web frontend. Delete the whole
> `docs/frontend/` folder if there is no UI.

## Requirements

Canonical URLs, metadata, structured data, OpenGraph, sitemap, hreflang — list
what this project must support and the rules for each.
