# Infrastructure

> Stub. Document the runtime, deployment, storage, and external systems — the
> "how it runs" layer. Keep stack specifics here, out of the domain.

## Runtime & deployment

Where and how the project runs and ships (hosting, build, environments, CI/CD).

## Storage & data

Databases, caches, queues, object storage — and their roles. Migration approach.

## External systems

Integrations and how they're wired (see `docs/providers/*` for per-provider detail).

## Operational concerns

Observability, retries, rate limits, scaling and consistency considerations.
